## 2026-03-20 - Task: 项目基础配置

### What was done:
- 补齐了根目录 `architecture.md` 和 `task.json`，避免项目状态只存在于隐藏目录。
- 创建了 `backend`、`frontend`、`data`、`tests` 的基础目录结构，并预留后续解析、映射、校验、匹配和导出模块目录。
- 新增后端依赖清单、最小 FastAPI 入口和后端健康检查冒烟测试。
- 新增前端 Vite + React + TypeScript 骨架、基础路由、导航布局和 API 客户端封装。
- 新增 `.env.example`、`.gitignore` 和 `README.md` 作为启动说明与环境配置模板。

### Testing:
- `py -m compileall backend`
- `.\.venv\Scripts\python.exe -c "from backend.app.main import app; print(app.title); print(app.version)"`
- `cmd /c npm.cmd install`
- `cmd /c npm.cmd run build`
- `.\.venv\Scripts\python.exe -m pytest backend\tests -o cache_dir=.pytest_cache`

### Risks / Open Questions:
- 真实地区样例文件、双模板文件和员工主数据尚未进入工作区，核心链路暂时无法验证。
- `pytest` 在当前机器上会尝试创建若干受限临时缓存目录，虽然测试已通过，但后续建议统一指定更干净的缓存目录或关闭缓存插件。
- Python 运行环境当前为 3.13.7，后续需继续确认依赖与目标部署版本兼容性。

### Notes:
- 当前已完成任务 1，并将 `task.json` 中对应任务标记为 `passes: true`。
- 下一步建议进入任务 2 或任务 3，优先补齐配置管理和数据库基础设施。
## 2026-03-20 - Task: 数据库 Schema 设计

### What was done:
- 新增 SQLAlchemy 基础模型基座、统一命名约定和状态枚举。
- 建立了 `import_batches`、`source_files`、`header_mappings`、`normalized_records`、`validation_issues`、`employee_master`、`match_results`、`export_jobs`、`export_artifacts` 9 张核心表。
- 为关键关系补充了外键、索引、唯一约束和双模板导出唯一性约束。
- 初始化 Alembic，新增 `backend/alembic.ini`、迁移环境和首个初始 migration。
- 新增数据库层测试，验证 metadata 注册和 Alembic `upgrade head` 建表结果。

### Testing:
- `py -m compileall backend`
- `.\.venv\Scripts\python.exe -c "from backend.app.models import Base; print(sorted(Base.metadata.tables.keys()))"`
- `.\\.venv\\Scripts\\python.exe -m pytest backend\\tests -p no:cacheprovider`\r\n- `$env:DATABASE_URL='sqlite:///D:/execl_mix/.test_artifacts/manual_upgrade.db'; .\\.venv\\Scripts\\python.exe -m alembic -c backend\\alembic.ini upgrade head`

### Risks / Open Questions:
- 当前迁移验证使用的是 sqlite 测试库，用于保证 schema 和 Alembic 链路可运行；正式 PostgreSQL 连接与会话管理仍需在任务 3 中补齐。
- `validation_issues.severity`、`export_jobs.status`、`export_artifacts.status` 目前保留为字符串，后续如果业务状态集收敛，可以再升级为明确枚举。
- 真实业务流程还未接入这些表，后续导入、标准化、匹配和导出服务需要继续对齐字段使用方式。

### Notes:
- 当前已完成任务 2，并将 `task.json` 中对应任务标记为 `passes: true`。
- 下一步建议进入任务 3，补齐配置管理、数据库连接与依赖注入。
## 2026-03-20 - Task: 配置管理与基础依赖注入

### What was done:
- 新增 `backend/app/core/config.py`，统一管理应用、数据库、目录、DeepSeek 和日志配置。
- 新增 `backend/app/core/database.py`，封装 SQLAlchemy engine、session factory 和基础数据库依赖。
- 新增 `backend/app/dependencies.py`，提供 `get_settings` 与 `get_db` 依赖入口。
- 新增 `backend/app/core/logging.py` 与 `backend/app/bootstrap.py`，用于日志初始化和运行时目录创建。
- 新增 `backend/run.py` 作为后端启动脚本，并补充 `.env.example` 中的日志相关环境变量。
- 新增运行时配置测试，验证目录解析、bootstrap、数据库 session 工厂和日志格式器。

### Testing:
- `py -m compileall backend`
- `.\.venv\Scripts\python.exe -m pytest backend\tests -p no:cacheprovider`
- `.\.venv\Scripts\python.exe -c "from backend.app.bootstrap import bootstrap_application; settings=bootstrap_application(); print(settings.database_url); print(settings.upload_path); print(settings.log_format)"`

### Risks / Open Questions:
- 当前数据库连接层使用同步 SQLAlchemy session，后续如果导入和解析任务转为异步执行，需要再评估 async session 是否值得引入。
- `SessionLocal` 在模块导入时基于当前环境变量创建，适合当前单体进程启动模式；若后续需要更灵活的多环境切换，可以再改成惰性 session factory。
- `main.py` 还没有正式接入这些配置和依赖，下一步任务 4 会把应用初始化真正串起来。

### Notes:
- 当前已完成任务 3，并将 `task.json` 中对应任务标记为 `passes: true`。
- 下一步建议进入任务 4，补齐 FastAPI 应用初始化、CORS、异常处理和统一响应格式。
## 2026-03-20 - Task: FastAPI 应用初始化

### What was done:
- 重构 `backend/app/main.py`，通过应用工厂统一接入 Settings、lifespan bootstrap、CORS、上传保护中间件和 `/api/v1` 路由注册。
- 新增统一响应封装 `backend/app/api/v1/responses.py`，统一成功与错误返回结构。
- 新增 `backend/app/api/v1/router.py` 与 `backend/app/api/v1/system.py`，提供 `/api/v1/system/health` 和测试用回显接口。
- 新增 `backend/app/core/upload_guard.py`，为 multipart 请求预留上传大小限制拦截。
- 更新健康检查测试，并新增应用初始化测试，覆盖健康检查、CORS、404/422 错误包装和超大上传拦截。

### Testing:
- `py -m compileall backend`
- `.\.venv\Scripts\python.exe -m pytest backend\tests -p no:cacheprovider`

### Risks / Open Questions:
- 当前上传保护基于 `content-length` 对 multipart 请求做前置拦截，后续接入真实上传接口时还需要补文件扩展名与 MIME 类型校验。
- 统一错误格式已落地，但业务错误码体系还未展开，后续导入、解析、映射等 API 需要继续保持同一协议。
- `/api/v1/system/echo/{value}` 当前主要用于验证异常包装，后续如果没有继续使用，可以在更完整的 system/status API 成型后移除或替换。

### Notes:
- 当前已完成任务 4，并将 `task.json` 中对应任务标记为 `passes: true`。
- 下一步建议进入任务 5，补齐 React 前端初始化、API 客户端错误拦截和基础页面容器。

## 2026-03-20 - Task: React 前端初始化

### What was done:
- 升级前端基础结构，补齐 `AppShell`、页面容器、全局反馈条和 404 页面，让路由、导航和页面骨架具备后续扩展能力。
- 重构 `frontend/src/services/api.ts`，加入环境变量读取、统一错误归一化和 axios 请求/响应拦截器。
- 新增前端 API 反馈 Provider 与 hook，用于统一展示请求中状态和全局错误提示。
- 新增系统健康检查服务 `frontend/src/services/system.ts`，让看板首页可直接验证后端 `/api/v1/system/health` 连通性。
- 补充 ESLint 配置和 `npm run lint` 脚本，并更新页面样式与基础内容，使前端能以统一容器继续承接导入、员工主档等后续任务。

### Testing:
- `cmd /c npm.cmd install`
- `cmd /c npm.cmd run lint`
- `cmd /c npm.cmd run build`

### Risks / Open Questions:
- 当前前端仍以占位页为主，上传、批次列表和员工主档接口还未真正接入，只完成了后续对接所需的壳层与反馈链路。
- 健康检查依赖本地后端 `/api/v1/system/health`，如果后端未启动，页面会通过全局错误条显式提示。
- 这次 lint 过程中遇到一次本地 sandbox 刷新异常，最终通过提升权限重跑完成验证；代码本身已 lint 通过。

### Notes:
- 当前已完成任务 5，并将 `task.json` 中对应任务标记为 `passes: true`。
- 下一步建议进入任务 6 或任务 7，优先补员工主数据导入或文件上传与导入批次管理。

## 2026-03-20 - Task: 文件上传与导入批次管理

### What was done:
- 新增 `backend/app/services/import_service.py`，实现多文件上传校验、批次创建、文件落盘、SHA256 哈希计算和 `source_files` 元信息写库。
- 新增 `backend/app/api/v1/imports.py`，提供 `POST /api/v1/imports`、`GET /api/v1/imports` 和 `GET /api/v1/imports/{id}` 基础接口。
- 新增 `backend/app/schemas/imports.py`，统一导入批次列表、详情和源文件响应结构。
- 为 `source_files` 模型补充 `file_size` 字段，并新增 Alembic migration `20260320_0002_source_file_size.py`，保证上传文件大小可追踪。
- 更新路由注册和 schema 测试，并新增导入批次 API 测试，覆盖多文件上传、元数据映射、列表/详情查询、非法扩展名和元数据数量不匹配场景。

### Testing:
- `py -m compileall backend`
- `.\.venv\Scripts\python.exe -m pytest backend\tests -p no:cacheprovider`

### Risks / Open Questions:
- 当前上传接口只完成扩展名校验，MIME 类型和更细粒度的内容校验还需要在后续解析链路里继续补强。
- `regions` / `company_names` 当前支持单值字符串或 JSON 数组，已经够前端对接，但后续如果要支持按文件对象更复杂的元数据，建议再升级为显式结构体。
- 批次状态目前在上传成功后固定写为 `uploaded`，后续接入解析、标准化和导出流程时还需要补完整状态流转。

### Notes:
- 当前已完成任务 7，并将 `task.json` 中对应任务标记为 `passes: true`。
- 由于主链路优先，这一步先实现了导入批次管理；下一步建议进入任务 8，继续做 Workbook / Sheet 发现服务。
