---
phase: 18-responsive-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useResponsiveViewport.ts
  - frontend/src/components/MobileStickyActionBar.tsx
  - frontend/src/components/WorkflowSteps.tsx
  - frontend/src/layouts/MainLayout.tsx
  - frontend/src/layouts/MainLayout.module.css
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "MainLayout 在 <768px 时使用 Drawer 导航，在 >=768px 时继续使用 Sider"
    - "手机端 Header 隐藏 breadcrumb，只保留页标题、汉堡按钮、主题切换和用户菜单"
    - "移动导航在路由变化后自动关闭"
    - "存在可复用的响应式断点 hook，页面不再各自手写 window.innerWidth"
    - "存在可复用的 MobileStickyActionBar，共后续长流程页面复用"
    - "WorkflowSteps 在手机端不挤压步骤标题，允许横向滚动查看"
  artifacts:
    - path: "frontend/src/hooks/useResponsiveViewport.ts"
      provides: "统一响应式断点布尔值"
      contains: "isMobile"
    - path: "frontend/src/components/MobileStickyActionBar.tsx"
      provides: "移动端固定底部主操作组件"
      contains: "env(safe-area-inset-bottom)"
    - path: "frontend/src/layouts/MainLayout.tsx"
      provides: "移动端 Drawer 导航和紧凑 Header"
      contains: "aria-label=\"打开导航菜单\""
    - path: "frontend/src/components/WorkflowSteps.tsx"
      provides: "小屏步骤条滚动容器"
      contains: "overflowX: 'auto'"
  key_links:
    - from: "frontend/src/hooks/useResponsiveViewport.ts"
      to: "frontend/src/layouts/MainLayout.tsx"
      via: "isMobile/isTablet/isCompactDesktop layout branching"
      pattern: "useResponsiveViewport"
    - from: "frontend/src/components/MobileStickyActionBar.tsx"
      to: "frontend/src/pages/SimpleAggregate.tsx"
      via: "shared mobile CTA component for downstream plans"
      pattern: "MobileStickyActionBar"
    - from: "frontend/src/components/WorkflowSteps.tsx"
      to: "frontend/src/pages/Dashboard.tsx"
      via: "workflow shell reused by dashboard/results/exports"
      pattern: "WorkflowSteps"
---

<objective>
建立 Phase 18 的共享响应式基础设施：统一断点 hook、移动端 Drawer 导航、紧凑 Header、可复用的 sticky 主操作组件，以及可在手机端滚动查看的流程步骤条。

Purpose: 先把“全局骨架”打稳，后续页面适配都复用同一套断点和共享组件，不再每页发明一套移动端行为。
Output: 响应式共享原语 + MainLayout 移动壳层 + WorkflowSteps 小屏适配
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-responsive-adaptation/18-CONTEXT.md
@.planning/phases/18-responsive-adaptation/18-RESEARCH.md
@.planning/phases/18-responsive-adaptation/18-UI-SPEC.md
@.planning/phases/18-responsive-adaptation/18-VALIDATION.md

<interfaces>
From frontend/src/layouts/MainLayout.tsx:
```tsx
function useResponsiveCollapse(breakpoint: number = 1440): boolean
const LABEL_MAP: Record<string, string>
const MENU_GROUPS: MenuGroupConfig[]
const TOP_ITEM: NavItem
```

Current shell behavior:
```tsx
<Sider theme="dark" collapsible collapsed={collapsed} width={220} collapsedWidth={64}>
<Header>
  <Breadcrumb items={buildBreadcrumbItems(location.pathname)} />
  <Space>theme toggle + user menu</Space>
</Header>
<Content style={{ padding: '24px', height: 'calc(100vh - 56px)', overflowY: 'auto' }}>
```

From frontend/src/components/WorkflowSteps.tsx:
```tsx
<Card style={{ marginBottom: 16 }} styles={{ body: { padding: '12px 24px' } }}>
  <Steps size="small" current={...} items={...} />
</Card>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建统一响应式 hook 与共享移动端主操作组件</name>
  <files>frontend/src/hooks/useResponsiveViewport.ts, frontend/src/components/MobileStickyActionBar.tsx, frontend/src/components/WorkflowSteps.tsx</files>
  <read_first>
    - frontend/src/components/WorkflowSteps.tsx
    - frontend/src/theme/useThemeMode.ts
    - frontend/src/theme/useSemanticColors.ts
    - frontend/src/layouts/MainLayout.tsx
  </read_first>
  <action>
1. 新增 `frontend/src/hooks/useResponsiveViewport.ts`，导出 `useResponsiveViewport()`，内部使用 `window.matchMedia` 创建并维护以下断点布尔值：
   - `isMobile`: `(max-width: 767px)`
   - `isTablet`: `(min-width: 768px) and (max-width: 991px)`
   - `isCompactDesktop`: `(min-width: 992px) and (max-width: 1440px)`
   - `isDesktopWide`: `(min-width: 1441px)`
   Hook 返回上述 4 个布尔值，并在 effect cleanup 中移除 listener。

2. 新增 `frontend/src/components/MobileStickyActionBar.tsx`，组件 props 至少包含：
   - `visible: boolean`
   - `primaryLabel: string`
   - `onPrimaryClick: () => void`
   - `primaryDisabled?: boolean`
   - `primaryLoading?: boolean`
   - `helperText?: string | null`
   - `icon?: React.ReactNode`
   当 `visible` 为 `false` 时返回 `null`；为 `true` 时渲染固定底部容器，样式必须包含：
   - `position: 'sticky'` 或 `position: 'fixed'` 的底部固定行为（优先 `position: 'sticky'` + `bottom: 0` 的页面内方案）
   - `padding: '12px 16px calc(12px + env(safe-area-inset-bottom))'`
   - 顶部分隔线
   - 仅一个 `block` 主按钮，按钮文案来自 `primaryLabel`
   - 如果有 `helperText`，按钮上方显示一行辅助说明

3. 修改 `frontend/src/components/WorkflowSteps.tsx`：
   - 接入 `useResponsiveViewport`
   - 小屏 `<768px` 时将 `Steps` 包裹进 `div`，该容器包含 `overflowX: 'auto'` 和 `paddingBottom: 4`
   - 小屏 card body padding 改为 `12px 16px`，桌面保持 `12px 24px`
   - `Steps` 本体设置 `responsive={false}`，并为移动端容器提供足够的最小宽度，避免 4 个步骤标题被压成两行
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/hooks/useResponsiveViewport.ts src/components/MobileStickyActionBar.tsx src/components/WorkflowSteps.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useResponsiveViewport.ts` 导出 `useResponsiveViewport`
    - `frontend/src/hooks/useResponsiveViewport.ts` 包含 `isMobile`、`isTablet`、`isCompactDesktop`
    - `frontend/src/components/MobileStickyActionBar.tsx` 包含 `env(safe-area-inset-bottom)`
    - `frontend/src/components/MobileStickyActionBar.tsx` 只渲染一个主按钮
    - `frontend/src/components/WorkflowSteps.tsx` 包含 `overflowX: 'auto'`
    - `frontend/src/components/WorkflowSteps.tsx` 包含 `responsive={false}`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/hooks/useResponsiveViewport.ts src/components/MobileStickyActionBar.tsx src/components/WorkflowSteps.tsx` 退出码为 0
  </acceptance_criteria>
  <done>共享断点 hook、MobileStickyActionBar 和小屏 WorkflowSteps 基础能力已就绪，可供后续页面复用</done>
</task>

<task type="auto">
  <name>Task 2: 重构 MainLayout 为移动端 Drawer 壳层并保持桌面侧栏逻辑</name>
  <files>frontend/src/layouts/MainLayout.tsx, frontend/src/layouts/MainLayout.module.css</files>
  <read_first>
    - frontend/src/layouts/MainLayout.tsx
    - frontend/src/layouts/MainLayout.module.css
    - frontend/src/hooks/useMenuOpenKeys.ts
    - frontend/src/hooks/useResponsiveViewport.ts
    - .planning/phases/18-responsive-adaptation/18-UI-SPEC.md
  </read_first>
  <action>
1. 在 `frontend/src/layouts/MainLayout.tsx` 中移除本地 `useResponsiveCollapse` 的职责分散方式，改为使用 `useResponsiveViewport()`：
   - `>=768px` 继续保留现有 `Sider` 模式
   - `<768px` 完全不渲染左侧 `Sider`，改为 Drawer 导航
   - `992px - 1440px` 仍沿用现有自动折叠逻辑，把 sider 收到 `64px`

2. 新增移动端状态 `mobileNavOpen`，并添加以下行为：
   - Header 左侧新增汉堡按钮 `<Button type="text" aria-label="打开导航菜单" />`
   - 使用 `Drawer` `placement="left"`，`width="min(320px, 100vw)"`，关闭按钮必须带 `aria-label="关闭导航抽屉"`
   - Drawer 内容顶部复用当前 logo 文案和菜单搜索输入框
   - Drawer 内部继续使用已有的 `menuItems`、`resolvedKey`、`navigate(key)`、`setMenuSearch('')`
   - 新增 effect：当 `location.pathname` 或 `isMobile` 变化时，如果当前是移动端则执行 `setMobileNavOpen(false)`

3. Header 行为改为：
   - `isMobile` 时隐藏 `Breadcrumb`
   - 使用 `LABEL_MAP` + `location.pathname` 生成当前页面标题，并在 header 中显示一个单行 `Typography.Text` / `span`
   - 右侧仅保留主题切换按钮和用户下拉按钮，去掉占空间的额外元素
   - `>=768px` 保持当前 breadcrumb + 右侧操作布局

4. Content 容器改为按断点设置 padding：
   - `isMobile`: `padding: '12px 12px 16px'`
   - `isTablet` 或 `isCompactDesktop`: `padding: '16px'`
   - `isDesktopWide`: `padding: '24px'`
   同时移除固定 `height: 'calc(100vh - 56px)'`，改为允许内容自然撑开并由 layout 自身滚动。

5. 在 `frontend/src/layouts/MainLayout.module.css` 中补充 drawer logo / 移动 header 标题所需样式，不引入新的硬编码深色背景色，继续使用现有暗色 logo 文本风格。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/layouts/MainLayout.tsx src/layouts/MainLayout.module.css</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/layouts/MainLayout.tsx` 包含 `Drawer`
    - `frontend/src/layouts/MainLayout.tsx` 包含 `aria-label="打开导航菜单"`
    - `frontend/src/layouts/MainLayout.tsx` 包含 `aria-label="关闭导航抽屉"`
    - `frontend/src/layouts/MainLayout.tsx` 在移动分支不渲染 `Breadcrumb`
    - `frontend/src/layouts/MainLayout.tsx` 包含 `setMobileNavOpen(false)` 的 route-change effect
    - `frontend/src/layouts/MainLayout.tsx` 包含 `padding: '12px 12px 16px'`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/layouts/MainLayout.tsx src/layouts/MainLayout.module.css` 退出码为 0
  </acceptance_criteria>
  <done>MainLayout 已具备移动 Drawer 导航、紧凑 Header、路由后自动关闭抽屉和响应式内容 padding</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated navigation | 移动导航不能绕过现有角色过滤和 feature flag 菜单控制 |
| Mobile primary action shell | 共享 sticky bar 不能引入第二个固定危险动作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | MainLayout mobile drawer | mitigate | Drawer 必须继续复用 `MENU_GROUPS` / `buildMenuItems` / role filtering，不创建第二套导航源 |
| T-18-02 | Tampering | MobileStickyActionBar | mitigate | 共享组件只接受一个 primary action props，不提供第二个固定 secondary button 插槽 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/hooks/useResponsiveViewport.ts src/components/MobileStickyActionBar.tsx src/components/WorkflowSteps.tsx src/layouts/MainLayout.tsx src/layouts/MainLayout.module.css`
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build`
3. 手动验证 `<768px` 时 Drawer 打开、点击任意菜单项后自动关闭；`>=768px` 时桌面侧栏和 breadcrumb 仍可用
</verification>

<success_criteria>
- 共享断点 hook 在 layout 和后续页面中可复用
- 手机端出现 Drawer 导航和紧凑 Header
- WorkflowSteps 在手机端可横向滚动查看完整步骤
- 存在可复用的 MobileStickyActionBar，为后续 workflow 页面直接接入
</success_criteria>

<output>
After completion, create `.planning/phases/18-responsive-adaptation/18-01-SUMMARY.md`
</output>
