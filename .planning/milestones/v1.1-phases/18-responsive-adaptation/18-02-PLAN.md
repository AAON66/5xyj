---
phase: 18-responsive-adaptation
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - frontend/src/pages/EmployeeSelfService.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "EmployeeSelfService 手机端使用卡片流，而不是桌面表格缩窄"
    - "页面顺序为个人资料 -> 当月汇总 -> 缴费历史"
    - "最新月份默认展开，其余月份默认收起"
    - "手机端社保明细和公积金明细上下堆叠，不使用 Tabs"
    - ">=768px 继续保留当前桌面表格 + expandable row 模式"
  artifacts:
    - path: "frontend/src/pages/EmployeeSelfService.tsx"
      provides: "移动端卡片流和历史月份折叠"
      contains: "Collapse"
  key_links:
    - from: "frontend/src/pages/EmployeeSelfService.tsx"
      to: "frontend/src/hooks/useResponsiveViewport.ts"
      via: "isMobile branch"
      pattern: "useResponsiveViewport"
---

<objective>
把员工自助查询页面改造成手机优先的卡片流布局，确保员工在手机端先看到个人信息和当月结果，再按月份查看历史明细。

Purpose: 员工自助查询是最高价值移动端场景，必须优先于其他后台页完成真实的手机可用性适配。
Output: 移动端资料卡 + 当月汇总卡 + 历史月份折叠卡片流，同时保留桌面端表格体验
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-responsive-adaptation/18-CONTEXT.md
@.planning/phases/18-responsive-adaptation/18-RESEARCH.md
@.planning/phases/18-responsive-adaptation/18-UI-SPEC.md
@.planning/phases/18-responsive-adaptation/18-VALIDATION.md

<interfaces>
From frontend/src/pages/EmployeeSelfService.tsx:
```tsx
const [expandedKeys, setExpandedKeys] = useState<string[]>([])
const latestRecord = useMemo(() => data?.records[0] ?? null, [data])

<Table
  dataSource={data.records}
  expandable={{
    expandedRowKeys: expandedKeys,
    expandedRowRender: (record) => <InsuranceDetailTable record={record} />,
  }}
/>
```

Current detail layout:
```tsx
<Row gutter={[16, 16]}>
  <Col xs={24} md={12}><Card title="社保明细"><Table ... /></Card></Col>
  <Col xs={24} md={12}><Card title="公积金明细"><Table ... /></Card></Col>
</Row>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 EmployeeSelfService 增加移动端卡片流和历史月份折叠模式</name>
  <files>frontend/src/pages/EmployeeSelfService.tsx</files>
  <read_first>
    - frontend/src/pages/EmployeeSelfService.tsx
    - frontend/src/hooks/useResponsiveViewport.ts
    - .planning/phases/18-responsive-adaptation/18-UI-SPEC.md
    - .planning/phases/05-employee-portal/05-CONTEXT.md
  </read_first>
  <action>
1. 在 `frontend/src/pages/EmployeeSelfService.tsx` 中接入 `useResponsiveViewport()`，把页面分成 `isMobile` 和非移动两套渲染分支：
   - 移动端根容器 padding 使用 `16px 12px 24px`
   - 非移动端继续保留当前 `maxWidth: 960` + `32px 24px` 的桌面布局

2. 调整 `expandedKeys` 初始化逻辑：当 `fetchPortalRecords()` 返回数据后，如果 `records[0]` 存在，设置
   `setExpandedKeys([records[0].normalized_record_id])`
   以保证最新月份默认展开；其他月份默认保持折叠。

3. 改造 `InsuranceDetailTable`：
   - 保留现有两张卡片内容
   - 移动端使用上下堆叠布局（两张卡片都占满宽度）
   - 非移动端继续使用 `md={12}` 的左右两列
   - 不引入 Tabs，严格按“社保明细卡 + 公积金明细卡”顺序渲染

4. 在主页面内容中，移动端不要继续使用历史 `Table` 作为主视图，而是改用 `Collapse` 或同等 accordion 结构：
   - 每个 panel header 至少展示 `billing_period`、`region`、`company_name`、`total_amount`
   - panel body 渲染 `InsuranceDetailTable`
   - 历史月份顺序保持 `data.records` 的现有顺序（即最新优先）

5. 桌面端 `>=768px` 继续保留现有 `Table + expandable row + pagination` 方案，不改变桌面用户已熟悉的操作方式。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/EmployeeSelfService.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/EmployeeSelfService.tsx` 导入 `Collapse`
    - `frontend/src/pages/EmployeeSelfService.tsx` 导入 `useResponsiveViewport`
    - `frontend/src/pages/EmployeeSelfService.tsx` 包含 `setExpandedKeys([` 且默认取首条记录的 `normalized_record_id`
    - `frontend/src/pages/EmployeeSelfService.tsx` 在移动分支不再把历史记录主视图渲染为 `Table`
    - `frontend/src/pages/EmployeeSelfService.tsx` 保留非移动端 `Table` 分支
    - `frontend/src/pages/EmployeeSelfService.tsx` 中明细卡片在移动分支为纵向堆叠
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/EmployeeSelfService.tsx` 退出码为 0
  </acceptance_criteria>
  <done>员工自助查询页已具备手机优先卡片流、最新月份默认展开和纵向堆叠明细卡，同时不破坏桌面端表格模式</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Employee portal personal data | 移动端改版不能暴露比当前更多的身份证等敏感字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Information Disclosure | EmployeeSelfService | mitigate | 继续只展示 `masked_id_number`，不要在移动卡片中替换成原始身份证号；历史明细仅展示已有金额字段 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/EmployeeSelfService.tsx`
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build`
3. 手动验证手机宽度下页面顺序为“资料卡 -> 当月汇总 -> 历史月份”，且最新月份默认展开
</verification>

<success_criteria>
- 员工在手机端进入页面后无需横向拖动表格即可看到当月核心结果
- 历史记录按月份折叠，默认只展开最新月份
- 明细部分在手机端是上下堆叠的两张卡片
- 桌面端现有历史表格体验不回退
</success_criteria>

<output>
After completion, create `.planning/phases/18-responsive-adaptation/18-02-SUMMARY.md`
</output>
