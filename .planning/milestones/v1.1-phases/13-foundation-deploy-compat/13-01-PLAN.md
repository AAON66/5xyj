---
phase: 13-foundation-deploy-compat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/parsers/header_extraction.py
  - backend/app/parsers/workbook_discovery.py
  - backend/app/parsers/workbook_loader.py
  - backend/app/exporters/export_utils.py
  - backend/app/exporters/template_exporter.py
  - backend/app/exporters/salary_exporter.py
  - backend/app/exporters/tool_exporter.py
  - backend/app/mappings/manual_field_aliases.py
  - backend/app/validators/non_detail_row_filter.py
  - backend/app/services/region_detection_service.py
  - backend/app/services/housing_fund_service.py
  - backend/app/services/matching_service.py
  - backend/app/services/header_normalizer.py
  - backend/app/services/validation_service.py
  - backend/app/services/employee_service.py
  - backend/app/services/batch_runtime_service.py
  - backend/app/services/normalization_service.py
  - backend/app/services/llm_mapping_service.py
  - backend/app/services/batch_export_service.py
  - backend/app/services/compare_service.py
  - backend/app/services/feishu_sync_service.py
  - backend/app/services/import_service.py
  - backend/app/services/aggregate_service.py
  - backend/app/schemas/imports.py
  - backend/app/schemas/compare.py
  - backend/app/schemas/employees.py
  - backend/app/api/v1/feishu_auth.py
  - backend/app/api/v1/aggregate.py
  - backend/app/core/api_doc_generator.py
  - backend/app/models/validation_issue.py
  - backend/app/models/employee_master_audit.py
  - backend/app/models/match_result.py
  - backend/app/models/import_batch.py
  - backend/requirements.txt
  - backend/requirements.server.txt
  - backend/tests/support/export_fixtures.py
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "所有 Python 文件不含 slots=True，Python 3.9 环境可正常导入"
    - "requirements.txt 中核心依赖版本锁定了上界，确保兼容 Python 3.9"
    - "未使用的依赖已移除，requirements.server.txt 中实际使用的依赖已合并到主文件"
    - "全部后端测试通过"
    - "requirements.txt 编码为 UTF-8"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "统一的依赖文件，包含版本上界"
      contains: "fastapi>=0.115.0,<0.130.0"
    - path: "backend/app/parsers/header_extraction.py"
      provides: "slots=True 已移除"
  key_links:
    - from: "backend/requirements.txt"
      to: "Python 3.9 runtime"
      via: "pip install"
      pattern: "<0\\.130\\.0|<3\\.0\\.0"
---

<objective>
Python 3.9 全面兼容性修复 + 依赖清理

Purpose: 系统部署到 Python 3.9 云服务器的阻断性问题，必须首先解决。涵盖 slots=True 移除、PEP 604 union 兼容性验证、依赖版本锁定、requirements 合并。
Output: 所有 `slots=True` 移除、PEP 604 union 兼容性确认、依赖版本锁定上界、未使用依赖清理、requirements 合并
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-foundation-deploy-compat/13-CONTEXT.md
@.planning/phases/13-foundation-deploy-compat/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除所有 @dataclass(slots=True) 并验证 PEP 604 union 兼容性</name>
  <files>
    backend/app/parsers/header_extraction.py,
    backend/app/parsers/workbook_discovery.py,
    backend/app/exporters/export_utils.py,
    backend/app/mappings/manual_field_aliases.py,
    backend/app/validators/non_detail_row_filter.py,
    backend/app/services/region_detection_service.py,
    backend/app/services/housing_fund_service.py,
    backend/app/services/matching_service.py,
    backend/app/services/header_normalizer.py,
    backend/app/services/validation_service.py,
    backend/app/services/employee_service.py,
    backend/app/services/batch_runtime_service.py,
    backend/app/services/normalization_service.py,
    backend/app/services/llm_mapping_service.py,
    backend/app/services/batch_export_service.py,
    backend/app/services/compare_service.py
  </files>
  <read_first>
    backend/app/parsers/header_extraction.py,
    backend/app/parsers/workbook_discovery.py,
    backend/app/exporters/export_utils.py,
    backend/app/mappings/manual_field_aliases.py,
    backend/app/validators/non_detail_row_filter.py,
    backend/app/services/region_detection_service.py,
    backend/app/services/housing_fund_service.py,
    backend/app/services/matching_service.py,
    backend/app/services/header_normalizer.py,
    backend/app/services/validation_service.py,
    backend/app/services/employee_service.py,
    backend/app/services/batch_runtime_service.py,
    backend/app/services/normalization_service.py,
    backend/app/services/llm_mapping_service.py,
    backend/app/services/batch_export_service.py,
    backend/app/services/compare_service.py
  </read_first>
  <action>
    Per D-01: 在所有上述文件中，将 `@dataclass(slots=True)` 替换为 `@dataclass`。如果原来同时有 `frozen=True`（即 `@dataclass(slots=True, frozen=True)`），则替换为 `@dataclass(frozen=True)`。

    共 31 处需修改，按 RESEARCH.md 清单：
    - parsers/header_extraction.py (3 处)
    - parsers/workbook_discovery.py (2 处)
    - exporters/export_utils.py (2 处)
    - validators/non_detail_row_filter.py (3 处)
    - services/region_detection_service.py (3 处)
    - services/housing_fund_service.py (2 处)
    - services/matching_service.py (1 处)
    - services/header_normalizer.py (2 处)
    - services/validation_service.py (2 处)
    - services/employee_service.py (2 处)
    - services/batch_runtime_service.py (3 处)
    - services/normalization_service.py (4 处)
    - services/llm_mapping_service.py (1 处)
    - services/batch_export_service.py (1 处)
    - services/compare_service.py -- 如果 grep 找到则修改，否则跳过

    Per D-04: 验证无 `match/case` 语法：
    ```
    grep -rn "^    match " backend/app/ --include="*.py"
    ```
    预期结果：0 行。

    PEP 604 union 兼容性验证（回应 REVIEWS.md HIGH 级别关注）：
    代码库中大量使用 `str | Path`、`dict[str, object | None]` 等 PEP 604 unions。
    经验证，所有包含 PEP 604 unions 的文件均已有 `from __future__ import annotations`（PEP 563），
    使得所有类型注解在解析阶段被延迟为字符串，Python 3.9 不会在语法层面报错。

    Pydantic v2 schemas（imports.py、compare.py、employees.py）中的 `dict[str, object | None]` 
    同样受 `from __future__ import annotations` 保护。Pydantic v2 使用自己的类型解析器 
    (typing-extensions)，可以在运行时正确解析字符串化的 PEP 604 unions。

    验证步骤：
    1. 运行 `grep -rn " | " backend/app/ --include="*.py" -l` 找所有含 PEP 604 union 的文件
    2. 对每个文件确认首行有 `from __future__ import annotations`
    3. 如果有任何文件缺少该 import 且包含 PEP 604 union，必须添加 import
    4. 搜索 `isinstance` 调用中的 `|` 用法（这是运行时用法，from future 不能保护）：
       `grep -rn "isinstance.*|" backend/app/ --include="*.py"` -- 预期 0 行
    5. 运行全部测试确认无运行时错误

    全局搜索 `__dict__` 和 `__slots__` 确认无代码依赖这些行为：
    ```
    grep -rn "__dict__\|__slots__" backend/app/ --include="*.py"
    ```
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -rn "slots=True" backend/app/ --include="*.py" | wc -l | xargs test 0 -eq && echo "PASS: no slots=True remaining" && pytest backend/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "slots=True" backend/app/ --include="*.py"` 返回 0 行
    - `grep -rn "isinstance.*|" backend/app/ --include="*.py"` 返回 0 行（无运行时 PEP 604）
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>所有 @dataclass(slots=True) 已移除，PEP 604 union 兼容性已验证（所有文件均有 from __future__ import annotations），后端测试全部通过</done>
</task>

<task type="auto">
  <name>Task 2: 依赖版本锁定 + 未使用依赖清理 + requirements 合并</name>
  <files>backend/requirements.txt, backend/requirements.server.txt</files>
  <read_first>
    backend/requirements.txt,
    backend/requirements.server.txt
  </read_first>
  <action>
    当前 `backend/requirements.txt` 是 ASCII 编码（已验证非 UTF-16LE），内容为精确版本锁定。
    当前 `backend/requirements.server.txt` 存在（已验证），包含 PyJWT、pwdlib[bcrypt] 等。
    当前 `xlrd` 不在 requirements.txt 中（已验证缺失）。

    Per D-03: 将 `backend/requirements.txt` 中的精确版本号改为带上界的范围约束。
    最终 requirements.txt 内容（UTF-8 编码，按字母排序）：

    ```
    alembic>=1.14.0
    fastapi>=0.115.0,<0.130.0
    httpx>=0.28.1
    openpyxl>=3.1.5
    pandas>=2.2.3,<3.0.0
    pydantic>=2.10.3,<2.13.0
    pydantic-settings>=2.6.1,<2.12.0
    PyJWT>=2.12.0
    pwdlib[bcrypt]>=0.3.0
    pytest>=8.3.4
    python-dotenv>=1.0.1
    python-multipart>=0.0.12
    sqlalchemy>=2.0.36,<2.1.0
    uvicorn[standard]>=0.32.0
    xlrd>=2.0.0
    ```

    变更明细：
    - `fastapi==0.115.0` -> `fastapi>=0.115.0,<0.130.0` (0.130.0 放弃 3.9)
    - `pydantic==2.10.3` -> `pydantic>=2.10.3,<2.13.0`
    - `pydantic-settings==2.6.1` -> `pydantic-settings>=2.6.1,<2.12.0` (2.12.0 放弃 3.9)
    - `pandas==2.2.3` -> `pandas>=2.2.3,<3.0.0` (3.0.0 要求 3.11+)
    - `sqlalchemy==2.0.36` -> `sqlalchemy>=2.0.36,<2.1.0` (2.1.0 放弃 3.9)
    - 其他包改为 `>=当前版本`（无需上界，因为不受 3.9 影响）

    Per D-09: 添加 `xlrd>=2.0.0`（.xls 旧格式支持，已在代码中使用但未声明）

    Per D-11: 移除以下未使用依赖：
    - `psycopg2-binary==2.9.10` -- 系统只用 SQLite
    - `asyncpg==0.30.0` -- 系统只用 SQLite
    - `loguru==0.7.3` -- 未使用，系统用 stdlib logging

    Per D-10: 从 `requirements.server.txt` 合并实际使用的依赖到主文件：
    - 添加 `PyJWT>=2.12.0` -- 被 auth 模块使用（验证：`grep -rn "import jwt" backend/app/`）
    - 添加 `pwdlib[bcrypt]>=0.3.0` -- 被 auth 模块使用（验证：`grep -rn "import pwdlib\|from pwdlib" backend/app/`）
    - 不添加 `python-jose[cryptography]` 和 `passlib[bcrypt]`（未使用）
    - 删除 `backend/requirements.server.txt` 文件

    写入文件时确保为 UTF-8 编码，无 BOM。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -c "psycopg2" backend/requirements.txt | xargs test 0 -eq && grep -c "asyncpg" backend/requirements.txt | xargs test 0 -eq && grep -c "loguru" backend/requirements.txt | xargs test 0 -eq && grep "fastapi" backend/requirements.txt | grep -q "<0.130.0" && grep "xlrd" backend/requirements.txt | grep -q ">=2.0.0" && grep "PyJWT" backend/requirements.txt | grep -q ">=2.12.0" && test ! -f backend/requirements.server.txt && echo "PASS: dependencies cleaned"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/requirements.txt` 包含 `fastapi>=0.115.0,<0.130.0`
    - `backend/requirements.txt` 包含 `pandas>=2.2.3,<3.0.0`
    - `backend/requirements.txt` 包含 `pydantic>=2.10.3,<2.13.0`
    - `backend/requirements.txt` 包含 `pydantic-settings>=2.6.1,<2.12.0`
    - `backend/requirements.txt` 包含 `sqlalchemy>=2.0.36,<2.1.0`
    - `backend/requirements.txt` 包含 `xlrd>=2.0.0`
    - `backend/requirements.txt` 包含 `PyJWT>=2.12.0`
    - `backend/requirements.txt` 包含 `pwdlib[bcrypt]>=0.3.0`
    - `backend/requirements.txt` 不包含 `psycopg2-binary`、`asyncpg`、`loguru`
    - `backend/requirements.server.txt` 已删除
    - `file backend/requirements.txt` 显示 ASCII 或 UTF-8（非 UTF-16）
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>依赖版本锁定上界确保 3.9 兼容，xlrd 已声明，未使用依赖已移除，server.txt 已合并删除</done>
</task>

</tasks>

<verification>
- `grep -rn "slots=True" backend/app/ --include="*.py"` 返回 0 行
- `grep "fastapi" backend/requirements.txt` 显示 `>=0.115.0,<0.130.0`
- `grep "xlrd" backend/requirements.txt` 显示 `>=2.0.0`
- `test ! -f backend/requirements.server.txt` 成功
- `pytest backend/tests/ -x -q` 全部通过
</verification>

<success_criteria>
- 后端代码在 Python 3.9 环境下可以正常导入（无 slots=True、PEP 604 unions 均受 from __future__ 保护）
- 依赖版本锁定确保不会自动升级到不兼容 Python 3.9 的版本
- 所有实际使用的依赖声明在单一 requirements.txt 中
- 全部后端测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-foundation-deploy-compat/13-01-SUMMARY.md`
</output>
