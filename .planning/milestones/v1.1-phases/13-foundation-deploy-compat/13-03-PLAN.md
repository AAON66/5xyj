---
phase: 13-foundation-deploy-compat
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - backend/app/mappings/regions.py
  - backend/app/utils/filename_utils.py
  - backend/app/validators/constants.py
  - backend/app/services/region_detection_service.py
  - backend/app/services/import_service.py
  - backend/app/services/aggregate_service.py
  - backend/app/services/matching_service.py
  - backend/app/exporters/template_exporter.py
  - backend/app/api/v1/employees.py
  - backend/tests/test_auth_api.py
  - backend/tests/test_employee_portal_api.py
  - backend/tests/test_employee_master_api.py
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "REGION_LABELS 只在 backend/app/mappings/regions.py 中定义，其他文件全部 import"
    - "FILENAME_NOISE, DATE_PATTERN, infer_company_name_from_filename 只在 backend/app/utils/filename_utils.py 中定义"
    - "ID_NUMBER_PATTERN, NON_MAINLAND_ID_NUMBER_PATTERN 只在 backend/app/validators/constants.py 中定义"
    - "自助查询端点有 require_authenticated_user 依赖，测试已更新"
    - "全部后端测试通过"
  artifacts:
    - path: "backend/app/mappings/regions.py"
      provides: "REGION_LABELS 单一定义"
      contains: "REGION_LABELS"
    - path: "backend/app/utils/filename_utils.py"
      provides: "文件名工具函数单一定义"
      contains: "FILENAME_NOISE"
    - path: "backend/app/validators/constants.py"
      provides: "ID 号正则单一定义"
      contains: "ID_NUMBER_PATTERN"
  key_links:
    - from: "backend/app/services/import_service.py"
      to: "backend/app/mappings/regions.py"
      via: "from backend.app.mappings.regions import REGION_LABELS"
      pattern: "from backend\\.app\\.mappings\\.regions import"
    - from: "backend/app/services/import_service.py"
      to: "backend/app/utils/filename_utils.py"
      via: "from backend.app.utils.filename_utils import"
      pattern: "from backend\\.app\\.utils\\.filename_utils import"
    - from: "backend/app/services/matching_service.py"
      to: "backend/app/validators/constants.py"
      via: "from backend.app.validators.constants import"
      pattern: "from backend\\.app\\.validators\\.constants import"
---

<objective>
技术债常量合并 + 自助查询端点认证修复

Purpose: 消除重复常量定义（REGION_LABELS、FILENAME_NOISE/DATE_PATTERN、ID_NUMBER_PATTERN），修复自助查询端点认证一致性并同步更新受影响测试
Output: 3 个新共享模块 + 调用方统一 import + 端点认证一致 + 测试更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-foundation-deploy-compat/13-CONTEXT.md
@.planning/phases/13-foundation-deploy-compat/13-RESEARCH.md
@.planning/phases/13-foundation-deploy-compat/13-01-SUMMARY.md

<interfaces>
From backend/app/services/region_detection_service.py (canonical REGION_LABELS, around line 20):
```python
REGION_LABELS = {
    'guangzhou': '广州', 'shenzhen': '深圳', 'hangzhou': '杭州',
    'xiamen': '厦门', 'wuhan': '武汉', 'changsha': '长沙',
}
```

From backend/app/services/import_service.py (duplicated constants):
```python
FILENAME_NOISE = (...)  # tuples of noise strings
DATE_PATTERN = re.compile(...)
def _infer_company_name_from_filename(...): ...
```

From backend/app/services/matching_service.py (duplicated regex):
```python
ID_NUMBER_PATTERN = re.compile(r'...')
NON_MAINLAND_ID_NUMBER_PATTERN = re.compile(r'...')
```

From backend/tests/test_auth_api.py (self-service treated as public):
```python
def test_employee_self_service_query_remains_public() -> None:
    client = build_test_client('self_service_public')
    with client:
        response = client.post(
            '/api/v1/employees/self-service/query',
            json={'person_name': 'missing-employee', 'id_number': '440101199001019999'},
        )
    assert response.status_code == 404
```

From backend/tests/test_employee_portal_api.py (self-service with auth_enabled=False):
```python
def test_old_query_endpoint_still_works() -> None:
    client, _settings, _session_factory = build_test_context('old_endpoint', auth_enabled=False)
    with client:
        response = client.post(
            '/api/v1/employees/self-service/query',
            json={'person_name': 'nobody', 'id_number': '000000000000000000'},
        )
    assert response.status_code == 404
```

From frontend/src/services/employees.ts (client calls with auth):
```typescript
const response = await apiClient.post<ApiSuccessResponse<EmployeeSelfServiceResult>>(
  '/employees/self-service/query', input
);
```
Note: apiClient already attaches auth headers, so frontend is compatible with auth requirement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 常量合并到共享模块（REGION_LABELS + FILENAME_NOISE + ID_NUMBER_PATTERN）</name>
  <files>
    backend/app/mappings/regions.py,
    backend/app/utils/filename_utils.py,
    backend/app/validators/constants.py,
    backend/app/services/region_detection_service.py,
    backend/app/services/import_service.py,
    backend/app/services/aggregate_service.py,
    backend/app/services/matching_service.py,
    backend/app/exporters/template_exporter.py
  </files>
  <read_first>
    backend/app/services/region_detection_service.py,
    backend/app/services/import_service.py,
    backend/app/services/aggregate_service.py,
    backend/app/services/matching_service.py,
    backend/app/exporters/template_exporter.py,
    backend/app/mappings/__init__.py,
    backend/app/utils/__init__.py,
    backend/app/validators/__init__.py
  </read_first>
  <action>
    Per D-12（REGION_LABELS 合并）：
    1. 新建 `backend/app/mappings/regions.py`，将 `REGION_LABELS` 字典从 `region_detection_service.py` 移入：
       ```python
       from __future__ import annotations

       REGION_LABELS: dict[str, str] = {
           'guangzhou': '广州',
           'shenzhen': '深圳',
           'hangzhou': '杭州',
           'xiamen': '厦门',
           'wuhan': '武汉',
           'changsha': '长沙',
       }
       ```
    2. 修改以下文件，删除本地 REGION_LABELS 定义，改为 import：
       - `region_detection_service.py`: 删除本地定义，添加 `from backend.app.mappings.regions import REGION_LABELS`
       - `import_service.py`: 删除本地定义，添加 `from backend.app.mappings.regions import REGION_LABELS`
       - `template_exporter.py`: 删除本地定义，添加 `from backend.app.mappings.regions import REGION_LABELS`
       - `aggregate_service.py`: 检查当前导入来源，改为 `from backend.app.mappings.regions import REGION_LABELS`

    回应 REVIEWS.md 建议：regions.py 放在 mappings/ 下（叶子模块），不是 services/ 下，
    避免循环导入和语义漂移。mappings/ 目录已有 manual_field_aliases.py，属性归类合理。

    Per D-13（FILENAME_NOISE / DATE_PATTERN / _infer_company_name_from_filename 合并）：
    1. 新建 `backend/app/utils/filename_utils.py`：
       - 从 `import_service.py` 中精确复制 `FILENAME_NOISE` 元组、`DATE_PATTERN` 正则、
         `_infer_company_name_from_filename` 函数的完整定义
       - 函数改名为 `infer_company_name_from_filename`（移除下划线前缀，现在是公开 API）
       - 文件首行添加 `from __future__ import annotations`
    2. 修改 `import_service.py`：删除这三个本地定义，添加：
       `from backend.app.utils.filename_utils import FILENAME_NOISE, DATE_PATTERN, infer_company_name_from_filename`
       将所有 `_infer_company_name_from_filename(...)` 调用改为 `infer_company_name_from_filename(...)`
    3. 修改 `aggregate_service.py`：同样删除本地重复定义，改为从 filename_utils 导入
       将所有 `_infer_company_name_from_filename(...)` 调用改为 `infer_company_name_from_filename(...)`

    回应 REVIEWS.md MEDIUM 级别关注：不仅合并常量，也合并 `_infer_company_name_from_filename` 
    函数逻辑到共享模块，避免 import_service 和 aggregate_service 各自维护一份。

    Per D-14（ID_NUMBER_PATTERN / NON_MAINLAND_ID_NUMBER_PATTERN 合并）：
    1. 新建 `backend/app/validators/constants.py`：
       - 从 `matching_service.py` 中精确复制两个正则常量的完整定义
       - 文件首行添加 `from __future__ import annotations`
       ```python
       from __future__ import annotations
       import re

       ID_NUMBER_PATTERN = re.compile(r'...')  # 精确复制 matching_service.py 中的值
       NON_MAINLAND_ID_NUMBER_PATTERN = re.compile(r'...')  # 精确复制
       ```
    2. 修改 `matching_service.py`：删除本地定义，添加：
       `from backend.app.validators.constants import ID_NUMBER_PATTERN, NON_MAINLAND_ID_NUMBER_PATTERN`
    3. 修改 `template_exporter.py`：删除本地定义，添加同样的 import

    关键注意事项：
    - 先阅读每个文件的完整常量定义值，确保提取的值完全一致
    - 如果不同文件的"重复"常量值略有差异，以 canonical source 为准并记录差异
    - 确保新模块的父目录 `__init__.py` 存在
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python -c "from backend.app.mappings.regions import REGION_LABELS; print('regions OK')" && python -c "from backend.app.utils.filename_utils import FILENAME_NOISE, DATE_PATTERN, infer_company_name_from_filename; print('filename_utils OK')" && python -c "from backend.app.validators.constants import ID_NUMBER_PATTERN, NON_MAINLAND_ID_NUMBER_PATTERN; print('constants OK')" && pytest backend/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/mappings/regions.py` 存在且包含 `REGION_LABELS`
    - `backend/app/utils/filename_utils.py` 存在且包含 `FILENAME_NOISE`、`DATE_PATTERN`、`infer_company_name_from_filename`
    - `backend/app/validators/constants.py` 存在且包含 `ID_NUMBER_PATTERN`、`NON_MAINLAND_ID_NUMBER_PATTERN`
    - `grep -rn "^REGION_LABELS\s*[=:]" backend/app/ --include="*.py"` 只在 `mappings/regions.py` 中出现
    - `grep -rn "^FILENAME_NOISE\s*=" backend/app/ --include="*.py"` 只在 `utils/filename_utils.py` 中出现
    - `grep -rn "^ID_NUMBER_PATTERN\s*=" backend/app/ --include="*.py"` 只在 `validators/constants.py` 中出现
    - `grep -rn "_infer_company_name_from_filename" backend/app/ --include="*.py"` 返回 0 行（已重命名）
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>3 组重复常量和共享函数各自合并到单一共享模块，所有调用方统一 import，测试全通过</done>
</task>

<task type="auto">
  <name>Task 2: 自助查询端点添加显式认证 + 更新受影响测试</name>
  <files>
    backend/app/api/v1/employees.py,
    backend/tests/test_auth_api.py,
    backend/tests/test_employee_portal_api.py,
    backend/tests/test_employee_master_api.py
  </files>
  <read_first>
    backend/app/api/v1/employees.py,
    backend/app/api/v1/router.py,
    backend/app/dependencies.py,
    backend/tests/test_auth_api.py,
    backend/tests/test_employee_portal_api.py,
    backend/tests/test_employee_master_api.py
  </read_first>
  <action>
    Per D-16: 给 `/employees/self-service/query` 端点添加显式的 `_user=Depends(require_authenticated_user)` 参数。

    回应 REVIEWS.md HIGH 级别关注：这是一个行为变更（breaking change）。
    当前状态：
    - router 级别有 `require_role("admin", "hr")` 保护，所以端点实际上已需要 admin/HR 角色
    - 但测试 `test_employee_self_service_query_remains_public` 用 `auth_enabled=False` 绕过了认证
    - 前端 `employees.ts` 通过 `apiClient` 调用，`apiClient` 已附带 auth headers

    修改策略（一致性修复，不改变安全模型）：
    1. 修改 `backend/app/api/v1/employees.py` 中 `employee_self_service_query_endpoint` 的签名，
       添加 `_user=Depends(require_authenticated_user)`
    2. 更新测试以匹配新行为：

    `backend/tests/test_auth_api.py`:
    找到 `test_employee_self_service_query_remains_public` 函数。
    这个测试断言该端点是 public（无需认证）。修改为：
    - 重命名为 `test_employee_self_service_query_requires_auth`
    - 改为使用不带认证的客户端请求，断言返回 401
    - 添加带认证的请求测试，断言返回 404（找不到员工时的正常响应）

    `backend/tests/test_employee_portal_api.py`:
    找到 `test_old_query_endpoint_still_works`。
    当前用 `auth_enabled=False` 构建 context。修改为：
    - 用 `auth_enabled=True` 构建 context
    - 请求时附带有效的 admin/HR token
    - 断言返回 404（正常的"未找到"响应）

    `backend/tests/test_employee_master_api.py`:
    检查所有引用 `self-service/query` 的测试，确认它们已经使用认证请求。
    如果有未使用认证的测试，添加 auth header。

    前端无需修改：`apiClient` 已自动附带认证 token。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -A 5 "self-service/query" backend/app/api/v1/employees.py | grep -q "require_authenticated_user" && pytest backend/tests/test_auth_api.py backend/tests/test_employee_portal_api.py backend/tests/test_employee_master_api.py -v && pytest backend/tests/ -x -q && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `employees.py` 中 `employee_self_service_query_endpoint` 包含 `Depends(require_authenticated_user)`
    - `test_auth_api.py` 中不再有 `test_employee_self_service_query_remains_public`（已重命名或更新）
    - `test_employee_portal_api.py` 中 `test_old_query_endpoint_still_works` 使用认证请求
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>自助查询端点有显式认证依赖，所有受影响测试已更新，前端无需修改（apiClient 已带 auth）</done>
</task>

</tasks>

<verification>
- `grep -rn "^REGION_LABELS\s*[=:]" backend/app/` 只返回 `mappings/regions.py`
- `grep -rn "^FILENAME_NOISE\s*=" backend/app/` 只返回 `utils/filename_utils.py`
- `grep -rn "^ID_NUMBER_PATTERN\s*=" backend/app/` 只返回 `validators/constants.py`
- self-service 端点有 require_authenticated_user
- 所有测试绿色通过
- `pytest backend/tests/ -x -q` 全部通过
</verification>

<success_criteria>
- 常量不再重复定义，任何修改只需改一处
- 共享函数 infer_company_name_from_filename 也已合并（不仅是常量）
- 自助查询端点认证一致，测试已同步更新，前端已兼容
- 全部后端测试通过
- 双模板导出功能不受影响（常量合并为纯重构，不改业务逻辑）
</success_criteria>

<output>
After completion, create `.planning/phases/13-foundation-deploy-compat/13-03-SUMMARY.md`
</output>
