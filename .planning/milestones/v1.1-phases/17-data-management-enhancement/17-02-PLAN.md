---
phase: 17-data-management-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/data_management.py
  - backend/app/services/data_management_service.py
  - backend/app/schemas/data_management.py
  - backend/tests/test_data_management_service.py
  - frontend/src/services/dataManagement.ts
  - frontend/src/pages/DataManagement.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "用户可多选地区、公司、账期，表格显示匹配结果"
    - "每个多选下拉顶部有全选选项"
    - "选地区后公司下拉只显示已选地区下的公司，级联联动正确"
    - "匹配状态过滤提供全部/已匹配/未匹配三个选项，默认已匹配"
    - "未匹配包含所有非 MATCHED 状态的记录"
    - "URL searchParams 正确保存多选状态"
  artifacts:
    - path: "backend/app/api/v1/data_management.py"
      provides: "多值 Query 参数端点"
      contains: "List[str]"
    - path: "backend/app/services/data_management_service.py"
      provides: "多值过滤 + 匹配状态过滤逻辑"
      contains: ".in_("
    - path: "backend/tests/test_data_management_service.py"
      provides: "多值过滤和匹配状态过滤的单元测试"
      contains: "test_list_records_multi_region"
    - path: "frontend/src/pages/DataManagement.tsx"
      provides: "多选下拉 + 匹配状态过滤 UI"
      contains: "mode=\"multiple\""
  key_links:
    - from: "frontend/src/pages/DataManagement.tsx"
      to: "frontend/src/services/dataManagement.ts"
      via: "fetchNormalizedRecords/fetchFilterOptions"
      pattern: "fetchNormalizedRecords"
    - from: "frontend/src/services/dataManagement.ts"
      to: "backend/app/api/v1/data_management.py"
      via: "HTTP GET with multi-value params"
      pattern: "urlParams.append"
    - from: "backend/app/api/v1/data_management.py"
      to: "backend/app/services/data_management_service.py"
      via: "list_normalized_records/get_filter_options"
      pattern: "list_normalized_records"
---

<objective>
将数据管理页面的地区/公司/账期筛选改为多选，新增匹配状态过滤下拉，后端支持多值参数和 JOIN MatchResult 过滤。

Purpose: HR 需要同时查看多个地区/公司的数据，并能快速区分已匹配/未匹配记录（D-01 至 D-07）。
Output: 后端多值 API + 前端多选 UI + 匹配状态过滤 + 单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-data-management-enhancement/17-CONTEXT.md
@.planning/phases/17-data-management-enhancement/17-RESEARCH.md
@.planning/phases/17-data-management-enhancement/17-UI-SPEC.md

<interfaces>
<!-- 后端当前接口签名 -->

From backend/app/api/v1/data_management.py:
```python
@router.get('/records')
def list_records_endpoint(
    region: Optional[str] = Query(default=None),
    company_name: Optional[str] = Query(default=None),
    billing_period: Optional[str] = Query(default=None),
    page: int = Query(default=0, ge=0),
    page_size: int = Query(default=20, ge=1, le=100),
    db: Session = Depends(get_db),
): ...

@router.get('/filter-options')
def filter_options_endpoint(
    region: Optional[str] = Query(default=None),
    company_name: Optional[str] = Query(default=None),
    db: Session = Depends(get_db),
): ...
```

From backend/app/services/data_management_service.py:
```python
def list_normalized_records(db, *, region=None, company_name=None, billing_period=None, page=0, page_size=20) -> PaginatedRecordsRead
def get_filter_options(db, *, region=None, company_name=None) -> FilterOptionsRead
def get_employee_summary(db, *, region=None, company_name=None, billing_period=None, page=0, page_size=20) -> PaginatedEmployeeSummaryRead
def get_period_summary(db, *, region=None, company_name=None, page=0, page_size=20) -> PaginatedPeriodSummaryRead
```

From backend/app/models/match_result.py:
```python
class MatchResult(Base):
    __table_args__ = (UniqueConstraint("normalized_record_id"),)
    batch_id: Mapped[str]
    normalized_record_id: Mapped[str]
    match_status: Mapped[MatchStatus]  # MATCHED/UNMATCHED/DUPLICATE/LOW_CONFIDENCE/MANUAL
```

From backend/app/models/enums.py:
```python
class MatchStatus(str, Enum):
    MATCHED = "matched"
    UNMATCHED = "unmatched"
    DUPLICATE = "duplicate"
    LOW_CONFIDENCE = "low_confidence"
    MANUAL = "manual"
```

From frontend/src/services/dataManagement.ts:
```typescript
export async function fetchNormalizedRecords(params: {
  region?: string; companyName?: string; billingPeriod?: string; page?: number; pageSize?: number;
}): Promise<PaginatedRecords>

export async function fetchFilterOptions(params?: {
  region?: string; companyName?: string;
}): Promise<FilterOptions>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 后端多值参数 + 匹配状态过滤 + 测试</name>
  <files>backend/app/api/v1/data_management.py, backend/app/services/data_management_service.py, backend/app/schemas/data_management.py, backend/tests/test_data_management_service.py</files>
  <read_first>
    - backend/app/api/v1/data_management.py
    - backend/app/services/data_management_service.py
    - backend/app/schemas/data_management.py
    - backend/app/models/match_result.py
    - backend/app/models/enums.py
    - backend/app/models/normalized_record.py
  </read_first>
  <behavior>
    - Test 1 (test_list_records_multi_region): 传入 regions=["guangzhou","shenzhen"] 返回两个地区的记录
    - Test 2 (test_list_records_multi_company): 传入 company_names=["A","B"] 返回两个公司的记录
    - Test 3 (test_list_records_match_filter_matched): match_filter="matched" 只返回 MatchStatus.MATCHED 的记录
    - Test 4 (test_list_records_match_filter_unmatched): match_filter="unmatched" 返回无 MatchResult 或状态非 MATCHED 的记录
    - Test 5 (test_filter_options_multi_region): 传入 regions=["guangzhou","shenzhen"] 返回两个地区下的公司列表
    - Test 6 (test_list_records_match_filter_all): match_filter=None 返回所有记录（不做 JOIN）
  </behavior>
  <action>
1. 在 backend/app/services/data_management_service.py 中修改所有函数签名：
   - `region: Optional[str]` -> `regions: Optional[list[str]] = None`
   - `company_name: Optional[str]` -> `company_names: Optional[list[str]] = None`
   - `billing_period: Optional[str]` -> `billing_periods: Optional[list[str]] = None`
   - 在 `list_normalized_records` 中新增参数 `match_filter: Optional[str] = None`

2. 修改 `list_normalized_records` 的过滤逻辑：
   ```python
   if regions:
       query = query.filter(NormalizedRecord.region.in_(regions))
   if company_names:
       query = query.filter(NormalizedRecord.company_name.in_(company_names))
   if billing_periods:
       query = query.filter(NormalizedRecord.billing_period.in_(billing_periods))

   if match_filter == 'matched':
       query = query.join(MatchResult, MatchResult.normalized_record_id == NormalizedRecord.id)
       query = query.filter(MatchResult.match_status == MatchStatus.MATCHED)
   elif match_filter == 'unmatched':
       query = query.outerjoin(MatchResult, MatchResult.normalized_record_id == NormalizedRecord.id)
       query = query.filter(
           or_(MatchResult.id.is_(None), MatchResult.match_status != MatchStatus.MATCHED)
       )
   ```
   需要 import: `from sqlalchemy import or_` 和 `from backend.app.models.match_result import MatchResult` 和 `from backend.app.models.enums import MatchStatus`

3. 修改 `get_filter_options` 同样改为 `regions: Optional[list[str]]` 和 `company_names: Optional[list[str]]`，内部使用 `.in_()`:
   ```python
   if regions:
       company_query = company_query.filter(NormalizedRecord.region.in_(regions))
   # period_query 同理
   if company_names:
       period_query = period_query.filter(NormalizedRecord.company_name.in_(company_names))
   ```

4. 修改 `get_employee_summary` 和 `get_period_summary` 同样改为多值参数并使用 `.in_()`。

5. 在 backend/app/api/v1/data_management.py 中修改端点参数：
   ```python
   from typing import List, Optional
   
   @router.get('/records')
   def list_records_endpoint(
       region: Optional[List[str]] = Query(default=None),
       company_name: Optional[List[str]] = Query(default=None),
       billing_period: Optional[List[str]] = Query(default=None),
       match_status: Optional[str] = Query(default=None),
       page: int = Query(default=0, ge=0),
       page_size: int = Query(default=20, ge=1, le=100),
       db: Session = Depends(get_db),
   ):
       result = list_normalized_records(
           db,
           regions=region,
           company_names=company_name,
           billing_periods=billing_period,
           match_filter=match_status,
           page=page,
           page_size=page_size,
       )
   ```
   所有四个端点 (records, filter-options, summary/employees, summary/periods) 都改为 `Optional[List[str]]`。

6. 创建 backend/tests/test_data_management_service.py，测试多值过滤和匹配状态过滤。使用 SQLite 内存数据库创建测试 fixture，插入测试数据后验证过滤结果。

7. 运行测试验证。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python -m pytest backend/tests/test_data_management_service.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/api/v1/data_management.py 包含 `region: Optional[List[str]] = Query(default=None)`
    - backend/app/api/v1/data_management.py 包含 `match_status: Optional[str] = Query(default=None)`
    - backend/app/services/data_management_service.py 包含 `regions: Optional[list[str]]`
    - backend/app/services/data_management_service.py 包含 `NormalizedRecord.region.in_(regions)`
    - backend/app/services/data_management_service.py 包含 `MatchResult.match_status == MatchStatus.MATCHED`
    - backend/app/services/data_management_service.py 包含 `MatchResult.match_status != MatchStatus.MATCHED`
    - backend/tests/test_data_management_service.py 包含 `def test_list_records_multi_region`
    - backend/tests/test_data_management_service.py 包含 `def test_list_records_match_filter_matched`
    - `pytest backend/tests/test_data_management_service.py -x` 退出码为 0
    - `pytest backend/tests/ -x` 退出码为 0
  </acceptance_criteria>
  <done>后端 4 个端点支持多值 Query 参数，records 端点支持 match_status 过滤，服务层使用 .in_() 和 JOIN MatchResult，测试覆盖多值过滤和匹配状态过滤</done>
</task>

<task type="auto">
  <name>Task 2: 前端多选下拉 + 匹配状态过滤 + URL 持久化</name>
  <files>frontend/src/pages/DataManagement.tsx, frontend/src/services/dataManagement.ts</files>
  <read_first>
    - frontend/src/pages/DataManagement.tsx
    - frontend/src/services/dataManagement.ts
    - .planning/phases/17-data-management-enhancement/17-UI-SPEC.md
  </read_first>
  <action>
1. 修改 frontend/src/services/dataManagement.ts:
   - `fetchNormalizedRecords` 参数类型: `region?: string` -> `regions?: string[]`，`companyName?: string` -> `companyNames?: string[]`，`billingPeriod?: string` -> `billingPeriods?: string[]`，新增 `matchStatus?: string`
   - 参数序列化改为多值: 
     ```typescript
     if (params.regions?.length) {
       params.regions.forEach(r => searchParams.append('region', r));
     }
     if (params.companyNames?.length) {
       params.companyNames.forEach(c => searchParams.append('company_name', c));
     }
     if (params.billingPeriods?.length) {
       params.billingPeriods.forEach(p => searchParams.append('billing_period', p));
     }
     if (params.matchStatus) searchParams.set('match_status', params.matchStatus);
     ```
   - `fetchFilterOptions` 参数同样改为 `regions?: string[]`、`companyNames?: string[]`
   - `fetchEmployeeSummary` 和 `fetchPeriodSummary` 同步修改参数类型

2. 修改 frontend/src/pages/DataManagement.tsx:
   - URL 参数读取改为多选（逗号分隔）:
     ```typescript
     const regions = searchParams.get('region')?.split(',').filter(Boolean) || [];
     const companies = searchParams.get('company')?.split(',').filter(Boolean) || [];
     const periods = searchParams.get('period')?.split(',').filter(Boolean) || [];
     const matchStatus = searchParams.get('matchStatus') || 'matched';
     ```
   - URL 参数写入: 多选值用逗号连接 `regions.join(',')` 存入 searchParams

   - 地区 Select 改为多选:
     ```tsx
     <Select
       mode="multiple"
       showSearch
       allowClear
       maxTagCount={2}
       maxTagPlaceholder={(omitted) => `+${omitted.length}...`}
       placeholder="请选择地区"
       value={regions.length > 0 ? regions : undefined}
       onChange={handleRegionChange}
       style={{ width: '100%', minWidth: 180 }}
       options={[
         { label: '全选', value: '__ALL__', style: { fontWeight: 600 } },
         ...filterOptions.regions.map(r => ({ label: r, value: r })),
       ]}
     />
     ```
   - 公司和账期 Select 同样改为 mode="multiple"，保持级联关系
   
   - 新增匹配状态 Select（单选，非多选）:
     ```tsx
     <Col xs={24} sm={12} md={4}>
       <Select
         value={matchStatus}
         onChange={handleMatchStatusChange}
         style={{ width: '100%' }}
         options={[
           { label: '全部', value: 'all' },
           { label: '已匹配', value: 'matched' },
           { label: '未匹配', value: 'unmatched' },
         ]}
       />
     </Col>
     ```

   - handleRegionChange 改为处理多选:
     ```typescript
     function handleRegionChange(newRegions: string[]) {
       if (newRegions.includes('__ALL__')) {
         // 全选/取消全选逻辑
         const allSelected = regions.length === filterOptions.regions.length;
         const value = allSelected ? '' : filterOptions.regions.join(',');
         updateParams({ region: value, company: '', period: '', page: '0' });
       } else {
         updateParams({ region: newRegions.join(','), company: '', period: '', page: '0' });
       }
     }
     ```
   - handleCompanyChange 和 handlePeriodChange 同样改为多选处理

   - handleMatchStatusChange:
     ```typescript
     function handleMatchStatusChange(value: string) {
       updateParams({ matchStatus: value, page: '0' });
     }
     ```

   - 级联过滤逻辑: fetchFilterOptions 调用时传入 regions 数组和 companyNames 数组
   - 级联清理: 当上级选择变化时，检查下级已选值是否仍在选项中，不在则清除

   - loadData 调用改为传入数组参数:
     ```typescript
     const filterParams = {
       regions: regions.length > 0 ? regions : undefined,
       companyNames: companies.length > 0 ? companies : undefined,
       billingPeriods: periods.length > 0 ? periods : undefined,
       matchStatus: matchStatus !== 'all' ? matchStatus : undefined,
       page,
       pageSize,
     };
     ```

   - 匹配状态列（detailColumns 中 key='match_status' 的列）保持现有渲染逻辑（通过 employee_id 判断），因为后端过滤逻辑已使用 MatchResult 表。

   - handleResetFilters 更新: 重置 matchStatus 回 'matched'
     ```typescript
     function handleResetFilters() {
       updateParams({ region: '', company: '', period: '', matchStatus: 'matched', page: '0' });
       setSearchText('');
     }
     ```

   - 调整筛选栏 Col 宽度: 4 个下拉 + 搜索框 + 按钮需要重新分配 Col 的 md 值。建议:
     - 地区: md={5}
     - 公司: md={5}
     - 账期: md={4}
     - 匹配状态: md={3}
     - 搜索框: md={4}
     - 按钮: 自适应

3. 确保 matchStatus 默认值为 'matched' — 当 URL 中没有 matchStatus 参数时默认使用 'matched'。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/DataManagement.tsx 包含 `mode="multiple"` 至少 3 处（地区/公司/账期）
    - frontend/src/pages/DataManagement.tsx 包含 `__ALL__` 全选选项
    - frontend/src/pages/DataManagement.tsx 包含 `maxTagCount={2}`
    - frontend/src/pages/DataManagement.tsx 包含 `matchStatus` 状态变量
    - frontend/src/pages/DataManagement.tsx 包含 `|| 'matched'` 作为 matchStatus 默认值
    - frontend/src/services/dataManagement.ts 包含 `regions?: string[]`
    - frontend/src/services/dataManagement.ts 包含 `searchParams.append('region', r)` 多值追加模式
    - frontend/src/services/dataManagement.ts 包含 `matchStatus` 参数
    - `npx tsc --noEmit` 退出码为 0
    - `npm run lint` 退出码为 0
  </acceptance_criteria>
  <done>数据管理页面地区/公司/账期改为多选下拉，每个下拉有全选选项，级联关系保持，新增匹配状态过滤（默认已匹配），URL 持久化正确，TypeScript 编译和 lint 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | 用户通过 URL query 参数传入筛选值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Tampering | data_management.py Query params | mitigate | FastAPI `Query(List[str])` 自动类型校验，只接受字符串列表 |
| T-17-03 | Denial of Service | data_management_service.py IN clause | accept | 已有 page_size 限制(max=100)；IN 子句值数量受限于实际地区/公司数量（< 50） |
</threat_model>

<verification>
1. `pytest backend/tests/test_data_management_service.py -x` 通过
2. `pytest backend/tests/ -x` 不破坏已有测试
3. `npm run lint && npm run build` 前端通过
</verification>

<success_criteria>
- 后端 4 个端点支持多值 Query 参数
- records 端点支持 match_status 过滤参数
- 前端 3 个筛选下拉改为多选，每个有全选选项
- 前端新增匹配状态过滤下拉，默认已匹配
- URL searchParams 正确保存多选状态（逗号分隔）
- 级联关系正确（地区 -> 公司 -> 账期）
</success_criteria>

<output>
After completion, create `.planning/phases/17-data-management-enhancement/17-02-SUMMARY.md`
</output>
