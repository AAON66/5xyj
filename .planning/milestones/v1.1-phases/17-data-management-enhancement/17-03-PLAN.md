---
phase: 17-data-management-enhancement
plan: 03
type: execute
wave: 2
depends_on:
  - 17-02
files_modified:
  - backend/app/api/v1/imports.py
  - backend/app/services/import_service.py
  - backend/app/schemas/imports.py
  - backend/tests/test_import_batches_api.py
  - frontend/src/pages/Imports.tsx
  - frontend/src/services/imports.ts
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "删除单个批次前弹窗显示关联的明细记录数、匹配结果数、校验问题数"
    - "批量删除弹窗提示将删除所有关联数据"
    - "删除后关联的 NormalizedRecords、MatchResults、ValidationIssues 被 cascade 清理"
    - "PRAGMA foreign_keys=ON 已在数据库连接中生效"
  artifacts:
    - path: "backend/app/api/v1/imports.py"
      provides: "批次删除影响范围预查询端点"
      contains: "deletion_impact"
    - path: "backend/app/services/import_service.py"
      provides: "影响范围计数函数"
      contains: "get_batch_deletion_impact"
    - path: "frontend/src/pages/Imports.tsx"
      provides: "增强的删除确认弹窗"
      contains: "条明细记录"
  key_links:
    - from: "frontend/src/pages/Imports.tsx"
      to: "backend/app/api/v1/imports.py"
      via: "GET /{batch_id}/deletion-impact"
      pattern: "deletion-impact"
    - from: "backend/app/api/v1/imports.py"
      to: "backend/app/services/import_service.py"
      via: "get_batch_deletion_impact"
      pattern: "get_batch_deletion_impact"
---

<objective>
增强批次删除确认弹窗，在删除前预查询并展示关联数据影响范围，确保 cascade delete 正确生效。

Purpose: 让用户在删除批次前了解将同时删除多少条关联数据（D-08, D-09, D-10），做出知情决策。
Output: 后端预查询端点 + 前端增强弹窗 + 测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-data-management-enhancement/17-CONTEXT.md
@.planning/phases/17-data-management-enhancement/17-RESEARCH.md
@.planning/phases/17-data-management-enhancement/17-UI-SPEC.md

<interfaces>
<!-- 后端现有删除接口 -->

From backend/app/api/v1/imports.py:
```python
@router.delete('/{batch_id}', status_code=status.HTTP_204_NO_CONTENT)
def delete_import_batch_endpoint(request: Request, batch_id: str, db: Session = Depends(get_db)):
    delete_import_batch(db, request.app.state.settings, batch_id)

@router.post('/bulk-delete')
def bulk_delete_import_batches_endpoint(request: Request, payload: DeleteImportBatchesInput, db: Session = Depends(get_db)):
    result = bulk_delete_import_batches(db, request.app.state.settings, payload.batch_ids)
```

From backend/app/services/import_service.py:
```python
def delete_import_batch(db: Session, settings: Settings, batch_id: str) -> None:
    batch = _get_import_batch_for_delete(db, batch_id)
    cleanup_paths = _collect_batch_cleanup_paths(batch, settings.outputs_path)
    db.delete(batch)
    db.commit()
    _cleanup_batch_artifacts(cleanup_paths, upload_batch_dir=settings.upload_path / batch_id)
```

From frontend/src/pages/Imports.tsx (handleDeleteBatch):
```typescript
async function handleDeleteBatch(batchId: string, name: string) {
    Modal.confirm({
      title: '确认删除',
      content: `确认删除批次"${name}"吗？这会同时删除已上传文件和相关导出结果，且无法撤销。`,
      okText: '确认删除',
      okType: 'danger',
      onOk: async () => { ... }
    });
}
```

From backend/app/models:
```python
# NormalizedRecord -> ForeignKey("import_batches.id", ondelete="CASCADE")
# MatchResult -> ForeignKey("import_batches.id", ondelete="CASCADE")
# ValidationIssue -> ForeignKey("import_batches.id", ondelete="CASCADE")
```

From backend/app/core/database.py:34:
```python
# event listener sets PRAGMA foreign_keys=ON for SQLite connections
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端批次删除影响范围预查询端点</name>
  <files>backend/app/api/v1/imports.py, backend/app/services/import_service.py, backend/app/schemas/imports.py, backend/tests/test_import_batches_api.py</files>
  <read_first>
    - backend/app/api/v1/imports.py
    - backend/app/services/import_service.py
    - backend/app/schemas/imports.py
    - backend/app/models/import_batch.py
    - backend/app/models/normalized_record.py
    - backend/app/models/match_result.py
    - backend/app/models/validation_issue.py
    - backend/app/core/database.py
    - backend/tests/test_import_batches_api.py
  </read_first>
  <action>
1. 在 backend/app/schemas/imports.py 中新增 schema:
   ```python
   class BatchDeletionImpactRead(BaseModel):
       batch_id: str
       batch_name: str
       record_count: int
       match_count: int
       issue_count: int
   ```

2. 在 backend/app/services/import_service.py 中新增函数:
   ```python
   def get_batch_deletion_impact(db: Session, batch_id: str) -> BatchDeletionImpactRead:
       from backend.app.models.normalized_record import NormalizedRecord
       from backend.app.models.match_result import MatchResult
       from backend.app.models.validation_issue import ValidationIssue

       batch = db.query(ImportBatch).filter(ImportBatch.id == batch_id).first()
       if not batch:
           raise BatchNotFoundError(f"Batch {batch_id} not found")

       record_count = db.query(func.count(NormalizedRecord.id)).filter(
           NormalizedRecord.batch_id == batch_id
       ).scalar() or 0
       match_count = db.query(func.count(MatchResult.id)).filter(
           MatchResult.batch_id == batch_id
       ).scalar() or 0
       issue_count = db.query(func.count(ValidationIssue.id)).filter(
           ValidationIssue.batch_id == batch_id
       ).scalar() or 0

       return BatchDeletionImpactRead(
           batch_id=batch_id,
           batch_name=batch.batch_name,
           record_count=record_count,
           match_count=match_count,
           issue_count=issue_count,
       )
   ```
   需要 import: `from sqlalchemy import func`，以及 `from backend.app.schemas.imports import BatchDeletionImpactRead`

3. 在 backend/app/api/v1/imports.py 中新增端点（在 delete 端点之前）:
   ```python
   @router.get('/{batch_id}/deletion-impact', summary="查询删除影响范围")
   def batch_deletion_impact_endpoint(
       batch_id: str,
       db: Session = Depends(get_db),
   ):
       from backend.app.services.import_service import get_batch_deletion_impact
       result = get_batch_deletion_impact(db, batch_id)
       return success_response(result.model_dump(mode='json'))
   ```
   注意引入 get_batch_deletion_impact 到顶部的 import 列表。

4. 在 backend/tests/test_import_batches_api.py 中新增测试 `test_batch_deletion_impact`，验证端点返回正确的 record_count、match_count、issue_count。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python -m pytest backend/tests/test_import_batches_api.py -x -k deletion_impact -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/schemas/imports.py 包含 `class BatchDeletionImpactRead`
    - backend/app/schemas/imports.py 中 BatchDeletionImpactRead 包含 `record_count: int`、`match_count: int`、`issue_count: int`
    - backend/app/services/import_service.py 包含 `def get_batch_deletion_impact`
    - backend/app/api/v1/imports.py 包含 `deletion-impact`
    - backend/tests/test_import_batches_api.py 包含 `test_batch_deletion_impact`
    - `pytest backend/tests/test_import_batches_api.py -x -k deletion_impact` 退出码为 0
  </acceptance_criteria>
  <done>后端新增 GET /{batch_id}/deletion-impact 端点，返回关联数据计数，测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 前端删除确认弹窗增强</name>
  <files>frontend/src/pages/Imports.tsx, frontend/src/services/imports.ts</files>
  <read_first>
    - frontend/src/pages/Imports.tsx
    - frontend/src/services/imports.ts
    - .planning/phases/17-data-management-enhancement/17-UI-SPEC.md
  </read_first>
  <action>
1. 在 frontend/src/services/imports.ts 中新增:
   ```typescript
   export interface BatchDeletionImpact {
     batch_id: string;
     batch_name: string;
     record_count: number;
     match_count: number;
     issue_count: number;
   }

   export async function fetchBatchDeletionImpact(batchId: string): Promise<BatchDeletionImpact> {
     const response = await apiClient.get<ApiSuccessResponse<BatchDeletionImpact>>(
       `/imports/${batchId}/deletion-impact`,
     );
     return response.data.data;
   }
   ```

2. 修改 frontend/src/pages/Imports.tsx 的 handleDeleteBatch 函数:
   - 点击删除时先设置按钮 loading 状态
   - 调用 fetchBatchDeletionImpact(batchId) 获取影响范围
   - 用获取到的数据构建弹窗内容:
     ```typescript
     async function handleDeleteBatch(batchId: string, name: string) {
       setDeletingBatchId(batchId);
       try {
         const impact = await fetchBatchDeletionImpact(batchId);
         Modal.confirm({
           title: '确认删除',
           content: (
             <div>
               <p>确认删除批次"{name}"吗？</p>
               <p>此操作将同时删除：</p>
               <ul>
                 <li>{impact.record_count} 条明细记录</li>
                 <li>{impact.match_count} 条匹配结果</li>
                 <li>{impact.issue_count} 条校验问题</li>
               </ul>
               <p>删除后无法恢复。</p>
             </div>
           ),
           okText: '确认删除',
           okType: 'danger',
           onOk: async () => {
             // 执行删除（现有逻辑）
             await deleteImportBatch(batchId);
             await reloadBatches(selectedBatchId === batchId ? null : undefined);
             setSelectedBatchIds((c) => c.filter((id) => id !== batchId));
             message.success(`批次已删除: ${name}`);
           },
           onCancel: () => {
             setDeletingBatchId(null);
           },
         });
       } catch (error) {
         message.error(normalizeApiError(error).message || '获取删除影响信息失败');
       } finally {
         setDeletingBatchId(null);
       }
     }
     ```

   - 批量删除弹窗 (handleBulkDelete) 内容更新为:
     ```typescript
     content: `确认批量删除 ${selectedBatchIds.length} 个批次吗？此操作将同时删除所有关联的明细记录、匹配结果和校验问题。删除后无法恢复。`,
     ```

3. 确保 import { fetchBatchDeletionImpact } from '../services/imports' 已添加。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/services/imports.ts 包含 `export interface BatchDeletionImpact`
    - frontend/src/services/imports.ts 包含 `export async function fetchBatchDeletionImpact`
    - frontend/src/services/imports.ts 包含 `/imports/${batchId}/deletion-impact`
    - frontend/src/pages/Imports.tsx 包含 `fetchBatchDeletionImpact`
    - frontend/src/pages/Imports.tsx 包含 `条明细记录`
    - frontend/src/pages/Imports.tsx 包含 `条匹配结果`
    - frontend/src/pages/Imports.tsx 包含 `条校验问题`
    - `npx tsc --noEmit` 退出码为 0
    - `npm run lint` 退出码为 0
  </acceptance_criteria>
  <done>删除单个批次弹窗显示关联数据条数，批量删除弹窗提示影响范围，TypeScript 和 lint 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | 用户传入 batch_id 请求删除影响范围 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Information Disclosure | deletion-impact endpoint | mitigate | 端点需要认证（现有 router 已有全局 auth middleware），只返回计数不返回具体数据 |
| T-17-05 | Tampering | batch_id parameter | mitigate | batch_id 为 UUID 格式，SQLAlchemy 参数化查询防止注入 |
</threat_model>

<verification>
1. `pytest backend/tests/test_import_batches_api.py -x` 通过
2. `pytest backend/tests/ -x` 不破坏已有测试
3. `npm run lint && npm run build` 前端通过
</verification>

<success_criteria>
- 后端新增 deletion-impact 端点返回关联数据计数
- 前端单个删除弹窗显示具体影响数量
- 前端批量删除弹窗提示将清理所有关联数据
- PRAGMA foreign_keys=ON 已验证在数据库连接中生效
- cascade delete 正确清理 NormalizedRecords + MatchResults + ValidationIssues
</success_criteria>

<output>
After completion, create `.planning/phases/17-data-management-enhancement/17-03-SUMMARY.md`
</output>
