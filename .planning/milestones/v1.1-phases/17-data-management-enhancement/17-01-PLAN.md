---
phase: 17-data-management-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/mappings/manual_field_aliases.py
  - backend/tests/test_header_normalizer.py
autonomous: true
requirements:
  - DATA-04

must_haves:
  truths:
    - "payment_base 只匹配独立的缴费基数列，不匹配险种子列中的缴费基数"
    - "payment_salary 只匹配独立的缴费工资列，不匹配险种子列中的缴费工资"
    - "修复后重新解析批次可获得正确的 payment_base 值"
  artifacts:
    - path: "backend/app/mappings/manual_field_aliases.py"
      provides: "payment_base 和 payment_salary 的 excludes 规则"
      contains: "excludes="
    - path: "backend/tests/test_header_normalizer.py"
      provides: "payment_base 排除规则的回归测试"
      contains: "test_payment_base"
  key_links:
    - from: "backend/app/mappings/manual_field_aliases.py"
      to: "backend/app/services/header_normalizer.py"
      via: "MANUAL_ALIAS_RULES import"
      pattern: "MANUAL_ALIAS_RULES"
---

<objective>
修复 payment_base 和 payment_salary 字段映射规则，使其不再匹配险种子列中的缴费基数/缴费工资。

Purpose: 解决所有地区个人险种缴费基数显示错误值的问题（D-11, D-12）。根因已定位：AliasRule("payment_base", ("缴费基数",)) 过于宽泛，会匹配到 "基本养老保险(单位缴纳) / 缴费基数" 等险种子列。
Output: 修复后的 AliasRule + 回归测试用例
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-data-management-enhancement/17-CONTEXT.md
@.planning/phases/17-data-management-enhancement/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/mappings/manual_field_aliases.py:
```python
@dataclass(frozen=True)
class AliasRule:
    canonical_field: str
    patterns: tuple[str, ...]
    confidence: float = 0.98
    regions: tuple[str, ...] = ()
    excludes: tuple[str, ...] = ()

    def matches(self, signature: str, region: Optional[str] = None) -> bool:
        # checks patterns are all present in signature, excludes are all absent
```

当前有问题的规则（第 108-110 行）:
```python
AliasRule("payment_salary", ("缴费工资",), confidence=0.94),
AliasRule("payment_base", ("缴费基数",), confidence=0.94),
AliasRule("payment_base", ("基数", "社保"), excludes=("补差",), confidence=0.92),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 payment_base 和 payment_salary 的 AliasRule excludes 并添加回归测试</name>
  <files>backend/app/mappings/manual_field_aliases.py, backend/tests/test_header_normalizer.py</files>
  <read_first>
    - backend/app/mappings/manual_field_aliases.py
    - backend/tests/test_header_normalizer.py
    - backend/app/services/header_normalizer.py
  </read_first>
  <behavior>
    - Test 1: AliasRule("payment_base", ("缴费基数",), excludes=...) 不匹配 "基本养老保险(单位缴纳) / 缴费基数"
    - Test 2: AliasRule("payment_base", ("缴费基数",), excludes=...) 不匹配 "失业保险 / 缴费基数"
    - Test 3: AliasRule("payment_base", ("缴费基数",), excludes=...) 不匹配 "工伤保险 / 缴费基数"
    - Test 4: AliasRule("payment_base", ("缴费基数",), excludes=...) 不匹配 "基本医疗保险(含生育) / 缴费基数"
    - Test 5: AliasRule("payment_base", ("缴费基数",), excludes=...) 匹配独立的 "缴费基数" 列
    - Test 6: AliasRule("payment_salary", ("缴费工资",), excludes=...) 不匹配 "基本养老保险 / 缴费工资"
    - Test 7: AliasRule("payment_salary", ("缴费工资",), excludes=...) 匹配独立的 "缴费工资" 列
  </behavior>
  <action>
1. 在 backend/tests/test_header_normalizer.py 中添加测试函数 `test_payment_base_excludes_insurance_columns` 和 `test_payment_salary_excludes_insurance_columns`，使用 MANUAL_ALIAS_RULES 中的实际规则，对以下签名调用 `rule.matches(signature)`:
   - 应拒绝: "基本养老保险(单位缴纳) / 缴费基数"、"失业保险 / 缴费基数"、"工伤保险 / 缴费基数"、"基本医疗保险(含生育) / 缴费基数"、"职工基本医疗保险 / 缴费基数"、"地方补充医疗 / 缴费基数"
   - 应匹配: "缴费基数" (独立列)
   - payment_salary 类似: 应拒绝 "基本养老保险 / 缴费工资"、应匹配 "缴费工资"

2. 运行测试确认失败 (RED)。

3. 修改 backend/app/mappings/manual_field_aliases.py 第 108-109 行:
   - 将 `AliasRule("payment_salary", ("缴费工资",), confidence=0.94)` 改为:
     `AliasRule("payment_salary", ("缴费工资",), confidence=0.94, excludes=("养老", "医疗", "失业", "工伤", "生育", "补充", "大病", "大额"))`
   - 将 `AliasRule("payment_base", ("缴费基数",), confidence=0.94)` 改为:
     `AliasRule("payment_base", ("缴费基数",), confidence=0.94, excludes=("养老", "医疗", "失业", "工伤", "生育", "补充", "大病", "大额"))`

4. 运行测试确认通过 (GREEN)。

5. 同时验证已有的 payment_base 测试（如果有）不受影响。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python -m pytest backend/tests/test_header_normalizer.py -x -k payment_base -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/mappings/manual_field_aliases.py 中 payment_base 规则包含 `excludes=("养老", "医疗", "失业", "工伤", "生育", "补充", "大病", "大额")`
    - backend/app/mappings/manual_field_aliases.py 中 payment_salary 规则包含 `excludes=("养老", "医疗", "失业", "工伤", "生育", "补充", "大病", "大额")`
    - backend/tests/test_header_normalizer.py 包含 `def test_payment_base_excludes_insurance_columns`
    - `pytest backend/tests/test_header_normalizer.py -x -k payment_base` 退出码为 0
    - `pytest backend/tests/test_header_normalizer.py -x` 退出码为 0（不破坏已有测试）
  </acceptance_criteria>
  <done>payment_base 和 payment_salary 规则添加了 excludes，险种子列中的缴费基数/缴费工资不再被错误映射，回归测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 本 plan 仅修改内部映射规则，不涉及用户输入或外部数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | manual_field_aliases.py | accept | 映射规则为内部代码，不接受外部输入，无篡改风险 |
</threat_model>

<verification>
1. `pytest backend/tests/test_header_normalizer.py -x` 全部通过
2. `pytest backend/tests/ -x` 不破坏已有测试
</verification>

<success_criteria>
- payment_base AliasRule 不再匹配包含险种关键词的签名
- payment_salary AliasRule 不再匹配包含险种关键词的签名
- 独立的 "缴费基数" 和 "缴费工资" 仍可正确匹配
- 所有回归测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/17-data-management-enhancement/17-01-SUMMARY.md`
</output>
