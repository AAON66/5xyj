---
phase: 14
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - frontend/src/pages/Results.tsx
  - frontend/src/pages/Exports.tsx
  - frontend/src/pages/Mappings.tsx
  - frontend/src/pages/AnomalyDetection.tsx
  - frontend/src/pages/Compare.tsx
  - frontend/src/pages/PeriodCompare.tsx
  - frontend/src/pages/FeishuFieldMapping.tsx
  - frontend/src/pages/FeishuSync.tsx
  - frontend/src/pages/ApiKeys.tsx
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "Results/Exports/Mappings/AnomalyDetection/Compare/PeriodCompare/FeishuFieldMapping/FeishuSync/ApiKeys 中的硬编码颜色已全部替换为 AntD token"
    - "Statistic valueStyle 使用 useSemanticColors"
    - "Table columns / 模块顶层 STATUS_COLORS 使用 getChartColors(isDark) 纯函数并包裹在 useMemo 中"
    - "FeishuFieldMapping 的 React Flow Background color 使用 theme.useToken() 动态 token"
    - "PeriodCompare 差异高亮色使用 useSemanticColors (增加=WARNING, 减少=ERROR, 新增=BRAND)"
  artifacts:
    - path: "frontend/src/pages/Results.tsx"
      provides: "三色 Statistic valueStyle token 化"
    - path: "frontend/src/pages/AnomalyDetection.tsx"
      provides: "STATUS_STYLES 映射 + Table render 色用 chartColors"
    - path: "frontend/src/pages/PeriodCompare.tsx"
      provides: "差异高亮色 token 化 + HIGHLIGHT_BG"
    - path: "frontend/src/pages/FeishuFieldMapping.tsx"
      provides: "9+ 硬编码色全部 token 化 + React Flow Background 动态 color"
    - path: "frontend/src/pages/FeishuSync.tsx"
      provides: "顶层 STATUS_COLORS 改为 getChartColors + HIGHLIGHT_BG token"
  key_links:
    - from: "9 个迁移文件"
      to: "useSemanticColors / chartColors / theme.useToken"
      via: "hook 或 getChartColors(isDark)"
      pattern: "useSemanticColors|getChartColors|theme.useToken"
    - from: "FeishuFieldMapping.tsx"
      to: "@xyflow/react Background color prop"
      via: "theme.useToken() colorBorder"
      pattern: "Background.*color"
---

<objective>
对结果/对比/飞书/API 页面（Results/Exports/Mappings/AnomalyDetection/Compare/PeriodCompare/FeishuFieldMapping/FeishuSync/ApiKeys）共约 16 处硬编码内联颜色进行 token 化迁移。重点处理 Table columns / 模块顶层常量（D-03 chartColors 适用场景）、React Flow Background、PeriodCompare 差异高亮色（D-07）。

Purpose: 完成 UX-01 后半部分。
Output: 9 个页面文件的颜色迁移完成，硬编码色清零。

Review feedback addressed in this plan:
- [HIGH] Acceptance criteria now use generic hex audit per file (catches ALL hex literals, not just known ones)
- [MEDIUM] useMemo wraps all factory functions (buildColumns, buildStatusMap) for stable references
- [MEDIUM] chartColors drift noted with comment for post-implementation validation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md
@.planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md
@.planning/phases/14-style-tokens-dark-mode/14-01-PLAN.md
@frontend/src/theme/useSemanticColors.ts
@frontend/src/theme/chartColors.ts
@frontend/src/theme/useThemeMode.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 迁移 Statistic valueStyle 页面（Results/Exports/Mappings/Compare）</name>
  <files>frontend/src/pages/Results.tsx, frontend/src/pages/Exports.tsx, frontend/src/pages/Mappings.tsx, frontend/src/pages/Compare.tsx</files>
  <read_first>
    - frontend/src/pages/Results.tsx (搜索所有 # 开头的颜色值)
    - frontend/src/pages/Exports.tsx (搜索所有 # 开头的颜色值)
    - frontend/src/pages/Mappings.tsx (搜索所有 # 开头的颜色值)
    - frontend/src/pages/Compare.tsx (搜索所有 # 开头的颜色值，特别注意 #3370FF, #f0f0f0, #fafafa 等可能被遗漏的值)
    - frontend/src/theme/useSemanticColors.ts
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Example 4 useSemanticColors 用法、Token Mapping Table)
  </read_first>
  <action>
  **Addresses review concern [HIGH]: 先对每个文件做完整 hex 扫描，确保不遗漏任何硬编码色。**

  对每个文件执行操作前先运行：`grep -n "'#[0-9a-fA-F]" <file>` 获取所有十六进制色位置，逐一替换。

  Results.tsx:
  - import useSemanticColors
  - `const colors = useSemanticColors();`
  - line 323 `color: '#00B42A'` (success) -> `color: colors.SUCCESS`
  - line 326 `color: '#F54A45'` (error) -> `color: colors.ERROR`
  - line 329 `color: '#FF7D00'` (warning) -> `color: colors.WARNING`
  - 扫描并替换任何其他遗漏的 hex 色

  Exports.tsx:
  - import useSemanticColors
  - `const colors = useSemanticColors();`
  - line 269 `color: '#00B42A'` -> `color: colors.SUCCESS`
  - 扫描并替换任何其他遗漏的 hex 色

  Mappings.tsx:
  - import useSemanticColors
  - `const colors = useSemanticColors();`
  - line 394 蓝色 valueStyle -> `color: colors.BRAND`
  - line 397 红色 valueStyle -> `color: colors.ERROR`
  - 扫描并替换任何其他遗漏的 hex 色

  Compare.tsx:
  - import useSemanticColors
  - `const colors = useSemanticColors();`
  - line 610 `background: '#fafafa'` -> `background: colors.FILL_QUATERNARY` (per D-04)
  - line 771-773 三色 valueStyle -> `colors.SUCCESS/ERROR/WARNING`（按原语义对应）
  - line 845 border 色 -> 对应 colors 字段
  - **特别注意（review 指出的遗漏）：** 搜索 `#3370FF`、`#f0f0f0`、`#FFF1F0` 等非标准 hex 色并替换：
    - `#3370FF` -> `colors.BRAND`
    - `#f0f0f0` -> `colors.FILL_QUATERNARY` 或 `colors.BORDER_SECONDARY`（按语义判断）
    - 其他发现的 hex 色按 Token Mapping Table 对应替换
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && for f in frontend/src/pages/Results.tsx frontend/src/pages/Exports.tsx frontend/src/pages/Mappings.tsx frontend/src/pages/Compare.tsx; do echo "=== $f ===" && grep -n "'#[0-9a-fA-F]" "$f" || echo "CLEAN"; done && cd frontend && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/Results.tsx` returns 0
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/Exports.tsx` returns 0
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/Mappings.tsx` returns 0
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/Compare.tsx` returns 0
    - `grep -c "useSemanticColors" frontend/src/pages/Results.tsx` returns >= 1
    - `grep -c "useSemanticColors" frontend/src/pages/Compare.tsx` returns >= 1
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>四个 Statistic 页面的所有硬编码 hex 色清零（通过通用 hex grep 验证，不限于已知色值），lint 通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 迁移 Table columns + 模块顶层常量（AnomalyDetection/PeriodCompare/FeishuSync/ApiKeys）</name>
  <files>frontend/src/pages/AnomalyDetection.tsx, frontend/src/pages/PeriodCompare.tsx, frontend/src/pages/FeishuSync.tsx, frontend/src/pages/ApiKeys.tsx</files>
  <read_first>
    - frontend/src/pages/AnomalyDetection.tsx (搜索所有 # 开头的颜色值 — 特别注意 STATUS_STYLES map 和 render 内颜色)
    - frontend/src/pages/PeriodCompare.tsx (搜索所有 # 开头的颜色值 — 特别注意 #FFF1F0 等 review 指出的遗漏)
    - frontend/src/pages/FeishuSync.tsx (搜索所有 # 开头的颜色值 — 特别注意 line 45-47 模块顶层常量)
    - frontend/src/pages/ApiKeys.tsx (搜索所有 # 开头的颜色值)
    - frontend/src/theme/chartColors.ts
    - frontend/src/theme/useThemeMode.ts
    - frontend/src/theme/useSemanticColors.ts
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Example 5 chartColors Table 用法、D-03/D-05/D-07)
  </read_first>
  <action>
  **Addresses review concern [HIGH]: 先对每个文件做完整 hex 扫描（`grep -n "'#[0-9a-fA-F]" <file>`），逐一替换，不限于 Token Mapping Table 已列出的值。**

  AnomalyDetection.tsx (per D-03):
  - import getChartColors + useThemeMode + useSemanticColors + useMemo
  - 组件体内:
    ```
    const { isDark } = useThemeMode();
    const chartCols = useMemo(() => getChartColors(isDark), [isDark]);
    const colors = useSemanticColors();
    ```
  - STATUS_STYLES map：如果在组件外 -> 移到组件内 useMemo 依赖 chartCols；map 中的 hex 色替换为 chartCols 对应字段。**用 useMemo 包裹保持引用稳定。**
  - render 内三/四色 -> `chartCols.success/error/warning/brand`
  - 扫描并替换任何其他遗漏的 hex 色

  PeriodCompare.tsx (per D-05, D-07):
  - import useSemanticColors + getChartColors + useThemeMode + useMemo
  - 组件体内：
    ```
    const colors = useSemanticColors();
    const { isDark } = useThemeMode();
    const chartCols = useMemo(() => getChartColors(isDark), [isDark]);
    ```
  - 差异色函数：如是组件外纯函数 -> 接受 colors/chartCols 作为参数或移到组件内 useMemo；按 D-07 映射：增加=WARNING、减少=ERROR、新增=BRAND
  - `'#FFF7E6'` -> `colors.HIGHLIGHT_BG` (per D-05)
  - **特别注意（review 指出的遗漏）：** `#FFF1F0` -> `colors.HIGHLIGHT_BG_ERROR`；`#3370FF` -> `colors.BRAND`；任何其他 hex 色按语义替换
  - 所有 Statistic valueStyle 中的 hex 色 -> 对应 colors 字段

  FeishuSync.tsx (per D-03, D-05):
  - import getChartColors + useThemeMode + useSemanticColors + useMemo
  - **line 45-47 模块顶层 STATUS_COLORS 常量（review 指出的遗漏）：删除常量**，改为组件体内 useMemo:
    ```
    const { isDark } = useThemeMode();
    const chartCols = useMemo(() => getChartColors(isDark), [isDark]);
    const STATUS_COLORS = useMemo(() => ({
      success: chartCols.success,
      error: chartCols.error,
      warning: chartCols.warning,
    }), [chartCols]);
    const colors = useSemanticColors();
    ```
  - `'#FFF7E6'` -> `colors.HIGHLIGHT_BG`
  - 扫描并替换任何其他遗漏的 hex 色

  ApiKeys.tsx (per D-04):
  - import useSemanticColors
  - `const colors = useSemanticColors();`
  - `background: '#f5f5f5'` -> `background: colors.FILL_QUATERNARY`
  - 扫描并替换任何其他遗漏的 hex 色
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && for f in frontend/src/pages/AnomalyDetection.tsx frontend/src/pages/PeriodCompare.tsx frontend/src/pages/FeishuSync.tsx frontend/src/pages/ApiKeys.tsx; do echo "=== $f ===" && grep -n "'#[0-9a-fA-F]" "$f" || echo "CLEAN"; done && cd frontend && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/AnomalyDetection.tsx` returns 0
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/PeriodCompare.tsx` returns 0
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/FeishuSync.tsx` returns 0
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/ApiKeys.tsx` returns 0
    - `grep -c "getChartColors" frontend/src/pages/AnomalyDetection.tsx` returns >= 1
    - `grep -c "useMemo" frontend/src/pages/AnomalyDetection.tsx` returns >= 1
    - `grep -c "getChartColors\|useSemanticColors" frontend/src/pages/PeriodCompare.tsx` returns >= 2
    - `grep -c "getChartColors" frontend/src/pages/FeishuSync.tsx` returns >= 1
    - `grep -c "useSemanticColors" frontend/src/pages/ApiKeys.tsx` returns >= 1
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>四个 Table/模块顶层页面迁移完成，所有 hex 色清零（通用 grep 验证），useMemo 包裹工厂函数，lint 通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 迁移 FeishuFieldMapping（含 React Flow Background）</name>
  <files>frontend/src/pages/FeishuFieldMapping.tsx</files>
  <read_first>
    - frontend/src/pages/FeishuFieldMapping.tsx (搜索所有 # 开头的颜色值 — 完整扫描而非仅已知位置)
    - frontend/src/theme/useSemanticColors.ts
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Pitfall 6 React Flow Background、D-20)
    - .planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md (D-20)
  </read_first>
  <action>
  FeishuFieldMapping.tsx (per D-20, Pitfall 6):
  - import useSemanticColors + antd theme:
    ```
    import { useSemanticColors } from '../theme/useSemanticColors';
    import { theme } from 'antd';
    ```
  - 组件体内:
    ```
    const colors = useSemanticColors();
    const { token } = theme.useToken();
    ```
  - **先完整扫描文件所有 hex 色**（`grep -n "'#[0-9a-fA-F]" FeishuFieldMapping.tsx`），逐一替换：
    - `'#FFFFFF'` -> `colors.BG_CONTAINER`
    - `'#3370FF'` -> `colors.BRAND`
    - `'#1F2329'` -> `colors.TEXT`
    - `'#DEE0E3'` -> `colors.BORDER`
    - `'#E8E8E8'` -> `colors.BORDER_SECONDARY`
    - `'#F5F6F7'` / `'#fafafa'` -> `colors.BG_LAYOUT` 或 `colors.FILL_QUATERNARY`（按语义）
    - `'#8F959E'` / `'#999'` -> `colors.TEXT_TERTIARY`
    - `'#FFF7E6'` -> `colors.HIGHLIGHT_BG`
    - 任何上表未列出的 hex 色 -> 按语义选择最接近的 token
  - **React Flow Background**：找到 `<Background color="..." ... />` 替换为 `<Background color={token.colorBorder} ... />`（per Pitfall 6，动态 token）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && echo "=== hex scan ===" && grep -n "'#[0-9a-fA-F]" frontend/src/pages/FeishuFieldMapping.tsx || echo "CLEAN" && echo "=== Background token ===" && grep -c "Background.*color=.token" frontend/src/pages/FeishuFieldMapping.tsx && cd frontend && npm run lint 2>&1 | tail -10 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/FeishuFieldMapping.tsx` returns 0
    - `grep -cE '"#[0-9a-fA-F]{3,8}"' frontend/src/pages/FeishuFieldMapping.tsx` returns 0 (also check double-quoted hex in JSX props like Background color)
    - `grep -c "useSemanticColors" frontend/src/pages/FeishuFieldMapping.tsx` returns >= 1
    - `grep -c "theme.useToken\|useToken" frontend/src/pages/FeishuFieldMapping.tsx` returns >= 1
    - `grep -cE "Background[^>]*color=" frontend/src/pages/FeishuFieldMapping.tsx` returns >= 1 (dynamic token, not string literal)
    - `cd frontend && npm run lint` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>FeishuFieldMapping 全部硬编码色 token 化（通用 hex grep 验证含单引号和双引号），React Flow Background 使用动态 token；lint+build 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 纯 UI token 替换 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | N/A | accept | 纯视觉迁移，无信任边界变化 |
</threat_model>

<verification>
- `npm run lint --prefix frontend` 通过
- `npm run build --prefix frontend` 通过
- 对每个迁移文件执行 `grep -cE "'#[0-9a-fA-F]{3,8}'"` 结果为 0
- 手动打开 FeishuFieldMapping 页面（/feishu-mapping/:configId），切换暗黑模式，验证 React Flow Background 点阵色也随主题变化
</verification>

<success_criteria>
1. 9 个文件共 16+ 处硬编码色 token 化
2. 通用 hex grep 验证每个文件无残留（不限于已知色值列表）
3. React Flow Background 动态跟随主题
4. PeriodCompare 差异色按 D-07 映射
5. FeishuSync 模块顶层常量改为组件内 useMemo
6. 所有工厂函数/columns/status maps 用 useMemo 包裹
7. lint + build 通过
</success_criteria>

<output>
创建 `.planning/phases/14-style-tokens-dark-mode/14-03-SUMMARY.md`
</output>
