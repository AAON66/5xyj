---
phase: 19-fusion-capability-enhancement
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - backend/app/schemas/aggregate.py
  - backend/app/api/v1/aggregate.py
  - backend/app/services/fusion_runtime_service.py
  - backend/app/services/aggregate_service.py
  - backend/app/exporters/export_utils.py
  - backend/app/exporters/tool_exporter.py
  - backend/app/exporters/salary_exporter.py
  - backend/tests/test_aggregate_api.py
  - backend/tests/test_aggregate_service.py
  - backend/tests/test_template_exporter.py
  - backend/tests/test_api_compatibility.py
autonomous: true
requirements:
  - FUSE-01
  - FUSE-03

must_haves:
  truths:
    - "aggregate endpoint 支持可选 burden source（Excel 或 Feishu）和可选 fusion_rule_ids"
    - "aggregate runtime 应用顺序为：burden source overlay -> special rule override -> Tool exporter"
    - "burden 命中优先 employee_id，其次 id_number，无法命中仅记录 diagnostics，不静默写错人"
    - "Tool 模板可输出显式 personal_social_burden / personal_housing_burden"
    - "Salary 模板不再包含承担额列，恢复为原稳定薪酬字段结构"
    - "不上传 burden source / 不选规则时，原有双模板导出仍可正常工作"
  artifacts:
    - path: "backend/app/api/v1/aggregate.py"
      provides: "aggregate 新输入字段 contract"
      contains: "burden_source_mode"
    - path: "backend/app/services/fusion_runtime_service.py"
      provides: "burden + rule overlay runtime"
      contains: "def build_fusion_overrides"
    - path: "backend/app/exporters/tool_exporter.py"
      provides: "Tool 模板承担额注入"
      contains: "personal_social_burden"
    - path: "backend/app/exporters/salary_exporter.py"
      provides: "无承担额的 Salary 模板结构"
      contains: "SALARY_HEADERS"
  key_links:
    - from: "backend/app/api/v1/aggregate.py"
      to: "backend/app/services/aggregate_service.py"
      via: "burden source / fusion rule request fields"
      pattern: "burden_source_mode"
    - from: "backend/app/services/aggregate_service.py"
      to: "backend/app/services/fusion_runtime_service.py"
      via: "build_fusion_overrides call"
      pattern: "build_fusion_overrides"
    - from: "backend/app/services/fusion_runtime_service.py"
      to: "backend/app/exporters/tool_exporter.py"
      via: "resolved burden overlay context"
      pattern: "personal_social_burden"
---

<objective>
把 Phase 19 的补充输入源与特殊规则真正接到快速融合主链路，并收口 Tool/Salary 两套模板的边界。

Purpose: 前两个 plans 只提供“规则库”和“输入适配器”。本 plan 负责把它们接入 aggregate 主链路，保证承担额只进入 Tool 模板，同时不破坏原有双模板导出。
Output: aggregate contract 扩展 + runtime overlay + Tool-only burden export + Salary regression fix
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-fusion-capability-enhancement/19-RESEARCH.md
@.planning/phases/19-fusion-capability-enhancement/19-VALIDATION.md

<interfaces>
Current aggregate request surface:
```python
files: list[UploadFile]
housing_fund_files: list[UploadFile]
employee_master_file: Optional[UploadFile]
employee_master_mode: Optional[str]
batch_name: Optional[str]
```

Required Phase 19 request additions:
```text
burden_source_mode: "none" | "excel" | "feishu"
burden_file: optional UploadFile
burden_feishu_config_id: optional string
fusion_rule_ids: optional JSON array of rule ids
```

Current Salary exporter conflict:
```python
assert len(SALARY_HEADERS) == 18
assert SALARY_HEADERS[-2] == '个人社保承担额'
assert SALARY_HEADERS[-1] == '个人公积金承担额'
```

Phase 19 target state:
```text
Salary 模板不包含上述两列
Tool 模板继续保留承担额两列
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 aggregate contract 并在 runtime 中应用 burden overlay + special rules</name>
  <files>backend/app/schemas/aggregate.py, backend/app/api/v1/aggregate.py, backend/app/services/fusion_runtime_service.py, backend/app/services/aggregate_service.py, backend/tests/test_aggregate_api.py, backend/tests/test_aggregate_service.py</files>
  <read_first>
    - backend/app/schemas/aggregate.py
    - backend/app/api/v1/aggregate.py
    - backend/app/services/aggregate_service.py
    - backend/app/services/batch_runtime_service.py
    - backend/app/services/fusion_rule_service.py
    - backend/app/services/fusion_input_service.py
    - backend/tests/test_aggregate_api.py
    - backend/tests/test_aggregate_service.py
  </read_first>
  <action>
1. 扩展 aggregate 请求 contract：
   - `backend/app/api/v1/aggregate.py` 与 `run_simple_aggregate_stream_endpoint()` / `run_simple_aggregate_endpoint()` 新增可选字段：
     - `burden_file: UploadFile | None = File(default=None)`
     - `burden_source_mode: Optional[str] = Form(default=None)`，允许值仅为 `none|excel|feishu`
     - `burden_feishu_config_id: Optional[str] = Form(default=None)`
     - `fusion_rule_ids: Optional[str] = Form(default=None)`，使用 JSON array string 解析

2. 新增 `backend/app/services/fusion_runtime_service.py`，至少实现：
   - `build_fusion_overrides(db, records, *, burden_rows, fusion_rule_ids) -> dict[str, dict[str, Decimal]]`
   - `apply_fusion_overrides(record, overrides) -> dict[str, Decimal]`
   覆盖顺序必须精确为：
   - 先应用 burden source row
   - 再应用 selected active rule
   - special rule 覆盖 burden source 同字段值

3. 在 `backend/app/services/aggregate_service.py` 中接入上述能力：
   - `burden_source_mode == "excel"` 时读取 `burden_file`
   - `burden_source_mode == "feishu"` 时读取 `burden_feishu_config_id`
   - `fusion_rule_ids` 为空时默认加载所有 active rules
   - overlay 命中优先 `employee_id`，其次 `id_number`
   - 无法命中的 burden rows 不阻塞导出，但要进入 diagnostics/message

4. 在 `backend/tests/test_aggregate_api.py` / `backend/tests/test_aggregate_service.py` 中补至少这些场景：
   - 上传 burden Excel 后 Tool export 有承担额
   - 通过 mocked Feishu config 后 Tool export 有承担额
   - special rule 覆盖 burden source 值
   - 不上传 burden source 时旧链路仍可导出
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_aggregate_api.py backend/tests/test_aggregate_service.py -q -k "burden or fusion_rule or special_rule"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/api/v1/aggregate.py` 包含 `burden_source_mode`
    - `backend/app/api/v1/aggregate.py` 包含 `burden_file`
    - `backend/app/api/v1/aggregate.py` 包含 `burden_feishu_config_id`
    - `backend/app/api/v1/aggregate.py` 包含 `fusion_rule_ids`
    - `backend/app/services/fusion_runtime_service.py` 包含 `def build_fusion_overrides`
    - `backend/app/services/aggregate_service.py` 包含 `burden_source_mode`
    - `backend/tests/test_aggregate_api.py` 包含 `burden`
    - `backend/tests/test_aggregate_service.py` 包含 `special_rule`
  </acceptance_criteria>
  <done>快速融合主链路已能接收 burden source 和持久化规则，并在 runtime 中构建覆盖结果</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 Tool 模板读取显式承担额，并恢复 Salary 模板原始结构</name>
  <files>backend/app/exporters/export_utils.py, backend/app/exporters/tool_exporter.py, backend/app/exporters/salary_exporter.py, backend/tests/test_template_exporter.py, backend/tests/test_api_compatibility.py</files>
  <read_first>
    - backend/app/exporters/export_utils.py
    - backend/app/exporters/tool_exporter.py
    - backend/app/exporters/salary_exporter.py
    - backend/tests/test_template_exporter.py
    - backend/tests/test_api_compatibility.py
    - .planning/phases/19-fusion-capability-enhancement/19-RESEARCH.md
  </read_first>
  <action>
1. 修改 `backend/app/exporters/export_utils.py`：
   - `_resolved_personal_social_burden(...)` 不再直接返回 `Decimal('0')`
   - `_resolved_personal_housing_burden(...)` 不再直接返回 `Decimal('0')`
   - 两者改为从 Phase 19 overlay context 中读取显式 burden 值；没有命中时回退 `Decimal('0')`

2. 修改 `backend/app/exporters/tool_exporter.py`：
   - 保持 Tool 模板承担额两列位置不变
   - `_tool_row_values()` 读取新的显式 burden 值

3. 修改 `backend/app/exporters/salary_exporter.py`：
   - 从 `SALARY_HEADERS` 中移除 `个人社保承担额`、`个人公积金承担额`
   - `_salary_row_values()` 只输出无承担额的稳定字段集合
   - 相应调整 header 数量断言与导出列映射

4. 更新 `backend/tests/test_template_exporter.py` 与 `backend/tests/test_api_compatibility.py`：
   - Tool 模板在 overlay 命中时输出显式承担额
   - Salary headers 不再包含 `个人社保承担额`、`个人公积金承担额`
   - 没有 overlay 时 Tool burden 默认 `0`
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_template_exporter.py backend/tests/test_api_compatibility.py -q -k "salary or tool or burden"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/exporters/salary_exporter.py` 不包含 `个人社保承担额`
    - `backend/app/exporters/salary_exporter.py` 不包含 `个人公积金承担额`
    - `backend/app/exporters/tool_exporter.py` 包含 `personal_social_burden`
    - `backend/app/exporters/tool_exporter.py` 包含 `personal_housing_burden`
    - `backend/tests/test_api_compatibility.py` 不再断言 Salary headers 包含承担额列
    - `cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_template_exporter.py backend/tests/test_api_compatibility.py -q -k "salary or tool or burden"` 退出码为 0
  </acceptance_criteria>
  <done>Tool 模板承担额变为显式来源驱动，Salary 模板恢复稳定结构，roadmap 边界被真正实现</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregate runtime overlay | 错误优先级或错误员工命中会直接污染导出结果 |
| Salary vs Tool exporter split | 如果边界没收口，会把新字段泄露到稳定薪酬模板 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | aggregate overlay precedence | mitigate | precedence 固定为 burden source -> special rule，且通过 aggregate/export tests 验证 |
| T-19-08 | Information Disclosure | salary exporter | mitigate | Salary 模板移除承担额列并以 regression tests 锁死结构 |
| T-19-09 | Spoofing | burden row matching | mitigate | runtime 只按 employee_id/id_number 命中，无法命中仅记录 diagnostics |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_aggregate_api.py backend/tests/test_aggregate_service.py -q -k "burden or fusion_rule or special_rule"`
2. `cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_template_exporter.py backend/tests/test_api_compatibility.py -q -k "salary or tool or burden"`
</verification>

<success_criteria>
- aggregate 主链路可接收 burden source 与 special rules
- Tool 模板可输出显式 burden 值
- Salary 模板恢复为无 burden 列的稳定结构
- 无额外输入时原有双模板导出行为不退化
</success_criteria>

<output>
After completion, create `.planning/phases/19-fusion-capability-enhancement/19-03-SUMMARY.md`
</output>
