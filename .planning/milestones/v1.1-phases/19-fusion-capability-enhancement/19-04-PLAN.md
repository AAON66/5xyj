---
phase: 19-fusion-capability-enhancement
plan: 04
type: execute
wave: 3
depends_on: ["19-03"]
files_modified:
  - frontend/src/services/fusionRules.ts
  - frontend/src/services/aggregate.ts
  - frontend/src/components/FusionRuleEditorDrawer.tsx
  - frontend/src/pages/SimpleAggregate.tsx
  - frontend/tests/e2e/fusion-aggregate.spec.ts
autonomous: true
requirements:
  - FUSE-01
  - FUSE-03

must_haves:
  truths:
    - "快速融合页可选择 burden source：none / excel / feishu"
    - "快速融合页可创建、保存、复用特殊规则"
    - "提交 aggregate 请求时会带上 burden_file、burden_source_mode、burden_feishu_config_id、fusion_rule_ids"
    - "手机端仍然只有一个 sticky 主按钮，不因新增配置区而出现重复主操作"
    - "可复用规则入口直接复用现有飞书配置服务 fetchSyncConfigs，而不是新造配置源"
  artifacts:
    - path: "frontend/src/services/fusionRules.ts"
      provides: "fusion-rules CRUD service"
      contains: "fetchFusionRules"
    - path: "frontend/src/components/FusionRuleEditorDrawer.tsx"
      provides: "特殊规则编辑抽屉"
      contains: "targetField"
    - path: "frontend/src/pages/SimpleAggregate.tsx"
      provides: "burden source + special rule aggregate UX"
      contains: "burdenSourceMode"
    - path: "frontend/tests/e2e/fusion-aggregate.spec.ts"
      provides: "quick aggregate Phase 19 e2e coverage"
      contains: "fusion rule"
  key_links:
    - from: "frontend/src/services/fusionRules.ts"
      to: "frontend/src/components/FusionRuleEditorDrawer.tsx"
      via: "CRUD calls"
      pattern: "createFusionRule"
    - from: "frontend/src/services/aggregate.ts"
      to: "frontend/src/pages/SimpleAggregate.tsx"
      via: "new AggregateInput fields"
      pattern: "burdenSourceMode"
    - from: "frontend/src/services/feishu.ts"
      to: "frontend/src/pages/SimpleAggregate.tsx"
      via: "fetchSyncConfigs reuse"
      pattern: "fetchSyncConfigs"
---

<objective>
为快速融合页接入承担额来源选择和特殊规则复用 UI，同时保持已有移动端 sticky CTA 与工作流体验。

Purpose: Phase 19 的后端能力只有在 `SimpleAggregate` 真正可配置、可保存、可复用时才对用户有价值。本 plan 负责把 burden source 和 fusion rules 暴露给用户，并补浏览器级回归测试。
Output: `SimpleAggregate` Phase 19 UI + request contract + Playwright coverage
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-fusion-capability-enhancement/19-RESEARCH.md
@.planning/phases/19-fusion-capability-enhancement/19-VALIDATION.md

<interfaces>
Existing frontend Feishu config service:
```ts
export async function fetchSyncConfigs(): Promise<SyncConfig[]>
```

Aggregate request target fields:
```ts
burdenFile?: File | null
burdenSourceMode?: 'none' | 'excel' | 'feishu'
burdenFeishuConfigId?: string | null
fusionRuleIds?: string[]
```

Fusion rule UI whitelist labels:
```text
personal_social_burden -> 个人社保承担额
personal_housing_burden -> 个人公积金承担额
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 fusion-rules 前端 service 与规则编辑抽屉</name>
  <files>frontend/src/services/fusionRules.ts, frontend/src/components/FusionRuleEditorDrawer.tsx, frontend/src/pages/SimpleAggregate.tsx</files>
  <read_first>
    - frontend/src/pages/SimpleAggregate.tsx
    - frontend/src/services/feishu.ts
    - frontend/src/services/api.ts
    - frontend/src/components/MobileStickyActionBar.tsx
    - frontend/src/hooks/useResponsiveViewport.ts
  </read_first>
  <action>
1. 新增 `frontend/src/services/fusionRules.ts`，实现：
   - `fetchFusionRules()`
   - `createFusionRule()`
   - `updateFusionRule()`
   - `deleteFusionRule()`
   所有接口路径精确对应：
   - `GET /fusion-rules`
   - `POST /fusion-rules`
   - `PUT /fusion-rules/{ruleId}`
   - `DELETE /fusion-rules/{ruleId}`

2. 新增 `frontend/src/components/FusionRuleEditorDrawer.tsx`，抽屉内表单必须包含：
   - `scopeType` 选择：`工号` / `身份证号`
   - `scopeValue` 输入
   - `targetField` 选择：`个人社保承担额` / `个人公积金承担额`
   - `overrideValue` 数字输入
   - `note` 备注输入
   - 保存按钮与删除按钮（编辑态）

3. 在 `frontend/src/pages/SimpleAggregate.tsx` 中接入规则列表与抽屉：
   - 页面加载时拉取已保存规则
   - 允许选择多个规则 id 参与本次 aggregate
   - 提供“新建规则”按钮打开抽屉
   - 成功保存后刷新规则列表
   - 移动端保持单一 sticky 主按钮，不新增第二个固定 CTA
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/services/fusionRules.ts src/components/FusionRuleEditorDrawer.tsx src/pages/SimpleAggregate.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/services/fusionRules.ts` 包含 `fetchFusionRules`
    - `frontend/src/services/fusionRules.ts` 包含 `/fusion-rules`
    - `frontend/src/components/FusionRuleEditorDrawer.tsx` 包含 `targetField`
    - `frontend/src/components/FusionRuleEditorDrawer.tsx` 包含 `overrideValue`
    - `frontend/src/pages/SimpleAggregate.tsx` 包含 `selectedFusionRuleIds`
    - `frontend/src/pages/SimpleAggregate.tsx` 包含 `FusionRuleEditorDrawer`
  </acceptance_criteria>
  <done>快速融合页已具备规则保存、编辑、复用的前端入口</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 aggregate 表单并补 quick aggregate 的 Playwright 回归测试</name>
  <files>frontend/src/services/aggregate.ts, frontend/src/pages/SimpleAggregate.tsx, frontend/tests/e2e/fusion-aggregate.spec.ts</files>
  <read_first>
    - frontend/src/services/aggregate.ts
    - frontend/src/pages/SimpleAggregate.tsx
    - frontend/src/services/feishu.ts
    - frontend/tests/e2e/responsive.spec.ts
    - frontend/playwright.config.ts
  </read_first>
  <action>
1. 在 `frontend/src/services/aggregate.ts` 中扩展 `AggregateInput` 与 `buildAggregateFormData()`：
   - `burdenFile?: File | null`
   - `burdenSourceMode?: 'none' | 'excel' | 'feishu'`
   - `burdenFeishuConfigId?: string | null`
   - `fusionRuleIds?: string[]`
   `FormData` 字段名必须精确为：
   - `burden_file`
   - `burden_source_mode`
   - `burden_feishu_config_id`
   - `fusion_rule_ids`（JSON array string）

2. 在 `frontend/src/pages/SimpleAggregate.tsx` 新增“承担额来源”配置区：
   - source mode 选项：`不使用`、`上传 Excel`、`选择飞书配置`
   - `上传 Excel` 模式显示一个 burden file 选择控件
   - `选择飞书配置` 模式复用 `fetchSyncConfigs()` 拉取可用配置并显示下拉选择
   - 点击主按钮时把上述字段带入 `startAggregateSession` / `runSimpleAggregateWithProgress`

3. 新增 `frontend/tests/e2e/fusion-aggregate.spec.ts`，通过 route mocking 覆盖：
   - 规则列表加载
   - 新建并选择一条规则
   - 选择 Feishu config 作为 burden source
   - 提交 aggregate 后请求体包含 `burden_source_mode`、`burden_feishu_config_id`、`fusion_rule_ids`
   - 手机视口下仍然只有一个 sticky 主按钮
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/services/aggregate.ts src/pages/SimpleAggregate.tsx && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/services/aggregate.ts` 包含 `burdenSourceMode`
    - `frontend/src/services/aggregate.ts` 包含 `burden_file`
    - `frontend/src/services/aggregate.ts` 包含 `fusion_rule_ids`
    - `frontend/src/pages/SimpleAggregate.tsx` 包含 `burdenFeishuConfigId`
    - `frontend/src/pages/SimpleAggregate.tsx` 包含 `fetchSyncConfigs`
    - `frontend/tests/e2e/fusion-aggregate.spec.ts` 包含 `burden_source_mode`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run test:e2e` 退出码为 0
  </acceptance_criteria>
  <done>快速融合页已能配置 burden source 并携带规则/来源数据发起聚合，请求与浏览器级回归测试已就绪</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quick aggregate form | 新增的 burden source 与规则选择不能破坏原有主流程或移动端 CTA 约束 |
| Rule editor UX | 用户不能被误导去编辑非白名单字段或以姓名命中员工 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Tampering | aggregate form payload | mitigate | 只提交 `burden_source_mode`、`burden_feishu_config_id`、`fusion_rule_ids` 白名单字段，并用 e2e 断言请求体 |
| T-19-11 | Information Disclosure | feishu config picker | mitigate | 复用现有 `fetchSyncConfigs()`，不自行拼接凭证信息，不在 UI 暴露敏感 secret 以外的新字段 |
| T-19-12 | Spoofing | rule editor scope | mitigate | UI 只允许 `工号` / `身份证号` 两种 scopeType，不暴露姓名作为可选项 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/services/fusionRules.ts src/components/FusionRuleEditorDrawer.tsx src/services/aggregate.ts src/pages/SimpleAggregate.tsx`
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run test:e2e`
</verification>

<success_criteria>
- 用户可在快速融合页选择 burden source 并复用持久化规则
- aggregate 请求包含 Phase 19 需要的新字段
- 移动端 sticky CTA 约束未被破坏
- 浏览器级自动化覆盖新流程
</success_criteria>

<output>
After completion, create `.planning/phases/19-fusion-capability-enhancement/19-04-SUMMARY.md`
</output>

