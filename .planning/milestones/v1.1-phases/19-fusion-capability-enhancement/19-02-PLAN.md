---
phase: 19-fusion-capability-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/fusion_inputs.py
  - backend/app/services/fusion_input_service.py
  - backend/app/services/feishu_sync_service.py
  - backend/tests/test_fusion_input_service.py
autonomous: true
requirements:
  - FUSE-01

must_haves:
  truths:
    - "系统可把 Excel burden 文件归一化为统一的 employee-keyed burden rows"
    - "系统可把 Feishu 配置读取为统一的 employee-keyed burden rows，而不是直接写回 NormalizedRecord"
    - "burden row 命中主键优先 employee_id，其次 id_number，不允许仅按姓名自动命中"
    - "无法命中、重复命中、缺少主键的 burden rows 会被记录为 diagnostics，而不是静默套用"
  artifacts:
    - path: "backend/app/services/fusion_input_service.py"
      provides: "Excel/Feishu burden source adapter"
      contains: "def parse_burden_workbook"
    - path: "backend/app/schemas/fusion_inputs.py"
      provides: "FusionBurdenRow / FusionBurdenDiagnostics schema"
      contains: "class FusionBurdenRow"
    - path: "backend/tests/test_fusion_input_service.py"
      provides: "burden adapter regression tests"
      contains: "test_parse_burden_workbook"
  key_links:
    - from: "backend/app/services/fusion_input_service.py"
      to: "backend/app/services/feishu_sync_service.py"
      via: "reuses FeishuClient + SyncConfig mapping, but not pull_records_from_feishu"
      pattern: "search_records"
---

<objective>
建立 Phase 19 的承担额输入适配层：把 burden Excel 上传和 Feishu 配置都归一化成统一 DTO，供 aggregate runtime 后续应用。

Purpose: FUSE-01 要求个人承担额既支持 Excel 上传，也支持飞书同步输入。这里先把“输入源差异”收敛到统一 adapter，而不是在 aggregate/exporter 里混杂两套解析逻辑。
Output: burden DTO + Excel parser + Feishu adapter + diagnostics tests
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-fusion-capability-enhancement/19-RESEARCH.md
@.planning/phases/19-fusion-capability-enhancement/19-VALIDATION.md

<interfaces>
From backend/app/services/feishu_sync_service.py:
```python
async def push_records_to_feishu(...)
async def pull_records_from_feishu(...)
async def check_push_conflicts(...)
```

Research decision:
```text
Phase 19 MUST NOT use pull_records_from_feishu() as the quick-fusion integration model.
Instead, reuse SyncConfig.field_mapping + FeishuClient.search_records() to build burden DTOs.
```

Allowed burden Excel aliases:
```text
employee_id: 工号, 员工工号
id_number: 身份证号, 身份证, 证件号码
personal_social_burden: 个人社保承担额, 社保个人承担额
personal_housing_burden: 个人公积金承担额, 公积金个人承担额
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 burden DTO 与 Excel 适配器</name>
  <files>backend/app/schemas/fusion_inputs.py, backend/app/services/fusion_input_service.py, backend/tests/test_fusion_input_service.py</files>
  <read_first>
    - backend/app/services/import_service.py
    - backend/app/services/housing_fund_service.py
    - backend/app/services/employee_service.py
    - backend/app/schemas/aggregate.py
    - .planning/phases/19-fusion-capability-enhancement/19-RESEARCH.md
  </read_first>
  <action>
1. 新增 `backend/app/schemas/fusion_inputs.py`，定义：
   - `FusionBurdenRow`
     - `employee_id: str | None`
     - `id_number: str | None`
     - `personal_social_burden: Decimal | None`
     - `personal_housing_burden: Decimal | None`
     - `source_kind: Literal["excel", "feishu"]`
     - `source_ref: str`
   - `FusionBurdenDiagnostics`
     - `missing_key_rows: int`
     - `duplicate_key_rows: int`
     - `unmatched_rows: int`
     - `messages: list[str]`

2. 新增 `backend/app/services/fusion_input_service.py`，至少实现：
   - `parse_burden_workbook(file_bytes: bytes, filename: str) -> tuple[list[FusionBurdenRow], FusionBurdenDiagnostics]`
   - `build_burden_key(employee_id: str | None, id_number: str | None) -> tuple[str, str] | None`
   解析规则必须精确支持这些 header aliases：
   - employee_id: `工号`、`员工工号`
   - id_number: `身份证号`、`身份证`、`证件号码`
   - personal_social_burden: `个人社保承担额`、`社保个人承担额`
   - personal_housing_burden: `个人公积金承担额`、`公积金个人承担额`
   行处理规则：
   - 没有 `employee_id` 且没有 `id_number` 的行计入 `missing_key_rows`
   - 同一 key 出现多次计入 `duplicate_key_rows`
   - 不读取姓名作为匹配主键
   - 允许两个 burden 字段其中一个为空，另一个存在

3. 在 `backend/tests/test_fusion_input_service.py` 中添加 Excel parser 测试，覆盖：
   - alias 命中
   - 缺少主键行被诊断
   - 重复 key 被诊断
   - `employee_id` 优先于 `id_number` 构 key
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_fusion_input_service.py -q -k "burden_workbook or build_burden_key"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/fusion_input_service.py` 包含 `def parse_burden_workbook`
    - `backend/app/services/fusion_input_service.py` 包含 `工号`
    - `backend/app/services/fusion_input_service.py` 包含 `个人社保承担额`
    - `backend/app/services/fusion_input_service.py` 包含 `个人公积金承担额`
    - `backend/app/schemas/fusion_inputs.py` 包含 `class FusionBurdenRow`
    - `backend/tests/test_fusion_input_service.py` 包含 `test_parse_burden_workbook`
  </acceptance_criteria>
  <done>Excel burden 文件可被稳定归一化为统一 burden rows，并带诊断信息</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 Feishu burden adapter，复用配置但不复用 pull 写库语义</name>
  <files>backend/app/services/fusion_input_service.py, backend/app/services/feishu_sync_service.py, backend/tests/test_fusion_input_service.py</files>
  <read_first>
    - backend/app/services/feishu_sync_service.py
    - backend/app/models/sync_config.py
    - backend/app/services/feishu_client.py
    - backend/app/api/v1/feishu_settings.py
    - backend/tests/test_aggregate_api.py
  </read_first>
  <action>
1. 在 `backend/app/services/fusion_input_service.py` 中新增异步函数：
   - `load_burden_rows_from_feishu(db: Session, client: FeishuClient, config_id: str) -> tuple[list[FusionBurdenRow], FusionBurdenDiagnostics]`

2. 该函数必须执行以下行为：
   - 通过 `db.get(SyncConfig, config_id)` 读取配置
   - 通过 `client.search_records(config.app_token, config.table_id)` 读取飞书行
   - 使用 `config.field_mapping` 的反向映射，只接受以下 system field：
     - `employee_id`
     - `id_number`
     - `personal_social_burden`
     - `personal_housing_burden`
   - 不调用 `pull_records_from_feishu`
   - 不创建 `NormalizedRecord`
   - 对缺少 employee key、重复 key 的 Feishu rows 同样返回 diagnostics

3. 在 `backend/tests/test_fusion_input_service.py` 中添加 mocked client 测试，验证：
   - 仅 burden 白名单字段被读取
   - `pull_records_from_feishu` 未被调用
   - diagnostics 会统计缺键/重复 key
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_fusion_input_service.py -q -k "feishu"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/fusion_input_service.py` 包含 `def load_burden_rows_from_feishu`
    - `backend/app/services/fusion_input_service.py` 包含 `search_records`
    - `backend/app/services/fusion_input_service.py` 不包含 `pull_records_from_feishu(`
    - `backend/tests/test_fusion_input_service.py` 包含 `feishu`
    - `cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_fusion_input_service.py -q -k "feishu"` 退出码为 0
  </acceptance_criteria>
  <done>Feishu burden source 可被读取为统一 burden rows，且不会污染 NormalizedRecord 主链路</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Excel burden import | 上传文件中的员工键和值可能缺失、重复或被伪造 |
| Feishu burden adapter | 外部表格字段映射可能配置错误，不能直接污染主数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering | burden workbook rows | mitigate | 仅接受白名单 header aliases，缺少 employee key 的行不应用，只进入 diagnostics |
| T-19-05 | Spoofing | employee targeting | mitigate | key 仅允许 employee_id / id_number，禁止姓名主键 |
| T-19-06 | Tampering | Feishu adapter | mitigate | 只读取 `SyncConfig.field_mapping` 中的白名单 burden fields，且不调用 `pull_records_from_feishu` |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_fusion_input_service.py -q`
2. `grep -n "search_records" /Users/mac/PycharmProjects/5xyj/backend/app/services/fusion_input_service.py`
</verification>

<success_criteria>
- burden Excel 与 Feishu 都能被归一化为统一 DTO
- 主键解析和 diagnostics 行为清晰、可测试
- Feishu 适配层不会直接写回 NormalizedRecord
</success_criteria>

<output>
After completion, create `.planning/phases/19-fusion-capability-enhancement/19-02-SUMMARY.md`
</output>
