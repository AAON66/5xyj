---
phase: 20-compare-redesign-and-feishu-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/compare.py
  - backend/app/services/compare_service.py
  - backend/app/api/v1/compare.py
  - backend/tests/test_compare_api.py
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "period compare API 返回明确的窗口元数据，前端不再猜测当前页和总页数"
    - "搜索与 diff_only 在服务端执行，summary_groups 与总计数不因前端分页而失真"
    - "左右记录值与 different_fields 的对齐语义保持不变，不破坏已有 batch compare/export"
    - "compare rows 顺序稳定，可被共享 workbook diff viewer 直接消费"
  artifacts:
    - path: "backend/app/schemas/compare.py"
      provides: "扩展后的 PeriodCompareRead 窗口字段"
      contains: "total_pages"
    - path: "backend/app/services/compare_service.py"
      provides: "period compare 服务端过滤与分页窗口逻辑"
      contains: "search_text"
    - path: "backend/app/api/v1/compare.py"
      provides: "对外 compare query params 契约"
      contains: "diff_only"
    - path: "backend/tests/test_compare_api.py"
      provides: "compare API 回归与分页过滤测试"
      contains: "returned_row_count"
  key_links:
    - from: "backend/app/services/compare_service.py"
      to: "backend/app/api/v1/compare.py"
      via: "search_text/diff_only/page contract"
      pattern: "compare_periods("
    - from: "backend/app/schemas/compare.py"
      to: "frontend/src/services/compare.ts"
      via: "window metadata payload"
      pattern: "total_pages"
---

<objective>
建立 Phase 20 的 compare 数据合同基础：让月度对比结果具备共享 diff viewer 所需的窗口元数据、服务端过滤和稳定排序。

Purpose: 如果 period compare 仍停留在“只返回一大包 rows”，前端只能继续一次性渲染大表或做浏览器端二次过滤，无法满足 diff 风格重做和 500+ 数据量流畅度要求。
Output: compare API window contract + backend regression tests
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-compare-redesign-and-feishu-enhancement/20-RESEARCH.md
@.planning/phases/20-compare-redesign-and-feishu-enhancement/20-VALIDATION.md

<interfaces>
Current period compare contract:
```python
class PeriodCompareRead(BaseModel):
    left_period: str
    right_period: str
    fields: list[str]
    total_row_count: int
    rows: list[CompareRowRead]
    summary_groups: list[PeriodCompareSummaryGroup] = []
```

Current endpoint query params:
```python
def compare_periods_endpoint(
    left_period: str,
    right_period: str,
    region: Optional[str] = None,
    company_name: Optional[str] = None,
    page: int = 0,
    page_size: int = 50,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 compare schema 与 service，补齐服务端过滤/分页窗口合同</name>
  <files>backend/app/schemas/compare.py, backend/app/services/compare_service.py</files>
  <read_first>
    - backend/app/schemas/compare.py
    - backend/app/services/compare_service.py
    - frontend/src/pages/PeriodCompare.tsx
    - .planning/phases/20-compare-redesign-and-feishu-enhancement/20-RESEARCH.md
  </read_first>
  <action>
1. 在 `backend/app/schemas/compare.py` 中扩展 `PeriodCompareRead`，至少增加以下字段：
   - `page`
   - `page_size`
   - `total_pages`
   - `returned_row_count`
   - `diff_only`
   - `search_text`

2. 在 `backend/app/services/compare_service.py` 中修改 `compare_periods()`：
   - 新增 `search_text: Optional[str] = None`
   - 新增 `diff_only: bool = False`
   - 搜索目标至少覆盖 `compare_key`、姓名、工号、证件号
   - `diff_only=True` 时只返回 `changed/left_only/right_only` 行
   - `summary_groups` 与 `total_row_count` 基于过滤后的完整结果集计算，再做分页切片
   - 返回的 rows 顺序必须稳定，仍按 identity + row index 排序

3. 保持现有 `different_fields`、`left/right.values`、`summary_groups` 语义不变，不允许为窗口化改动 compare 核心匹配逻辑。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_compare_api.py -q -k "period"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/compare.py` 包含 `total_pages`
    - `backend/app/schemas/compare.py` 包含 `returned_row_count`
    - `backend/app/services/compare_service.py` 包含 `search_text`
    - `backend/app/services/compare_service.py` 包含 `diff_only`
    - `backend/app/services/compare_service.py` 仍包含 `summary_groups`
  </acceptance_criteria>
  <done>period compare 已具备共享 diff viewer 可直接消费的窗口化合同</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 compare endpoint 并补 compare API 回归测试</name>
  <files>backend/app/api/v1/compare.py, backend/tests/test_compare_api.py</files>
  <read_first>
    - backend/app/api/v1/compare.py
    - backend/tests/test_compare_api.py
    - backend/app/services/compare_service.py
  </read_first>
  <action>
1. 在 `backend/app/api/v1/compare.py` 的 `/compare/periods` 端点中新增 query params：
   - `search_text: Optional[str]`
   - `diff_only: bool = False`
   并把它们透传给 `compare_periods()`。

2. 更新 `backend/tests/test_compare_api.py`，至少覆盖：
   - `diff_only=true` 时 `same` 行不会返回
   - `search_text` 只返回命中的员工/compare_key
   - `page/page_size` 变化时 `returned_row_count`、`total_pages` 正确
   - `summary_groups` 不因分页而丢失完整计数
   - 现有 batch compare export 测试继续通过
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_compare_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/api/v1/compare.py` 包含 `search_text`
    - `backend/app/api/v1/compare.py` 包含 `diff_only`
    - `backend/tests/test_compare_api.py` 包含 `returned_row_count`
    - `backend/tests/test_compare_api.py` 包含 `diff_only`
    - `cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_compare_api.py -q` 退出码为 0
  </acceptance_criteria>
  <done>compare endpoint 已支持 Phase 20 所需过滤/分页合同，回归测试可保护 batch compare 与 period compare 双路径</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compare filtering | 过滤和分页不能改变左右值对齐或差异统计语义 |
| Existing compare export | 新 schema 不能破坏既有 batch compare 导出契约 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | compare_periods filtering | mitigate | 以测试断言 `summary_groups` 和总计数基于过滤后的完整集，不因分页错误截断 |
| T-20-02 | Denial of Service | large compare result | mitigate | 用服务端分页 + `diff_only/search_text` 降低单次返回量，避免前端一次性渲染整批 rows |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_compare_api.py -q -k "period"`
2. `cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_compare_api.py -q`
</verification>

<success_criteria>
- compare API 明确返回窗口元数据
- `diff_only/search_text` 在服务端执行
- 现有 compare export/batch compare 语义未回归
</success_criteria>

<output>
After completion, create `.planning/phases/20-compare-redesign-and-feishu-enhancement/20-01-SUMMARY.md`
</output>
