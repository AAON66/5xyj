---
phase: 20-compare-redesign-and-feishu-enhancement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/system_setting.py
  - backend/app/models/__init__.py
  - backend/app/schemas/feishu.py
  - backend/app/services/system_setting_service.py
  - backend/app/api/v1/feishu_settings.py
  - backend/app/api/v1/system.py
  - backend/app/api/v1/feishu_auth.py
  - backend/app/api/v1/feishu_sync.py
  - backend/app/services/feishu_client.py
  - backend/alembic/versions/20260409_0011_add_system_settings.py
  - backend/tests/test_feishu_settings_api.py
autonomous: true
requirements:
  - FEISHU-01

must_haves:
  truths:
    - "飞书运行时设置可持久化，effective settings 读取顺序为 DB override 优先、环境变量回退"
    - "admin 可查看脱敏后的 app_id/secret 状态并更新开关/凭证；secret 永不回显"
    - "feature flags、OAuth、field discovery、sync client 共用同一套 effective settings 解析"
    - "现有 SyncConfig CRUD 继续可用，不因 runtime settings 持久化而回归"
  artifacts:
    - path: "backend/app/models/system_setting.py"
      provides: "运行时设置持久化模型"
      contains: "__tablename__ = \"system_settings\""
    - path: "backend/app/services/system_setting_service.py"
      provides: "effective feishu settings 解析服务"
      contains: "get_effective_feishu_settings"
    - path: "backend/app/api/v1/feishu_settings.py"
      provides: "脱敏读/写凭证与功能开关 API"
      contains: "/credentials"
    - path: "backend/tests/test_feishu_settings_api.py"
      provides: "飞书运行时设置权限与脱敏回归测试"
      contains: "masked_app_id"
  key_links:
    - from: "backend/app/services/system_setting_service.py"
      to: "backend/app/api/v1/system.py"
      via: "统一 feature flags 来源"
      pattern: "get_effective_feishu_settings"
    - from: "backend/app/services/system_setting_service.py"
      to: "backend/app/services/feishu_client.py"
      via: "client credentials source"
      pattern: "feishu_app_id"
---

<objective>
为 Phase 20 建立可由前端直接修改的飞书运行时配置基础，并让 feature flags、OAuth、字段发现、同步逻辑统一使用新设置来源。

Purpose: 当前飞书配置完全依赖环境变量，前端即使有页面也没有可写目标。这个 plan 负责补齐持久化模型、effective settings service 和 admin-only API。
Output: database-backed runtime settings + masked credentials API + backend tests
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-compare-redesign-and-feishu-enhancement/20-RESEARCH.md
@.planning/phases/20-compare-redesign-and-feishu-enhancement/20-VALIDATION.md

<interfaces>
Current env-backed settings:
```python
class Settings(BaseSettings):
    feishu_sync_enabled: bool = False
    feishu_oauth_enabled: bool = False
    feishu_app_id: str = ''
    feishu_app_secret: str = ''
```

Current SyncConfig model:
```python
class SyncConfig(Base):
    name: Mapped[str]
    app_token: Mapped[str]
    table_id: Mapped[str]
    field_mapping: Mapped[dict]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 system_settings 持久化与 effective feishu settings service</name>
  <files>backend/app/models/system_setting.py, backend/app/models/__init__.py, backend/app/services/system_setting_service.py, backend/alembic/versions/20260409_0011_add_system_settings.py</files>
  <read_first>
    - backend/app/models/base.py
    - backend/app/models/sync_config.py
    - backend/app/core/config.py
    - backend/app/services/feishu_client.py
    - .planning/phases/20-compare-redesign-and-feishu-enhancement/20-RESEARCH.md
  </read_first>
  <action>
1. 新增 `backend/app/models/system_setting.py`，定义最小 `SystemSetting` 模型：
   - `id` / timestamps 继承现有 mixin
   - `key: String(100)`，唯一
   - `value: Text` 或足以容纳 secret 的字符串列
   - `__tablename__ = "system_settings"`

2. 在 `backend/app/models/__init__.py` 中导出 `SystemSetting`，确保 metadata 可建表。

3. 新增 `backend/app/services/system_setting_service.py`，至少实现：
   - `get_setting(db, key)`
   - `set_setting(db, key, value)`
   - `get_effective_feishu_settings(db, env_settings)`
   返回值至少统一包含：
   - `feishu_sync_enabled`
   - `feishu_oauth_enabled`
   - `feishu_app_id`
   - `feishu_app_secret`
   - `credentials_configured`

4. 新增 Alembic 迁移 `backend/alembic/versions/20260409_0011_add_system_settings.py` 创建 `system_settings` 表。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_feishu_settings_api.py -q -k "runtime or feature"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/models/system_setting.py` 包含 `__tablename__ = "system_settings"`
    - `backend/app/services/system_setting_service.py` 包含 `get_effective_feishu_settings`
    - `backend/app/services/system_setting_service.py` 包含 `credentials_configured`
    - `backend/alembic/versions/20260409_0011_add_system_settings.py` 包含 `system_settings`
  </acceptance_criteria>
  <done>飞书运行时设置已具备持久化与统一读取服务，前端可写能力有了真实目标</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 feishu settings/system/auth/sync API，并补权限与脱敏测试</name>
  <files>backend/app/schemas/feishu.py, backend/app/api/v1/feishu_settings.py, backend/app/api/v1/system.py, backend/app/api/v1/feishu_auth.py, backend/app/api/v1/feishu_sync.py, backend/app/services/feishu_client.py, backend/tests/test_feishu_settings_api.py</files>
  <read_first>
    - backend/app/api/v1/feishu_settings.py
    - backend/app/api/v1/system.py
    - backend/app/api/v1/feishu_auth.py
    - backend/app/api/v1/feishu_sync.py
    - backend/app/services/feishu_client.py
    - backend/app/dependencies.py
  </read_first>
  <action>
1. 在 `backend/app/schemas/feishu.py` 中新增/扩展运行时设置 schema，至少覆盖：
   - 脱敏 read model：`masked_app_id`、`secret_configured`、`feishu_sync_enabled`、`feishu_oauth_enabled`
   - write model：`app_id`、`app_secret`、`feishu_sync_enabled`、`feishu_oauth_enabled`

2. 修改 `backend/app/api/v1/feishu_settings.py`：
   - 让 `/credentials/status` 和读写凭证端点基于 `get_effective_feishu_settings()`
   - 写入凭证前先执行现有在线校验
   - 读取接口不返回原始 secret
   - 写接口限制为 `admin`
   - SyncConfig CRUD 保持现有行为；如需读权限区分，至少保证写 runtime settings 不向 HR/employee 开放

3. 修改 `backend/app/api/v1/system.py`、`backend/app/api/v1/feishu_auth.py`、`backend/app/api/v1/feishu_sync.py`、`backend/app/services/feishu_client.py`，统一走 effective settings，而不是直接读取 `request.app.state.settings` 中的 env 值。

4. 新增 `backend/tests/test_feishu_settings_api.py`，至少覆盖：
   - admin 可读脱敏后的 runtime settings
   - admin 可更新 flags 和凭证
   - secret 不被回显
   - HR/employee 写入返回 403
   - feature flags endpoint 在 DB override 后返回新值
   - field discovery / sync disabled 分支仍按新设置工作
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_feishu_settings_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/feishu.py` 包含 `masked_app_id`
    - `backend/app/schemas/feishu.py` 包含 `secret_configured`
    - `backend/app/api/v1/feishu_settings.py` 包含 `admin`
    - `backend/app/api/v1/system.py` 包含 `get_effective_feishu_settings`
    - `backend/tests/test_feishu_settings_api.py` 包含 `masked_app_id`
    - `cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_feishu_settings_api.py -q` 退出码为 0
  </acceptance_criteria>
  <done>飞书 runtime settings 已成为可安全读写、且能真正影响 OAuth/同步行为的后端能力</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime credential storage | secret 可写但不可回显，且不能让非 admin 修改 |
| Effective settings resolution | 所有飞书入口必须读取同一设置源，避免 UI 与实际运行状态分叉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | credentials read API | mitigate | 读取接口仅返回 `masked_app_id` 和 `secret_configured`，不返回原始 secret |
| T-20-06 | Elevation of Privilege | runtime settings write API | mitigate | 仅 admin 可写，测试覆盖 HR/employee 403 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_feishu_settings_api.py -q -k "runtime or feature"`
2. `cd /Users/mac/PycharmProjects/5xyj && python3 -m pytest backend/tests/test_feishu_settings_api.py -q`
</verification>

<success_criteria>
- 飞书运行时配置可持久化并前端可写
- 所有飞书入口统一读取 effective settings
- secret 不被回显，权限边界清晰
</success_criteria>

<output>
After completion, create `.planning/phases/20-compare-redesign-and-feishu-enhancement/20-03-SUMMARY.md`
</output>
