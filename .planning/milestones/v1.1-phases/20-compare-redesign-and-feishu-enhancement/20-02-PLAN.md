---
phase: 20-compare-redesign-and-feishu-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - frontend/src/services/compare.ts
  - frontend/src/components/CompareWorkbookDiff.tsx
  - frontend/src/pages/PeriodCompare.tsx
  - frontend/src/pages/Compare.tsx
  - frontend/tests/e2e/compare-diff.spec.ts
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "存在共享 CompareWorkbookDiff 组件，PeriodCompare 与 Compare 复用同一套左右 workbook 视图"
    - "diff viewer 左右面板横向与纵向滚动同步，表头保持对齐"
    - "changed 单元格高亮，left_only/right_only 行有明确背景区分"
    - "PeriodCompare 使用服务端分页/过滤，不再固定请求 500 行后在前端嵌套表格里二次处理"
    - "月度对比页面在 500+ 员工数据窗口下仍然只渲染当前页 rows"
  artifacts:
    - path: "frontend/src/components/CompareWorkbookDiff.tsx"
      provides: "共享左右 workbook diff viewer"
      contains: "syncScroll"
    - path: "frontend/src/pages/PeriodCompare.tsx"
      provides: "月度对比 Phase 20 diff 重做"
      contains: "CompareWorkbookDiff"
    - path: "frontend/src/pages/Compare.tsx"
      provides: "批次对比与月度对比对齐的 diff 视图"
      contains: "CompareWorkbookDiff"
    - path: "frontend/tests/e2e/compare-diff.spec.ts"
      provides: "compare diff 浏览器级回归"
      contains: "差异高亮"
  key_links:
    - from: "frontend/src/services/compare.ts"
      to: "frontend/src/pages/PeriodCompare.tsx"
      via: "page/window metadata"
      pattern: "total_pages"
    - from: "frontend/src/components/CompareWorkbookDiff.tsx"
      to: "frontend/src/pages/Compare.tsx"
      via: "shared diff viewer reuse"
      pattern: "CompareWorkbookDiff"
---

<objective>
把 compare 页面真正重做成 diff 风格的左右 Excel 视图，并让月度对比与批次对比复用同一套共享 viewer。

Purpose: Phase 20 的用户价值在于“看到差异像看两张对照表”，而不是继续在 summary table 或卡片列表里找左右值。这个 plan 负责实现可复用的 workbook diff viewer，并接到两个 compare 页面。
Output: shared diff viewer + compare UI rebuild + e2e
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-compare-redesign-and-feishu-enhancement/20-RESEARCH.md
@.planning/phases/20-compare-redesign-and-feishu-enhancement/20-VALIDATION.md

<interfaces>
Expected period compare window fields:
```ts
page: number
page_size: number
total_pages: number
returned_row_count: number
diff_only: boolean
search_text: string | null
```

Existing compare row shape:
```ts
interface CompareRow {
  compare_key: string
  diff_status: string
  different_fields: string[]
  left: { values: Record<string, CompareCellValue> }
  right: { values: Record<string, CompareCellValue> }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建共享 CompareWorkbookDiff 组件并扩展 compare service 类型</name>
  <files>frontend/src/services/compare.ts, frontend/src/components/CompareWorkbookDiff.tsx</files>
  <read_first>
    - frontend/src/services/compare.ts
    - frontend/src/pages/Compare.tsx
    - frontend/src/pages/PeriodCompare.tsx
    - frontend/src/theme/useSemanticColors.ts
    - .planning/phases/20-compare-redesign-and-feishu-enhancement/20-RESEARCH.md
  </read_first>
  <action>
1. 在 `frontend/src/services/compare.ts` 中补齐 PeriodCompare 的窗口字段类型，并暴露 `searchText` / `diffOnly` 参数给 `fetchPeriodCompare()`。

2. 新增 `frontend/src/components/CompareWorkbookDiff.tsx`，组件 props 至少包含：
   - `fields`
   - `rows`
   - `leftLabel`
   - `rightLabel`
   - `loading?`
   - `identityFields?`
   - `emptyDescription?`
   组件行为要求：
   - 左右两侧以并排表格/工作表样式展示
   - 姓名/工号/证件号等身份列固定在左侧
   - 横向滚动和纵向滚动同步
   - changed 字段的单元格高亮
   - `left_only` / `right_only` 整行有明显底色
   - 不引入新依赖
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/services/compare.ts src/components/CompareWorkbookDiff.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/services/compare.ts` 包含 `searchText`
    - `frontend/src/services/compare.ts` 包含 `diffOnly`
    - `frontend/src/components/CompareWorkbookDiff.tsx` 包含 `CompareWorkbookDiff`
    - `frontend/src/components/CompareWorkbookDiff.tsx` 包含 `different_fields`
    - `frontend/src/components/CompareWorkbookDiff.tsx` 包含同步滚动逻辑
  </acceptance_criteria>
  <done>共享 diff viewer 已具备左右 workbook、滚动同步和差异高亮能力</done>
</task>

<task type="auto">
  <name>Task 2: 用共享 viewer 重构 PeriodCompare 与 Compare 页面</name>
  <files>frontend/src/pages/PeriodCompare.tsx, frontend/src/pages/Compare.tsx, frontend/src/components/CompareWorkbookDiff.tsx</files>
  <read_first>
    - frontend/src/pages/PeriodCompare.tsx
    - frontend/src/pages/Compare.tsx
    - frontend/src/services/compare.ts
    - frontend/src/components/CompareWorkbookDiff.tsx
  </read_first>
  <action>
1. 重构 `frontend/src/pages/PeriodCompare.tsx`：
   - 保留账期/地区/公司/搜索入口
   - 新增 `仅看差异` 开关
   - 使用服务端分页，前端不再固定请求 `pageSize=500`
   - 结果区改为 `CompareWorkbookDiff`
   - summary statistics 继续保留，但不再使用 expandable nested table

2. 重构 `frontend/src/pages/Compare.tsx`：
   - 保留已有“现有批次 / 本地文件”来源选择和导出能力
   - 结果展示统一切到 `CompareWorkbookDiff`
   - 对比类型切换（salary / final_tool）仍然保留
   - 原有过滤、关键词搜索、onlyDifferences 继续存在，但以共享 viewer 展示

3. 保持 Phase 18 的窄屏可用性，不允许因为 Phase 20 diff viewer 重做而破坏手机/平板下的横向滚动合同。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/pages/PeriodCompare.tsx src/pages/Compare.tsx src/components/CompareWorkbookDiff.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/PeriodCompare.tsx` 包含 `CompareWorkbookDiff`
    - `frontend/src/pages/PeriodCompare.tsx` 包含 `diffOnly`
    - `frontend/src/pages/Compare.tsx` 包含 `CompareWorkbookDiff`
    - `frontend/src/pages/Compare.tsx` 仍包含 `compareTableType`
  </acceptance_criteria>
  <done>月度对比和批次对比已共享同一套 diff 视图与交互骨架</done>
</task>

<task type="auto">
  <name>Task 3: 补 compare diff Playwright 回归测试</name>
  <files>frontend/tests/e2e/compare-diff.spec.ts, frontend/src/pages/PeriodCompare.tsx, frontend/src/pages/Compare.tsx</files>
  <read_first>
    - frontend/tests/e2e/responsive.spec.ts
    - frontend/playwright.config.ts
    - frontend/src/pages/PeriodCompare.tsx
    - frontend/src/pages/Compare.tsx
  </read_first>
  <action>
1. 新增 `frontend/tests/e2e/compare-diff.spec.ts`，使用 route mocking 覆盖：
   - `PeriodCompare` 的左右 workbook 渲染
   - changed 单元格高亮
   - left_only / right_only 行底色
   - 页码切换只请求当前窗口
   - `Compare` 页面复用同一 viewer 后仍可切换表类型和过滤 onlyDifferences

2. 断言同步滚动和窄屏横向滚动的 DOM 合同存在，不要求像素级动画断言，但要校验关键容器和属性。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run test:e2e -- compare-diff.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/tests/e2e/compare-diff.spec.ts` 包含 `PeriodCompare`
    - `frontend/tests/e2e/compare-diff.spec.ts` 包含 `Compare`
    - `frontend/tests/e2e/compare-diff.spec.ts` 包含 `different_fields`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run test:e2e -- compare-diff.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>compare diff viewer 有浏览器级回归保护，Phase 20 的 UI 重做不再只靠人工验收</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared diff viewer | 不能因视觉重做而隐藏关键身份/金额列或打乱左右对齐 |
| Period compare pagination | 翻页与过滤组合不能产生“看的是 A 页，统计来自 B 页”的错配 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | shared diff viewer state | mitigate | compare viewer 只消费后端 rows/window metadata；e2e 断言切页后只请求当前窗口 |
| T-20-04 | Denial of Service | 500+ compare rendering | mitigate | 仅渲染当前分页窗口，不引入一次性渲染 500+ rows 的 nested tables |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/services/compare.ts src/components/CompareWorkbookDiff.tsx src/pages/PeriodCompare.tsx src/pages/Compare.tsx`
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run test:e2e -- compare-diff.spec.ts`
</verification>

<success_criteria>
- 存在共享 workbook diff viewer
- 月度对比页面符合左右 Excel diff 风格和差异高亮要求
- Compare/PeriodCompare 不再各维护一套差异渲染逻辑
</success_criteria>

<output>
After completion, create `.planning/phases/20-compare-redesign-and-feishu-enhancement/20-02-SUMMARY.md`
</output>
