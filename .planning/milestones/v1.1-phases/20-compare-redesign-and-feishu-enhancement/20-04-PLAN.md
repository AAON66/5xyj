---
phase: 20-compare-redesign-and-feishu-enhancement
plan: 04
type: execute
wave: 2
depends_on: ["20-03"]
files_modified:
  - frontend/src/services/feishu.ts
  - frontend/src/hooks/useFeishuFeatureFlag.ts
  - frontend/src/pages/FeishuSettings.tsx
  - frontend/src/pages/FeishuSync.tsx
  - frontend/tests/e2e/feishu-settings.spec.ts
autonomous: true
requirements:
  - FEISHU-01

must_haves:
  truths:
    - "FeishuSettings 页面可直接查看和修改功能开关、凭证状态与同步目标"
    - "凭证表单保存后只展示脱敏状态，不会在 UI 或 toast 中回显 secret"
    - "保存 runtime settings 后，feature flags hook 会刷新，sync/settings 页面状态保持一致"
    - "FeishuSync 页面在功能关闭或凭证缺失时给出明确状态和返回设置页入口"
    - "移动端仍可完成飞书设置编辑，不因新增配置区破坏响应式布局"
  artifacts:
    - path: "frontend/src/services/feishu.ts"
      provides: "runtime settings read/write client"
      contains: "updateFeishuRuntimeSettings"
    - path: "frontend/src/hooks/useFeishuFeatureFlag.ts"
      provides: "可刷新 feature flags 的 hook"
      contains: "refreshFlags"
    - path: "frontend/src/pages/FeishuSettings.tsx"
      provides: "飞书设置工作台"
      contains: "应用凭证"
    - path: "frontend/tests/e2e/feishu-settings.spec.ts"
      provides: "飞书前端配置浏览器级回归"
      contains: "masked_app_id"
  key_links:
    - from: "frontend/src/services/feishu.ts"
      to: "frontend/src/hooks/useFeishuFeatureFlag.ts"
      via: "runtime settings save -> flags refresh"
      pattern: "fetchFeatureFlags"
    - from: "frontend/src/hooks/useFeishuFeatureFlag.ts"
      to: "frontend/src/pages/FeishuSync.tsx"
      via: "disabled/missing-credentials state"
      pattern: "feishu_credentials_configured"
---

<objective>
把飞书相关能力收口成真正可用的前端配置闭环：设置页可改凭证和开关，同步页能感知新状态并给出明确引导。

Purpose: 后端即使具备 runtime settings，如果前端仍停留在“只会列 SyncConfig”，Phase 20 的 FEISHU-01 仍然没有完成。这个 plan 负责把 admin 配置流和浏览器级回归补齐。
Output: Feishu settings hub + feature flag refresh + e2e
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-compare-redesign-and-feishu-enhancement/20-RESEARCH.md
@.planning/phases/20-compare-redesign-and-feishu-enhancement/20-VALIDATION.md

<interfaces>
Current frontend Feishu pages:
```ts
fetchSyncConfigs()
createSyncConfig()
updateSyncConfig()
deleteSyncConfig()
```

Current flags hook:
```ts
export function useFeishuFeatureFlag() {
  const [flags, setFlags] = useState(DEFAULT_FLAGS)
  const [loading, setLoading] = useState(true)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展前端 feishu client 与 feature flag hook</name>
  <files>frontend/src/services/feishu.ts, frontend/src/hooks/useFeishuFeatureFlag.ts</files>
  <read_first>
    - frontend/src/services/feishu.ts
    - frontend/src/hooks/useFeishuFeatureFlag.ts
    - frontend/src/pages/FeishuSettings.tsx
    - frontend/src/pages/FeishuSync.tsx
  </read_first>
  <action>
1. 在 `frontend/src/services/feishu.ts` 中新增 runtime settings 相关 client，至少包含：
   - `fetchFeishuRuntimeSettings()`
   - `updateFeishuRuntimeSettings()`
   - 如后端分离为单独凭证端点，可按实际接口拆分，但要保持 settings 页调用清晰

2. 重构 `useFeishuFeatureFlag()`，增加显式刷新能力，例如：
   - `refreshFlags()`
   - 保存 runtime settings 后可主动刷新，而不是强依赖整页重载

3. 保持现有 SyncConfig、sync history、push/pull client 不回归。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/services/feishu.ts src/hooks/useFeishuFeatureFlag.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/services/feishu.ts` 包含 `fetchFeishuRuntimeSettings`
    - `frontend/src/services/feishu.ts` 包含 `updateFeishuRuntimeSettings`
    - `frontend/src/hooks/useFeishuFeatureFlag.ts` 包含 `refreshFlags`
  </acceptance_criteria>
  <done>前端已有统一的 runtime settings client 和可刷新的 feature flags hook</done>
</task>

<task type="auto">
  <name>Task 2: 重构 FeishuSettings 与 FeishuSync 页面形成配置闭环</name>
  <files>frontend/src/pages/FeishuSettings.tsx, frontend/src/pages/FeishuSync.tsx, frontend/src/services/feishu.ts, frontend/src/hooks/useFeishuFeatureFlag.ts</files>
  <read_first>
    - frontend/src/pages/FeishuSettings.tsx
    - frontend/src/pages/FeishuSync.tsx
    - frontend/src/hooks/useFeishuFeatureFlag.ts
    - frontend/src/services/feishu.ts
    - frontend/src/hooks/useResponsiveViewport.ts
  </read_first>
  <action>
1. 重构 `frontend/src/pages/FeishuSettings.tsx`，至少包含：
   - 功能开关编辑区（飞书同步 / 飞书登录）
   - 应用凭证状态区（显示 `masked_app_id`、secret 状态）
   - 凭证编辑表单（app id / app secret）
   - 现有 SyncConfig CRUD 列表继续保留
   - 保存成功后刷新 feature flags，并给出明确但不泄露 secret 的提示

2. 调整 `frontend/src/pages/FeishuSync.tsx`：
   - 当功能关闭时显示禁用态提示
   - 当凭证缺失时显示“去设置页配置”的 CTA
   - 已配置时继续保留现有 push/pull/history 体验

3. 保持移动端 drawer/form 布局可操作，不允许把凭证表单和同步目标区堆成不可滚动的大块内容。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/pages/FeishuSettings.tsx src/pages/FeishuSync.tsx src/services/feishu.ts src/hooks/useFeishuFeatureFlag.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/FeishuSettings.tsx` 包含运行时开关编辑
    - `frontend/src/pages/FeishuSettings.tsx` 包含凭证编辑表单
    - `frontend/src/pages/FeishuSettings.tsx` 包含 `masked_app_id`
    - `frontend/src/pages/FeishuSync.tsx` 包含“去设置页”或等价 CTA
  </acceptance_criteria>
  <done>飞书设置页和同步页已形成可配置、可感知状态变化的前端闭环</done>
</task>

<task type="auto">
  <name>Task 3: 新增 feishu settings Playwright 回归测试</name>
  <files>frontend/tests/e2e/feishu-settings.spec.ts, frontend/src/pages/FeishuSettings.tsx, frontend/src/pages/FeishuSync.tsx</files>
  <read_first>
    - frontend/tests/e2e/responsive.spec.ts
    - frontend/playwright.config.ts
    - frontend/src/pages/FeishuSettings.tsx
    - frontend/src/pages/FeishuSync.tsx
  </read_first>
  <action>
1. 新增 `frontend/tests/e2e/feishu-settings.spec.ts`，使用 route mocking 覆盖：
   - settings 页加载 runtime settings
   - admin 编辑并保存 flags/credentials
   - 保存后页面只显示脱敏状态，不出现明文 secret
   - SyncConfig 创建/编辑仍可用
   - `FeishuSync` 在凭证缺失时显示引导，在配置完成后恢复可操作状态

2. 额外覆盖一个窄屏视口，确认移动端 drawer / 表单 / CTA 不会失效。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run test:e2e -- feishu-settings.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/tests/e2e/feishu-settings.spec.ts` 包含 `masked_app_id`
    - `frontend/tests/e2e/feishu-settings.spec.ts` 包含 settings save flow
    - `frontend/tests/e2e/feishu-settings.spec.ts` 包含 sync disabled/missing credentials state
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run test:e2e -- feishu-settings.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>飞书配置闭环具备浏览器级回归保护，可覆盖保存/刷新/引导三类核心行为</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Credentials UI | 表单保存与展示不能泄露 secret，也不能让 UI 状态与后端 effective settings 分叉 |
| Sync page state | disabled / missing credentials 时必须给出可恢复入口，而不是沉默失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Information Disclosure | settings page | mitigate | UI 只显示 `masked_app_id` 和配置状态，toast/alert 不拼接原始 secret |
| T-20-08 | Spoofing | feature flag refresh | mitigate | 保存后显式 `refreshFlags()`，e2e 覆盖 settings -> sync 状态联动 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && ./node_modules/.bin/eslint src/services/feishu.ts src/hooks/useFeishuFeatureFlag.ts src/pages/FeishuSettings.tsx src/pages/FeishuSync.tsx`
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run test:e2e -- feishu-settings.spec.ts`
</verification>

<success_criteria>
- admin 可在前端直接修改飞书开关与凭证
- 保存后 flags 与 sync 页面状态即时一致
- secret 不在 UI 与浏览器级测试中泄露
</success_criteria>

<output>
After completion, create `.planning/phases/20-compare-redesign-and-feishu-enhancement/20-04-SUMMARY.md`
</output>
