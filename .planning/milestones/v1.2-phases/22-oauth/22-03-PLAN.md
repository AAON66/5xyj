---
phase: 22-oauth
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/src/pages/Settings.tsx
  - frontend/src/services/feishu.ts
autonomous: false
requirements:
  - OAUTH-04
must_haves:
  truths:
    - "已登录用户在设置页看到'飞书账号绑定'卡片"
    - "未绑定时显示绑定按钮，点击后跳转飞书授权"
    - "已绑定时显示飞书昵称 + 解绑按钮"
    - "解绑后卡片恢复为未绑定状态"
  artifacts:
    - path: "frontend/src/pages/Settings.tsx"
      provides: "飞书账号绑定卡片"
      contains: "feishu-bind"
    - path: "frontend/src/services/feishu.ts"
      provides: "fetchBindAuthorizeUrl + unbindFeishu API"
      contains: "unbindFeishu"
  key_links:
    - from: "frontend/src/pages/Settings.tsx"
      to: "frontend/src/services/feishu.ts"
      via: "fetchBindAuthorizeUrl + unbindFeishu 调用"
      pattern: "unbindFeishu"
---

<objective>
在个人设置页新增"飞书账号绑定"卡片，支持已登录用户主动绑定/解绑飞书账号。

Purpose: 实现 OAUTH-04，让非飞书登录的用户也能关联飞书身份。
Output: Settings.tsx 飞书绑定卡片 + feishu.ts bind/unbind API。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-oauth/22-CONTEXT.md
@.planning/phases/22-oauth/22-01-SUMMARY.md

@frontend/src/pages/Settings.tsx
@frontend/src/services/feishu.ts
@frontend/src/hooks/useAuth.ts
@frontend/src/services/authSession.ts

<interfaces>
From Plan 01 后端 API 契约:
```
GET /auth/feishu/bind-authorize-url -> { url: string } (需 JWT)
POST /auth/feishu/bind-callback -> { feishu_name: string } (需 JWT)
POST /auth/feishu/unbind -> {} (需 JWT)
GET /api/v1/auth/me -> { ..., feishu_bound: boolean, feishu_display_name: string | null }
```

From frontend/src/pages/Settings.tsx:
```typescript
interface SettingsCardConfig {
  key: string;
  title: string;
  description: string;
  keywords: string[];
  linkTo?: string;
  roles: string[];
  feishuOnly?: boolean;
}
const SETTINGS_CARDS: SettingsCardConfig[] = [...]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: feishu.ts bind/unbind API + Settings.tsx 绑定飞书卡片</name>
  <files>
    frontend/src/services/feishu.ts
    frontend/src/pages/Settings.tsx
  </files>
  <read_first>
    frontend/src/services/feishu.ts
    frontend/src/pages/Settings.tsx
    frontend/src/hooks/useAuth.ts
    frontend/src/services/api.ts
    frontend/src/hooks/useFeishuFeatureFlag.ts
  </read_first>
  <action>
    1. **扩展 `feishu.ts`** (注意: Plan 02 也修改此文件但修改不同区域，Plan 02 改 OAuth 区域，此 Plan 改 bind 区域):
       - 新增 `fetchBindAuthorizeUrl()`: GET `/auth/feishu/bind-authorize-url`，返回 `string`（URL）
       - 新增 `feishuBindCallback(code: string, state: string)`: POST `/auth/feishu/bind-callback`，返回 `{ feishu_name: string }`
       - 新增 `unbindFeishu()`: POST `/auth/feishu/unbind`，返回 `void`

    2. **修改 `Settings.tsx`** (per D-07, D-08, D-10):
       - 在 SETTINGS_CARDS 数组新增一个卡片配置:
         ```
         { key: 'feishu-bind', title: '飞书账号绑定', description: '绑定或解绑飞书账号，绑定后可使用飞书快捷登录',
           keywords: ['飞书', '绑定', '账号', '解绑', 'feishu'], roles: ['admin', 'hr', 'employee'] }
         ```
       - 此卡片不使用 `linkTo`，而是在 Settings 组件内自定义渲染
       - 新增 state: `feishuBound: boolean`, `feishuDisplayName: string | null`, `bindLoading: boolean`
       - 组件挂载时调用 `/me` 获取当前用户的 feishu_bound 和 feishu_display_name 状态（可通过 useAuth hook 获取，或单独 fetch）
       - 处理 bind 回调: 检测 URL 参数 `code` + `state` + `action=bind`，存在则调用 `feishuBindCallback` 完成绑定，成功后更新状态
       - 飞书绑定卡片内容渲染:
         - **未绑定**: 显示"未绑定飞书账号" + Button "绑定飞书"，点击调用 `fetchBindAuthorizeUrl()` 然后 `window.location.href = url`
         - **已绑定**: 显示 Tag "已绑定" + feishu_display_name + Button "解绑"（type="text" danger）
         - 解绑按钮点击: 弹出 `Modal.confirm("确定要解绑飞书账号吗？解绑后将无法使用飞书快捷登录。")`，确认后调用 `unbindFeishu()`，成功后更新状态
       - 仅在 `feishu_oauth_enabled` 为 true 时显示此卡片（通过 `useFeishuFeatureFlag` 控制）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchBindAuthorizeUrl" frontend/src/services/feishu.ts
    - grep -q "unbindFeishu" frontend/src/services/feishu.ts
    - grep -q "feishu-bind" frontend/src/pages/Settings.tsx
    - grep -q "飞书账号绑定" frontend/src/pages/Settings.tsx
    - grep -q "unbindFeishu" frontend/src/pages/Settings.tsx
  </acceptance_criteria>
  <done>
    - feishu.ts 新增 fetchBindAuthorizeUrl / feishuBindCallback / unbindFeishu 三个 API 函数
    - Settings.tsx 新增飞书账号绑定卡片
    - 未绑定显示绑定按钮，已绑定显示飞书昵称+解绑按钮
    - lint + build 通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 设置页绑定飞书验证</name>
  <files>frontend/src/pages/Settings.tsx</files>
  <action>
    人工验证设置页"飞书账号绑定"卡片功能。验证步骤：
    1. 用密码登录系统（admin 或 hr 账号）
    2. 进入"系统设置"页面
    3. 确认看到"飞书账号绑定"卡片，显示"未绑定"状态 + 绑定按钮
    4. 点击"绑定飞书"按钮，应跳转到飞书授权页
    5. 完成飞书授权后，应回到设置页并显示"已绑定" + 飞书昵称 + 解绑按钮
    6. 点击"解绑"按钮，确认弹窗后应恢复为未绑定状态
    7. 搜索"飞书"关键词，确认卡片能被搜索匹配
  </action>
  <verify>人工验证，用户输入 "approved" 或描述问题</verify>
  <done>用户确认设置页飞书绑定/解绑功能正常工作</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings bind -> 后端 bind-callback | 已登录用户发起绑定，后端验证 JWT + state |
| Settings unbind -> 后端 unbind | 已登录用户发起解绑，后端验证 JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Tampering | Settings 页面 unbind | mitigate | unbind 操作前弹出 Modal.confirm 二次确认，后端 require_authenticated_user 确保只能解绑自己 |
| T-22-11 | Spoofing | bind-callback | accept | 安全保障由后端 T-22-02 覆盖（JWT + state cookie 双重验证） |
</threat_model>

<verification>
1. `npm run lint` 通过
2. `npm run build` 通过
3. 人工验证设置页绑定/解绑飞书流程
</verification>

<success_criteria>
- 设置页展示飞书账号绑定卡片
- 未绑定 -> 点击绑定 -> 飞书授权 -> 回到设置页已绑定状态
- 已绑定 -> 点击解绑 -> 确认 -> 恢复未绑定状态
- 所有角色（admin/hr/employee）均可看到此卡片
- lint + build 通过
</success_criteria>

<output>
After completion, create `.planning/phases/22-oauth/22-03-SUMMARY.md`
</output>
