---
phase: 23-login-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified:
  - frontend/src/components/BrandPanel.tsx
  - frontend/src/pages/Login.tsx
autonomous: false
requirements:
  - LOGIN-01
  - LOGIN-02
  - LOGIN-03
  - LOGIN-04
user_setup: []
nyquist_compliant: true

must_haves:
  truths:
    - "桌面 >=1024px 登录页呈现左右分栏 60:40；平板 768-1023px 呈 50:50；移动 <768px 单列表单"
    - "左侧面板在桌面显示 BrandPanel（品牌 wordmark + slogan + sub-slogan + copyright），移动端不渲染"
    - "桌面 WebGL 可用：左侧渲染 ParticleWave（React.lazy 懒加载），Suspense fallback 是 CssGradientBackground"
    - "桌面 WebGL 不可用：左侧直接渲染 CssGradientBackground，不尝试加载 ParticleWave chunk"
    - "暗黑模式下表单卡片 backdrop-filter: blur(20px) saturate(180%) 生效（两前缀写全）；亮模式实色白背景"
    - "原认证逻辑零丢失 — 账号登录、员工查询、飞书 OAuth 回调、CandidateSelectModal、Navigate 重定向全部保留"
    - "document.title = 登录 · 社保公积金管理系统 通过 useEffect 设置"
    - "Form Card 含 data-testid='login-form-card'；BrandPanel 含 data-testid='brand-panel' — 供 Wave 0 E2E 测试"
    - "npm run build 产物 dist/assets 下出现独立的 ParticleWave 相关 chunk"
    - "5 个 Wave 0 E2E 测试 (layout-desktop / layout-mobile / particle / webgl-fallback / dark-mode) 在 chromium 下全部绿灯"
  artifacts:
    - path: "frontend/src/components/BrandPanel.tsx"
      provides: "左侧品牌文字层（wordmark/slogan/sub-slogan/copyright）"
      exports: ["BrandPanel"]
      min_lines: 50
    - path: "frontend/src/pages/Login.tsx"
      provides: "重构后的登录页（左右分栏 + lazy ParticleWave + 玻璃卡片 + 原认证逻辑）"
      contains: "React.lazy"
      min_lines: 300
  key_links:
    - from: "Login.tsx"
      to: "ParticleWave"
      via: "React.lazy dynamic import"
      pattern: "lazy\\(\\(\\) => import"
    - from: "Login.tsx"
      to: "CssGradientBackground"
      via: "Suspense fallback + WebGL fallback branch"
      pattern: "CssGradientBackground"
    - from: "Login.tsx"
      to: "useWebGLSupport"
      via: "hook 调用决定 canvas 分支"
      pattern: "useWebGLSupport"
    - from: "Login.tsx"
      to: "window.matchMedia('(min-width: 1024px)')"
      via: "UI-SPEC 桌面断点，独立于 useResponsiveViewport 的 992px 断点"
      pattern: "min-width: 1024px"
    - from: "Login.tsx form card"
      to: "backdrop-filter"
      via: "isDark 分支玻璃态"
      pattern: "backdropFilter"
---

<objective>
把 Plan 01/02 的基础件装配到 `Login.tsx`，完成登录页外观重构 — 左右分栏 + 懒加载粒子背景 + WebGL 降级 + 暗黑玻璃卡片 + 品牌文字层。必须：
1. 保留原 Login.tsx 全部认证逻辑（账号登录/员工查询/飞书 OAuth/候选人选择/Navigate）— 零回归
2. 新增 BrandPanel 组件承载左侧文字（wordmark/slogan/sub-slogan/copyright）
3. 桌面根据 useWebGLSupport 结果分流 ParticleWave（lazy）或 CssGradientBackground
4. 移动端仅渲染右侧表单
5. 暗模式表单卡片套玻璃拟态
6. 让 Plan 01 写的 5 个 E2E 测试全部转绿

Purpose: LOGIN-01（分栏布局）+ LOGIN-03（WebGL 降级集成）+ LOGIN-04（玻璃卡片）+ 完成 LOGIN-02 的用户可见路径

Output: 新 BrandPanel 组件 + 重构后的 Login.tsx + Wave 0 测试绿 + human-verify checkpoint
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-login-redesign/23-CONTEXT.md
@.planning/phases/23-login-redesign/23-RESEARCH.md
@.planning/phases/23-login-redesign/23-UI-SPEC.md
@.planning/phases/23-login-redesign/23-VALIDATION.md
@.planning/phases/23-login-redesign/23-01-SUMMARY.md
@.planning/phases/23-login-redesign/23-02-SUMMARY.md

<interfaces>
Plan 01 已导出:

```typescript
// frontend/src/hooks/useWebGLSupport.ts
export type WebGLSupport = 'loading' | 'webgl' | 'fallback';
export function useWebGLSupport(): WebGLSupport;

// frontend/src/components/CssGradientBackground.tsx
export function CssGradientBackground(props: { isDark: boolean }): JSX.Element;
```

Plan 02 已导出（lazy import）:

```typescript
// frontend/src/components/ParticleWave.tsx — default export
interface ParticleWaveProps { isDark: boolean; }
export default function ParticleWave(props: ParticleWaveProps): JSX.Element;
// canvas 节点含 data-testid="particle-wave-canvas" 和 data-particle-color attr
```

项目既有 hooks（不改动）:

```typescript
// frontend/src/hooks/useResponsiveViewport.ts
// 断点：isMobile <=767, isTablet 768-991, isCompactDesktop 992-1440, isDesktopWide >=1441
// ⚠️ 本 hook 的 tablet/desktop 边界是 992px（Ant lg），不是 UI-SPEC 要求的 1024px
export function useResponsiveViewport(): {
  isMobile: boolean; isTablet: boolean; isCompactDesktop: boolean; isDesktopWide: boolean;
};

// frontend/src/theme/useThemeMode.ts
export function useThemeMode(): { isDark: boolean; mode: 'light' | 'dark'; setMode: (m: ...) => void; };

// frontend/src/theme/useSemanticColors.ts
export function useSemanticColors(): { BG_LAYOUT: string; };
```

Wave 0 测试锁定的 DOM 契约:
- data-testid="brand-panel"            — BrandPanel 根元素（Task 1）
- data-testid="particle-wave-canvas"   — ParticleWave canvas（Plan 02 已暴露）
- data-testid="css-gradient-background" — CssGradientBackground（Plan 01 已暴露）
- data-testid="login-form-card"        — 表单 Card（Task 2）
- localStorage key = 'theme-mode'（**核实自 ThemeModeProvider.tsx:14**，非 'social-security-theme-mode'），值 'dark' 用于 dark mode 测试

UI-SPEC 锁定文案（Simplified Chinese）:
- Brand wordmark: 社保公积金管理系统（20px/600）
- Slogan: 多地区社保数据，一个系统统一汇聚（28px/600）
- Sub-slogan: 自动识别 · 智能映射 · 一键导出（14px/400，· 是 U+00B7 中文间隔符）
- Copyright: © 社保公积金管理系统（14px/400）
- Document title: 登录 · 社保公积金管理系统

Layout（UI-SPEC 锁定，与 useResponsiveViewport 992px 断点冲突 → 本 plan 用独立的 1024px matchMedia 解决）:
- desktop >=1024: 60:40 (left:right)
- tablet 768-1023: 50:50
- mobile <768: 仅右侧

暗模式 Glass Card:
- background: rgba(30, 35, 45, 0.72)
- WebkitBackdropFilter 和 backdropFilter 两个都写: blur(20px) saturate(180%)
- border: 1px solid rgba(255, 255, 255, 0.08)
- 亮模式: 白底实色，无 backdrop-filter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 BrandPanel 组件（左侧品牌文字层）</name>
  <files>frontend/src/components/BrandPanel.tsx</files>
  <read_first>
    - .planning/phases/23-login-redesign/23-UI-SPEC.md（Brand Panel Content 节与 Typography 节逐字对齐）
    - frontend/src/theme/index.ts（字体/字重 token 只读，不修改）
    - frontend/src/components/CandidateSelectModal.tsx（项目组件 named export 风格）
  </read_first>
  <action>
    创建 `frontend/src/components/BrandPanel.tsx`，实现左侧品牌文字叠层 — 不含 canvas 或背景图（背景由 ParticleWave 或 CssGradientBackground 负责，本组件只是上层文字）。

    【import 示例】顶部必需：

    ```typescript
    import type { CSSProperties } from 'react';
    import { Typography } from 'antd';

    const { Text } = Typography;

    interface BrandPanelProps {
      isDark: boolean;
    }
    ```

    结构要点（逐字严守，按 UI-SPEC 锁定）：

    - 命名导出 `export function BrandPanel({ isDark }: BrandPanelProps)`
    - 根 div 样式：`position: absolute; inset: 0; padding: 48; pointerEvents: 'none'; display: flex; flexDirection: column; justifyContent: space-between; zIndex: 2`
    - 根 div 带 `data-testid="brand-panel"` 和 `data-is-dark={isDark ? 'true' : 'false'}`
    - 顶部：Ant Design `<Text>`，文案 `社保公积金管理系统`，样式 `fontSize: 20, fontWeight: 600, lineHeight: 1.3`，颜色亮模式 `#FFFFFF`、暗模式 `#F0F5FF`，textShadow `0 1px 2px rgba(0, 0, 0, 0.25)`
    - 中部容器（`display: flex; flexDirection: column; gap: 16; maxWidth: 420`），内含：
      - `<h1>` slogan 文案 `多地区社保数据，一个系统统一汇聚`（注意是中文逗号 `，`），样式 `fontSize: 28, fontWeight: 600, lineHeight: 1.25`，颜色亮模式 `#FFFFFF`、暗模式 `#E8F0FF`，margin 0，textShadow `0 2px 6px rgba(0, 0, 0, 0.25)`
      - `<p>` sub-slogan 文案 `自动识别 · 智能映射 · 一键导出`（`·` 必须是 U+00B7 中文间隔符，不是 `.` / `*` / `•`），样式 `fontSize: 14, fontWeight: 400, lineHeight: 1.5714`，颜色 `rgba(255, 255, 255, 0.72)`，margin 0
    - 底部：Ant Design `<Text>`，文案 `© 社保公积金管理系统`（`©` 是 U+00A9），样式 `fontSize: 14, fontWeight: 400, lineHeight: 1.5714`，颜色 `rgba(255, 255, 255, 0.5)`

    严格规则：
    - **3 个字号字面量只有 `20`, `28`, `14`**（UI-SPEC Typography 节锁定）
    - **2 个字重字面量只有 `400` 和 `600`**
    - `pointerEvents: 'none'` 必须存在 — 否则 ParticleWave 的 mousemove 事件会被文字层吞掉
    - `zIndex: 2` 确保文字在 canvas 之上
    - 文本有 textShadow 保障浅色文字在浅蓝渐变上的对比度 >= 4.5:1（UI-SPEC Accessibility）
    - 使用命名导出（named export），与 Plan 01 Component 风格一致
    - 顶部和底部的纯文本必须通过 `<Text>`（`Typography.Text`）渲染 — 这是 UI-SPEC Component Inventory 锁定的 Ant Design Typography 用法

    不得：
    - 不引入任何背景/渐变 CSS（背景由 Parent 控制）
    - 不引入新字号（仅 3 个）或新字重（仅 2 个）
    - 不使用 `dangerouslySetInnerHTML`
    - 不引入 colorPrimary / colorError 等 accent 色（UI-SPEC 明令 accent 仅用于 CTA）
    - 不引入 react-router-dom 的 `<Link>`（本组件纯展示）
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; test -f src/components/BrandPanel.tsx &amp;&amp; grep -Fq "export function BrandPanel" src/components/BrandPanel.tsx &amp;&amp; grep -Fq "from 'antd'" src/components/BrandPanel.tsx &amp;&amp; grep -Fq "Typography" src/components/BrandPanel.tsx &amp;&amp; grep -Fq "多地区社保数据，一个系统统一汇聚" src/components/BrandPanel.tsx &amp;&amp; grep -Fq "自动识别 · 智能映射 · 一键导出" src/components/BrandPanel.tsx &amp;&amp; grep -Fq "© 社保公积金管理系统" src/components/BrandPanel.tsx &amp;&amp; grep -Fq 'data-testid="brand-panel"' src/components/BrandPanel.tsx &amp;&amp; grep -Fq "pointerEvents" src/components/BrandPanel.tsx &amp;&amp; ! grep -Fq "dangerouslySetInnerHTML" src/components/BrandPanel.tsx &amp;&amp; npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `frontend/src/components/BrandPanel.tsx` 存在、> 50 行
    - 命名导出 `export function BrandPanel` 存在
    - 4 条文案字面量精确命中：`社保公积金管理系统`、`多地区社保数据，一个系统统一汇聚`、`自动识别 · 智能映射 · 一键导出`、`© 社保公积金管理系统`
    - 包含 `from 'antd'` 导入
    - 包含 `Typography`（或解构后的 `Text`）字面量
    - 包含 `data-testid="brand-panel"` 字面量
    - 包含 `pointerEvents` 属性（React 内联样式 camelCase）
    - 字号字面量总共只有 3 个不同值：20、28、14（可通过 `grep -oE 'fontSize: [0-9]+' | sort -u` 验证）
    - 字重字面量总共只有 2 个不同值：400、600
    - **不含** `dangerouslySetInnerHTML`
    - `cd frontend && npx tsc --noEmit` 无错误
    - `cd frontend && npm run lint` 对该文件无 error
  </acceptance_criteria>
  <done>
    BrandPanel 组件通过 TS/lint；使用 Ant Design `Typography.Text`；暴露 testid；pointer-events:none 保证 ParticleWave 鼠标事件不被拦截
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 Login.tsx — 分栏 + lazy ParticleWave + 玻璃卡片</name>
  <files>frontend/src/pages/Login.tsx</files>
  <read_first>
    - frontend/src/pages/Login.tsx（当前完整实现，**必须原样保留所有 hooks / handlers / Form 结构 / CandidateSelectModal 使用**；特别注意第 118-120 行 isAuthenticated 早返回）
    - .planning/phases/23-login-redesign/23-UI-SPEC.md（Layout Contract / Color / Typography / Interaction & Motion / Accessibility 全章）
    - .planning/phases/23-login-redesign/23-RESEARCH.md（Pattern 3 + Login.tsx 分栏骨架代码示例）
    - frontend/src/hooks/useWebGLSupport.ts（Plan 01 创建的 hook 签名）
    - frontend/src/components/CssGradientBackground.tsx（Plan 01）
    - frontend/src/components/ParticleWave.tsx（Plan 02 — default export，必须 lazy import）
    - frontend/src/components/BrandPanel.tsx（本 Plan Task 1 刚创建）
    - frontend/src/hooks/useResponsiveViewport.ts（仅用其 `isMobile` — 因 UI-SPEC 的 1024 边界与本 hook 的 992 不一致，tablet/desktop 分流改走独立 matchMedia）
    - frontend/src/theme/useThemeMode.ts（isDark）
    - frontend/src/theme/useSemanticColors.ts（BG_LAYOUT）
    - frontend/src/theme/ThemeModeProvider.tsx（**第 14 行 STORAGE_KEY = 'theme-mode'**；Plan 01 的 E2E dark mode 测试依赖此 key）
  </read_first>
  <action>
    重写 `frontend/src/pages/Login.tsx`。**战术：先保留现有所有 hook + handler + form JSX 不动，仅在外层套一个新的分栏布局壳子，再调整 Card 样式。** 这是重构不是重写，**不允许任何认证逻辑改动**。

    按以下顺序修改（Step A 到 Step J）：

    【Step A — 新增 imports（在文件顶部追加）】
    ```typescript
    import { lazy, Suspense, useEffect, useState } from 'react';
    import { useResponsiveViewport } from '../hooks/useResponsiveViewport';
    import { useWebGLSupport } from '../hooks/useWebGLSupport';
    import { useThemeMode } from '../theme/useThemeMode';
    import { CssGradientBackground } from '../components/CssGradientBackground';
    import { BrandPanel } from '../components/BrandPanel';

    const ParticleWave = lazy(() => import('../components/ParticleWave'));
    ```

    - 注意 `lazy(...)` 必须在组件外（module top-level），否则每次 render 会重复创建 lazy 组件
    - 必须保留现有所有 imports（useState, useEffect 已有的合并 — 如果现在没 useEffect 导入要加上）
    - 不删除 `import { App, Button, Card, Divider, Form, Input, Radio, Tabs, Typography }`

    【Step B — 在 LoginPage 函数体开头新增 hooks】
    在现有 `useAuth` / `message` / `colors` / `feishu_oauth_enabled` 之后追加：
    ```typescript
    const { isMobile } = useResponsiveViewport();
    const { isDark } = useThemeMode();
    const webgl = useWebGLSupport();

    // UI-SPEC 锁定的 1024px 桌面断点：直接用 matchMedia，绕开 useResponsiveViewport 的 992px 边界
    const [isDesktop1024, setIsDesktop1024] = useState<boolean>(() => {
      if (typeof window === 'undefined') return true;
      return window.matchMedia('(min-width: 1024px)').matches;
    });
    useEffect(() => {
      if (typeof window === 'undefined') return;
      const mq = window.matchMedia('(min-width: 1024px)');
      const sync = () => setIsDesktop1024(mq.matches);
      sync();
      if ('addEventListener' in mq) {
        mq.addEventListener('change', sync);
        return () => mq.removeEventListener('change', sync);
      }
      // Legacy Safari fallback
      const legacy = mq as MediaQueryList & {
        addListener?: (l: () => void) => void;
        removeListener?: (l: () => void) => void;
      };
      legacy.addListener?.(sync);
      return () => legacy.removeListener?.(sync);
    }, []);
    ```

    不要删除任何现有 hook 调用。

    【Step C — 追加 document.title useEffect】
    在现有 Feishu OAuth useEffect **之后**新增：
    ```typescript
    useEffect(() => {
      const prev = document.title;
      document.title = '登录 · 社保公积金管理系统';
      return () => { document.title = prev; };
    }, []);
    ```

    【Step D — 构造 LeftCanvas 子组件（定义在 LoginPage 函数体内）】
    ```typescript
    const LeftCanvas = () => {
      if (webgl === 'fallback') return <CssGradientBackground isDark={isDark} />;
      // 'webgl' 和 'loading' 都显示 ParticleWave（loading 时 Suspense 接管 fallback 到 CssGradientBackground）
      return (
        <Suspense fallback={<CssGradientBackground isDark={isDark} />}>
          <ParticleWave isDark={isDark} />
        </Suspense>
      );
    };
    ```

    注意：`webgl === 'loading'` 不应立即显示 CssGradientBackground — 否则每次 mount 都会出现短暂渐变闪烁。让 Suspense fallback 接管这 0-50ms 的 hook-loading 阶段即可。

    【Step E — 构建 Card 样式（暗模式玻璃态）】
    替换现有 Card 的内联 style。新的 style 逻辑：
    ```typescript
    const cardStyle: React.CSSProperties = isDark
      ? {
          width: 400,
          background: 'rgba(30, 35, 45, 0.72)',
          WebkitBackdropFilter: 'blur(20px) saturate(180%)',
          backdropFilter: 'blur(20px) saturate(180%)',
          border: '1px solid rgba(255, 255, 255, 0.08)',
          borderRadius: 12,
          boxShadow: 'none', // UI-SPEC: 不与 backdrop-filter 共用 box-shadow（Safari bug）
        }
      : {
          width: 400,
          background: '#FFFFFF',
          borderRadius: 12,
          boxShadow: '0 1px 2px rgba(0,0,0,0.06)', // 保留原阴影
        };
    ```

    Card 元素：
    - 加 `data-testid="login-form-card"` 属性
    - 用 `cardStyle` 替代原有 `style={{ width: 400, boxShadow: ... }}`

    【Step F — 外层容器重构（UI-SPEC 1024px 边界方案）】

    **关键决策（回应 checker ISS-04）：** UI-SPEC 锁定桌面断点为 1024px，而现有 `useResponsiveViewport` 的桌面分水岭是 992px（Ant `lg`），差距 32px。Checker 明确要求：**不能单方面将 1024 降级为 992**。因此本 plan 引入一个独立的 `isDesktop1024` 状态（Step B 已定义），由 `window.matchMedia('(min-width: 1024px)')` 驱动，不复用 hook 的 992 断点。`useResponsiveViewport` 仅提供 `isMobile`（≤767）用于"是否渲染左侧"。

    断点分辨率：
    - `isMobile` (≤ 767px): 不渲染左侧，右侧 flex `1 1 100%`
    - 非 mobile 且 `!isDesktop1024`（即 768-1023px 平板）: 50:50
    - `isDesktop1024`（≥ 1024px 桌面）: 60:40

    Flex 变量：
    ```typescript
    const leftFlex = isDesktop1024 ? '0 0 60%' : '0 0 50%';
    const rightFlex = isMobile ? '1 1 100%' : isDesktop1024 ? '0 0 40%' : '0 0 50%';
    ```

    整个返回 JSX 包成：
    ```tsx
    return (
      <div
        style={{
          display: 'flex',
          minHeight: '100vh',
          background: isDark ? '#141414' : colors.BG_LAYOUT,
        }}
      >
        {!isMobile && (
          <div
            style={{
              flex: leftFlex,
              position: 'relative',
              overflow: 'hidden',
              minHeight: '100vh',
            }}
          >
            <LeftCanvas />
            <BrandPanel isDark={isDark} />
          </div>
        )}
        <div
          style={{
            flex: rightFlex,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            padding: 32,
            minHeight: '100vh',
          }}
        >
          <Card style={cardStyle} data-testid="login-form-card">
            {/* 保留所有现有内容：Title + Tabs + Feishu 按钮 + 员工查询链接 */}
          </Card>
        </div>
        <CandidateSelectModal ... /> {/* 保留原样，放在最外层 div 内作为兄弟 */}
      </div>
    );
    ```

    - `leftFlex` / `rightFlex` 必须是 Step B 里通过 matchMedia 驱动的 `isDesktop1024` 所得，**不得**改回 `useResponsiveViewport` 的 `isTablet`。grep 校验：`min-width: 1024px` 字面量必须出现在 Login.tsx 中。

    【Step G — 保留项清单（代码审查用，必须逐条核对）】
    **以下现有逻辑必须保持字节级相同或仅做 style 微调**：
    1. `useAuth` 和 `isAuthenticated` 早返回（`<Navigate to={DEFAULT_WORKSPACE_BY_ROLE[user.role]} replace />`）
    2. Feishu OAuth 回调 useEffect（完整保留 code/state 处理、bind: 分支转发、`feishuOAuthCallback` 调用、`pending_candidates` 分支 + `writeAuthSession`）
    3. `handleCandidateSelect`, `handleFeishuLogin`, `handleCredentialSubmit`, `handleEmployeeSubmit` 四个 handler 原封不动
    4. `passwordWarning`, `submitting`, `activeTab` 等 state
    5. `DEFAULT_WORKSPACE_BY_ROLE` 和 `resolveTargetPath`
    6. Tabs `items` 两个 tab 的 Form 结构（Radio.Group、Input、Input.Password、Button type="primary"）
    7. 原有文案：`管理员入口`/`HR 入口`/`用户名`/`密码`/`工号`/`身份证号`/`姓名`/`登录`/`验证身份`/`当前密码为默认密码，请尽快修改。` 等
    8. 原有 message.error 文案
    9. `CandidateSelectModal` 的使用方式
    10. `Link to="/employee/query"` 和"员工自助查询仍可直接使用。"文本

    【Step H — 不得的改动】
    - 不修改 `useAuth` hook
    - 不修改 `authSession` service
    - 不修改 `feishu` service
    - 不修改任何 API endpoint 调用
    - 不移除 Feishu feature flag (`feishu_oauth_enabled`) 的条件渲染
    - 不改变 DEFAULT_WORKSPACE_BY_ROLE 映射
    - 不删除 `passwordWarning` 的 `Text type="warning"` 提示
    - 不使用 `dangerouslySetInnerHTML`
    - 不从 CDN 加载 three（Plan 01 已 pin 为 npm 依赖）
    - 不把 UI-SPEC 的 1024 桌面断点降级为 992（必须用 `matchMedia('(min-width: 1024px)')`）

    【Step I — Form Card 位置上的覆盖】
    亮模式卡片：依旧 `background: #FFFFFF`，borderRadius 改为 12（UI-SPEC），boxShadow 保留
    暗模式卡片：玻璃态（两前缀 backdrop-filter 都写）

    【Step J — 验证】
    1. `cd frontend && npx tsc --noEmit` 无错
    2. `cd frontend && npm run lint` 无 error（warnings 可接受）
    3. `cd frontend && npm run test:e2e -- tests/e2e/login-redesign.spec.ts` — 5 个测试全部绿（`layout — desktop split`、`layout — mobile form-only`、`particle — canvas renders`、`webgl fallback`、`dark mode`）
    4. Acceptance gate（在 Task 3 human-verify 之前跑一次，作为门槛）：`cd frontend && npm run build && npm run test:e2e` — build 成功 + 全量 E2E 无回归；检查 `dist/assets/` 里出现独立的 ParticleWave chunk（文件名形如 `ParticleWave-*.js` 或含 `three` 的 chunk）

    本任务同时关闭 LOGIN-01 / LOGIN-02（用户可见路径）/ LOGIN-03（集成降级）/ LOGIN-04（玻璃卡片部分）。
  </action>
  <verify>
    <!--
      Fast feedback <automated>: tsc + lint + 仅跑 login-redesign.spec.ts（<15s 反馈，满足 VALIDATION "Max feedback latency: 15s"）
      完整 build + 全量 E2E 在 <acceptance_criteria> 的 "Acceptance gate" 条目，和 Task 3 checkpoint 之前各跑一次
    -->
    <automated>cd frontend &amp;&amp; grep -Fq "lazy(() => import('../components/ParticleWave'" src/pages/Login.tsx &amp;&amp; grep -Fq "useWebGLSupport" src/pages/Login.tsx &amp;&amp; grep -Fq "useResponsiveViewport" src/pages/Login.tsx &amp;&amp; grep -Fq "useThemeMode" src/pages/Login.tsx &amp;&amp; grep -Fq "CssGradientBackground" src/pages/Login.tsx &amp;&amp; grep -Fq "BrandPanel" src/pages/Login.tsx &amp;&amp; grep -Fq "Suspense" src/pages/Login.tsx &amp;&amp; grep -Fq 'data-testid="login-form-card"' src/pages/Login.tsx &amp;&amp; grep -Fq "登录 · 社保公积金管理系统" src/pages/Login.tsx &amp;&amp; grep -Fq "rgba(30, 35, 45, 0.72)" src/pages/Login.tsx &amp;&amp; grep -Fq "WebkitBackdropFilter" src/pages/Login.tsx &amp;&amp; grep -Fq "blur(20px) saturate(180%)" src/pages/Login.tsx &amp;&amp; grep -Fq "min-width: 1024px" src/pages/Login.tsx &amp;&amp; grep -Fq "handleCredentialSubmit" src/pages/Login.tsx &amp;&amp; grep -Fq "handleEmployeeSubmit" src/pages/Login.tsx &amp;&amp; grep -Fq "handleFeishuLogin" src/pages/Login.tsx &amp;&amp; grep -Fq "handleCandidateSelect" src/pages/Login.tsx &amp;&amp; grep -Fq "feishuOAuthCallback" src/pages/Login.tsx &amp;&amp; grep -Fq "CandidateSelectModal" src/pages/Login.tsx &amp;&amp; grep -Fq "DEFAULT_WORKSPACE_BY_ROLE" src/pages/Login.tsx &amp;&amp; ! grep -Fq "dangerouslySetInnerHTML" src/pages/Login.tsx &amp;&amp; npx tsc --noEmit --project tsconfig.json &amp;&amp; npm run lint &amp;&amp; npm run test:e2e -- tests/e2e/login-redesign.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/Login.tsx` 含以下字面量全部命中：
      - `lazy(() => import('../components/ParticleWave'`
      - `useWebGLSupport`, `useResponsiveViewport`, `useThemeMode`
      - `CssGradientBackground`, `BrandPanel`, `Suspense`
      - `data-testid="login-form-card"`
      - 文档标题字面量 `登录 · 社保公积金管理系统`
      - 玻璃态字面量 `rgba(30, 35, 45, 0.72)` 和 `blur(20px) saturate(180%)` 和 `WebkitBackdropFilter`
      - **UI-SPEC 1024 断点字面量**：`min-width: 1024px` 精确出现（不得改成 992 或其他值）
    - 原认证逻辑保留（全部 grep 命中）：
      - `handleCredentialSubmit`, `handleEmployeeSubmit`, `handleFeishuLogin`, `handleCandidateSelect`
      - `feishuOAuthCallback`, `CandidateSelectModal`, `DEFAULT_WORKSPACE_BY_ROLE`
    - **不含** `dangerouslySetInnerHTML`
    - `cd frontend && npx tsc --noEmit` 无错（快速反馈，<15s 目标）
    - `cd frontend && npm run lint` 无 error
    - `cd frontend && npm run test:e2e -- tests/e2e/login-redesign.spec.ts` 5 个测试全绿（layout-desktop / layout-mobile / particle / webgl-fallback / dark-mode）
    - **Acceptance gate（Task 3 checkpoint 之前必须跑一次）：**
      - `cd frontend && npm run build` 成功
      - `cd frontend && npm run test:e2e` 全量回归无新红
      - `ls frontend/dist/assets/` 存在独立的 ParticleWave 或 three chunk（非 main bundle 内联）
  </acceptance_criteria>
  <done>
    Login.tsx 重构完成，分栏 + 玻璃卡片 + lazy ParticleWave 全部就位；Wave 0 五项 E2E 测试从 RED → GREEN；Acceptance gate (build + 全量 E2E) 无回归；无 any 类型、无 dangerouslySetInnerHTML、build 产物独立 chunk；UI-SPEC 1024px 断点使用独立 matchMedia
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工视觉 & 无障碍验证（checkpoint）</name>
  <what-built>
    - Login.tsx 分栏布局（60:40 / 50:50 / mobile 表单），品牌文字层，ParticleWave 懒加载背景，CssGradientBackground 降级
    - 暗模式下表单卡片玻璃拟态
    - 粒子波浪鼠标跟随
    - 原认证逻辑完整保留
  </what-built>
  <how-to-verify>
    **先决条件：** Task 2 快速反馈 + acceptance gate 全绿后再执行本 checkpoint。

    **验证步骤 1 — 桌面 WebGL 可用：**
    1. `cd frontend && npm run dev -- --host 127.0.0.1 --port 4173` 启动 dev server（端口与 `playwright.config.ts` strictPort 4173 对齐）
    2. 浏览器访问 http://127.0.0.1:4173/login
    3. 确认左右分栏 ≈ 60:40，左侧出现粒子波浪（蓝色海浪流动）
    4. 左侧品牌文字清晰可见：顶部"社保公积金管理系统"、中部大字"多地区社保数据，一个系统统一汇聚"、下方"自动识别 · 智能映射 · 一键导出"、底部"© 社保公积金管理系统"
    5. 鼠标在左侧 canvas 上移动，粒子在鼠标位置柔和隆起跟随；移出 canvas 后 0.8s 内平滑回落
    6. 浏览器控制台**不得**出现 "Too many active WebGL contexts" 警告
    7. 多次刷新页面（Ctrl+R × 5），控制台依旧无 WebGL 警告；粒子每次正常渲染

    **验证步骤 2 — 暗模式切换：**
    1. 点击右上角（或现有 ThemeSwitcher）切换暗黑模式
    2. 确认粒子色谱变为"深海荧光"（亮粒子 `#7FA7FF`、深色背景）
    3. 表单卡片变为玻璃拟态（半透明，透出背后粒子）
    4. 文案对比度充足，表单可读
    5. Safari（macOS 原生）实机打开同一路径 `http://127.0.0.1:4173/login`，暗模式下玻璃效果必须可见（不是纯色兜底）

    **验证步骤 3 — 响应式：**
    1. Chrome DevTools 切换 iPhone 14 / 375px 宽度
    2. 左侧面板消失，只剩右侧表单卡片；表单居中
    3. 切换 iPad / 820px 宽度
    4. 左右分栏接近 50:50
    5. 切换到 1024px 宽度，分栏应**立即**切换到 60:40（验证 UI-SPEC 的 1024 边界由 `matchMedia` 精确驱动）

    **验证步骤 4 — WebGL 降级：**
    1. Chrome DevTools → More Tools → Rendering → 勾选"Disable WebGL"（或在 chrome://flags 搜索 WebGL 关闭）
    2. 刷新登录页
    3. 左侧应为 CSS 线性渐变，带缓慢浮动
    4. 登录功能不受影响（点"登录"依然可用）
    5. 开关恢复后粒子恢复

    **验证步骤 5 — Reduced motion：**
    1. macOS System Settings → 辅助功能 → 显示 → 勾选"减少动态效果"
    2. 刷新登录页
    3. 粒子变为静态网格，鼠标移动不再产生跟随
    4. CSS 渐变降级下也应停止浮动动画

    **验证步骤 6 — 认证回归：**
    1. 用现有测试账号（admin / admin123 或项目约定账号）登录，能进入 `/workspace/admin`
    2. 切换到"员工查询"Tab，用测试员工信息验证，能进入 `/employee/query`
    3. 如有飞书环境，测试 `使用飞书登录` 跳转 OAuth → 回调 → 进入系统 或 候选人选择 Modal
    4. 刷新 `/login?code=xxx&state=xxx` 不报错（OAuth 回调 useEffect 仍生效）

    **性能抽检（可选）：**
    1. Chrome DevTools → Performance → 录制 5 秒（带 dev server）
    2. 帧率应接近 60fps，无 Long task > 100ms
    3. Network tab 确认 ParticleWave chunk 独立懒加载（route 切入 /login 时才请求）
  </how-to-verify>
  <files>N/A — 人工验证 checkpoint，不修改文件</files>
  <action>按下方 how-to-verify 步骤 1-6 逐一在浏览器/Safari/DevTools 执行视觉与交互验证；如发现任何一项不通过，回到 Task 1/2 修复后重跑 E2E + 本 checkpoint</action>
  <verify>人工签字 approved；或记录具体问题现象</verify>
  <done>用户输入 approved，代表桌面分栏、粒子波浪、鼠标跟随、暗模式玻璃态、响应式断点、WebGL 降级、reduced-motion、认证回归 8 项人工验证全部通过</done>
  <resume-signal>
    输入 `approved` 继续；若发现问题，描述具体现象（如 "iOS Safari 暗模式卡片无玻璃效果" 或 "粒子色谱切换时闪烁")，回到 Task 1/2 修复后重来
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Login.tsx OAuth callback useEffect | URL query string (code/state) 来自 Feishu，已在 Phase 22 做过校验 — 本 plan 不重做 |
| Form inputs → backend /api/v1/auth/* | 沿用现有 Ant Design Form rules + authSession 逻辑 — 无改动 |
| React.lazy() dynamic import URL | 模块路径是静态字符串字面量，无用户输入拼接 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Denial of Service | 继承自 Plan 02 — WebGL context 泄露 | mitigate | Plan 02 已实现 cleanup；本 Plan 通过 React.lazy 懒加载，进一步减少未使用页面的 WebGL 负担 |
| T-23-03 | Tampering | dangerouslySetInnerHTML 注入 | mitigate | Task 1/2 明令禁用；grep 校验不含该字符串；所有文案通过 JSX 文本节点或 Typography 组件渲染 |
| T-23-08 | Elevation of Privilege | OAuth callback 重定向劫持 | accept | 现有 Login.tsx bind: 分支通过 `window.location.href` 同域跳转 `/settings?...`；Phase 22 已验证；本 Plan 不改此逻辑，继承该安全边界 |
| T-23-09 | Information Disclosure | document.title 泄露路由信息 | accept | title `登录 · 社保公积金管理系统` 仅泄露应用名 — 与任何公开登录页一致 |
| T-23-10 | Tampering | 懒加载 chunk 在 CDN/代理层被篡改 | accept | Vite 构建已使用 Subresource Integrity 默认；dev 环境不涉及；prod 部署需 HTTPS — 与项目其他 lazy 组件（现未使用）风险同级 |

**ASVS L1 mapping:**
- V5.1 Input Validation: 所有表单输入沿用现有 Ant Design Form rules（V5 minimal 标记符合 RESEARCH Security Domain）
- V14.2 Dependency: three 已 pin；lazy 导入路径为静态 — Mitigated
- WCAG 2.1 AA 对比度：BrandPanel textShadow 保障浅色文字在蓝渐变上 ≥ 4.5:1 — Mitigated by Task 1
- WCAG 2.3.3 Reduced motion: ParticleWave + CssGradientBackground 均已处理 — Mitigated by Plan 01/02
</threat_model>

<verification>
## Plan-Level Checks

**Fast feedback (< 15s latency, 对齐 VALIDATION sampling contract)：**
1. `cd frontend && npx tsc --noEmit` 无错
2. `cd frontend && npm run lint` 无 error
3. `cd frontend && npm run test:e2e -- tests/e2e/login-redesign.spec.ts` 5/5 绿（快速 E2E 集）

**Acceptance gate（Task 3 checkpoint 前跑一次）：**
4. `cd frontend && npm run build` 成功
5. `cd frontend && npm run test:e2e` 全量绿（无回归）
6. `ls frontend/dist/assets/*.js | head -20` 出现 ParticleWave 或 three 相关独立 chunk
7. Human-verify checkpoint 签字通过

## Decision Coverage

| Decision | Implemented in Task | Status |
|----------|--------------------|--------|
| D-07 暗黑玻璃拟态 | Task 2 Step E | Full |
| LOGIN-01 分栏布局 | Task 2 Step F（1024px matchMedia） | Full |
| LOGIN-02 用户可见路径 | Task 2 Step D (LeftCanvas) | Full |
| LOGIN-03 降级集成 | Task 2 Step D (webgl==='fallback' 分支) | Full |
| LOGIN-04 玻璃卡片 | Task 2 Step E | Full |

## Requirement Coverage

| Requirement | Evidence |
|-------------|----------|
| LOGIN-01 | `useResponsiveViewport.isMobile` 分流移动；`matchMedia('(min-width: 1024px)')` 精确分流平板/桌面；isMobile 单列；tablet 50:50；desktop 60:40 |
| LOGIN-02 | ParticleWave 懒加载 + isDark prop 驱动；Plan 02 实现 + Plan 03 集成 |
| LOGIN-03 | useWebGLSupport 分流；webgl='fallback' → CssGradientBackground；Suspense fallback 同一组件 |
| LOGIN-04 | isDark 分支玻璃卡片 backdrop-filter（含两前缀）+ ParticleWave isDark 切色谱 |
</verification>

<success_criteria>
- Login.tsx 重构完成，保留全部认证逻辑（11 处 grep 验证点命中）
- BrandPanel 组件通过 TS/lint，4 条文案精确命中，使用 Ant Design Typography
- Wave 0 E2E 测试 5/5 绿
- 全量 E2E 无回归
- build 产物 dist 下存在 ParticleWave/three 独立 chunk（bundle splitting 生效）
- 人工 checkpoint 通过（含桌面/移动/暗黑/降级/reduced-motion/认证回归 6 项）
- UI-SPEC 1024 桌面断点使用独立 `matchMedia('(min-width: 1024px)')`，不降级为 992
- 无 `any` 类型、无 `dangerouslySetInnerHTML`、无 CDN 加载 three
</success_criteria>

<output>
完成后写 `.planning/phases/23-login-redesign/23-03-SUMMARY.md`，记录：
- Login.tsx 重构前后文件行数对比
- Wave 0 5 个测试结果 (RED → GREEN)
- dist/assets 下的独立 chunk 名称列表
- 人工 checkpoint 签字内容（包含任何未解决的视觉小瑕疵）
- Safari/reduced-motion 实机验证结果
- 更新 VALIDATION.md 的 Per-Task Verification Map（把 Task ID TBD 换成真实 Plan/Task 编号）
</output>
</output>
