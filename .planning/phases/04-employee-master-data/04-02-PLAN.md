---
phase: 04-employee-master-data
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/services/employees.ts
  - frontend/src/pages/Employees.tsx
  - frontend/src/pages/EmployeeCreate.tsx
autonomous: false
requirements:
  - MASTER-01
  - MASTER-03

must_haves:
  truths:
    - "员工列表页有地区(region)下拉筛选，选项从 API 获取"
    - "员工列表页有公司(company_name)下拉筛选，选项从 API 获取"
    - "筛选联动分页：切换筛选条件后自动回到第一页"
    - "创建/编辑员工表单包含 region 下拉选择框"
    - "批量导入完成后显示新增/更新/失败统计和失败明细"
  artifacts:
    - path: "frontend/src/services/employees.ts"
      provides: "region 字段 + 筛选参数 + fetchRegions/fetchCompanies API"
      contains: "fetchRegions"
    - path: "frontend/src/pages/Employees.tsx"
      provides: "筛选下拉 + region 展示 + 导入结果增强"
      contains: "region"
    - path: "frontend/src/pages/EmployeeCreate.tsx"
      provides: "创建表单含 region 下拉"
      contains: "region"
  key_links:
    - from: "frontend/src/pages/Employees.tsx"
      to: "frontend/src/services/employees.ts"
      via: "fetchRegions() + fetchCompanies() 调用"
      pattern: "fetchRegions|fetchCompanies"
    - from: "frontend/src/services/employees.ts"
      to: "GET /api/v1/employees/regions"
      via: "apiClient.get('/employees/regions')"
      pattern: "employees/regions"
---

<objective>
前端完善：员工列表添加地区/公司筛选下拉、创建/编辑表单添加 region 字段、导入结果反馈增强。

Purpose: 让 HR 可以在界面上按地区和公司筛选员工，创建/编辑时设置地区，导入后看到完整的统计反馈。
Output: 前端 3 个文件更新，完成 MASTER-01 和 MASTER-03 的前端部分
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-employee-master-data/04-CONTEXT.md
@.planning/phases/04-employee-master-data/04-01-SUMMARY.md

<interfaces>
<!-- 执行者必须先读取以下现有代码和 Plan 01 产出 -->

From frontend/src/services/employees.ts (Plan 01 后已更新):
```typescript
export interface EmployeeMasterItem {
  id: string; employee_id: string; person_name: string;
  id_number: string | null; company_name: string | null;
  department: string | null; region: string | null;  // Plan 01 新增
  active: boolean; created_at: string; updated_at: string;
}

export interface EmployeeMasterCreateInput {
  employee_id: string; person_name: string; id_number: string | null;
  company_name: string | null; department: string | null;
  region: string | null;  // Plan 01 新增
  active: boolean;
}

export interface EmployeeImportResult {
  file_name: string; total_rows: number; imported_count: number;
  created_count: number; updated_count: number; skipped_count: number;
  errors: string[];  // Plan 01 后会有实际错误内容
  items: EmployeeMasterItem[];
}

// Plan 01 新增的 API 函数：
export async function fetchRegions(): Promise<string[]>
export async function fetchCompanies(): Promise<string[]>
export async function fetchEmployeeMasters(params?: {
  query?: string; activeOnly?: boolean; limit?: number; offset?: number;
  region?: string; companyName?: string;  // Plan 01 新增
}): Promise<EmployeeMasterList>
```

From frontend/src/pages/Employees.tsx:
```typescript
// 现有状态（需扩展）：
const [query, setQuery] = useState('');
const [activeOnly, setActiveOnly] = useState(false);
const [pageSize, setPageSize] = useState<number>(10);
const [pageIndex, setPageIndex] = useState(0);
const [importResult, setImportResult] = useState<EmployeeImportResult | null>(null);
// L396-402: importResult 展示（基本实现，需增强展示错误明细）
```

From backend API (Plan 01 新增):
- GET /api/v1/employees/regions -> ["广州", "杭州", "厦门", "深圳", "武汉", "长沙"]
- GET /api/v1/employees/companies -> ["公司A", "公司B", ...]
- GET /api/v1/employees?region=深圳&company_name=xxx -> 筛选结果
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 前端 API 层 + 筛选/表单/导入反馈 UI</name>
  <files>
    frontend/src/services/employees.ts
    frontend/src/pages/Employees.tsx
    frontend/src/pages/EmployeeCreate.tsx
  </files>
  <read_first>
    frontend/src/services/employees.ts
    frontend/src/pages/Employees.tsx
    frontend/src/pages/EmployeeCreate.tsx
    frontend/src/config/env.ts
  </read_first>
  <action>
**注意：** Plan 01 可能已经更新了 `employees.ts` 的类型定义。先读取文件确认当前状态再修改。

**1. employees.ts API 层扩展**

a) 确认 `EmployeeMasterItem` 已有 `region: string | null` 字段。如果 Plan 01 没加，在此添加。

b) 确认 `EmployeeMasterCreateInput` 和 `EmployeeMasterUpdateInput` 已有 `region: string | null`。如果没加，在此添加。

c) 更新 `fetchEmployeeMasters` 参数类型，添加筛选参数：
```typescript
export async function fetchEmployeeMasters(params?: {
  query?: string;
  activeOnly?: boolean;
  limit?: number;
  offset?: number;
  region?: string;       // 新增 per D-11
  companyName?: string;  // 新增 per D-12
}): Promise<EmployeeMasterList> {
  const response = await apiClient.get<ApiSuccessResponse<EmployeeMasterList>>('/employees', {
    params: {
      query: params?.query || undefined,
      active_only: params?.activeOnly ?? undefined,
      limit: params?.limit ?? undefined,
      offset: params?.offset ?? undefined,
      region: params?.region || undefined,
      company_name: params?.companyName || undefined,
    },
  });
  return response.data.data;
}
```

d) 新增两个 API 函数：
```typescript
export async function fetchRegions(): Promise<string[]> {
  const response = await apiClient.get<ApiSuccessResponse<string[]>>('/employees/regions');
  return response.data.data;
}

export async function fetchCompanies(): Promise<string[]> {
  const response = await apiClient.get<ApiSuccessResponse<string[]>>('/employees/companies');
  return response.data.data;
}
```

**2. Employees.tsx 筛选下拉**

a) 新增状态变量：
```typescript
const [regions, setRegions] = useState<string[]>([]);
const [companies, setCompanies] = useState<string[]>([]);
const [selectedRegion, setSelectedRegion] = useState<string>('');
const [selectedCompany, setSelectedCompany] = useState<string>('');
```

b) 在组件挂载时加载地区和公司列表（与员工列表加载并行）：
```typescript
useEffect(() => {
  fetchRegions().then(setRegions).catch(() => {});
  fetchCompanies().then(setCompanies).catch(() => {});
}, []);
```

c) 更新员工列表加载 useEffect，将 selectedRegion 和 selectedCompany 加入依赖和参数：
```typescript
// 在调用 fetchEmployeeMasters 时传入筛选参数
fetchEmployeeMasters({
  query: query || undefined,
  activeOnly: activeOnly || undefined,
  limit: pageSize,
  offset: pageIndex * pageSize,
  region: selectedRegion || undefined,
  companyName: selectedCompany || undefined,
})
```
依赖数组添加 `selectedRegion, selectedCompany`。

d) 切换筛选条件时自动回到第一页：
```typescript
// selectedRegion 或 selectedCompany 变化时重置 pageIndex
useEffect(() => { setPageIndex(0); }, [selectedRegion, selectedCompany]);
```

e) 在搜索框下方（或旁边）添加两个 `<select>` 下拉：

```tsx
<select value={selectedRegion} onChange={e => setSelectedRegion(e.target.value)}>
  <option value="">全部地区</option>
  {regions.map(r => <option key={r} value={r}>{r}</option>)}
</select>

<select value={selectedCompany} onChange={e => setSelectedCompany(e.target.value)}>
  <option value="">全部公司</option>
  {companies.map(c => <option key={c} value={c}>{c}</option>)}
</select>
```

f) 员工表格中新增"地区"列，显示 `employee.region || '-'`。放在"公司"列之后。

g) 编辑表单 (formState) 中添加 region 字段：
- `EmployeeFormState` interface 添加 `region: string`
- `EMPTY_FORM` 添加 `region: ''`
- `toFormState` 函数添加 `region: employee.region ?? ''`
- 表单提交时包含 region
- 编辑表单中添加 region 下拉选择框（使用与筛选相同的 regions 列表）

**3. Employees.tsx 导入结果反馈增强 (per D-06, D-14)**

找到现有 importResult 展示区域（约 L396-402），增强为：

```tsx
{importResult && (
  <div className="import-result-panel">
    <h4>导入结果</h4>
    <p>总行数: {importResult.total_rows} | 新增: {importResult.created_count} | 更新: {importResult.updated_count} | 失败: {importResult.skipped_count}</p>
    {importResult.errors.length > 0 && (
      <details>
        <summary>失败明细 ({importResult.errors.length} 条)</summary>
        <ul>
          {importResult.errors.map((err, i) => <li key={i}>{err}</li>)}
        </ul>
      </details>
    )}
  </div>
)}
```

导入成功后也要刷新 companies 列表（因为新导入的员工可能包含新公司名）：
```typescript
// 导入成功回调中添加
fetchCompanies().then(setCompanies).catch(() => {});
```

**4. EmployeeCreate.tsx 添加 region 下拉**

a) 读取文件确认当前表单字段。

b) 新增状态：
```typescript
const [regions, setRegions] = useState<string[]>([]);
const [region, setRegion] = useState<string>('');
```

c) 挂载时加载地区列表：
```typescript
useEffect(() => { fetchRegions().then(setRegions).catch(() => {}); }, []);
```

d) 在表单中"公司"字段后添加 region 下拉：
```tsx
<label>地区</label>
<select value={region} onChange={e => setRegion(e.target.value)}>
  <option value="">请选择地区</option>
  {regions.map(r => <option key={r} value={r}>{r}</option>)}
</select>
```

e) 提交时将 region 包含在 payload 中（空字符串传 null）：
```typescript
region: region || null,
```
  </action>
  <verify>
    <automated>cd D:/execl_mix/frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` 成功，无 TypeScript 类型错误
    - `npm run lint` 通过
    - `EmployeeMasterItem` 接口包含 `region: string | null`
    - `fetchRegions()` 和 `fetchCompanies()` 函数存在且调用正确的 API 路径
    - `fetchEmployeeMasters` 接受 `region` 和 `companyName` 参数
    - Employees.tsx 包含两个 `<select>` 下拉（地区 + 公司）
    - Employees.tsx 表格包含"地区"列
    - Employees.tsx 导入结果展示 created_count/updated_count/skipped_count 和 errors 列表
    - EmployeeCreate.tsx 表单包含 region 下拉选择框
    - 筛选变化时 pageIndex 重置为 0
  </acceptance_criteria>
  <done>前端员工列表支持地区/公司筛选下拉，创建/编辑含 region 字段，导入反馈显示完整统计和失败明细</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证前端筛选和导入体验</name>
  <what-built>
    员工主数据全功能：region 字段全链路、地区/公司下拉筛选、导入容错、双维度匹配。
  </what-built>
  <how-to-verify>
    1. 启动后端 (`uvicorn backend.app.main:app --reload`) 和前端 (`npm run dev`)
    2. 访问员工列表页，确认：
       - 地区下拉包含 6 个选项（广州/杭州/厦门/深圳/武汉/长沙）
       - 公司下拉包含已有公司名
       - 选择筛选条件后列表正确过滤
       - 切换筛选后分页回到第一页
    3. 点击新增员工，确认表单包含"地区"下拉选择框
    4. 创建一个含地区的员工，确认列表中显示地区列
    5. 准备一个含"地区"列和一些缺少工号/姓名行的 Excel 文件，执行批量导入：
       - 确认导入结果显示新增/更新/失败统计
       - 确认失败明细可展开查看
       - 确认有效行被正确导入
    6. 编辑已有员工，确认可修改地区字段
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` — 前端构建成功
2. `npm run lint` — 无 lint 错误
3. 人工验证筛选、创建、导入、编辑流程
</verification>

<success_criteria>
- 员工列表页可按地区和公司筛选
- 创建/编辑表单含 region 下拉
- 导入结果反馈包含完整统计和失败明细
- 前端构建和 lint 通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-employee-master-data/04-02-SUMMARY.md`
</output>
