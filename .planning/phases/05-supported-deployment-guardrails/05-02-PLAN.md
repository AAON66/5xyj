---
phase: 05-supported-deployment-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - DEPLOYMENT.md
  - OPERATIONS.md
  - backend/tests/test_supported_deployment_docs.py
autonomous: true
requirements:
  - OPS-01
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "Operators following the supported deployment path are told to set `RUNTIME_ENVIRONMENT` to a non-local value and to use `ADMIN_LOGIN_PASSWORD`, `HR_LOGIN_PASSWORD`, and `AUTH_SECRET_KEY`."
    - "The canonical deployment checklist and the canonical operations handoff point to the same supported contract instead of presenting deprecated env names as valid."
    - "Doc drift around the supported deployment env contract fails automated verification instead of reappearing silently."
  artifacts:
    - path: "DEPLOYMENT.md"
      provides: "Canonical supported deployment checklist aligned to the executable auth guardrail contract"
      contains: "RUNTIME_ENVIRONMENT=production"
    - path: "OPERATIONS.md"
      provides: "Top-level supported deployment handoff that points operators at the canonical checklist"
      contains: "DEPLOYMENT.md"
    - path: "backend/tests/test_supported_deployment_docs.py"
      provides: "Regression coverage for `.env.example`, `DEPLOYMENT.md`, and `OPERATIONS.md` contract alignment"
      contains: "ADMIN_LOGIN_PASSWORD"
  key_links:
    - from: "OPERATIONS.md"
      to: "DEPLOYMENT.md"
      via: "Supported deployment section links operators to the canonical checklist"
      pattern: "DEPLOYMENT.md"
    - from: "DEPLOYMENT.md"
      to: "backend/app/core/config.py"
      via: "Documented env keys mirror the actual `Settings` field names and require non-local runtime mode"
      pattern: "RUNTIME_ENVIRONMENT|ADMIN_LOGIN_PASSWORD|HR_LOGIN_PASSWORD|AUTH_SECRET_KEY"
    - from: "backend/tests/test_supported_deployment_docs.py"
      to: ".env.example"
      via: "Doc regression asserts the template and runbooks stay aligned on the same env contract"
      pattern: "DEPLOYMENT.md|OPERATIONS.md|.env.example"
---

<objective>
Align the supported deployment runbooks to the executable guardrail contract and make future documentation drift fail automatically.

Purpose: Close `OPS-01` and the remaining contract-facing portions of `SEC-01` and `SEC-02` by making the operator path concrete, canonical, and regression-tested.
Output: Updated `DEPLOYMENT.md` and `OPERATIONS.md` plus a documentation contract test that locks the supported deployment env surface to the code-backed settings contract.
</objective>

<execution_context>
@C:/Users/AAON/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AAON/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-supported-deployment-guardrails/05-CONTEXT.md
@.planning/phases/05-supported-deployment-guardrails/05-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-supported-operations-path/04-01-PLAN.md
@.planning/phases/04-supported-operations-path/04-01-SUMMARY.md
@backend/app/core/config.py
@backend/app/bootstrap.py
@.env.example
@DEPLOYMENT.md
@OPERATIONS.md

<interfaces>
Canonical deployment env keys from `backend/app/core/config.py`:
```python
class Settings(BaseSettings):
    runtime_environment: str = 'local'
    auth_secret_key: str = DEFAULT_AUTH_SECRET_KEY
    admin_login_password: str = DEFAULT_ADMIN_LOGIN_PASSWORD
    hr_login_password: str = DEFAULT_HR_LOGIN_PASSWORD
```

Current supported deployment entrypoint from `backend/run.py`:
```python
def main() -> None:
    settings = bootstrap_application()
    uvicorn.run("backend.app.main:app", host="127.0.0.1", port=8000, reload=False)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the supported deployment docs to the canonical env contract</name>
  <read_first>
    - DEPLOYMENT.md
    - OPERATIONS.md
    - .env.example
    - backend/app/core/config.py
    - backend/app/bootstrap.py
    - .planning/phases/05-supported-deployment-guardrails/05-CONTEXT.md
    - .planning/phases/05-supported-deployment-guardrails/05-RESEARCH.md
  </read_first>
  <files>DEPLOYMENT.md, OPERATIONS.md</files>
  <action>Update `DEPLOYMENT.md` so the supported deployment environment example uses the canonical keys and one explicit non-local runtime value: `RUNTIME_ENVIRONMENT=production`, `ADMIN_LOGIN_PASSWORD=<set-a-non-default-password>`, `HR_LOGIN_PASSWORD=<set-a-non-default-password>`, `AUTH_SECRET_KEY=<generate-a-random-secret>`, `SALARY_TEMPLATE_PATH=...`, and `FINAL_TOOL_TEMPLATE_PATH=...`. Replace any supported-path references to `ADMIN_PASSWORD` and `HR_PASSWORD` with the canonical names, and add one short rule block stating that the supported deployment path is invalid if `runtime_environment` stays `local`. Update `OPERATIONS.md` so its supported deployment section explicitly says the canonical checklist in `DEPLOYMENT.md` uses `RUNTIME_ENVIRONMENT`, `ADMIN_LOGIN_PASSWORD`, `HR_LOGIN_PASSWORD`, and `AUTH_SECRET_KEY`, and that operators should not treat deprecated env names as supported inputs. Keep the existing Linux virtualenv + frontend build + `systemd` deployment path; do not redesign deployment architecture or touch local-start instructions.</action>
  <acceptance_criteria>
    - `Select-String -Path 'DEPLOYMENT.md' -Pattern 'RUNTIME_ENVIRONMENT=production'`
    - `Select-String -Path 'DEPLOYMENT.md' -Pattern 'ADMIN_LOGIN_PASSWORD=<set-a-non-default-password>'`
    - `Select-String -Path 'DEPLOYMENT.md' -Pattern 'HR_LOGIN_PASSWORD=<set-a-non-default-password>'`
    - `Select-String -Path 'DEPLOYMENT.md' -Pattern 'AUTH_SECRET_KEY=<generate-a-random-secret>'`
    - `Select-String -Path 'DEPLOYMENT.md' -Pattern 'invalid if .*runtime_environment.*local|must not leave runtime_environment as local'`
    - `Select-String -Path 'OPERATIONS.md' -Pattern 'DEPLOYMENT.md'`
    - `Select-String -Path 'OPERATIONS.md' -Pattern 'RUNTIME_ENVIRONMENT|ADMIN_LOGIN_PASSWORD|HR_LOGIN_PASSWORD|AUTH_SECRET_KEY'`
    - `Select-String -Path 'DEPLOYMENT.md','OPERATIONS.md' -Pattern 'ADMIN_PASSWORD|HR_PASSWORD'` returns no supported-path matches
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "Select-String -Path 'DEPLOYMENT.md','OPERATIONS.md' -Pattern 'RUNTIME_ENVIRONMENT=production','ADMIN_LOGIN_PASSWORD','HR_LOGIN_PASSWORD','AUTH_SECRET_KEY','DEPLOYMENT.md'"</automated>
  </verify>
  <done>The supported deployment and operator docs now describe one canonical non-local env contract that matches the executable guardrail settings names and rejects the old password env names.</done>
</task>

<task type="auto">
  <name>Task 2: Add documentation contract regression for the supported deployment path</name>
  <read_first>
    - backend/tests/test_supported_deployment_docs.py
    - DEPLOYMENT.md
    - OPERATIONS.md
    - .env.example
    - backend/app/core/config.py
  </read_first>
  <files>backend/tests/test_supported_deployment_docs.py</files>
  <action>Create `backend/tests/test_supported_deployment_docs.py` to lock the supported deployment runbooks to the code-backed env contract. Add one test that reads `.env.example`, `DEPLOYMENT.md`, and `OPERATIONS.md` as UTF-8 text and asserts all three surfaces contain `RUNTIME_ENVIRONMENT`, `AUTH_SECRET_KEY`, `ADMIN_LOGIN_PASSWORD`, and `HR_LOGIN_PASSWORD`. Add one test that asserts `DEPLOYMENT.md` contains the exact example `RUNTIME_ENVIRONMENT=production` and that `OPERATIONS.md` still points operators to `DEPLOYMENT.md`. Add one negative test that asserts `DEPLOYMENT.md` and `OPERATIONS.md` do not present `ADMIN_PASSWORD` or `HR_PASSWORD` as supported deployment inputs. Keep this suite text-only and deterministic; do not shell out or depend on workstation paths.</action>
  <acceptance_criteria>
    - `Select-String -Path 'backend/tests/test_supported_deployment_docs.py' -Pattern 'DEPLOYMENT.md|OPERATIONS.md|\\.env.example'`
    - `Select-String -Path 'backend/tests/test_supported_deployment_docs.py' -Pattern 'RUNTIME_ENVIRONMENT|AUTH_SECRET_KEY|ADMIN_LOGIN_PASSWORD|HR_LOGIN_PASSWORD'`
    - `Select-String -Path 'backend/tests/test_supported_deployment_docs.py' -Pattern 'RUNTIME_ENVIRONMENT=production'`
    - `Select-String -Path 'backend/tests/test_supported_deployment_docs.py' -Pattern 'ADMIN_PASSWORD|HR_PASSWORD'`
    - `Select-String -Path 'backend/tests/test_supported_deployment_docs.py' -Pattern 'read_text\\(encoding=\"utf-8\"\\)'`
  </acceptance_criteria>
  <verify>
    <automated>.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_supported_deployment_docs.py -x -p no:cacheprovider</automated>
  </verify>
  <done>Future drift between `.env.example`, `DEPLOYMENT.md`, and `OPERATIONS.md` will fail the test suite before another audit can miss the contract mismatch.</done>
</task>

</tasks>

<verification>
Run `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_supported_deployment_docs.py -x -p no:cacheprovider` and confirm the suite proves the runbooks use `RUNTIME_ENVIRONMENT`, `AUTH_SECRET_KEY`, `ADMIN_LOGIN_PASSWORD`, and `HR_LOGIN_PASSWORD` while rejecting deprecated password env names.
</verification>

<success_criteria>
- `DEPLOYMENT.md` requires a non-local `RUNTIME_ENVIRONMENT` and the canonical auth env keys.
- `OPERATIONS.md` points operators at that same canonical deployment contract.
- Documentation drift around the supported deployment env surface is covered by an automated regression test.
</success_criteria>

<output>
After completion, create `.planning/phases/05-supported-deployment-guardrails/05-02-SUMMARY.md`
</output>
