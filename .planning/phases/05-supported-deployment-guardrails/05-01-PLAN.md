---
phase: 05-supported-deployment-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - backend/tests/test_deployment_env_contract.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "The repo-controlled environment template exposes the actual guardrail keys `RUNTIME_ENVIRONMENT`, `AUTH_SECRET_KEY`, `ADMIN_LOGIN_PASSWORD`, and `HR_LOGIN_PASSWORD`."
    - "A settings object loaded from environment variables enters non-local mode only when `RUNTIME_ENVIRONMENT` is non-local and then fails bootstrap if default credentials or an unsafe auth secret remain."
    - "Legacy `ADMIN_PASSWORD` and `HR_PASSWORD` names do not silently satisfy the supported contract."
  artifacts:
    - path: ".env.example"
      provides: "Canonical deployment environment template aligned with `Settings`"
      contains: "RUNTIME_ENVIRONMENT=local"
    - path: "backend/tests/test_deployment_env_contract.py"
      provides: "Environment-loading and bootstrap regressions for the canonical guardrail contract"
      contains: "Settings(_env_file=None)"
  key_links:
    - from: ".env.example"
      to: "backend/app/core/config.py"
      via: "Template keys mirror the `Settings` field names that pydantic-settings loads from env"
      pattern: "RUNTIME_ENVIRONMENT|ADMIN_LOGIN_PASSWORD|HR_LOGIN_PASSWORD|AUTH_SECRET_KEY"
    - from: "backend/tests/test_deployment_env_contract.py"
      to: "backend/app/bootstrap.py"
      via: "Env-loaded settings are passed into bootstrap validation to prove the supported contract activates existing guardrails"
      pattern: "bootstrap_application|validate_auth_runtime_guardrails"
---

<objective>
Make the supported deployment environment contract executable instead of implicit by exposing the real guardrail keys in the repo template and proving env-driven bootstrap behavior with regression tests.

Purpose: Close `SEC-01` and `SEC-02` at the contract boundary where the Phase 1 guardrails are already implemented but not yet proven through the documented env path.
Output: A repo-controlled `.env.example` aligned to `Settings` plus a dedicated regression suite that loads settings from environment variables and proves non-local guardrail activation.
</objective>

<execution_context>
@C:/Users/AAON/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AAON/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-supported-deployment-guardrails/05-CONTEXT.md
@.planning/phases/05-supported-deployment-guardrails/05-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/01-deployment-security-guardrails/01-01-PLAN.md
@.planning/phases/01-deployment-security-guardrails/01-01-SUMMARY.md
@backend/app/core/config.py
@backend/app/bootstrap.py
@backend/run.py
@.env.example
@backend/tests/test_runtime_config.py
@backend/tests/test_app_initialization.py

<interfaces>
From `backend/app/core/config.py`:
```python
DEFAULT_AUTH_SECRET_KEY = 'change-this-auth-secret'
DEFAULT_ADMIN_LOGIN_PASSWORD = 'admin123'
DEFAULT_HR_LOGIN_PASSWORD = 'hr123'

class Settings(BaseSettings):
    runtime_environment: str = 'local'
    auth_secret_key: str = DEFAULT_AUTH_SECRET_KEY
    admin_login_password: str = DEFAULT_ADMIN_LOGIN_PASSWORD
    hr_login_password: str = DEFAULT_HR_LOGIN_PASSWORD
```

From `backend/app/bootstrap.py`:
```python
def validate_auth_runtime_guardrails(settings: Optional[Settings] = None) -> Settings: ...
def bootstrap_application(settings: Optional[Settings] = None) -> Settings: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the canonical guardrail env keys in the repo template</name>
  <read_first>
    - .env.example
    - backend/app/core/config.py
    - .planning/phases/05-supported-deployment-guardrails/05-CONTEXT.md
    - .planning/phases/05-supported-deployment-guardrails/05-RESEARCH.md
  </read_first>
  <files>.env.example</files>
  <action>Update `.env.example` so the auth/deployment section exposes the exact canonical keys that `Settings` already reads: add `RUNTIME_ENVIRONMENT=local`, `AUTH_SECRET_KEY=change-this-auth-secret`, `ADMIN_LOGIN_PASSWORD=admin123`, and `HR_LOGIN_PASSWORD=hr123`. Keep the existing local-safe defaults in the template, but add short comments immediately above these lines stating that a supported deployment must change `RUNTIME_ENVIRONMENT` to a non-local value such as `production` and must replace all three shipped auth defaults. Do not introduce alias keys such as `ADMIN_PASSWORD` or `HR_PASSWORD`, and do not remove unrelated existing keys like `DATABASE_URL`, `SALARY_TEMPLATE_PATH`, `FINAL_TOOL_TEMPLATE_PATH`, or `VITE_API_BASE_URL`.</action>
  <acceptance_criteria>
    - `Select-String -Path '.env.example' -Pattern '^RUNTIME_ENVIRONMENT=local$'`
    - `Select-String -Path '.env.example' -Pattern '^AUTH_SECRET_KEY=change-this-auth-secret$'`
    - `Select-String -Path '.env.example' -Pattern '^ADMIN_LOGIN_PASSWORD=admin123$'`
    - `Select-String -Path '.env.example' -Pattern '^HR_LOGIN_PASSWORD=hr123$'`
    - `Select-String -Path '.env.example' -Pattern 'supported deployment must change RUNTIME_ENVIRONMENT'`
    - `Select-String -Path '.env.example' -Pattern '^ADMIN_PASSWORD=|^HR_PASSWORD='` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "Select-String -Path '.env.example' -Pattern '^RUNTIME_ENVIRONMENT=local$','^AUTH_SECRET_KEY=change-this-auth-secret$','^ADMIN_LOGIN_PASSWORD=admin123$','^HR_LOGIN_PASSWORD=hr123$','supported deployment must change RUNTIME_ENVIRONMENT'"</automated>
  </verify>
  <done>The repo template now surfaces the real guardrail keys and explicitly tells operators that supported deployment must leave local mode and replace the shipped auth defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Add env-driven regression coverage for the supported guardrail contract</name>
  <read_first>
    - backend/tests/test_deployment_env_contract.py
    - backend/app/core/config.py
    - backend/app/bootstrap.py
    - backend/run.py
    - backend/tests/test_runtime_config.py
    - backend/tests/test_app_initialization.py
    - .env.example
  </read_first>
  <files>backend/tests/test_deployment_env_contract.py</files>
  <action>Create `backend/tests/test_deployment_env_contract.py` as the dedicated executable proof for the supported env contract. Add tests that use `monkeypatch` plus `Settings(_env_file=None)` so env-name resolution is exercised instead of bypassed. Cover four exact cases: 1) canonical env keys `RUNTIME_ENVIRONMENT`, `AUTH_SECRET_KEY`, `ADMIN_LOGIN_PASSWORD`, and `HR_LOGIN_PASSWORD` populate `Settings` as expected; 2) `bootstrap_application(settings)` accepts a non-local env-loaded settings object when those canonical keys are safe; 3) a non-local env-loaded settings object still raises `UnsafeAuthConfigurationError` when the canonical password or secret values remain the shipped defaults; 4) setting only `ADMIN_PASSWORD` and `HR_PASSWORD` while omitting `ADMIN_LOGIN_PASSWORD` and `HR_LOGIN_PASSWORD` leaves `Settings` on default credentials and therefore still fails guardrail validation. Keep the new suite self-contained under `.test_artifacts` only if file-system artifacts are needed; otherwise prefer pure env and bootstrap tests.</action>
  <acceptance_criteria>
    - `Select-String -Path 'backend/tests/test_deployment_env_contract.py' -Pattern 'Settings\\(_env_file=None\\)'`
    - `Select-String -Path 'backend/tests/test_deployment_env_contract.py' -Pattern 'RUNTIME_ENVIRONMENT|AUTH_SECRET_KEY|ADMIN_LOGIN_PASSWORD|HR_LOGIN_PASSWORD'`
    - `Select-String -Path 'backend/tests/test_deployment_env_contract.py' -Pattern 'ADMIN_PASSWORD|HR_PASSWORD'`
    - `Select-String -Path 'backend/tests/test_deployment_env_contract.py' -Pattern 'UnsafeAuthConfigurationError|bootstrap_application|validate_auth_runtime_guardrails'`
    - `Select-String -Path 'backend/tests/test_deployment_env_contract.py' -Pattern 'test_.*canonical.*env|test_.*legacy.*admin_password|test_.*non_local.*bootstrap'`
  </acceptance_criteria>
  <verify>
    <automated>.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_deployment_env_contract.py backend/tests/test_runtime_config.py backend/tests/test_app_initialization.py -x -p no:cacheprovider</automated>
  </verify>
  <done>The test suite now proves that the supported env variable names load correctly, activate the existing Phase 1 guardrails in non-local mode, and reject deprecated env-name drift.</done>
</task>

</tasks>

<verification>
Run `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_deployment_env_contract.py backend/tests/test_runtime_config.py backend/tests/test_app_initialization.py -x -p no:cacheprovider` and confirm the env-loaded non-local cases fail on unsafe defaults while the safe canonical env case passes.
</verification>

<success_criteria>
- `.env.example` exposes the actual guardrail keys from `Settings`.
- Env-loaded non-local settings prove `SEC-01` and `SEC-02` through bootstrap validation instead of direct constructor-only tests.
- Deprecated `ADMIN_PASSWORD` and `HR_PASSWORD` no longer remain an untested shadow contract.
</success_criteria>

<output>
After completion, create `.planning/phases/05-supported-deployment-guardrails/05-01-SUMMARY.md`
</output>
