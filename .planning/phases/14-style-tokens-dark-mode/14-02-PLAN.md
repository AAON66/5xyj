---
phase: 14
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/Login.tsx
  - frontend/src/pages/Workspace.tsx
  - frontend/src/pages/Portal.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/Employees.tsx
  - frontend/src/pages/ImportBatchDetail.tsx
  - frontend/src/pages/SimpleAggregate.tsx
  - frontend/src/pages/Imports.tsx
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "App/Login/Workspace/Portal/Dashboard/Employees/ImportBatchDetail/SimpleAggregate/Imports 中的硬编码十六进制颜色已全部替换为 AntD token"
    - "Card 状态边框色通过 useCardStatusColors hook 获取"
    - "Table columns render 中的颜色通过 getChartColors(isDark) 纯函数获取"
    - "暗黑模式切换后，这些页面颜色正确响应"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "登录加载屏背景 token 化"
    - path: "frontend/src/pages/Login.tsx"
      provides: "Login 背景 token 化"
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "Card 错误边框 + 条件品牌色 token 化"
    - path: "frontend/src/pages/ImportBatchDetail.tsx"
      provides: "confidence 阈值色用 chartColors + 边框/灰色 token 化"
    - path: "frontend/src/pages/SimpleAggregate.tsx"
      provides: "三状态 Card 边框 + 多处灰蓝色 + render 状态色 token 化"
    - path: "frontend/src/pages/Imports.tsx"
      provides: "蓝/灰/浅蓝 token 化"
  key_links:
    - from: "9 个迁移文件"
      to: "useSemanticColors / useCardStatusColors / chartColors"
      via: "hook 调用或 getChartColors(isDark)"
      pattern: "useSemanticColors|useCardStatusColors|getChartColors"
---

<objective>
对核心业务页面（App/Login/Workspace/Portal/Dashboard/Employees/ImportBatchDetail/SimpleAggregate/Imports）共约 20 处硬编码内联颜色进行 token 化迁移，使用 Wave 1 创建的 useSemanticColors / useCardStatusColors / chartColors 接口。

Purpose: 完成 UX-01 前半部分（核心业务页面 token 化）。
Output: 9 个页面文件的颜色迁移，硬编码色清零。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md
@.planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md
@.planning/phases/14-style-tokens-dark-mode/14-01-PLAN.md
@frontend/src/theme/useSemanticColors.ts
@frontend/src/theme/useCardStatusColors.ts
@frontend/src/theme/chartColors.ts
@frontend/src/theme/useThemeMode.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 迁移入口页 + 引导页（App/Login/Workspace/Portal）</name>
  <files>frontend/src/App.tsx, frontend/src/pages/Login.tsx, frontend/src/pages/Workspace.tsx, frontend/src/pages/Portal.tsx</files>
  <read_first>
    - frontend/src/App.tsx (line 41 当前硬编码 background '#F5F6F7')
    - frontend/src/pages/Login.tsx (line 217, 221 当前硬编码)
    - frontend/src/pages/Workspace.tsx (line 130 当前硬编码)
    - frontend/src/pages/Portal.tsx (line 17, 24, 31 当前硬编码)
    - frontend/src/theme/useSemanticColors.ts (可用字段列表)
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Token Mapping Table)
  </read_first>
  <action>
  每个文件顶部添加 useSemanticColors import，组件函数体第一行 `const colors = useSemanticColors();`

  App.tsx: line 41 `background: '#F5F6F7'` -> `background: colors.BG_LAYOUT`

  Login.tsx (per D-19):
  - line 217 `background: '#F5F6F7'` -> `background: colors.BG_LAYOUT`
  - line 221 boxShadow 的 rgba 值保留（透明度对暗模也合理）

  Workspace.tsx (per D-21): line 130 `color: '#3370FF'` -> `color: colors.BRAND`

  Portal.tsx (per D-21): line 17/24/31 三处 `color: '#3370FF'` -> `color: colors.BRAND`。如果这些定义在组件外数组中，把数组移到组件内 useMemo 或用 render 函数动态生成。

  **注意：** Portal.tsx 当前未被路由（App.tsx 无 /portal 路由），但仍需迁移以保证代码一致性。优先保证其他 3 个活跃路由页面正确。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'#F5F6F7'" frontend/src/App.tsx` returns 0
    - `grep -c "'#F5F6F7'" frontend/src/pages/Login.tsx` returns 0
    - `grep -c "'#3370FF'" frontend/src/pages/Workspace.tsx` returns 0
    - `grep -c "'#3370FF'" frontend/src/pages/Portal.tsx` returns 0
    - `grep -c "useSemanticColors" frontend/src/App.tsx` returns >= 1
    - `grep -c "useSemanticColors" frontend/src/pages/Login.tsx` returns >= 1
    - `grep -c "useSemanticColors" frontend/src/pages/Workspace.tsx` returns >= 1
    - `grep -c "useSemanticColors" frontend/src/pages/Portal.tsx` returns >= 1
    - `grep -c "colors.BRAND" frontend/src/pages/Portal.tsx` returns >= 3
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>四个入口/引导页硬编码色全部移除，lint 通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 迁移 Card 状态边框页面（Dashboard/Employees/ImportBatchDetail）</name>
  <files>frontend/src/pages/Dashboard.tsx, frontend/src/pages/Employees.tsx, frontend/src/pages/ImportBatchDetail.tsx</files>
  <read_first>
    - frontend/src/pages/Dashboard.tsx (line 257 borderColor, line 279 条件色)
    - frontend/src/pages/Employees.tsx (line 433 borderColor)
    - frontend/src/pages/ImportBatchDetail.tsx (line 77-79 confidence 色阵, line 261, 314, 358)
    - frontend/src/theme/useCardStatusColors.ts
    - frontend/src/theme/useSemanticColors.ts
    - frontend/src/theme/chartColors.ts
    - frontend/src/theme/useThemeMode.ts
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Token Mapping Table + Example 5)
  </read_first>
  <action>
  Dashboard.tsx:
  - import useCardStatusColors + useSemanticColors
  - 组件体内 `const cardColors = useCardStatusColors(); const colors = useSemanticColors();`
  - line 257 `borderColor: '#F54A45'` -> `borderColor: cardColors.errorBorder`
  - line 279 `'#3370FF'` -> `colors.BRAND`（保留条件判断）

  Employees.tsx:
  - import useCardStatusColors
  - `const cardColors = useCardStatusColors();`
  - line 433 `borderColor: '#F54A45'` -> `borderColor: cardColors.errorBorder`

  ImportBatchDetail.tsx:
  - import useSemanticColors + useCardStatusColors + getChartColors + useThemeMode
  - 组件体内:
    ```
    const colors = useSemanticColors();
    const cardColors = useCardStatusColors();
    const { isDark } = useThemeMode();
    const chartColors = useMemo(() => getChartColors(isDark), [isDark]);
    ```
  - line 77-79 confidence 阈值色（如在组件外定义 -> 移到组件内 useMemo）:
    - `'#00B42A'` -> `chartColors.success`
    - `'#FF7D00'` -> `chartColors.warning`
    - `'#F54A45'` -> `chartColors.error`
  - line 261 `color: '#999'` -> `color: colors.TEXT_TERTIARY` (per D-04)
  - line 314 `borderColor: '#F54A45'` -> `borderColor: cardColors.errorBorder`
  - line 358 `borderColor: '#3370FF'` -> `borderColor: cardColors.primaryBorder`

  **注意：** 将组件外定义的颜色常量/columns 移到组件内时，用 useMemo 包裹保持引用稳定。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'#F54A45'|'#3370FF'" frontend/src/pages/Dashboard.tsx` returns 0
    - `grep -cE "'#F54A45'" frontend/src/pages/Employees.tsx` returns 0
    - `grep -cE "'#F54A45'|'#3370FF'|'#00B42A'|'#FF7D00'" frontend/src/pages/ImportBatchDetail.tsx` returns 0
    - `grep -c "'#999'" frontend/src/pages/ImportBatchDetail.tsx` returns 0
    - `grep -c "useCardStatusColors" frontend/src/pages/Dashboard.tsx` returns >= 1
    - `grep -c "useCardStatusColors" frontend/src/pages/Employees.tsx` returns >= 1
    - `grep -c "getChartColors" frontend/src/pages/ImportBatchDetail.tsx` returns >= 1
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
  <done>Dashboard/Employees/ImportBatchDetail 硬编码色全部移除，Card 边框使用 cardColors，confidence 色使用 chartColors；useMemo 包裹保持引用稳定；lint 通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 迁移 SimpleAggregate + Imports（多色混合页面）</name>
  <files>frontend/src/pages/SimpleAggregate.tsx, frontend/src/pages/Imports.tsx</files>
  <read_first>
    - frontend/src/pages/SimpleAggregate.tsx (line 375/381/397 三色边框, line 432/434/459/461 蓝+灰, line 555/606 边框, line 662 render 状态色)
    - frontend/src/pages/Imports.tsx (line 327/329/376)
    - frontend/src/theme/useCardStatusColors.ts
    - frontend/src/theme/useSemanticColors.ts
    - frontend/src/theme/chartColors.ts
    - frontend/src/theme/useThemeMode.ts
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Token Mapping Table 行 SimpleAggregate/Imports)
  </read_first>
  <action>
  SimpleAggregate.tsx (per D-06):
  - import useSemanticColors + useCardStatusColors + getChartColors + useThemeMode + useMemo (if not already)
  - 组件体内:
    ```
    const colors = useSemanticColors();
    const cardColors = useCardStatusColors();
    const { isDark } = useThemeMode();
    const chartColors = useMemo(() => getChartColors(isDark), [isDark]);
    ```
  - line 375 `borderColor: '#00B42A'` -> `borderColor: cardColors.successBorder`
  - line 381 `borderColor: '#F54A45'` -> `borderColor: cardColors.errorBorder`
  - line 397 `borderColor: '#FF7D00'` -> `borderColor: cardColors.warningBorder`
  - line 432/434 `'#3370FF'` / `'#999'` -> `colors.BRAND` / `colors.TEXT_TERTIARY`
  - line 459/461 同上替换为 `colors.BRAND` / `colors.TEXT_TERTIARY`
  - line 555 `'#DEE0E3'` -> `colors.BORDER` (per D-04)
  - line 606 `'#E8E8E8'` -> `colors.BORDER_SECONDARY`
  - line 662 三状态色 render 回调内 -> 使用 `chartColors.success/error/warning`（根据具体状态逻辑对应替换，保留原条件判断）

  Imports.tsx:
  - import useSemanticColors
  - 组件体内 `const colors = useSemanticColors();`
  - line 327 `'#3370FF'` -> `colors.BRAND`
  - line 329 `'#999'` -> `colors.TEXT_TERTIARY` (per D-04)
  - line 376 `'#F0F5FF'` -> `colors.HIGHLIGHT_BG_PRIMARY` (per D-04)
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint 2>&1 | tail -10 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'#00B42A'|'#F54A45'|'#FF7D00'|'#3370FF'|'#999'|'#DEE0E3'|'#E8E8E8'" frontend/src/pages/SimpleAggregate.tsx` returns 0
    - `grep -cE "'#3370FF'|'#999'|'#F0F5FF'" frontend/src/pages/Imports.tsx` returns 0
    - `grep -c "useCardStatusColors" frontend/src/pages/SimpleAggregate.tsx` returns >= 1
    - `grep -c "getChartColors" frontend/src/pages/SimpleAggregate.tsx` returns >= 1
    - `grep -c "useSemanticColors" frontend/src/pages/Imports.tsx` returns >= 1
    - `cd frontend && npm run lint` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>SimpleAggregate/Imports 所有硬编码色清零，lint + build 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 纯 UI token 替换，无新增信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | N/A | accept | 纯视觉迁移，无用户输入/无信任边界变化 |
</threat_model>

<verification>
- `npm run lint --prefix frontend` 通过
- `npm run build --prefix frontend` 通过
- grep 9 个文件中无硬编码十六进制色（Sider/logo 除外）
- 手动切换暗黑模式，验证这 9 个页面颜色随 mode 正确响应
</verification>

<success_criteria>
1. 9 个文件的 20 处硬编码色全部 token 化
2. lint + build 通过
3. 暗黑模式下这些页面无"半白半黑"混合色
</success_criteria>

<output>
创建 `.planning/phases/14-style-tokens-dark-mode/14-02-SUMMARY.md`
</output>
