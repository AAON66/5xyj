---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/theme/index.ts
  - frontend/src/theme/semanticColors.ts
  - frontend/src/theme/useSemanticColors.ts
  - frontend/src/theme/useCardStatusColors.ts
  - frontend/src/theme/chartColors.ts
  - frontend/src/theme/ThemeModeProvider.tsx
  - frontend/src/theme/useThemeMode.ts
  - frontend/src/main.tsx
  - frontend/src/layouts/MainLayout.tsx
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "ThemeModeProvider 在 React 启动时从 <html data-theme> 读取初始 mode（与 FOUC 脚本同源）"
    - "用户点击 Header 太阳/月亮图标可瞬间切换亮暗模式"
    - "切换后 localStorage.theme-mode 写入新值，刷新页面保持选择"
    - "切换回 light 模式时 html/body 的 inline dark background 被清除"
    - "index.html 同步脚本在 React 加载前给 <html> 设置 data-theme 防止 FOUC"
    - "ConfigProvider 在 dark mode 下使用 antdTheme.darkAlgorithm"
    - "Layout.Sider 在亮暗两种模式下都保持 #1F2329 深色"
  artifacts:
    - path: "frontend/src/theme/ThemeModeProvider.tsx"
      provides: "Context 管理 mode state + localStorage 持久化 + 系统偏好检测"
      contains: "ThemeModeContext"
    - path: "frontend/src/theme/useThemeMode.ts"
      provides: "消费 ThemeModeContext 的 hook"
      exports: ["useThemeMode"]
    - path: "frontend/src/theme/useSemanticColors.ts"
      provides: "组件内语义色 hook（消费 theme.useToken）"
      exports: ["useSemanticColors"]
    - path: "frontend/src/theme/useCardStatusColors.ts"
      provides: "Card 状态边框色 hook"
      exports: ["useCardStatusColors"]
    - path: "frontend/src/theme/chartColors.ts"
      provides: "非 hook 纯函数版本供 Table columns / 模块顶层使用"
      exports: ["getChartColors", "LIGHT_CHART_COLORS", "DARK_CHART_COLORS"]
    - path: "frontend/src/theme/semanticColors.ts"
      provides: "静态语义色常量（fallback 占位符）"
    - path: "frontend/src/theme/index.ts"
      provides: "buildTheme(mode) 函数"
      exports: ["buildTheme", "theme"]
    - path: "frontend/index.html"
      provides: "FOUC 预防同步脚本"
      contains: "theme-mode"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "ThemeModeProvider + buildTheme(mode)"
      via: "ThemedConfig 子组件调用 useThemeMode() 并传入 ConfigProvider theme"
      pattern: "buildTheme\\(mode\\)"
    - from: "frontend/src/layouts/MainLayout.tsx"
      to: "useThemeMode + @ant-design/icons"
      via: "Header 右上角切换按钮"
      pattern: "SunOutlined|MoonOutlined"
    - from: "frontend/index.html"
      to: "<html data-theme>"
      via: "同步 script 在 React 启动前执行"
      pattern: "data-theme"
---

<objective>
建立暗黑模式切换的完整基础设施：ThemeModeProvider Context、buildTheme(mode) 函数、useSemanticColors/useCardStatusColors/chartColors 三套颜色消费接口、Header 切换按钮、index.html FOUC 预防脚本。同时对 MainLayout.tsx 自身的硬编码颜色做 token 化。

Purpose: 提供 Wave 2 36 处硬编码色迁移所依赖的全部 hook / 纯函数接口，并完成 UX-02（暗黑模式切换 + 持久化）全部后端机制。
Output: 8 个新增/改造的 theme 模块文件、MainLayout Header 切换按钮、index.html FOUC 脚本、main.tsx ThemeModeProvider 包裹。

Review feedback addressed in this plan:
- [HIGH] FOUC script stale backgrounds: ThemeModeProvider now clears html/body inline backgrounds on every mode change
- [MEDIUM] Dual boot path: readInitialMode() reads from document.documentElement.dataset.theme first, falling back only if absent
- [MEDIUM] chartColors drift: added comment noting DARK_CHART_COLORS should be validated against useToken output post-implementation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md
@.planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md
@frontend/src/theme/index.ts
@frontend/src/main.tsx
@frontend/src/layouts/MainLayout.tsx
@frontend/index.html

<interfaces>
<!-- 执行者必须使用这些接口形状，Wave 2 迁移任务依赖此处的导出签名 -->

frontend/src/theme/ThemeModeProvider.tsx (NEW):
```typescript
export type ThemeMode = 'light' | 'dark';
export interface ThemeModeContextValue {
  mode: ThemeMode;
  isDark: boolean;
  toggleMode: () => void;
  setMode: (m: ThemeMode) => void;
}
export const ThemeModeContext: React.Context<ThemeModeContextValue | null>;
export function ThemeModeProvider(props: React.PropsWithChildren): JSX.Element;
```

frontend/src/theme/useThemeMode.ts (NEW):
```typescript
export function useThemeMode(): ThemeModeContextValue;  // throws if outside provider
```

frontend/src/theme/useSemanticColors.ts (NEW) - 返回字段必须完全匹配:
```typescript
export interface SemanticColors {
  BRAND: string;             // token.colorPrimary
  SUCCESS: string;           // token.colorSuccess
  ERROR: string;             // token.colorError
  WARNING: string;           // token.colorWarning
  TEXT_TERTIARY: string;     // token.colorTextTertiary
  TEXT: string;              // token.colorText
  HIGHLIGHT_BG: string;      // token.colorWarningBg  (D-05)
  HIGHLIGHT_BG_PRIMARY: string;  // token.colorPrimaryBg
  HIGHLIGHT_BG_ERROR: string;    // token.colorErrorBg
  BG_CONTAINER: string;      // token.colorBgContainer
  BG_LAYOUT: string;         // token.colorBgLayout
  BORDER: string;            // token.colorBorder
  BORDER_SECONDARY: string;  // token.colorBorderSecondary
  FILL_QUATERNARY: string;   // token.colorFillQuaternary
}
export function useSemanticColors(): SemanticColors;
```

frontend/src/theme/useCardStatusColors.ts (NEW):
```typescript
export interface CardStatusColors {
  successBorder: string;  // token.colorSuccess
  errorBorder: string;    // token.colorError
  warningBorder: string;  // token.colorWarning
  primaryBorder: string;  // token.colorPrimary
}
export function useCardStatusColors(): CardStatusColors;
```

frontend/src/theme/chartColors.ts (NEW) - Table columns 用:
```typescript
export interface ChartColors {
  brand: string;
  success: string;
  error: string;
  warning: string;
  textTertiary: string;
  highlightBg: string;
}
export const LIGHT_CHART_COLORS: ChartColors;
export const DARK_CHART_COLORS: ChartColors;
export function getChartColors(isDark: boolean): ChartColors;
```

frontend/src/theme/index.ts (CHANGED):
```typescript
export function buildTheme(mode: 'light' | 'dark'): ThemeConfig;
export const theme: ThemeConfig;  // 兼容旧 import = buildTheme('light')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 改造 theme/index.ts 为 buildTheme + 新建 semanticColors/chartColors 常量模块</name>
  <files>frontend/src/theme/index.ts, frontend/src/theme/semanticColors.ts, frontend/src/theme/chartColors.ts</files>
  <read_first>
    - frontend/src/theme/index.ts (当前 ThemeConfig 完整结构，必须完整保留所有现有 token/components 字段)
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Example 2 buildTheme 示例、Token Mapping Table、Pitfall 5 Sider 深色与 bodyBg 区分)
    - .planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md (D-14, D-15, D-16, D-17, D-18 暗模决策)
  </read_first>
  <action>
  **1) 改造 frontend/src/theme/index.ts**（per D-14, D-16）:
  - 将默认导出的 ThemeConfig 重构为 `export function buildTheme(mode: 'light' | 'dark'): ThemeConfig`
  - 首行添加 `import { theme as antdTheme } from 'antd';`
  - 在返回的 ThemeConfig 顶层添加 `algorithm: isDark ? antdTheme.darkAlgorithm : antdTheme.defaultAlgorithm`
  - `colorPrimary: '#3370FF'` 保留（两模式共用，darkAlgorithm 会自动提亮）
  - 亮模下保留所有现有 token 值（colorBgContainer/colorBgLayout/colorText 等）；暗模下这些字段不设置，交由 darkAlgorithm
  - `components.Layout` 改为：
    ```typescript
    Layout: {
      siderBg: '#1F2329',  // D-16 亮暗统一
      headerBg: isDark ? '#1F1F1F' : '#FFFFFF',
      bodyBg: isDark ? '#1F1F1F' : '#F5F6F7',  // Pitfall 5: 比默认 #141414 稍亮，与 Sider 区分
      headerHeight: 56,
      headerPadding: '0 24px',
    }
    ```
  - `components.Table.rowHoverBg`: `isDark ? undefined : '#F0F5FF'` （per D-18 暗模交由 darkAlgorithm）
  - 其余 components 配置保持不变
  - 在文件末尾保留兼容导出：`export const theme = buildTheme('light');`
  - **用 useMemo 包裹 buildTheme 调用方（在 main.tsx Task 3 处理）**，避免每次渲染重新生成 theme 对象

  **2) 新建 frontend/src/theme/semanticColors.ts**（per D-01，纯常量 fallback）:
  ```typescript
  // 静态 fallback 常量（运行时应使用 useSemanticColors hook 获取动态值）
  export const LIGHT_SEMANTIC_COLORS = {
    BRAND: '#3370FF',
    SUCCESS: '#00B42A',
    ERROR: '#F54A45',
    WARNING: '#FF7D00',
    TEXT: '#1F2329',
    TEXT_TERTIARY: '#8F959E',
    HIGHLIGHT_BG: '#FFF7E6',
    HIGHLIGHT_BG_PRIMARY: '#F0F5FF',
    HIGHLIGHT_BG_ERROR: '#FFF1F0',
    BG_CONTAINER: '#FFFFFF',
    BG_LAYOUT: '#F5F6F7',
    BORDER: '#DEE0E3',
    BORDER_SECONDARY: '#E8E8E8',
    FILL_QUATERNARY: '#F5F5F5',
  } as const;
  export type SemanticColorKey = keyof typeof LIGHT_SEMANTIC_COLORS;
  ```

  **3) 新建 frontend/src/theme/chartColors.ts**（per D-03，Table columns 专用）:
  完整代码参考 RESEARCH.md Pattern 3，导出 `ChartColors` 接口、`LIGHT_CHART_COLORS`、`DARK_CHART_COLORS`、`getChartColors(isDark)` 函数。暗模色值使用：brand '#3C89E8'、success '#49AA19'、error '#DC4446'、warning '#D89614'、textTertiary '#7D8390'、highlightBg '#2B2111'。

  **Addresses review concern [MEDIUM] chartColors drift:** 在 DARK_CHART_COLORS 对象上方添加注释：
  ```typescript
  // NOTE: 暗模色值为 AntD darkAlgorithm 推导近似值。
  // 实现后建议在 DevTools 中用 useToken() 校准实际值并更新此处。
  // 若需精确值，可在组件内 useMemo 中用 useToken 替代此常量。
  ```
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit 2>&1 | grep -E "(theme/index|semanticColors|chartColors)" || echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function buildTheme" frontend/src/theme/index.ts` returns >= 1
    - `grep -c "antdTheme.darkAlgorithm" frontend/src/theme/index.ts` returns >= 1
    - `grep -c "siderBg: '#1F2329'" frontend/src/theme/index.ts` returns 1
    - `grep -c "export const theme" frontend/src/theme/index.ts` returns >= 1
    - `test -f frontend/src/theme/semanticColors.ts` exits 0
    - `test -f frontend/src/theme/chartColors.ts` exits 0
    - `grep -c "LIGHT_CHART_COLORS\|DARK_CHART_COLORS\|getChartColors" frontend/src/theme/chartColors.ts` returns >= 3
    - `grep -c "darkAlgorithm 推导近似" frontend/src/theme/chartColors.ts` returns >= 1
    - `cd frontend && npx tsc --noEmit` 对这三个文件无报错
  </acceptance_criteria>
  <done>buildTheme(mode) 工作，亮暗两模式都返回有效 ThemeConfig；semanticColors/chartColors 常量模块已创建且类型正确；chartColors 暗模色值标注了校准提示。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 ThemeModeProvider + useThemeMode + useSemanticColors + useCardStatusColors</name>
  <files>frontend/src/theme/ThemeModeProvider.tsx, frontend/src/theme/useThemeMode.ts, frontend/src/theme/useSemanticColors.ts, frontend/src/theme/useCardStatusColors.ts</files>
  <read_first>
    - frontend/src/theme/index.ts (Task 1 刚创建的 buildTheme 签名)
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Pattern 5 ThemeModeProvider 完整代码、Pattern 2 useToken 示例、Pitfall 4 Safari localStorage)
    - .planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md (D-02, D-06, D-09, D-10, D-12 决策)
  </read_first>
  <action>
  **1) frontend/src/theme/ThemeModeProvider.tsx**（per D-09, D-10, D-12）:

  **Addresses review concern [HIGH] stale backgrounds + [MEDIUM] dual boot path:**

  - `STORAGE_KEY = 'theme-mode'`（per D-12）
  - `readInitialMode()` 必须优先从 `document.documentElement.dataset.theme` 读取（与 index.html FOUC 脚本同源），只有 dataset.theme 不存在时才 fallback 到 localStorage 和 matchMedia：
    ```typescript
    function readInitialMode(): ThemeMode {
      if (typeof window === 'undefined') return 'light';
      // 优先读 FOUC 脚本已设置的 data-theme（消除双启动路径分歧）
      const htmlTheme = document.documentElement.getAttribute('data-theme');
      if (htmlTheme === 'light' || htmlTheme === 'dark') return htmlTheme;
      // Fallback: localStorage > matchMedia > 'light'
      try {
        const saved = localStorage.getItem(STORAGE_KEY);
        if (saved === 'light' || saved === 'dark') return saved;
      } catch {}
      return window.matchMedia?.('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
    }
    ```
  - useEffect 中必须同时处理 data-theme 属性和 html/body 背景色，**切换回 light 时必须清除暗模的 inline background**：
    ```typescript
    useEffect(() => {
      document.documentElement.setAttribute('data-theme', mode);
      try { localStorage.setItem(STORAGE_KEY, mode); } catch {}
      // 同步 html/body 背景色，防止 FOUC 脚本设置的暗色残留
      if (mode === 'dark') {
        document.documentElement.style.backgroundColor = '#1F1F1F';
        document.body.style.backgroundColor = '#1F1F1F';
      } else {
        document.documentElement.style.backgroundColor = '';
        document.body.style.backgroundColor = '';
      }
    }, [mode]);
    ```
  - 导出 `ThemeModeContext`、`ThemeModeProvider`、`type ThemeMode`、`interface ThemeModeContextValue`

  **2) frontend/src/theme/useThemeMode.ts**:
  ```typescript
  import { useContext } from 'react';
  import { ThemeModeContext } from './ThemeModeProvider';
  export function useThemeMode() {
    const ctx = useContext(ThemeModeContext);
    if (!ctx) throw new Error('useThemeMode must be used within ThemeModeProvider');
    return ctx;
  }
  ```

  **3) frontend/src/theme/useSemanticColors.ts**（per D-01, D-02）:
  ```typescript
  import { theme } from 'antd';

  export interface SemanticColors {
    BRAND: string;
    SUCCESS: string;
    ERROR: string;
    WARNING: string;
    TEXT: string;
    TEXT_TERTIARY: string;
    HIGHLIGHT_BG: string;
    HIGHLIGHT_BG_PRIMARY: string;
    HIGHLIGHT_BG_ERROR: string;
    BG_CONTAINER: string;
    BG_LAYOUT: string;
    BORDER: string;
    BORDER_SECONDARY: string;
    FILL_QUATERNARY: string;
  }

  export function useSemanticColors(): SemanticColors {
    const { token } = theme.useToken();
    return {
      BRAND: token.colorPrimary,
      SUCCESS: token.colorSuccess,
      ERROR: token.colorError,
      WARNING: token.colorWarning,
      TEXT: token.colorText,
      TEXT_TERTIARY: token.colorTextTertiary,
      HIGHLIGHT_BG: token.colorWarningBg,           // D-05
      HIGHLIGHT_BG_PRIMARY: token.colorPrimaryBg,
      HIGHLIGHT_BG_ERROR: token.colorErrorBg,
      BG_CONTAINER: token.colorBgContainer,
      BG_LAYOUT: token.colorBgLayout,
      BORDER: token.colorBorder,
      BORDER_SECONDARY: token.colorBorderSecondary,
      FILL_QUATERNARY: token.colorFillQuaternary,
    };
  }
  ```

  **4) frontend/src/theme/useCardStatusColors.ts**（per D-06）:
  ```typescript
  import { theme } from 'antd';

  export interface CardStatusColors {
    successBorder: string;
    errorBorder: string;
    warningBorder: string;
    primaryBorder: string;
  }

  export function useCardStatusColors(): CardStatusColors {
    const { token } = theme.useToken();
    return {
      successBorder: token.colorSuccess,
      errorBorder: token.colorError,
      warningBorder: token.colorWarning,
      primaryBorder: token.colorPrimary,
    };
  }
  ```
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit 2>&1 | grep -E "theme/(ThemeModeProvider|useThemeMode|useSemanticColors|useCardStatusColors)" || echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/theme/ThemeModeProvider.tsx` exits 0
    - `test -f frontend/src/theme/useThemeMode.ts` exits 0
    - `test -f frontend/src/theme/useSemanticColors.ts` exits 0
    - `test -f frontend/src/theme/useCardStatusColors.ts` exits 0
    - `grep -c "'theme-mode'" frontend/src/theme/ThemeModeProvider.tsx` returns >= 1
    - `grep -c "dataset.theme\|getAttribute.*data-theme" frontend/src/theme/ThemeModeProvider.tsx` returns >= 1
    - `grep -c "backgroundColor = ''" frontend/src/theme/ThemeModeProvider.tsx` returns >= 1
    - `grep -c "try" frontend/src/theme/ThemeModeProvider.tsx` returns >= 1
    - `grep -c "theme.useToken" frontend/src/theme/useSemanticColors.ts` returns 1
    - `grep -c "BRAND\|SUCCESS\|ERROR\|WARNING" frontend/src/theme/useSemanticColors.ts` returns >= 4
    - `grep -c "theme.useToken" frontend/src/theme/useCardStatusColors.ts` returns 1
    - `cd frontend && npx tsc --noEmit` 对这四个文件无报错
  </acceptance_criteria>
  <done>四个主题 hook/Provider 文件创建完成；ThemeModeProvider 从 data-theme 读初始 mode（消除双启动路径）；切换回 light 时清除 html/body inline background（防止残留）；localStorage 读写已 try/catch 保护。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 改造 main.tsx 包裹 ThemeModeProvider + index.html 插入 FOUC 脚本 + MainLayout Header 切换按钮与 token 化</name>
  <files>frontend/src/main.tsx, frontend/index.html, frontend/src/layouts/MainLayout.tsx</files>
  <read_first>
    - frontend/src/main.tsx (当前 ConfigProvider 包裹结构)
    - frontend/index.html (当前 head 结构，同步脚本插入位置)
    - frontend/src/layouts/MainLayout.tsx (Header 现有结构 + line 271/276/291 硬编码色位置)
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Example 1 main.tsx 改造、Example 3 切换按钮、Pattern 4 FOUC 脚本、Token Mapping Table)
    - .planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md (D-08 按钮位置、D-13 FOUC 脚本)
  </read_first>
  <action>
  **1) 改造 frontend/src/main.tsx**（per Example 1）:
  - 新增 import：`import { ThemeModeProvider } from "./theme/ThemeModeProvider";`、`import { useThemeMode } from "./theme/useThemeMode";`、`import { buildTheme } from "./theme";`
  - 新增 import：`import { useMemo } from 'react';`
  - 创建内部 `ThemedConfig` 组件：调用 `useThemeMode()` 拿 mode，**使用 useMemo 包裹 buildTheme(mode)**：
    ```tsx
    function ThemedConfig({ children }: { children: React.ReactNode }) {
      const { mode } = useThemeMode();
      const themeConfig = useMemo(() => buildTheme(mode), [mode]);
      return (
        <ConfigProvider theme={themeConfig} componentSize="small" locale={zhCN}>
          <AntApp>{children}</AntApp>
        </ConfigProvider>
      );
    }
    ```
  - 包裹顺序（外到内）：`<React.StrictMode><ThemeModeProvider><ThemedConfig><BrowserRouter><AuthProvider><ApiFeedbackProvider><App /></ApiFeedbackProvider></AuthProvider></BrowserRouter></ThemedConfig></ThemeModeProvider></React.StrictMode>`
  - 删除原来直接 `<ConfigProvider theme={theme}>` 的写法

  **2) 改造 frontend/index.html**（per D-13, Pattern 4）:
  在 `<head>` 底部、`<script type="module" src="/src/main.tsx">` 之前插入同步脚本：
  ```html
  <script>
    (function() {
      try {
        var saved = localStorage.getItem('theme-mode');
        var prefersDark = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
        var mode = (saved === 'light' || saved === 'dark') ? saved : (prefersDark ? 'dark' : 'light');
        document.documentElement.setAttribute('data-theme', mode);
        if (mode === 'dark') {
          document.documentElement.style.backgroundColor = '#1F1F1F';
          if (document.body) document.body.style.backgroundColor = '#1F1F1F';
        }
      } catch(e) {}
    })();
  </script>
  ```

  **3) 改造 frontend/src/layouts/MainLayout.tsx**:
  - 新增 import：`import { SunOutlined, MoonOutlined } from '@ant-design/icons';`、`import { useThemeMode } from '../theme/useThemeMode';`、`import { useSemanticColors } from '../theme/useSemanticColors';`
  - 在 MainLayout 函数体内调用 `const colors = useSemanticColors();` 和 `const { isDark, toggleMode } = useThemeMode();`
  - **硬编码色 token 化**（per Token Mapping Table）：
    - line 271 `background: '#fff'` -> `background: colors.BG_CONTAINER`
    - line 276 `border: '1px solid #DEE0E3'` -> `` border: `1px solid ${colors.BORDER}` ``
    - line 291 `background: '#F5F6F7'` -> `background: colors.BG_LAYOUT`
  - **Header 右上角切换按钮**（per D-08）：在用户菜单（Dropdown）旁边插入 Button：
    ```tsx
    <Button
      type="text"
      icon={isDark ? <SunOutlined /> : <MoonOutlined />}
      onClick={toggleMode}
      aria-label={isDark ? '切换到亮色模式' : '切换到暗色模式'}
    />
    ```
  - 注意：MainLayout.module.css 中 Sider 内 logo 的 `color: #fff` **保留不动**（D-16 Sider 始终深色）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint 2>&1 | tail -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ThemeModeProvider" frontend/src/main.tsx` returns >= 2
    - `grep -c "buildTheme(mode)" frontend/src/main.tsx` returns 1
    - `grep -c "useMemo.*buildTheme" frontend/src/main.tsx` returns 1
    - `grep -c "theme-mode" frontend/index.html` returns 1
    - `grep -c "prefers-color-scheme: dark" frontend/index.html` returns 1
    - `grep -c "data-theme" frontend/index.html` returns >= 1
    - `grep -c "SunOutlined\|MoonOutlined" frontend/src/layouts/MainLayout.tsx` returns >= 2
    - `grep -c "toggleMode" frontend/src/layouts/MainLayout.tsx` returns >= 1
    - `grep -c "useSemanticColors" frontend/src/layouts/MainLayout.tsx` returns >= 1
    - `grep -cE "'#fff'\|'#F5F6F7'\|'#DEE0E3'" frontend/src/layouts/MainLayout.tsx` returns 0
    - `cd frontend && npm run lint` returns exit 0
    - `cd frontend && npm run build` returns exit 0
  </acceptance_criteria>
  <done>main.tsx 包裹 ThemeModeProvider 并 useMemo buildTheme；index.html 有 FOUC 脚本；MainLayout Header 出现切换按钮并可切换；MainLayout 三处硬编码色已 token 化；lint+build 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | 客户端存储 theme-mode（非敏感数据） |
| React Context -> DOM | mode state 通过 useEffect 写入 data-theme 属性 + html/body background |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | localStorage theme-mode value | accept | 仅影响 UI 显示，恶意值被 readInitialMode() 白名单 ('light'/'dark') 过滤；非 'light'/'dark' 的值回落到系统偏好 |
| T-14-02 | DoS | Safari 隐私模式 localStorage 抛错 | mitigate | ThemeModeProvider useEffect + readInitialMode 的所有 localStorage 读写都用 try/catch 包裹（Pitfall 4） |
| T-14-03 | Information Disclosure | FOUC 脚本在 head 中执行 | accept | 脚本只读写 localStorage 'theme-mode' 单一键，不访问任何敏感数据 |
</threat_model>

<verification>
- `npm run lint --prefix frontend` 通过
- `npm run build --prefix frontend` 通过
- 手动打开 http://localhost:5173，验证 Header 右上角出现切换按钮
- 点击按钮：主题瞬间切换，Sider 保持深色，Content 背景从灰白切换到深色
- 再点一次切回亮模：html/body 背景色被清除，无暗色残留
- 打开 DevTools Application -> Local Storage -> 查看 `theme-mode` key 值正确
- 刷新页面：模式保持，无 FOUC 闪烁
- 检查 `<html data-theme="dark"|"light">` 属性随 state 变化
</verification>

<success_criteria>
1. ThemeModeProvider + useThemeMode hook 工作正常
2. ThemeModeProvider 从 data-theme 属性读初始 mode（与 FOUC 脚本同源）
3. 切换回 light 时 html/body inline background 被清除
4. buildTheme(mode) 在亮暗两模式下都返回有效 ThemeConfig（含 siderBg: #1F2329）
5. buildTheme 调用被 useMemo 包裹
6. useSemanticColors / useCardStatusColors / chartColors 三套接口可用于 Wave 2 迁移
7. Header 切换按钮可瞬间切换主题
8. localStorage theme-mode 持久化，刷新保持
9. index.html FOUC 脚本在 React 启动前设置 data-theme，刷新无闪烁
10. MainLayout 自身 3 处硬编码色已 token 化
11. lint + build 通过
</success_criteria>

<output>
创建 `.planning/phases/14-style-tokens-dark-mode/14-01-SUMMARY.md`
</output>
