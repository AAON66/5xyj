---
phase: 14
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - frontend/src/styles.css
  - scripts/check-hardcoded-colors.sh
autonomous: false
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "frontend/src/styles.css 死代码已删除（3520 行零 import）"
    - "scripts/check-hardcoded-colors.sh 存在且可执行，通用 hex 扫描模式检测所有非白名单硬编码色"
    - "全项目 grep 只剩允许的白名单色：buildTheme 种子色、Sider #1F2329、MainLayout.module.css logo #fff、index.html #1F1F1F"
    - "用户手动验证所有页面在亮暗两模式下视觉一致、无半白半黑"
    - "用户验证刷新页面无 FOUC 闪烁"
    - "用户验证 localStorage theme-mode 持久化工作"
  artifacts:
    - path: "scripts/check-hardcoded-colors.sh"
      provides: "通用 hex 色白名单检查脚本（CI 可复用）"
      min_lines: 15
  key_links:
    - from: "check-hardcoded-colors.sh"
      to: "frontend/src/**/*.tsx + frontend/src/**/*.ts + frontend/index.html"
      via: "grep -rEn 通用 hex pattern + 白名单排除"
      pattern: "grep.*#\\[0-9a-fA-F\\]"
---

<objective>
完成 Phase 14 收尾：(1) 删除 frontend/src/styles.css 死代码（3520 行零 import）；(2) 创建 check-hardcoded-colors.sh 通用 hex 检查脚本（解决 review 指出的窄 pattern 问题）；(3) 全项目 grep 审计确认硬编码色清零；(4) 用户视觉验证 checkpoint（基于 App.tsx 真实路由的页面清单、FOUC、持久化）。

Purpose: 关闭 UX-01（验证完全 token 化）和 UX-02（验证切换+持久化 UX），删除死代码防止未来维护者误导。
Output: 死代码删除、grep 脚本、已验证的视觉一致性。

Review feedback addressed in this plan:
- [HIGH] Audit script too narrow: rewrote to scan ALL hex patterns (#xxx through #xxxxxxxx), not just inline-style
- [HIGH] Manual route checklist uses invalid paths: rewritten against actual App.tsx routes
- [MEDIUM] index.html #1F1F1F not in whitelist: added to allowlist
- [MEDIUM] Checkpoint prerequisites: added login/data requirements section
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md
@.planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md
@.planning/phases/14-style-tokens-dark-mode/14-01-PLAN.md
@.planning/phases/14-style-tokens-dark-mode/14-02-PLAN.md
@.planning/phases/14-style-tokens-dark-mode/14-03-PLAN.md
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 确认 styles.css 零 import 并物理删除</name>
  <files>frontend/src/styles.css</files>
  <read_first>
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Hardcoded Color Audit 部分 - 死代码段落)
  </read_first>
  <action>
  1. 双重确认零 import：
     ```
     grep -rn "styles.css" frontend/src/ frontend/index.html frontend/vite.config.ts 2>/dev/null
     grep -rn "from.*styles\\.css\\|import.*styles\\.css" frontend/ 2>/dev/null
     ```
     结果必须为空（或仅自引用）。如果发现引用必须 STOP 并报告。

  2. 物理删除文件：`rm frontend/src/styles.css`

  3. 验证 build 依然通过：`cd frontend && npm run build`
  </action>
  <verify>
    <automated>test ! -f /Users/mac/PycharmProjects/5xyj/frontend/src/styles.css && cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/styles.css` returns exit 1 (文件已删除)
    - `grep -rn "styles\\.css" frontend/src/ frontend/index.html 2>/dev/null | wc -l` returns 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>styles.css 已删除，build 通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建通用硬编码色白名单检查脚本 + 执行全项目 grep 审计</name>
  <files>scripts/check-hardcoded-colors.sh</files>
  <read_first>
    - .planning/phases/14-style-tokens-dark-mode/14-RESEARCH.md (Phase gate 3 白名单规则)
    - frontend/src/theme/index.ts (确认 buildTheme 中的种子色值，加入白名单)
    - frontend/index.html (确认 FOUC 脚本中的 #1F1F1F，加入白名单)
    - frontend/src/layouts/MainLayout.module.css (确认 Sider logo #fff，加入白名单)
  </read_first>
  <action>
  **Addresses review concern [HIGH]: 使用通用 hex 扫描而非仅 inline-style pattern。**

  1) 创建 scripts/check-hardcoded-colors.sh:
  ```bash
  #!/bin/bash
  # Phase 14: 硬编码色审计脚本
  # 扫描 frontend/src 和 frontend/index.html 中所有 hex 色值
  # 白名单文件和值排除后，任何残留视为 FAIL
  set -e
  cd "$(dirname "$0")/.."

  echo "Scanning for hardcoded hex colors..."

  # 白名单文件（允许包含 hex 色的文件）
  WHITELIST_FILES=(
    "theme/index.ts"          # buildTheme 种子色
    "theme/semanticColors.ts" # 静态 fallback 常量
    "theme/chartColors.ts"    # 暗模近似常量
    "MainLayout.module.css"   # Sider logo #fff (D-16)
  )

  # 构建 grep 排除 pattern
  EXCLUDE_PATTERN=$(printf "|%s" "${WHITELIST_FILES[@]}")
  EXCLUDE_PATTERN="${EXCLUDE_PATTERN:1}"  # 去掉开头的 |

  # 扫描 .tsx/.ts 文件中的所有 hex 色（单引号、双引号、冒号后）
  VIOLATIONS_TSX=$(grep -rEn "'#[0-9a-fA-F]{3,8}'|\"#[0-9a-fA-F]{3,8}\"" \
    frontend/src/ \
    --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js" \
    --exclude-dir=node_modules --exclude-dir=dist 2>/dev/null \
    | grep -vE "($EXCLUDE_PATTERN)" || true)

  # 扫描 index.html 中的 hex 色（排除 FOUC 脚本中的 #1F1F1F）
  VIOLATIONS_HTML=$(grep -En "'#[0-9a-fA-F]{3,8}'|\"#[0-9a-fA-F]{3,8}\"" \
    frontend/index.html 2>/dev/null \
    | grep -vE "#1F1F1F" || true)

  # 也扫描 CSS 模块文件（排除白名单）
  VIOLATIONS_CSS=$(grep -rEn "#[0-9a-fA-F]{3,8}" \
    frontend/src/ \
    --include="*.css" --include="*.module.css" \
    --exclude-dir=node_modules --exclude-dir=dist 2>/dev/null \
    | grep -vE "($EXCLUDE_PATTERN)" || true)

  ALL_VIOLATIONS=""
  [ -n "$VIOLATIONS_TSX" ] && ALL_VIOLATIONS="$ALL_VIOLATIONS\n--- TSX/TS ---\n$VIOLATIONS_TSX"
  [ -n "$VIOLATIONS_HTML" ] && ALL_VIOLATIONS="$ALL_VIOLATIONS\n--- HTML ---\n$VIOLATIONS_HTML"
  [ -n "$VIOLATIONS_CSS" ] && ALL_VIOLATIONS="$ALL_VIOLATIONS\n--- CSS ---\n$VIOLATIONS_CSS"

  if [ -n "$ALL_VIOLATIONS" ]; then
    echo "FAIL: Hardcoded hex colors found outside whitelist:"
    echo -e "$ALL_VIOLATIONS"
    exit 1
  fi

  echo "PASS: No hardcoded hex colors outside whitelist."
  ```

  2) 执行：`chmod +x scripts/check-hardcoded-colors.sh && bash scripts/check-hardcoded-colors.sh`

  3) 如有残留，回去修复对应文件后重新运行直到 PASS。
  </action>
  <verify>
    <automated>bash /Users/mac/PycharmProjects/5xyj/scripts/check-hardcoded-colors.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/check-hardcoded-colors.sh` exits 0
    - `bash scripts/check-hardcoded-colors.sh` exits 0 (显示 PASS 消息)
    - `wc -l scripts/check-hardcoded-colors.sh | awk '{print $1}'` returns >= 15
    - `grep -c "WHITELIST_FILES\|EXCLUDE_PATTERN\|VIOLATIONS" scripts/check-hardcoded-colors.sh` returns >= 3
    - `grep -c "#1F1F1F" scripts/check-hardcoded-colors.sh` returns >= 1 (index.html FOUC 脚本色在白名单中)
  </acceptance_criteria>
  <done>check-hardcoded-colors.sh 创建并可执行，通用 hex 扫描覆盖 TSX/TS/CSS/HTML，白名单含 index.html #1F1F1F，全项目审计通过。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 用户视觉验证 - 亮暗模式切换全页面巡检</name>
  <files>N/A (manual verification)</files>
  <read_first>
    - .planning/phases/14-style-tokens-dark-mode/14-CONTEXT.md (Success Criteria)
    - frontend/src/App.tsx (确认真实路由路径)
  </read_first>
  <action>
  **已完成工作：**
  Phase 14 全部工作已完成：
  - 主题基础设施（ThemeModeProvider + buildTheme + useSemanticColors + useCardStatusColors + chartColors）
  - 36 处硬编码色迁移到 AntD token
  - Header 切换按钮（太阳/月亮图标）
  - localStorage theme-mode 持久化
  - index.html FOUC 预防脚本
  - styles.css 死代码删除（3520 行）
  - check-hardcoded-colors.sh 通用审计脚本

  **前置条件：**
  - 以 admin 账号登录（验证管理员页面）
  - 系统中需要有至少一个已导入批次（用于 /imports/:batchId、/results、/exports 等页面有数据显示）
  - 如需验证 /feishu-mapping/:configId，需要至少一个飞书配置 ID

  **用户验证步骤：**

  1. 启动前端：`cd frontend && npm run dev`

  2. 打开 http://localhost:5173/login，验证基础功能：
     - [ ] 切换按钮可见：MainLayout Header 右上角用户菜单旁看到太阳/月亮图标
     - [ ] 点击按钮全站瞬间切换（无过渡动画，per D-11）
     - [ ] 暗模下 Sider 仍是深色（per D-16），与 Content 背景可区分
     - [ ] DevTools Application -> Local Storage 显示 theme-mode=dark
     - [ ] F5 刷新主题保持，无 FOUC 闪烁（per D-13）
     - [ ] 再点一次切回亮模，html/body 背景恢复（无暗色残留）

  3. 暗模下逐一打开以下真实路由（基于 App.tsx），确认无"半白半黑"混合色：
     - [ ] /login
     - [ ] /dashboard
     - [ ] /workspace/admin
     - [ ] /workspace/hr
     - [ ] /employees
     - [ ] /imports
     - [ ] /imports/:batchId（点击任一批次进入）
     - [ ] /aggregate
     - [ ] /results
     - [ ] /exports
     - [ ] /mappings
     - [ ] /anomaly-detection
     - [ ] /compare
     - [ ] /period-compare
     - [ ] /feishu-sync
     - [ ] /feishu-mapping/:configId（需要有效 configId）
     - [ ] /api-keys
     - [ ] /audit-logs
     - [ ] /data-management
     - [ ] /employee/query（需 HR/employee 账号）

  4. 语义色验证：
     - [ ] Statistic 彩色数字在暗模下可读（Results/Compare/Mappings）
     - [ ] Card 状态边框（红/绿/黄）在暗模下可见（Dashboard/Employees/SimpleAggregate）
     - [ ] PeriodCompare 差异高亮 HIGHLIGHT_BG 在暗模下变为深色背景（per D-05）
     - [ ] FeishuFieldMapping React Flow 背景点阵在暗模下可见

  5. 再点一次按钮切回亮模，验证所有页面视觉正确恢复。

  输入 "approved" 完成，或描述发现的问题（文件名+行号+描述）等待修复。
  </action>
  <verify>
    <automated>echo "MISSING -- manual visual verification required (checkpoint)"</automated>
  </verify>
  <acceptance_criteria>
    - 用户点击切换按钮可见暗模切换
    - 用户确认 localStorage.theme-mode 持久化
    - 用户确认刷新无 FOUC
    - 用户确认 20+ 真实路由页面暗模无"半白半黑"
    - 用户输入 "approved"
  </acceptance_criteria>
  <done>用户确认 Phase 14 所有四个 Success Criteria 达成：硬编码色全部 token 化、切换按钮工作、localStorage 持久化、无混合色。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 死代码清理 + 验证，无新增边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | N/A | 删除 styles.css | accept | 全项目 grep 零 import 已双重确认；build 通过验证 |
</threat_model>

<verification>
- `bash scripts/check-hardcoded-colors.sh` 通过
- `cd frontend && npm run build` 通过
- 用户 checkpoint 验证 20+ 真实路由页面无视觉问题
</verification>

<success_criteria>
1. styles.css 死代码删除，build 仍通过
2. check-hardcoded-colors.sh 脚本使用通用 hex 扫描且审计通过
3. 白名单包含 index.html FOUC 脚本的 #1F1F1F
4. 视觉验证清单基于 App.tsx 真实路由
5. 用户确认 Phase 14 四个 Success Criteria 全部达成
</success_criteria>

<output>
创建 `.planning/phases/14-style-tokens-dark-mode/14-04-SUMMARY.md`
</output>
