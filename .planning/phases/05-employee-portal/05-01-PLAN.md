---
phase: 05-employee-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/employees.py
  - backend/app/services/employee_service.py
  - backend/app/api/v1/employees.py
  - backend/tests/test_employee_portal_api.py
autonomous: true
requirements:
  - PORTAL-01
  - PORTAL-02
  - PORTAL-03
  - PORTAL-04
  - PORTAL-05

must_haves:
  truths:
    - "GET /employees/self-service/my-records 使用 employee token 返回完整社保险种明细（pension_company, medical_company 等）"
    - "GET /employees/self-service/my-records 同时返回公积金字段（housing_fund_personal/company/total）"
    - "返回多期记录按 billing_period 倒序排列"
    - "员工 A 的 token 无法获取员工 B 的数据"
    - "返回结果包含 payment_base 和各险种单位/个人金额"
  artifacts:
    - path: "backend/app/schemas/employees.py"
      provides: "EmployeeSelfServiceRecordRead 含 9 个新险种明细字段"
      contains: "pension_company"
    - path: "backend/app/api/v1/employees.py"
      provides: "token 绑定的 /self-service/my-records 端点"
      contains: "my-records"
    - path: "backend/app/services/employee_service.py"
      provides: "lookup_employee_portal 函数 + _to_self_service_record 扩展"
      contains: "lookup_employee_portal"
    - path: "backend/tests/test_employee_portal_api.py"
      provides: "数据隔离 + 险种明细 + 多期浏览测试"
      contains: "test_employee_cannot_access_others"
  key_links:
    - from: "backend/app/api/v1/employees.py"
      to: "backend/app/services/employee_service.py"
      via: "lookup_employee_portal(db, employee_id=user.username)"
      pattern: "lookup_employee_portal"
    - from: "backend/app/services/employee_service.py"
      to: "backend/app/schemas/employees.py"
      via: "_to_self_service_record 填充新字段"
      pattern: "pension_company=record.pension_company"
---

<objective>
后端扩展：为员工门户提供完整的社保险种明细查询能力，包含 token 绑定的安全端点和数据隔离保障。

Purpose: 员工通过 token 绑定端点安全查询个人社保/公积金各险种明细，为前端门户页面提供数据基础。
Output: 扩展后的 Schema、新增 lookup_employee_portal 服务函数、/self-service/my-records 端点、完整测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-employee-portal/05-CONTEXT.md
@.planning/phases/05-employee-portal/05-RESEARCH.md

<interfaces>
<!-- 执行器直接使用的现有接口 -->

From backend/app/schemas/employees.py (需扩展):
```python
class EmployeeSelfServiceRecordRead(BaseModel):
    normalized_record_id: str
    batch_id: str
    batch_name: str
    batch_status: str
    employee_id: Optional[str]
    region: Optional[str]
    company_name: Optional[str]
    billing_period: Optional[str]
    period_start: Optional[str]
    period_end: Optional[str]
    source_file_name: Optional[str]
    source_row_number: int
    total_amount: Optional[Decimal]
    company_total_amount: Optional[Decimal]
    personal_total_amount: Optional[Decimal]
    housing_fund_personal: Optional[Decimal]
    housing_fund_company: Optional[Decimal]
    housing_fund_total: Optional[Decimal]
    created_at: datetime
```

From backend/app/dependencies.py:
```python
def require_role(*allowed_roles: str) -> Callable:
    # 返回 AuthUser，user.username = employee_id（对 employee 角色）
    # user.role = "employee"
```

From backend/app/core/auth.py:
```python
@dataclass(frozen=True)
class AuthUser:
    username: str  # employee token: sub = employee_id
    role: AuthRole  # Literal['admin', 'hr', 'employee']

def issue_access_token(secret_key: str, sub: str, role: str, expire_minutes: int) -> tuple[str, datetime]:
```

From backend/app/services/employee_service.py (671行):
```python
def _to_self_service_record(record: NormalizedRecord, batch: ImportBatch) -> EmployeeSelfServiceRecordRead:
    return EmployeeSelfServiceRecordRead(
        # ... 现有字段映射（缺少险种明细字段）
        total_amount=record.total_amount,
        company_total_amount=record.company_total_amount,
        personal_total_amount=record.personal_total_amount,
        housing_fund_personal=record.housing_fund_personal,
        housing_fund_company=record.housing_fund_company,
        housing_fund_total=record.housing_fund_total,
        created_at=record.created_at,
    )
```

From backend/app/api/v1/employees.py (108行，旧端点保留不动):
```python
@router.post('/self-service/query')
def employee_self_service_query_endpoint(
    payload: EmployeeSelfServiceQueryInput = Body(...),
    db: Session = Depends(get_db),
):
```

From backend/tests/test_employee_master_api.py (测试模式):
```python
def build_test_context(test_name: str):
    # 创建独立数据库 + Settings(auth_enabled=False) + TestClient
    # 返回 client, session_factory, artifacts_dir 等
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 Schema + 服务层 + 新增 token 绑定端点</name>
  <files>
    backend/app/schemas/employees.py,
    backend/app/services/employee_service.py,
    backend/app/api/v1/employees.py,
    backend/tests/test_employee_portal_api.py
  </files>
  <read_first>
    backend/app/schemas/employees.py,
    backend/app/services/employee_service.py,
    backend/app/api/v1/employees.py,
    backend/app/dependencies.py,
    backend/app/core/auth.py,
    backend/app/models/normalized_record.py,
    backend/tests/test_employee_master_api.py
  </read_first>
  <behavior>
    - Test 1 (PORTAL-01/05): GET /employees/self-service/my-records 用 employee token 返回记录，每条记录含 pension_company, pension_personal, medical_company, medical_personal, unemployment_company, unemployment_personal, injury_company, maternity_amount, payment_base 字段
    - Test 2 (PORTAL-02): 返回记录同时含 housing_fund_personal, housing_fund_company, housing_fund_total 字段
    - Test 3 (PORTAL-03): 多期记录按 billing_period 倒序排列（最新月份在前）
    - Test 4 (PORTAL-04/D-12): 员工 A 的 token 调用端点，只返回 A 的数据，不返回 B 的数据
    - Test 5 (PORTAL-04): 无 token 调用返回 401
    - Test 6: 旧端点 POST /self-service/query 仍正常工作（不破坏回归）
  </behavior>
  <action>
    **1. 扩展 EmployeeSelfServiceRecordRead（per D-03）:**
    在 backend/app/schemas/employees.py 的 EmployeeSelfServiceRecordRead 类中，在 housing_fund_total 之后、created_at 之前，新增以下 Optional[Decimal] 字段：
    - payment_base
    - pension_company
    - pension_personal
    - medical_company
    - medical_personal
    - unemployment_company
    - unemployment_personal
    - injury_company
    - maternity_amount

    **2. 扩展 _to_self_service_record（per D-04）:**
    在 backend/app/services/employee_service.py 的 _to_self_service_record 函数中，在 housing_fund_total 行之后添加对应的字段映射：
    ```python
    payment_base=record.payment_base,
    pension_company=record.pension_company,
    pension_personal=record.pension_personal,
    medical_company=record.medical_company,
    medical_personal=record.medical_personal,
    unemployment_company=record.unemployment_company,
    unemployment_personal=record.unemployment_personal,
    injury_company=record.injury_company,
    maternity_amount=record.maternity_amount,
    ```

    **3. 新增 lookup_employee_portal 服务函数（per D-10/D-11）:**
    在 backend/app/services/employee_service.py 中新增函数，签名为：
    ```python
    def lookup_employee_portal(db: Session, employee_id: str) -> EmployeeSelfServiceRead:
    ```
    逻辑：
    - 用 employee_id 查 EmployeeMaster（filter active=True），不存在则 raise EmployeeSelfServiceNotFoundError
    - 用 employee_id 匹配 NormalizedRecord（复用 lookup_employee_self_service 的三条件 OR 逻辑：MatchResult.employee_master_id, NormalizedRecord.employee_id, person_name+id_number）
    - 按 ImportBatch.created_at DESC, NormalizedRecord.created_at DESC 排序
    - 去重后构建 EmployeeSelfServiceRead 返回
    - profile 使用 _to_self_service_profile_from_employee
    - records 使用已扩展的 _to_self_service_record

    **4. 新增 GET /self-service/my-records 端点（per D-10）:**
    在 backend/app/api/v1/employees.py 中，在 self_service_query_endpoint 之后新增：
    ```python
    @router.get('/self-service/my-records')
    def employee_portal_records_endpoint(
        db: Session = Depends(get_db),
        user: AuthUser = Depends(require_role("employee")),
    ):
    ```
    - 从 user.username 获取 employee_id（employee token 的 sub = employee_id）
    - 调用 lookup_employee_portal(db, employee_id=user.username)
    - 返回 success_response(result.model_dump(mode='json'))
    - 需要在文件顶部 import require_role, lookup_employee_portal

    **5. 编写测试 backend/tests/test_employee_portal_api.py:**
    参考 test_employee_master_api.py 的 build_test_context 模式，但 auth_enabled=True。
    - 创建 build_test_context 函数（Settings 中 auth_enabled=True, auth_secret_key='test-secret', employee_token_expire_minutes=30）
    - helper 函数：issue_employee_token(employee_id) 直接调用 issue_access_token 生成 token
    - helper 函数：seed_employee_and_records(db, employee_id, person_name, id_number, billing_periods=['202602','202601'])，创建 EmployeeMaster + ImportBatch + NormalizedRecord（填充部分险种金额）
    - test_portal_returns_insurance_breakdown: 创建员工+记录 -> GET /my-records -> 验证返回 pension_company 等字段
    - test_portal_returns_housing_fund: 验证返回 housing_fund_personal 等字段
    - test_portal_returns_multiple_periods: 创建两个 billing_period 的记录 -> 验证结果 record_count=2，且第一条 billing_period > 第二条
    - test_employee_cannot_access_others: 创建员工 A 和 B -> 用 A 的 token 调用 -> 验证返回数据全属于 A（无 B 的 normalized_record_id）
    - test_portal_requires_auth: 不带 token 调用 -> 401
    - test_old_query_endpoint_still_works: auth_enabled=False 的 context 调用旧 POST /self-service/query -> 正常返回

    注意：保留旧端点不变（per Research Pitfall 2）。
  </action>
  <verify>
    <automated>cd D:/execl_mix && python -m pytest backend/tests/test_employee_portal_api.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "pension_company" backend/app/schemas/employees.py
    - grep -q "payment_base" backend/app/schemas/employees.py
    - grep -q "maternity_amount" backend/app/schemas/employees.py
    - grep -q "lookup_employee_portal" backend/app/services/employee_service.py
    - grep -q "my-records" backend/app/api/v1/employees.py
    - grep -q "require_role" backend/app/api/v1/employees.py
    - grep -q "test_employee_cannot_access_others" backend/tests/test_employee_portal_api.py
    - grep -q "test_portal_returns_insurance_breakdown" backend/tests/test_employee_portal_api.py
    - grep -q "test_portal_returns_multiple_periods" backend/tests/test_employee_portal_api.py
    - python -m pytest backend/tests/test_employee_portal_api.py -x 通过
    - python -m pytest backend/tests/ -x 全套通过（旧端点不破坏）
  </acceptance_criteria>
  <done>
    - EmployeeSelfServiceRecordRead 包含 9 个新险种明细字段
    - GET /self-service/my-records 端点存在并强制 employee role token
    - lookup_employee_portal 通过 employee_id 查询，不接受前端传参
    - 6 个测试全部通过，覆盖 PORTAL-01~05 和数据隔离
    - 旧 POST /self-service/query 端点未受影响
  </done>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/test_employee_portal_api.py -x -v` 全部通过
- `python -m pytest backend/tests/ -x` 全套回归通过
- 端点 GET /api/v1/employees/self-service/my-records 存在
- 旧端点 POST /api/v1/employees/self-service/query 仍可用
</verification>

<success_criteria>
1. 新端点用 employee token 可返回包含各险种明细的完整记录
2. 数据隔离测试验证员工只能查看自己的数据
3. 全套测试通过，旧功能无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/05-employee-portal/05-01-SUMMARY.md`
</output>
