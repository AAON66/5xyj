---
phase: 05-employee-portal
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - frontend/src/services/employees.ts
  - frontend/src/pages/EmployeeSelfService.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - PORTAL-01
  - PORTAL-02
  - PORTAL-03
  - PORTAL-04
  - PORTAL-05

must_haves:
  truths:
    - "员工验证后看到概览首页：个人信息卡片 + 最新月份缴费汇总"
    - "概览首页下方显示所有月份记录列表，按时间倒序（最新在前）"
    - "点击任意月份记录可展开查看各险种明细（养老/医疗/失业/工伤/生育+缴费基数）"
    - "社保和公积金在同一页面并列展示"
    - "无社保记录时显示友好提示而非空表格"
    - "Token 过期时显示提示并 2 秒后跳回验证页"
    - "员工门户页面受 ProtectedRoute + RoleRoute 保护"
  artifacts:
    - path: "frontend/src/services/employees.ts"
      provides: "EmployeeSelfServiceRecord 扩展字段 + fetchPortalRecords 函数"
      contains: "fetchPortalRecords"
    - path: "frontend/src/pages/EmployeeSelfService.tsx"
      provides: "概览+可展开明细的门户页面"
      contains: "pension_company"
    - path: "frontend/src/App.tsx"
      provides: "员工门户路由在 ProtectedRoute + RoleRoute 内"
      contains: "RoleRoute"
  key_links:
    - from: "frontend/src/pages/EmployeeSelfService.tsx"
      to: "frontend/src/services/employees.ts"
      via: "fetchPortalRecords() 调用"
      pattern: "fetchPortalRecords"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/EmployeeSelfService.tsx"
      via: "Route path=/employee/query"
      pattern: "employee/query"
---

<objective>
前端改造：将 EmployeeSelfService 页面从"查询表单+列表"改造为"概览+可展开险种明细"的安全门户页面。

Purpose: 员工验证后能在一个页面内看到个人信息概览、最新月份缴费汇总、历史记录列表（可展开各险种明细），公积金与社保并列展示。
Output: 改造后的门户页面、扩展的 TypeScript 接口、路由保护、token 过期处理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-employee-portal/05-CONTEXT.md
@.planning/phases/05-employee-portal/05-RESEARCH.md
@.planning/phases/05-employee-portal/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01 产出的后端接口，前端需要对齐 -->

GET /api/v1/employees/self-service/my-records
Headers: Authorization: Bearer {employee_token}
Response (success_response 封装):
```json
{
  "success": true,
  "data": {
    "matched_employee_master": true,
    "profile": {
      "employee_id": "EMP001",
      "person_name": "张三",
      "masked_id_number": "310***1234",
      "company_name": "深圳零一数科",
      "department": "技术部",
      "active": true,
      "source": "employee_master"
    },
    "record_count": 2,
    "records": [
      {
        "normalized_record_id": "xxx",
        "batch_id": "xxx",
        "batch_name": "202602导入",
        "batch_status": "completed",
        "employee_id": "EMP001",
        "region": "深圳",
        "company_name": "深圳零一数科",
        "billing_period": "202602",
        "period_start": "2026-02-01",
        "period_end": "2026-02-28",
        "source_file_name": "深圳零一数科202602社保明细.xlsx",
        "source_row_number": 5,
        "total_amount": "3456.78",
        "company_total_amount": "2345.67",
        "personal_total_amount": "1111.11",
        "housing_fund_personal": "500.00",
        "housing_fund_company": "500.00",
        "housing_fund_total": "1000.00",
        "payment_base": "10000.00",
        "pension_company": "1600.00",
        "pension_personal": "800.00",
        "medical_company": "400.00",
        "medical_personal": "200.00",
        "unemployment_company": "50.00",
        "unemployment_personal": "30.00",
        "injury_company": "20.00",
        "maternity_amount": "45.67",
        "created_at": "2026-02-15T10:30:00"
      }
    ]
  }
}
```

From frontend/src/services/employees.ts (现有接口，需扩展):
```typescript
export interface EmployeeSelfServiceRecord {
  // ... 现有 17 个字段 ...
  // 需新增: payment_base, pension_company, pension_personal,
  // medical_company, medical_personal, unemployment_company,
  // unemployment_personal, injury_company, maternity_amount
}
```

From frontend/src/services/authSession.ts:
```typescript
export function readAuthSession(): AuthSession | null;
export function clearAuthSession(): void;
export function isAuthSessionExpired(session: AuthSession): boolean;
```

From frontend/src/App.tsx (路由结构):
```typescript
// 当前: <Route path="/employee/query" element={<EmployeeSelfServicePage />} />
// 位置: 在 ProtectedRoute 之外（需迁入）

function ProtectedRoute() { /* 检查 isAuthenticated */ }
function RoleRoute({ allowedRoles }: { allowedRoles: AuthRole[] }) { /* 检查 user.role */ }
```

From frontend/src/pages/EmployeeSelfService.tsx (现有工具函数):
```typescript
function formatDateTime(value: string): string { /* 日期格式化 */ }
function formatMoney(value: string | number | null): string { /* 金额格式化 */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 TS 接口 + 新增 API 函数 + 路由保护</name>
  <files>
    frontend/src/services/employees.ts,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/services/employees.ts,
    frontend/src/services/api.ts,
    frontend/src/services/authSession.ts,
    frontend/src/App.tsx,
    frontend/src/contexts/AuthContext.tsx
  </read_first>
  <action>
    **1. 扩展 EmployeeSelfServiceRecord 接口（per D-03）:**
    在 frontend/src/services/employees.ts 的 EmployeeSelfServiceRecord 接口中，在 housing_fund_total 字段之后新增：
    ```typescript
    payment_base: string | number | null;
    pension_company: string | number | null;
    pension_personal: string | number | null;
    medical_company: string | number | null;
    medical_personal: string | number | null;
    unemployment_company: string | number | null;
    unemployment_personal: string | number | null;
    injury_company: string | number | null;
    maternity_amount: string | number | null;
    ```

    **2. 新增 fetchPortalRecords 函数:**
    在 employees.ts 文件末尾新增：
    ```typescript
    export async function fetchPortalRecords(): Promise<EmployeeSelfServiceResult> {
      const response = await apiClient.get<ApiSuccessResponse<EmployeeSelfServiceResult>>(
        '/employees/self-service/my-records'
      );
      return response.data.data;
    }
    ```
    注意：此接口使用 GET 方法，不需要传参（后端从 token 提取 employee_id）。apiClient 的拦截器会自动附加 Bearer token。

    **3. 路由保护迁移（per D-10, Research Pitfall 4）:**
    在 frontend/src/App.tsx 中：
    - 删除当前 `<Route path="/employee/query" element={<EmployeeSelfServicePage />} />` 行（在 ProtectedRoute 外面）
    - 在 `<Route element={<ProtectedRoute />}>` 内、`<Route element={<ProtectedLayout />}>` 内新增一个 RoleRoute 块：
    ```tsx
    <Route element={<RoleRoute allowedRoles={['employee']} />}>
      <Route path="/employee/query" element={<EmployeeSelfServicePage />} />
    </Route>
    ```
    这样员工门户页面需要登录 + employee 角色才能访问。
  </action>
  <verify>
    <automated>cd D:/execl_mix/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "pension_company" frontend/src/services/employees.ts
    - grep -q "fetchPortalRecords" frontend/src/services/employees.ts
    - grep -q "my-records" frontend/src/services/employees.ts
    - App.tsx 中 /employee/query 路由在 ProtectedRoute 和 RoleRoute 内
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>
    - EmployeeSelfServiceRecord 接口包含 9 个新险种明细字段
    - fetchPortalRecords 函数可调用 GET /self-service/my-records
    - /employee/query 路由受 ProtectedRoute + RoleRoute(['employee']) 保护
  </done>
</task>

<task type="auto">
  <name>Task 2: 改造门户页面为概览+可展开明细模式</name>
  <files>
    frontend/src/pages/EmployeeSelfService.tsx
  </files>
  <read_first>
    frontend/src/pages/EmployeeSelfService.tsx,
    frontend/src/services/employees.ts,
    frontend/src/services/authSession.ts,
    frontend/src/contexts/AuthContext.tsx
  </read_first>
  <action>
    **完全重写 EmployeeSelfService.tsx**，保留 formatDateTime 和 formatMoney 工具函数。

    **页面结构（per D-13/D-14/D-15 -- 单页面，概览+历史）：**

    1. **数据加载层：**
       - useEffect 中调用 fetchPortalRecords()
       - 状态：loading, error, data (EmployeeSelfServiceResult | null)
       - 检测 401 错误时设置 expired 状态

    2. **Token 过期处理（per D-17/D-18）：**
       - 当检测到 expired 状态时，显示提示信息"登录已过期，请重新验证"
       - useEffect 中设置 setTimeout 2000ms 后 navigate('/login')
       - 清理 timer 的 cleanup function

    3. **无数据状态（per D-16）：**
       - 当 data.record_count === 0 时，显示："暂无社保缴费记录，请联系 HR 确认"
       - 使用居中的提示文字，不显示空表格

    4. **概览首页区域（per D-13）：**
       - **个人信息卡片：**
         - 显示 profile.person_name, profile.employee_id, profile.company_name
         - 身份证号已脱敏（profile.masked_id_number）
         - 使用卡片式布局，简洁明了
       - **最新月份缴费汇总：**
         - 取 records[0]（已按倒序排列）
         - 显示 billing_period, total_amount, company_total_amount, personal_total_amount
         - 如有公积金数据则并列显示 housing_fund_total

    5. **历史记录列表（per D-08/D-09/D-14）：**
       - 标题："缴费历史"
       - 所有 records 按时间倒序显示（后端已排序，无需前端排序）
       - 每条记录默认显示：
         - billing_period（格式化为 "YYYY年MM月"）
         - region
         - company_name
         - total_amount / company_total_amount / personal_total_amount
         - 展开/折叠图标按钮

    6. **展开险种明细（per D-01/D-02/D-05/D-09）：**
       - 使用 useState<Set<string>> 记录展开的 normalized_record_id 集合
       - 展开后显示两列网格：

       **社保明细（per D-02）：**
       | 险种 | 单位 | 个人 |
       | 养老保险 | pension_company | pension_personal |
       | 医疗保险 | medical_company | medical_personal |
       | 失业保险 | unemployment_company | unemployment_personal |
       | 工伤保险 | injury_company | - |
       | 生育保险 | maternity_amount | - |
       | 缴费基数 | payment_base（跨列显示） | |

       **公积金明细（per D-05/D-06）：**
       | 项目 | 单位 | 个人 | 合计 |
       | 住房公积金 | housing_fund_company | housing_fund_personal | housing_fund_total |

       所有金额使用 formatMoney 格式化。

    7. **样式（Claude 自行决定）：**
       - 使用 inline styles 或 CSS module（跟随项目现有模式）
       - 卡片式布局，圆角边框
       - 展开/折叠使用简单的条件渲染（{expanded && <div>...</div>}）
       - 响应式：移动端单列，桌面端双列（概览区域）
  </action>
  <verify>
    <automated>cd D:/execl_mix/frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchPortalRecords" frontend/src/pages/EmployeeSelfService.tsx
    - grep -q "pension_company" frontend/src/pages/EmployeeSelfService.tsx
    - grep -q "housing_fund" frontend/src/pages/EmployeeSelfService.tsx
    - grep -q "暂无社保缴费记录" frontend/src/pages/EmployeeSelfService.tsx
    - grep -q "登录已过期" frontend/src/pages/EmployeeSelfService.tsx
    - grep -q "navigate" frontend/src/pages/EmployeeSelfService.tsx
    - TypeScript 编译无错误
    - npm run build 成功
  </acceptance_criteria>
  <done>
    - 门户页面显示个人信息概览 + 最新月份缴费汇总
    - 历史记录按时间倒序列出，可展开查看各险种明细
    - 社保和公积金在展开区域并列展示
    - 无数据时显示友好提示
    - Token 过期时提示并 2 秒后跳转
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证员工门户完整功能</name>
  <files>N/A</files>
  <action>
    人工验证员工门户的完整功能，包括概览首页、险种明细展开、路由保护和 token 过期处理。
  </action>
  <what-built>
    员工门户完整功能：后端 token 绑定查询端点 + 前端概览+可展开明细页面 + 路由保护 + token 过期处理
  </what-built>
  <how-to-verify>
    1. 启动后端: `cd D:/execl_mix && uvicorn backend.app.main:app --reload`
    2. 启动前端: `cd D:/execl_mix/frontend && npm run dev`
    3. 先以 HR 身份登录，确认存在至少一名员工主数据和对应的社保导入记录
    4. 退出登录，使用员工验证（工号+身份证号+姓名）登录
    5. 验证概览首页：
       - 个人信息卡片显示正确（姓名、工号、公司、脱敏身份证号）
       - 最新月份缴费汇总金额正确
    6. 验证历史记录：
       - 所有月份按倒序排列
       - 点击某条记录展开，查看各险种明细（养老/医疗/失业/工伤/生育 + 缴费基数）
       - 公积金与社保并列显示
    7. 验证安全：
       - 直接访问 /employee/query URL（未登录状态）应跳转到登录页
       - 非 employee 角色（如 admin/hr）访问该页面应提示权限不足
    8. 验证无数据状态：如果员工无社保记录，应显示"暂无社保缴费记录，请联系 HR 确认"
  </how-to-verify>
  <verify>人工验证通过</verify>
  <done>用户确认门户页面功能、安全性和 UI 表现均符合预期</done>
  <resume-signal>输入 "approved" 或描述需要修改的问题</resume-signal>
</task>

</tasks>

<verification>
- TypeScript 编译无错误: `cd frontend && npx tsc --noEmit`
- 前端构建成功: `cd frontend && npm run build`
- 后端全套测试通过: `python -m pytest backend/tests/ -x`
- 门户页面可正常加载和交互（人工验证）
</verification>

<success_criteria>
1. 员工验证后看到概览首页（个人信息 + 最新月份汇总）
2. 历史记录按倒序展示，可展开查看各险种明细
3. 社保和公积金在同一页面并列展示
4. 无数据时显示友好提示
5. Token 过期时 2 秒后自动跳回验证页
6. /employee/query 受 ProtectedRoute + RoleRoute 保护
</success_criteria>

<output>
完成后创建 `.planning/phases/05-employee-portal/05-02-SUMMARY.md`
</output>
