---
phase: 16-account-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/auth.py
  - backend/app/api/v1/auth.py
  - backend/app/services/user_service.py
  - backend/app/api/v1/users.py
  - tests/test_users.py
autonomous: true
requirements:
  - ACCT-03
  - ACCT-04

must_haves:
  truths:
    - "PUT /api/v1/auth/change-password 验证旧密码后才能设置新密码"
    - "改密成功后 must_change_password 被清除为 False"
    - "管理员重置密码后 must_change_password 被设置为 True"
    - "新创建用户的 must_change_password 为 True"
    - "管理员不能禁用自己的账号（返回 403）"
    - "管理员不能修改自己的角色（返回 403）"
  artifacts:
    - path: "backend/app/schemas/auth.py"
      provides: "ChangePasswordRequest schema"
      contains: "class ChangePasswordRequest"
    - path: "backend/app/api/v1/auth.py"
      provides: "change-password endpoint"
      contains: "change-password"
    - path: "backend/app/services/user_service.py"
      provides: "change_own_password service method"
      contains: "def change_own_password"
    - path: "tests/test_users.py"
      provides: "TestChangePassword + TestSelfProtection test classes"
      contains: "TestChangePassword"
  key_links:
    - from: "backend/app/api/v1/auth.py"
      to: "backend/app/services/user_service.py"
      via: "change_own_password function call"
      pattern: "change_own_password"
    - from: "backend/app/api/v1/auth.py"
      to: "backend/app/schemas/auth.py"
      via: "ChangePasswordRequest schema import"
      pattern: "ChangePasswordRequest"
---

<objective>
后端账号管理补全：新增用户自改密码端点、修复 must_change_password 标记逻辑、添加管理员自我操作保护。

Purpose: 为 Phase 16 前端页面提供完整且正确的后端 API 支撑。当前后端缺失 change-password 端点（ACCT-04），reset_user_password 将 must_change_password 错误设为 False（ACCT-03 bug），create_user 未设置 must_change_password=True（新用户不会被强制改密），且管理员可以禁用自己导致系统锁死（D-11 安全漏洞）。

Output: 功能完整的后端 API + 全部测试通过
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-account-management/16-CONTEXT.md
@.planning/phases/16-account-management/16-RESEARCH.md
@.planning/phases/16-account-management/16-VALIDATION.md

<interfaces>
<!-- 后端现有接口 — executor 必须了解这些才能正确扩展 -->

From backend/app/schemas/auth.py:
```python
AuthRole = Literal['admin', 'hr', 'employee']

class AuthUserRead(BaseModel):
    username: str
    role: AuthRole
    display_name: str
    must_change_password: bool = False

class AuthLoginRequest(BaseModel):
    username: str = Field(min_length=1, max_length=100)
    password: str = Field(min_length=1, max_length=200)
    role: AuthRole = 'admin'
```

From backend/app/schemas/users.py:
```python
class UserCreate(BaseModel):
    username: str = Field(min_length=1, max_length=100)
    password: str = Field(min_length=8, max_length=200)
    role: Literal["admin", "hr"] = "hr"
    display_name: str = Field(default="", max_length=255)

class UserUpdate(BaseModel):
    username: Optional[str] = Field(default=None, min_length=1, max_length=100)
    role: Optional[Literal["admin", "hr"]] = None
    display_name: Optional[str] = Field(default=None, max_length=255)
    is_active: Optional[bool] = None

class UserPasswordReset(BaseModel):
    new_password: str = Field(min_length=8, max_length=200)

class UserRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: str
    username: str
    role: str
    display_name: str
    is_active: bool
    must_change_password: bool
    created_at: datetime
    updated_at: datetime
```

From backend/app/services/user_service.py:
```python
def hash_password(plain: str) -> str: ...
def verify_password(plain: str, hashed: str) -> bool: ...
def get_user_by_username(db: Session, username: str) -> Optional[User]: ...
def create_user(db, username, password, role, display_name=""): ...  # line 68: must_change_password=False ← BUG
def update_user(db, user_id, **kwargs): ...  # no self-protection ← BUG
def reset_user_password(db, user_id, new_password): ...  # line 107: must_change_password=False ← BUG
def list_users(db): ...
def get_user_by_id(db, user_id): ...
```

From backend/app/dependencies.py:
```python
def require_authenticated_user(...) -> AuthUser: ...
def require_role(*allowed_roles: str) -> Callable: ...
```

From backend/app/api/v1/responses.py:
```python
def success_response(data, message=None, status_code=200): ...
def error_response(code, message, status_code): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 change-password 端点 + ChangePasswordRequest schema</name>
  <files>backend/app/schemas/auth.py, backend/app/services/user_service.py, backend/app/api/v1/auth.py, tests/test_users.py</files>
  <read_first>
    - backend/app/schemas/auth.py
    - backend/app/services/user_service.py
    - backend/app/api/v1/auth.py
    - backend/app/dependencies.py
    - backend/app/api/v1/responses.py
    - tests/test_users.py
    - tests/conftest.py
  </read_first>
  <behavior>
    - Test 1 (TestChangePassword::test_change_password_success): 已登录用户提交正确旧密码 + 新密码（>=8位），返回 200，之后用新密码可登录
    - Test 2 (TestChangePassword::test_change_password_wrong_old): 旧密码错误时返回 400，detail 包含 "Old password is incorrect"
    - Test 3 (TestChangePassword::test_change_password_too_short): 新密码少于 8 位时返回 422（Pydantic 验证）
    - Test 4 (TestChangePassword::test_change_password_clears_must_change): 用户 must_change_password=True 时，改密成功后查询该用户 must_change_password=False
    - Test 5 (TestChangePassword::test_change_password_unauthenticated): 无 token 时返回 401
  </behavior>
  <action>
1. 在 `backend/app/schemas/auth.py` 末尾新增 schema（per D-06, D-07, D-08）：
```python
class ChangePasswordRequest(BaseModel):
    old_password: str = Field(min_length=1, max_length=200)
    new_password: str = Field(min_length=8, max_length=200)
```

2. 在 `backend/app/services/user_service.py` 新增 `change_own_password` 方法：
```python
def change_own_password(db: Session, username: str, old_password: str, new_password: str) -> User:
    """Change a user's own password after verifying old password.
    Raises InvalidCredentialsError if old password is wrong.
    Raises ValueError if user not found.
    """
    user = get_user_by_username(db, username)
    if user is None:
        raise ValueError("User not found.")
    if not verify_password(old_password, user.hashed_password):
        raise InvalidCredentialsError("Old password is incorrect.")
    user.hashed_password = hash_password(new_password)
    user.must_change_password = False
    db.commit()
    db.refresh(user)
    return user
```
注意：需要在文件顶部确认 `InvalidCredentialsError` 已从 `backend.app.core.auth` 导入（已导入）。

3. 在 `backend/app/api/v1/auth.py` 新增端点（放在 `/me` 端点之前）：
```python
from backend.app.schemas.auth import ChangePasswordRequest
from backend.app.services.user_service import change_own_password

@router.put('/change-password', summary="修改密码", description="用户修改自己的密码，需验证旧密码。")
def change_password_endpoint(
    request: Request,
    body: ChangePasswordRequest,
    db: Session = Depends(get_db),
    current_user: AuthUser = Depends(require_authenticated_user),
):
    try:
        change_own_password(db, current_user.username, body.old_password, body.new_password)
    except InvalidCredentialsError:
        log_audit(db, action="change_password_failed", actor_username=current_user.username,
                  actor_role=current_user.role, ip_address=get_client_ip(request),
                  resource_type="user", resource_id=current_user.username,
                  detail={"reason": "wrong_old_password"}, success=False)
        raise HTTPException(status_code=400, detail="Old password is incorrect.")
    log_audit(db, action="change_password", actor_username=current_user.username,
              actor_role=current_user.role, ip_address=get_client_ip(request),
              resource_type="user", resource_id=current_user.username,
              detail={}, success=True)
    return success_response({"message": "Password changed successfully."})
```
注意：需要从 `backend.app.core.auth` 导入 `InvalidCredentialsError`，从 `backend.app.services.audit_service` 导入 `log_audit`，从 `backend.app.utils.request_helpers` 导入 `get_client_ip`。检查已有 import 列表，按需添加缺失的。

4. 在 `tests/test_users.py` 末尾新增 `TestChangePassword` 测试类，使用已有的 `_login` 和 `_get_token` helper。测试中先用 admin 登录获取 token，然后调用 PUT /api/v1/auth/change-password。对于 test_change_password_clears_must_change，需要先通过 admin 创建一个 must_change_password=True 的用户，这可以通过创建用户（Task 2 会修复 create_user 设置 must_change_password=True）来实现——但由于 Task 1 先执行，此时 create_user 还是 False。所以在此测试中，直接通过 db fixture 手动设置 `seed_test_hr.must_change_password = True` 并 commit，然后用 hr 登录并改密，验证改密后 must_change_password 变为 False。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/backend && python -m pytest tests/test_users.py -k "TestChangePassword" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/schemas/auth.py 包含 `class ChangePasswordRequest(BaseModel):`
    - backend/app/schemas/auth.py 包含 `old_password: str = Field(min_length=1`
    - backend/app/schemas/auth.py 包含 `new_password: str = Field(min_length=8`
    - backend/app/services/user_service.py 包含 `def change_own_password(`
    - backend/app/api/v1/auth.py 包含 `'/change-password'`
    - backend/app/api/v1/auth.py 包含 `ChangePasswordRequest`
    - tests/test_users.py 包含 `class TestChangePassword:`
    - `cd backend && python -m pytest tests/test_users.py -k "TestChangePassword" -x -q` 退出码 0
  </acceptance_criteria>
  <done>change-password 端点工作正常：验证旧密码、设置新密码、清除 must_change_password、审计日志记录、所有 5 个测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修复 must_change_password 标记 + 管理员自我保护 + 测试</name>
  <files>backend/app/services/user_service.py, backend/app/api/v1/users.py, tests/test_users.py</files>
  <read_first>
    - backend/app/services/user_service.py
    - backend/app/api/v1/users.py
    - tests/test_users.py
    - backend/app/core/auth.py
  </read_first>
  <behavior>
    - Test 1 (TestResetPassword::test_reset_sets_must_change_password): 重置密码后查询用户 must_change_password=True
    - Test 2 (TestCreateUser::test_create_user_sets_must_change_password): 创建用户后返回的 data 中 must_change_password=True
    - Test 3 (TestSelfProtection::test_admin_cannot_disable_self): 管理员 PUT /users/{own_id} with is_active=False 返回 403
    - Test 4 (TestSelfProtection::test_admin_cannot_change_own_role): 管理员 PUT /users/{own_id} with role="hr" 返回 403
    - Test 5 (TestSelfProtection::test_admin_can_update_own_display_name): 管理员可以修改自己的 display_name（返回 200）
    - Test 6 (TestSelfProtection::test_admin_cannot_reset_own_password): 管理员不能通过 reset 端点重置自己的密码（返回 403）
  </behavior>
  <action>
1. 修改 `backend/app/services/user_service.py` 中的 `create_user` 函数：
   - 第 68 行 `must_change_password=False` 改为 `must_change_password=True`

2. 修改 `backend/app/services/user_service.py` 中的 `reset_user_password` 函数：
   - 第 107 行 `user.must_change_password = False` 改为 `user.must_change_password = True`

3. 修改 `backend/app/api/v1/users.py` 中的 `update_user_endpoint`，在调用 `update_user` 之前添加自我保护检查（per D-11）：
```python
# Self-protection: admin cannot disable self or change own role
user_record = get_user_by_id(db, user_id)
if user_record and user_record.username == current_user.username:
    if body.is_active is not None and body.is_active is False:
        raise HTTPException(status_code=403, detail="Cannot disable your own account.")
    if body.role is not None and body.role != user_record.role:
        raise HTTPException(status_code=403, detail="Cannot change your own role.")
```
需要在文件顶部添加 `from backend.app.services.user_service import get_user_by_id` 的导入（检查是否已导入）。

4. 修改 `backend/app/api/v1/users.py` 中的 `reset_password_endpoint`，添加自我保护检查：
```python
# Self-protection: admin cannot reset own password via this endpoint
user_record = get_user_by_id(db, user_id)
if user_record and user_record.username == current_user.username:
    raise HTTPException(status_code=403, detail="Cannot reset your own password. Use change-password instead.")
```

5. 在 `tests/test_users.py` 中：
   - 在 `TestResetPassword` 类中新增 `test_reset_sets_must_change_password` 测试：重置密码后 GET /users/{id} 查看 must_change_password=True
   - 在 `TestCreateUser` 类中新增 `test_create_user_sets_must_change_password` 测试：创建用户后返回数据中 must_change_password=True
   - 新增 `TestSelfProtection` 类，包含上述 4 个测试。需要获取当前 admin 的 user_id，可通过 `str(seed_test_admin.id)` 获取。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/backend && python -m pytest tests/test_users.py -k "test_reset_sets_must_change or test_create_user_sets_must_change or TestSelfProtection" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/user_service.py 的 create_user 函数中包含 `must_change_password=True`
    - backend/app/services/user_service.py 的 reset_user_password 函数中包含 `must_change_password = True`
    - backend/app/api/v1/users.py 包含 `"Cannot disable your own account."`
    - backend/app/api/v1/users.py 包含 `"Cannot change your own role."`
    - backend/app/api/v1/users.py 包含 `"Cannot reset your own password"`
    - tests/test_users.py 包含 `class TestSelfProtection:`
    - tests/test_users.py 包含 `test_reset_sets_must_change_password`
    - tests/test_users.py 包含 `test_create_user_sets_must_change_password`
    - `cd backend && python -m pytest tests/test_users.py -x -q` 退出码 0（全部测试通过，含新旧测试）
  </acceptance_criteria>
  <done>must_change_password 在创建用户和重置密码时均设为 True；管理员自我保护（禁用/改角色/重置密码）均返回 403；所有后端测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /auth/change-password | 已认证用户提交旧密码和新密码 |
| client → /users/{id} (PUT) | 管理员修改其他用户属性 |
| client → /users/{id}/password (PUT) | 管理员重置其他用户密码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | /auth/change-password | mitigate | 验证旧密码 via verify_password()，新密码 min_length=8 via Pydantic schema |
| T-16-02 | Denial of Service | /users/{id} PUT is_active | mitigate | D-11: 后端 403 禁止管理员禁用自己 + 禁止改自己角色 |
| T-16-03 | Elevation | /auth/change-password | accept | JWT 无法吊销，但 TTL 短（默认 30 分钟），改密不影响已发 token |
| T-16-04 | Spoofing | /auth/change-password | mitigate | require_authenticated_user 依赖确保只有已认证用户可调用 |
</threat_model>

<verification>
```bash
cd /Users/mac/PycharmProjects/5xyj/backend && python -m pytest tests/test_users.py tests/test_auth.py -x -q
```
全部测试通过，包括新增的 TestChangePassword、TestSelfProtection、以及扩展的 TestResetPassword 和 TestCreateUser。
</verification>

<success_criteria>
1. PUT /api/v1/auth/change-password 端点正常工作，验证旧密码后设置新密码
2. 改密成功后 must_change_password 清除为 False
3. 创建用户时 must_change_password 设为 True
4. 重置密码后 must_change_password 设为 True
5. 管理员不能禁用自己（403）、不能改自己角色（403）、不能重置自己密码（403）
6. 所有后端测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/16-account-management/16-01-SUMMARY.md`
</output>
