---
phase: 16-account-management
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - frontend/src/services/users.ts
  - frontend/src/services/auth.ts
  - frontend/src/pages/Users.tsx
  - frontend/src/pages/index.ts
  - frontend/src/components/ChangePasswordModal.tsx
  - frontend/src/layouts/MainLayout.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - ACCT-01
  - ACCT-02
  - ACCT-03
  - ACCT-04

must_haves:
  truths:
    - "管理员可在 /users 页面看到用户列表表格"
    - "管理员可通过弹窗创建新用户并分配 admin 或 hr 角色"
    - "管理员可通过弹窗编辑用户显示名和角色"
    - "管理员可通过行内 Switch 启用/禁用用户"
    - "管理员可通过 Popconfirm + 密码弹窗重置用户密码"
    - "管理员不能禁用自己或修改自己角色（UI 灰色禁用）"
    - "所有角色用户可通过 Header 下拉菜单修改自己密码"
    - "mustChangePassword=true 时弹出不可关闭的修改密码弹窗"
  artifacts:
    - path: "frontend/src/pages/Users.tsx"
      provides: "账号管理页面 Table + Modal CRUD"
      min_lines: 100
    - path: "frontend/src/services/users.ts"
      provides: "用户管理 API 客户端"
      exports: ["fetchUsers", "createUser", "updateUser", "resetUserPassword"]
    - path: "frontend/src/components/ChangePasswordModal.tsx"
      provides: "共用修改密码弹窗（强制/自主两种模式）"
      exports: ["ChangePasswordModal"]
    - path: "frontend/src/layouts/MainLayout.tsx"
      provides: "菜单增加账号管理 + Header 增加修改密码 + 强制改密拦截"
      contains: "账号管理"
  key_links:
    - from: "frontend/src/pages/Users.tsx"
      to: "frontend/src/services/users.ts"
      via: "API 调用"
      pattern: "fetchUsers|createUser|updateUser|resetUserPassword"
    - from: "frontend/src/layouts/MainLayout.tsx"
      to: "frontend/src/components/ChangePasswordModal.tsx"
      via: "组件引用"
      pattern: "ChangePasswordModal"
    - from: "frontend/src/components/ChangePasswordModal.tsx"
      to: "frontend/src/services/auth.ts"
      via: "changeOwnPassword API 调用"
      pattern: "changeOwnPassword"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Users.tsx"
      via: "路由注册"
      pattern: "/users"
---

<objective>
前端账号管理完整实现：用户管理页面 + 修改密码弹窗 + 强制改密拦截 + 路由和菜单注册。

Purpose: 让管理员可在前端界面完成用户 CRUD 操作（ACCT-01/02/03），让所有用户可自主修改密码（ACCT-04），并在 mustChangePassword=true 时强制拦截。

Output: 完整可用的前端账号管理功能，包含 Users 页面、ChangePasswordModal 共用组件、MainLayout 集成、路由注册。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-account-management/16-CONTEXT.md
@.planning/phases/16-account-management/16-RESEARCH.md
@.planning/phases/16-account-management/16-UI-SPEC.md
@.planning/phases/16-account-management/16-01-SUMMARY.md

<interfaces>
<!-- 后端 API 接口 — Plan 01 已完成，以下是前端需调用的完整 API 清单 -->

用户管理 API（admin-only, require_role("admin")）:
```
POST   /api/v1/users/                → 创建用户 { username, password, role, display_name } → 201 + UserRead
GET    /api/v1/users/                → 用户列表 → 200 + UserRead[]
GET    /api/v1/users/{id}            → 用户详情 → 200 + UserRead
PUT    /api/v1/users/{id}            → 更新用户 { username?, role?, display_name?, is_active? } → 200 + UserRead
PUT    /api/v1/users/{id}/password   → 重置密码 { new_password } → 200 + { message }
```

自改密码 API（任意已认证用户）:
```
PUT    /api/v1/auth/change-password  → 改密 { old_password, new_password } → 200 + { message }
```

UserRead 类型:
```typescript
interface UserRead {
  id: string;
  username: string;
  role: 'admin' | 'hr';
  display_name: string;
  is_active: boolean;
  must_change_password: boolean;
  created_at: string;  // ISO datetime
  updated_at: string;  // ISO datetime
}
```

<!-- 前端现有接口 — executor 必须了解 -->

From frontend/src/services/api.ts:
```typescript
export const apiClient: AxiosInstance;  // 带 baseURL=/api/v1, 自动注入 Authorization header
export interface ApiSuccessResponse<T> { status: string; data: T; message?: string; }
```

From frontend/src/services/authSession.ts:
```typescript
export type AuthRole = 'admin' | 'hr' | 'employee';
export interface AuthSession {
  accessToken: string; expiresAt: string; username: string;
  role: AuthRole; displayName: string; signedInAt: string;
  mustChangePassword?: boolean;
}
export function readAuthSession(): AuthSession | null;
export function writeAuthSession(session: AuthSession): void;
export function clearAuthSession(): void;
```

From frontend/src/hooks/authContext.ts:
```typescript
export interface AuthenticatedUser {
  username: string; role: AuthRole; displayName: string;
  mustChangePassword?: boolean;
}
```

From frontend/src/hooks/useAuth.ts:
```typescript
export function useAuth(): {
  session: AuthSession | null; user: AuthenticatedUser | null;
  isAuthenticated: boolean; isInitializing: boolean;
  login: (credentials) => Promise<void>;
  logout: () => void;
};
```

From frontend/src/services/auth.ts:
```typescript
export async function loginWithPassword(input: LoginCredentials): Promise<AuthSession>;
export async function fetchAuthenticatedUser(): Promise<AuthenticatedUserProfile>;
// changeOwnPassword 需要新增
```

From frontend/src/layouts/MainLayout.tsx:
```typescript
// MENU_GROUPS 数组中 group-admin 的 children 需要新增 { key: '/users', ... }
// userMenuItems 数组需要新增「修改密码」项
// MainLayout 组件需要渲染 ChangePasswordModal（强制模式）
// LABEL_MAP 需要新增 users: '账号管理'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建用户管理 API 客户端 + 账号管理页面</name>
  <files>frontend/src/services/users.ts, frontend/src/services/auth.ts, frontend/src/pages/Users.tsx</files>
  <read_first>
    - frontend/src/services/api.ts
    - frontend/src/services/auth.ts
    - frontend/src/services/authSession.ts
    - frontend/src/pages/Employees.tsx
    - frontend/src/hooks/useAuth.ts
    - frontend/src/hooks/authContext.ts
    - .planning/phases/16-account-management/16-UI-SPEC.md
    - .planning/phases/16-account-management/16-CONTEXT.md
  </read_first>
  <action>
**Step A: 创建 `frontend/src/services/users.ts`（新文件）**

参考 `frontend/src/services/employees.ts` 的模式创建用户管理 API 客户端：

```typescript
import { apiClient, type ApiSuccessResponse } from './api';

export interface UserItem {
  id: string;
  username: string;
  role: 'admin' | 'hr';
  display_name: string;
  is_active: boolean;
  must_change_password: boolean;
  created_at: string;
  updated_at: string;
}

export interface CreateUserInput {
  username: string;
  password: string;
  role: 'admin' | 'hr';
  display_name: string;
}

export interface UpdateUserInput {
  username?: string;
  role?: 'admin' | 'hr';
  display_name?: string;
  is_active?: boolean;
}

export async function fetchUsers(): Promise<UserItem[]> {
  const resp = await apiClient.get<ApiSuccessResponse<UserItem[]>>('/users/');
  return resp.data.data;
}

export async function createUser(input: CreateUserInput): Promise<UserItem> {
  const resp = await apiClient.post<ApiSuccessResponse<UserItem>>('/users/', input);
  return resp.data.data;
}

export async function updateUser(id: string, input: UpdateUserInput): Promise<UserItem> {
  const resp = await apiClient.put<ApiSuccessResponse<UserItem>>(`/users/${id}`, input);
  return resp.data.data;
}

export async function resetUserPassword(id: string, newPassword: string): Promise<void> {
  await apiClient.put(`/users/${id}/password`, { new_password: newPassword });
}
```

**Step B: 在 `frontend/src/services/auth.ts` 末尾新增 `changeOwnPassword` 函数：**

```typescript
export async function changeOwnPassword(oldPassword: string, newPassword: string): Promise<void> {
  await apiClient.put('/auth/change-password', {
    old_password: oldPassword,
    new_password: newPassword,
  });
}
```

**Step C: 创建 `frontend/src/pages/Users.tsx`（新文件，per D-01/D-02/D-09/D-10/D-11/D-12）**

使用 Ant Design Table + Modal CRUD 模式（参考 Employees.tsx）。完整实现包含：

1. **用户列表表格**（per D-12）：
   - 列: 用户名(username, 140px), 显示名(display_name, 140px), 角色(role, 100px, Tag color: admin=blue/hr=green), 状态(is_active, 80px, Switch), 创建时间(created_at, 160px, dayjs YYYY-MM-DD HH:mm), 操作(140px, 编辑+重置密码)
   - Table props: `size="small"`, `rowKey="id"`, `loading={loading}`, `pagination={{ pageSize: 20, hideOnSinglePage: true }}`
   - 页面标题区: 左侧 "账号管理" 文字（fontWeightStrong），右侧 Button type="primary" icon={UserAddOutlined} "创建用户"
   - 空状态: locale={{ emptyText: "暂无用户" }}

2. **Switch 行内状态切换**（per D-10, D-11）：
   - `disabled={record.id === currentUserId}` — 当前管理员不能禁用自己
   - `loading={togglingId === record.id}` — 切换时 loading 态
   - onChange 调用 `updateUser(record.id, { is_active: checked })`
   - 成功后 `message.success('用户信息已更新')`
   - currentUserId 从 `useAuth()` 获取：需要先 GET /users/ 匹配当前 username 找到 id，或在 state 中存储

3. **创建用户 Modal**（per D-09）：
   - title="创建用户", width={480}, okText="确定", cancelText="取消"
   - Form fields: 用户名(required, max 50), 显示名(required, max 50), 角色(Select: [{label:"管理员",value:"admin"},{label:"HR",value:"hr"}], required), 初始密码(Input.Password, required, min 8, prefix LockOutlined), 确认密码(Input.Password, required, must match)
   - 提交调用 createUser，成功后 message.success('用户创建成功')，刷新列表，关闭弹窗

4. **编辑用户 Modal**：
   - title="编辑用户", width={480}
   - 用户名 Input disabled（不可编辑）
   - 显示名 Input（required）
   - 角色 Select — `disabled={editingUser.id === currentUserId}`（per D-11: 管理员不能改自己角色）
   - 提交调用 updateUser，成功后 message.success('用户信息已更新')

5. **重置密码流程**（per UI-SPEC reset password 两步流程）：
   - 操作列 "重置密码" Button type="link" danger
   - 点击弹出 Popconfirm: title="确认重置该用户的密码？" description="重置后用户将被要求首次登录时修改密码。" okText="确认重置" okType="danger"
   - 确认后弹出一个小 Modal title="设置新密码" 包含一个 Input.Password（min 8 位）
   - 提交调用 resetUserPassword，成功后 message.success('密码已重置，用户下次登录需修改密码')

注意获取 currentUserId 的方式：由于 `useAuth()` 返回的 user 只有 username 没有 id，而 D-11 需要比较 record.id。方案：在加载用户列表后，根据 `user.username` 从列表中找到匹配的用户获取 id 存入 state。

export 方式: `export function UsersPage() { ... }` + 末尾 `export default UsersPage;`（与其他页面一致，如 SettingsPage）。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/services/users.ts 存在且包含 `export async function fetchUsers(`
    - frontend/src/services/users.ts 包含 `export async function createUser(`
    - frontend/src/services/users.ts 包含 `export async function updateUser(`
    - frontend/src/services/users.ts 包含 `export async function resetUserPassword(`
    - frontend/src/services/auth.ts 包含 `export async function changeOwnPassword(`
    - frontend/src/pages/Users.tsx 存在且包含 `export function UsersPage(`
    - frontend/src/pages/Users.tsx 包含 `Table` 和 `Modal`
    - frontend/src/pages/Users.tsx 包含 `Switch` 和 `disabled={`（D-11 前端保护）
    - frontend/src/pages/Users.tsx 包含 `Popconfirm`（重置密码确认）
    - frontend/src/pages/Users.tsx 包含 `color="blue"` 和 `color="green"`（角色 Tag 颜色）
    - `cd frontend && npx tsc --noEmit` 无错误退出
  </acceptance_criteria>
  <done>Users.tsx 页面完整实现 Table + Modal CRUD（创建/编辑/重置密码/状态切换），users.ts API 客户端完整，auth.ts 新增 changeOwnPassword</done>
</task>

<task type="auto">
  <name>Task 2: ChangePasswordModal 组件 + MainLayout 集成 + 路由注册</name>
  <files>frontend/src/components/ChangePasswordModal.tsx, frontend/src/layouts/MainLayout.tsx, frontend/src/App.tsx, frontend/src/pages/index.ts</files>
  <read_first>
    - frontend/src/layouts/MainLayout.tsx
    - frontend/src/App.tsx
    - frontend/src/pages/index.ts
    - frontend/src/components/AuthProvider.tsx
    - frontend/src/services/auth.ts
    - frontend/src/services/authSession.ts
    - frontend/src/hooks/authContext.ts
    - frontend/src/hooks/useAuth.ts
    - .planning/phases/16-account-management/16-UI-SPEC.md
    - .planning/phases/16-account-management/16-CONTEXT.md
  </read_first>
  <action>
**Step A: 创建 `frontend/src/components/ChangePasswordModal.tsx`（新文件，per D-04/D-05/D-06/D-07）**

共用修改密码弹窗，支持两种模式：

```typescript
import { useState } from 'react';
import { Modal, Form, Input, message, Alert } from 'antd';
import { LockOutlined } from '@ant-design/icons';
import { changeOwnPassword } from '../services/auth';
import { readAuthSession, writeAuthSession } from '../services/authSession';

interface ChangePasswordModalProps {
  open: boolean;
  forced?: boolean;  // true = 不可关闭（mustChangePassword 模式）
  onSuccess: () => void;
  onCancel?: () => void;
}

export function ChangePasswordModal({ open, forced = false, onSuccess, onCancel }: ChangePasswordModalProps) {
  // ...
}
```

核心逻辑：
1. Modal title="修改密码", width={420}
2. **强制模式**（forced=true, per D-05）：closable={false}, maskClosable={false}, keyboard={false}, footer 只有提交按钮没有取消
3. **自主模式**（forced=false）：closable={true}, 正常 footer（取消+确定）
4. Form fields（per D-06）：
   - 当前密码 (old_password): Input.Password, required, prefix={LockOutlined}, label="当前密码"
   - 新密码 (new_password): Input.Password, required, min 8, prefix={LockOutlined}, label="新密码"
   - 确认新密码 (confirm_password): Input.Password, required, 必须与新密码一致, label="确认新密码"
5. 提交逻辑：
   - 调用 `changeOwnPassword(old_password, new_password)`
   - 成功后：更新 session `mustChangePassword = false`
     ```typescript
     const session = readAuthSession();
     if (session) {
       writeAuthSession({ ...session, mustChangePassword: false });
     }
     ```
   - message.success('密码修改成功')
   - 调用 onSuccess()
   - form.resetFields()
6. 错误处理：
   - 400 错误（旧密码错误）：form.setFields([{ name: 'old_password', errors: ['当前密码错误，请重新输入'] }])
   - 其他错误：message.error(...)
7. 强制模式下在 Form 上方显示 Alert: type="info" message="您的密码需要修改，请设置新密码后继续使用系统"
8. destroyOnClose={true}

**Step B: 修改 `frontend/src/layouts/MainLayout.tsx`**

1. **在 MENU_GROUPS 的 group-admin children 数组中新增账号管理项（per D-01）：**
   在 `{ key: '/employees', ... }` 之前或之后（放在管理组第一个位置）添加：
   ```typescript
   { key: '/users', icon: <TeamOutlined />, label: '账号管理', roles: ['admin'] },
   ```
   注意：使用 `TeamOutlined` 图标（已导入），roles 仅 `['admin']`（admin-only）。

2. **在 LABEL_MAP 中新增条目：**
   ```typescript
   users: '账号管理',
   ```

3. **在 userMenuItems 数组中新增「修改密码」项（per D-04）：**
   在 `'logout'` 项之前插入：
   ```typescript
   {
     key: 'change-password',
     icon: <KeyOutlined />,
     label: '修改密码',
     onClick: () => setChangePasswordOpen(true),
   },
   { type: 'divider' as const },
   ```
   KeyOutlined 图标已导入。

4. **新增 ChangePasswordModal 状态和渲染（per D-04 + D-05）：**
   在 MainLayout 组件内部添加 state:
   ```typescript
   const [changePasswordOpen, setChangePasswordOpen] = useState(false);
   ```

   在 Content 组件之后（Layout 闭合标签之前）渲染两个 ChangePasswordModal：
   ```tsx
   {/* 自主修改密码（Header 下拉菜单触发） */}
   <ChangePasswordModal
     open={changePasswordOpen}
     forced={false}
     onSuccess={() => setChangePasswordOpen(false)}
     onCancel={() => setChangePasswordOpen(false)}
   />
   {/* 强制修改密码（mustChangePassword=true 时自动弹出） */}
   {user?.mustChangePassword && (
     <ChangePasswordModal
       open={true}
       forced={true}
       onSuccess={() => {
         // session 已在 Modal 内部更新，这里触发 re-render
         // AuthProvider 会通过 AUTH_SESSION_EVENT 自动更新 user
       }}
     />
   )}
   ```

5. **新增 import：**
   ```typescript
   import { ChangePasswordModal } from '../components/ChangePasswordModal';
   ```

**Step C: 修改 `frontend/src/App.tsx`（per D-01）**

1. 在 import 列表中从 pages 导入 UsersPage：
   ```typescript
   import { ..., UsersPage } from './pages';
   ```
   确保 `UsersPage` 被正确解构。

2. 在 admin-only 路由区域（`<Route element={<RoleRoute allowedRoles={['admin']} />}>` 内部）新增：
   ```tsx
   <Route path="/users" element={<UsersPage />} />
   ```

**Step D: 修改 `frontend/src/pages/index.ts`**

新增导出：
```typescript
export { default as UsersPage } from './Users';
```
或如果 Users.tsx 使用 named export:
```typescript
export * from './Users';
```
取决于 Task 1 中 Users.tsx 的 export 方式。如果 Users.tsx 同时有 `export function UsersPage` 和 `export default UsersPage`，则使用 `export { default as UsersPage } from './Users';` 模式与其他页面一致（如 SettingsPage）。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ChangePasswordModal.tsx 存在且包含 `export function ChangePasswordModal(`
    - frontend/src/components/ChangePasswordModal.tsx 包含 `closable={false}` 和 `maskClosable={false}`（强制模式）
    - frontend/src/components/ChangePasswordModal.tsx 包含 `changeOwnPassword`
    - frontend/src/components/ChangePasswordModal.tsx 包含 `writeAuthSession`（更新 session）
    - frontend/src/layouts/MainLayout.tsx 包含 `key: '/users'` 和 `label: '账号管理'`
    - frontend/src/layouts/MainLayout.tsx 包含 `key: 'change-password'` 和 `label: '修改密码'`
    - frontend/src/layouts/MainLayout.tsx 包含 `ChangePasswordModal`
    - frontend/src/layouts/MainLayout.tsx 包含 `mustChangePassword`
    - frontend/src/App.tsx 包含 `path="/users"` 和 `UsersPage`
    - frontend/src/pages/index.ts 包含 `UsersPage`
    - `cd frontend && npm run lint` 退出码 0
    - `cd frontend && npm run build` 退出码 0
  </acceptance_criteria>
  <done>ChangePasswordModal 共用组件完成（支持强制/自主模式），MainLayout 集成菜单+Header+强制改密，/users 路由注册完成，所有 lint/build 通过</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 验证账号管理完整功能</name>
  <files>N/A</files>
  <action>
人工验证前端账号管理完整功能。启动前后端后按以下步骤验证：
1. 管理员账号管理页面（ACCT-01/02）：admin 登录后左侧菜单「管理」分组下可见「账号管理」，点击进入 /users，创建/编辑用户
2. 管理员自我保护（D-11）：当前管理员行 Switch 灰色禁用，编辑自己时角色下拉灰色禁用
3. 重置密码（ACCT-03）：Popconfirm + 输入新密码
4. 用户自改密码（ACCT-04）：Header 下拉菜单「修改密码」弹窗
5. 强制改密（D-05）：重置密码后该用户登录弹出不可关闭 Modal
6. 状态切换（D-10）：禁用用户后该用户无法登录
  </action>
  <verify>人工验证 — 按上述 6 个场景逐一测试</verify>
  <done>所有 6 个验证场景通过，用户输入 approved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v1/users/* | 前端通过 apiClient 访问管理员 API |
| browser → /api/v1/auth/change-password | 前端通过 apiClient 访问自改密码 API |
| MainLayout render → user.mustChangePassword | 强制改密弹窗的触发条件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Denial of Service | Users.tsx Switch | mitigate | D-11: `disabled={record.id === currentUserId}` 禁止管理员禁用自己 + 后端 403 双保险（Plan 01 已实现） |
| T-16-06 | Tampering | ChangePasswordModal | mitigate | 旧密码必须验证，AntD Form rules 前端校验 + 后端 Pydantic 验证 |
| T-16-07 | Spoofing | /users route | mitigate | RoleRoute allowedRoles={['admin']} 限制非管理员访问 + 后端 require_role("admin") |
| T-16-08 | Tampering | display_name input | mitigate | AntD 自动 HTML 转义 + 后端 max_length=255 限制 + Pydantic 验证 |
| T-16-09 | Elevation | 强制改密绕过 | mitigate | MainLayout 级别持续检查 user.mustChangePassword，closable=false + maskClosable=false + keyboard=false，刷新页面后 AuthProvider 重新读取 session 仍会触发 |
</threat_model>

<verification>
```bash
# 前端编译检查
cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint && npm run build

# 后端测试（确保 Plan 01 的测试仍然通过）
cd /Users/mac/PycharmProjects/5xyj/backend && python -m pytest tests/test_users.py tests/test_auth.py -x -q
```
</verification>

<success_criteria>
1. 管理员可在 /users 页面查看用户列表（Table 格式，包含 D-12 所有列）
2. 管理员可通过 Modal 创建新用户（admin/hr 角色可选）
3. 管理员可通过 Modal 编辑用户显示名和角色
4. 管理员可通过行内 Switch 启用/禁用用户
5. 管理员可通过 Popconfirm + Modal 重置用户密码
6. 管理员不能禁用自己或修改自己角色（Switch/Select 灰色禁用）
7. 所有角色用户可通过 Header 下拉菜单修改自己密码
8. mustChangePassword=true 时弹出不可关闭的修改密码弹窗
9. 修改密码成功后弹窗消失，session 更新
10. npm run lint + npm run build 通过
</success_criteria>

<output>
After completion, create `.planning/phases/16-account-management/16-02-SUMMARY.md`
</output>
