---
phase: 01-deployment-security-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/bootstrap.py
  - backend/app/main.py
  - backend/tests/test_runtime_config.py
  - backend/tests/test_app_initialization.py
  - backend/tests/test_auth_api.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "A non-local runtime refuses startup when the admin or HR password is still the shipped default."
    - "A non-local runtime refuses startup when the auth signing secret is still the shipped default or another explicitly blocked unsafe value."
    - "Local development remains bootable with the current defaults so agent and developer workflows still work."
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "Explicit runtime-environment setting plus unsafe-auth default metadata/helpers"
      contains: "runtime_environment"
    - path: "backend/app/bootstrap.py"
      provides: "Fail-fast startup validation for auth guardrails"
      contains: "validate_auth_runtime_guardrails"
    - path: "backend/tests/test_runtime_config.py"
      provides: "Regression tests for non-local rejection and local allowance"
      contains: "test_non_local"
  key_links:
    - from: "backend/app/bootstrap.py"
      to: "backend/app/core/config.py"
      via: "bootstrap_application calls the auth guardrail validator before returning runtime settings"
      pattern: "validate_auth_runtime_guardrails"
    - from: "backend/app/main.py"
      to: "backend/app/bootstrap.py"
      via: "lifespan startup path executes bootstrap before serving requests"
      pattern: "bootstrap_application"
    - from: "backend/tests/test_app_initialization.py"
      to: "backend/app/main.py"
      via: "startup tests prove guarded settings fail before the app is usable"
      pattern: "create_app"
---

<objective>
Enforce fail-fast startup/runtime auth guardrails for non-local environments without changing the current role model or breaking local development.

Purpose: Close the production-dangerous gap where default passwords and a predictable signing secret still allow authenticated startup outside local use.
Output: A bootstrap-enforced non-local auth guardrail contract plus automated tests proving both rejection and allowed local behavior.
</objective>

<execution_context>
@C:/Users/AAON/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AAON/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-deployment-security-guardrails/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@backend/app/core/config.py
@backend/app/bootstrap.py
@backend/app/main.py
@backend/app/core/auth.py
@backend/app/dependencies.py
@backend/tests/test_runtime_config.py
@backend/tests/test_app_initialization.py
@backend/tests/test_auth_api.py

<interfaces>
From `backend/app/core/config.py`:
```python
class Settings(BaseSettings):
    auth_enabled: bool = True
    auth_secret_key: str = 'change-this-auth-secret'
    admin_login_username: str = 'admin'
    admin_login_password: str = 'admin123'
    hr_login_username: str = 'hr'
    hr_login_password: str = 'hr123'
```

From `backend/app/bootstrap.py`:
```python
def bootstrap_application(settings: Optional[Settings] = None) -> Settings:
    runtime_settings = settings or get_settings()
    configure_logging(runtime_settings)
    ensure_runtime_directories(runtime_settings)
    return runtime_settings
```

From `backend/app/main.py`:
```python
def create_lifespan(runtime_settings: Settings):
    @asynccontextmanager
    async def lifespan(app: FastAPI) -> AsyncIterator[None]:
        settings = bootstrap_application(runtime_settings)
        app.state.settings = settings
        yield
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit non-local auth guardrail validation to runtime startup</name>
  <read_first>
    - backend/app/core/config.py
    - backend/app/bootstrap.py
    - backend/app/main.py
    - backend/app/dependencies.py
    - .planning/phases/01-deployment-security-guardrails/01-CONTEXT.md
  </read_first>
  <files>backend/app/core/config.py, backend/app/bootstrap.py, backend/app/main.py</files>
  <behavior>
    - Test 1: D-locked non-local runtime rejects startup when `admin_login_password` or `hr_login_password` remains the shipped default (`SEC-01`).
    - Test 2: D-locked non-local runtime rejects startup when `auth_secret_key` remains the shipped default or another explicitly blocked unsafe placeholder (`SEC-02`).
    - Test 3: Explicit local-development runtime still boots with defaults so current dev and agent loops remain usable.
  </behavior>
  <action>Per the locked phase decisions, keep the existing `admin`/`hr` auth model intact and keep local development usable. Add an explicit, testable runtime-environment setting in `backend/app/core/config.py` such as `runtime_environment` with a local default, plus a small validator/helper that knows the shipped default admin password, HR password, and auth signing secret. Anchor the guardrail in startup by calling a dedicated validation function from `bootstrap_application()` before the app becomes usable; the validator must raise a deterministic, actionable exception when `auth_enabled` is true and the runtime is non-local while any unsafe default remains. Keep the change scoped to startup/runtime validation only: do not redesign token format, roles, route protection, or frontend auth behavior.</action>
  <acceptance_criteria>
    - `rg -n "runtime_environment" backend/app/core/config.py`
    - `rg -n "validate_auth_runtime_guardrails|ensure_secure_auth_configuration|unsafe auth" backend/app/core/config.py backend/app/bootstrap.py backend/app/main.py`
    - `rg -n "admin123|hr123|change-this-auth-secret" backend/app/core/config.py`
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_runtime_config.py backend/tests/test_app_initialization.py -x</automated>
  </verify>
  <done>Bootstrap fails before serving requests in non-local mode when default passwords or signing secret remain, while local mode still initializes successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests that prove guarded startup and preserved local auth behavior</name>
  <read_first>
    - backend/tests/test_runtime_config.py
    - backend/tests/test_app_initialization.py
    - backend/tests/test_auth_api.py
    - backend/app/core/auth.py
    - backend/app/main.py
  </read_first>
  <files>backend/tests/test_runtime_config.py, backend/tests/test_app_initialization.py, backend/tests/test_auth_api.py</files>
  <behavior>
    - Test 1: A non-local runtime with default admin password fails loudly during bootstrap/startup.
    - Test 2: A non-local runtime with default HR password fails loudly during bootstrap/startup.
    - Test 3: A non-local runtime with default signing secret fails loudly during bootstrap/startup.
    - Test 4: Local runtime with safe or existing test credentials can still issue and verify bearer tokens through the current auth API.
  </behavior>
  <action>Extend the existing backend tests so they prove the real startup path rather than only unit-testing helpers. In `backend/tests/test_runtime_config.py`, add focused cases for local allowance and non-local rejection of default credentials/secrets. In `backend/tests/test_app_initialization.py`, add startup-level assertions that guarded settings fail before `TestClient` can use the app. In `backend/tests/test_auth_api.py`, keep one explicit successful login/token flow with non-default credentials so this phase proves the guardrails did not regress normal authenticated behavior. Keep the test surface tight to this phase; do not expand into upload hardening or broader auth redesign.</action>
  <acceptance_criteria>
    - `rg -n "test_.*default_admin.*non_local|test_.*default_hr.*non_local|test_.*default.*secret.*non_local" backend/tests/test_runtime_config.py backend/tests/test_app_initialization.py`
    - `rg -n "test_.*local.*(allows|boots|startup)" backend/tests/test_runtime_config.py backend/tests/test_app_initialization.py`
    - `rg -n "test_login_endpoint_returns_bearer_token_for_admin|test_me_endpoint_returns_authenticated_user_profile" backend/tests/test_auth_api.py`
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_runtime_config.py backend/tests/test_app_initialization.py backend/tests/test_auth_api.py -x</automated>
  </verify>
  <done>The test suite proves both requirements end to end: unsafe non-local auth settings fail fast, and the supported local/test login flow still works with explicit safe credentials.</done>
</task>

</tasks>

<verification>
Run `pytest backend/tests/test_runtime_config.py backend/tests/test_app_initialization.py backend/tests/test_auth_api.py -x` and confirm the new non-local rejection cases fail before serving requests while the existing auth API success path still passes.
</verification>

<success_criteria>
- Non-local startup rejects default admin and HR passwords.
- Non-local startup rejects a predictable default signing secret.
- Local development remains usable without changing the current auth model.
</success_criteria>

<output>
After completion, create `.planning/phases/01-deployment-security-guardrails/01-01-SUMMARY.md`
</output>
