---
phase: 03-security-hardening
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/pages/AuditLogs.tsx
  - frontend/src/pages/index.ts
  - frontend/src/App.tsx
autonomous: false
requirements: [SEC-03]

must_haves:
  truths:
    - "管理员在前端界面可查看审计日志列表"
    - "审计日志可按操作类型筛选"
    - "审计日志可按时间范围筛选"
    - "审计日志按时间倒序显示（最新在前）"
    - "审计日志服务端分页显示"
    - "非管理员无法访问审计日志页面"
  artifacts:
    - path: "frontend/src/pages/AuditLogs.tsx"
      provides: "审计日志管理页面"
      contains: "AuditLogsPage"
      min_lines: 80
  key_links:
    - from: "frontend/src/pages/AuditLogs.tsx"
      to: "/api/v1/audit-logs"
      via: "fetch API 调用"
      pattern: "audit-logs"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/AuditLogs.tsx"
      via: "Route 定义"
      pattern: "audit"
---

<objective>
创建前端审计日志查看页面，管理员可在系统界面查看、筛选和分页浏览审计日志。然后通过具体的验收矩阵人工验证 Phase 3 全部安全功能。

Purpose: 满足 D-06 要求"管理员可在系统界面查看审计日志，支持按时间和操作类型筛选"，并通过结构化验收矩阵验证 Phase 3 全部交付物。
Output: AuditLogs 页面组件 + 结构化验收结果。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-security-hardening/03-CONTEXT.md
@.planning/phases/03-security-hardening/03-01-SUMMARY.md

@frontend/src/App.tsx
@frontend/src/pages/index.ts
@frontend/src/pages/Workspace.tsx
@frontend/src/components/AuthProvider.tsx

<interfaces>
<!-- Plan 01 创建的审计日志 API 接口 -->

GET /api/v1/audit-logs 端点参数：
- action: string (可选) — 操作类型筛选（login, login_failed, export, import, aggregate, user_create, user_update, user_disable, employee_verify, employee_verify_failed）
- start_time: string (可选) — ISO 格式起始时间
- end_time: string (可选) — ISO 格式结束时间
- page: int (默认1) — 页码
- page_size: int (默认20, 最大100) — 每页条数

响应格式（包在 success_response 信封内）：
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "uuid",
        "action": "login",
        "actor_username": "admin",
        "actor_role": "admin",
        "ip_address": "127.0.0.1",
        "detail": "{\"method\": \"password\"}",
        "resource_type": null,
        "resource_id": null,
        "success": true,
        "created_at": "2026-03-28T14:00:00Z"
      }
    ],
    "total": 100,
    "page": 1,
    "page_size": 20
  }
}
```

前端路由权限模式（from App.tsx）：
```tsx
<RoleRoute allowedRoles={['admin']}>
  <Route path="/users" element={<UsersPage />} />
  <!-- 审计日志路由添加在此处 -->
</RoleRoute>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建审计日志前端页面并注册路由</name>
  <files>
    frontend/src/pages/AuditLogs.tsx,
    frontend/src/pages/index.ts,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/App.tsx,
    frontend/src/pages/index.ts,
    frontend/src/pages/Workspace.tsx,
    frontend/src/components/AuthProvider.tsx,
    frontend/src/components/PageContainer.tsx
  </read_first>
  <action>
    1. 创建 `frontend/src/pages/AuditLogs.tsx`：
       - 导出 `AuditLogsPage` 函数组件
       - 使用与项目现有页面一致的样式和结构（参考 Workspace.tsx 的 PageContainer 包装模式）
       - 页面标题：`审计日志`
       - 顶部筛选区域（flex 布局，一行排列）：
         - 操作类型 `<select>` 下拉选择，选项列表：
           - `""` 显示 "全部操作"
           - `"login"` 显示 "登录成功"
           - `"login_failed"` 显示 "登录失败"
           - `"export"` 显示 "数据导出"
           - `"import"` 显示 "数据导入"
           - `"aggregate"` 显示 "数据融合"
           - `"user_create"` 显示 "创建用户"
           - `"user_update"` 显示 "编辑用户"
           - `"user_disable"` 显示 "禁用用户"
           - `"employee_verify"` 显示 "员工验证成功"
           - `"employee_verify_failed"` 显示 "员工验证失败"
         - 起始日期 `<input type="date">`
         - 结束日期 `<input type="date">`
         - `<button>` 查询按钮
       - 表格 `<table>` 列定义：
         - 时间（created_at 格式化为 `YYYY-MM-DD HH:mm:ss`，使用 `new Date(created_at).toLocaleString('zh-CN')`）
         - 操作类型（action 值映射为上述中文标签）
         - 操作人（actor_username）
         - 角色（actor_role 映射：admin=管理员, hr=HR, employee=员工, unknown=未知）
         - IP 地址（ip_address，null 显示 "-"）
         - 结果（success 为 true 显示绿色 "成功"，false 显示红色 "失败"）
         - 详情（detail 为 JSON 字符串，截取前50字符显示，鼠标 hover 用 title 属性显示完整内容）
       - 分页控件（表格下方）：
         - 显示 `共 {total} 条记录`
         - 上一页 / 下一页按钮（disabled 状态在首页/末页）
         - 当前页码 / 总页数显示
         - 服务端分页：page 和 page_size 作为查询参数传递给 API
       - API 调用：
         ```typescript
         const token = localStorage.getItem('token');
         const params = new URLSearchParams();
         if (action) params.set('action', action);
         if (startTime) params.set('start_time', startTime + 'T00:00:00');
         if (endTime) params.set('end_time', endTime + 'T23:59:59');
         params.set('page', String(page));
         params.set('page_size', '20');

         fetch(`/api/v1/audit-logs?${params}`, {
           headers: { 'Authorization': `Bearer ${token}` }
         })
         ```
       - 加载状态：数据获取中显示 "加载中..."
       - 错误状态：请求失败显示错误信息
       - 空状态：无记录时显示 "暂无审计日志"
       - 默认排序：created_at 降序（API 层已处理，前端不需要额外排序）

    2. 在 `frontend/src/pages/index.ts` 中添加：
       ```typescript
       export { AuditLogsPage } from './AuditLogs';
       ```
       或根据现有文件的 export 风格统一（可能是 `export *` 或命名导出）。

    3. 在 `frontend/src/App.tsx` 中：
       - 添加 import：`import { AuditLogsPage } from './pages';`（或从 `'./pages/AuditLogs'`）
       - 在 admin-only 的 `<RoleRoute allowedRoles={['admin']}>` 块内添加路由：
         ```tsx
         <Route path="/audit-logs" element={<AuditLogsPage />} />
         ```
       - 确保 audit-logs 路由在 ProtectedRoute 和 admin RoleRoute 内部

    4. 如果侧边栏/导航菜单组件中有管理员菜单项列表，添加审计日志入口：
       - 查看 Sidebar/Navigation 组件中 admin 菜单的定义方式
       - 添加 `{ label: '审计日志', path: '/audit-logs' }` 或等效项
  </action>
  <verify>
    <automated>cd D:/execl_mix/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/AuditLogs.tsx 文件存在且包含 `export` 和 `AuditLogsPage`
    - frontend/src/pages/AuditLogs.tsx 包含 `audit-logs`（API 调用）
    - frontend/src/pages/AuditLogs.tsx 包含 `全部操作`（中文标签）
    - frontend/src/pages/AuditLogs.tsx 包含分页相关代码（`page` 参数）
    - frontend/src/pages/index.ts 包含 `AuditLogs`
    - frontend/src/App.tsx 包含 `AuditLogsPage` 和 `audit-logs` 路由路径
    - frontend/src/App.tsx 中 audit-logs 路由在 admin RoleRoute 块内
    - TypeScript 编译无错误（`npx tsc --noEmit` 退出码 0）
  </acceptance_criteria>
  <done>
    管理员可在 /audit-logs 页面查看审计日志，支持按操作类型和时间范围筛选，服务端分页浏览，中文显示操作类型和角色，TypeScript 编译通过。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 3 安全加固结构化验收</name>
  <files>N/A</files>
  <action>
    人工验证 Phase 3 安全加固全部功能。按照以下验收矩阵逐项确认。

    **启动环境：**
    1. 启动后端：`cd D:/execl_mix && uvicorn backend.app.main:app --reload`
    2. 启动前端：`cd D:/execl_mix/frontend && npm run dev`

    **验收矩阵（逐项确认 PASS/FAIL）：**

    | # | 需求 | 验收步骤 | 预期结果 |
    |---|------|---------|---------|
    | 1 | SEC-01: PII 端点认证 | 浏览器无痕模式直接访问 http://localhost:8000/api/v1/employees（不带 token） | 返回 401 或 403 |
    | 2 | SEC-01: PII 端点认证 | 浏览器无痕模式直接访问 http://localhost:8000/api/v1/audit-logs | 返回 401 或 403 |
    | 3 | SEC-02: 登录限流 | 连续用错误密码登录同一用户名5次 | 前5次返回 401 |
    | 4 | SEC-02: 登录限流 | 第6次用错误密码登录同一用户名 | 返回 429 |
    | 5 | SEC-03: 审计日志记录 | 用管理员正确密码登录 | 登录成功 |
    | 6 | SEC-03: 审计日志查看 | 登录后访问 /audit-logs 页面 | 看到登录失败和成功记录 |
    | 7 | SEC-03: 审计日志筛选 | 选择操作类型"登录失败"点击查询 | 只显示 login_failed 记录 |
    | 8 | SEC-03: 审计日志分页 | 如果记录超过20条，点击下一页 | 显示下一页数据 |
    | 9 | SEC-03: 审计日志只读 | 在 audit-logs 页面确认无删除/编辑按钮 | 只有查看和筛选功能 |
    | 10 | SEC-04: 管理员完整 ID | 管理员登录后访问员工列表页 | 身份证号完整显示 |
    | 11 | CORS 修复 | grep "allow_origins" backend/app/main.py | 显示 runtime_settings.backend_cors_origins |
    | 12 | 自动化测试 | cd D:/execl_mix && python -m pytest tests/ -v | 全部通过 |

    **判定标准：** 12项全部 PASS 即验收通过。
  </action>
  <verify>人工确认验收矩阵12项全部 PASS</verify>
  <done>用户确认 Phase 3 安全加固全部功能正常工作，验收矩阵12项全部 PASS</done>
</task>

</tasks>

<verification>
全部任务完成后验证：
1. 管理员可在 /audit-logs 看到审计日志，按时间倒序显示
2. 筛选功能正常（按操作类型、时间范围）
3. 服务端分页功能正常
4. 非管理员无法访问审计日志页面（前端路由守卫 + 后端 403）
5. TypeScript 编译通过
6. 验收矩阵12项全部 PASS
</verification>

<success_criteria>
- D-06: 管理员可在系统界面查看审计日志，支持按时间和操作类型筛选
- 审计日志页面仅管理员可访问（前端 RoleRoute + 后端 require_role）
- 页面显示中文操作类型和角色名称
- 服务端分页正常工作
- Phase 3 全部安全需求通过结构化验收矩阵验证
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-hardening/03-02-SUMMARY.md`
</output>
