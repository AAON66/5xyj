---
phase: 03-security-hardening
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/pages/AuditLogs.tsx
  - frontend/src/pages/index.ts
  - frontend/src/App.tsx
autonomous: false
requirements: [SEC-03]

must_haves:
  truths:
    - "管理员在前端界面可查看审计日志列表"
    - "审计日志可按操作类型筛选"
    - "审计日志可按时间范围筛选"
    - "审计日志分页显示"
  artifacts:
    - path: "frontend/src/pages/AuditLogs.tsx"
      provides: "审计日志管理页面"
      contains: "AuditLogsPage"
      min_lines: 50
  key_links:
    - from: "frontend/src/pages/AuditLogs.tsx"
      to: "/api/v1/audit-logs"
      via: "fetch API 调用"
      pattern: "audit-logs"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/AuditLogs.tsx"
      via: "Route 定义"
      pattern: "audit"
---

<objective>
创建前端审计日志查看页面，管理员可在系统界面查看、筛选和分页浏览审计日志。

Purpose: 满足 D-06 要求"管理员可在系统界面查看审计日志，支持按时间和操作类型筛选"。
Output: AuditLogs 页面组件，集成到管理员路由中。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-security-hardening/03-CONTEXT.md
@.planning/phases/03-security-hardening/03-01-SUMMARY.md

@frontend/src/App.tsx
@frontend/src/pages/index.ts
@frontend/src/pages/Workspace.tsx
@frontend/src/components/AuthProvider.tsx

<interfaces>
<!-- Plan 01 创建的审计日志 API 接口 -->

GET /api/v1/audit-logs 端点参数：
- action: string (可选) — 操作类型筛选（login, login_failed, export, import, aggregate, user_create, user_update, user_disable, employee_verify, employee_verify_failed）
- start_time: string (可选) — ISO 格式起始时间
- end_time: string (可选) — ISO 格式结束时间
- page: int (默认1) — 页码
- page_size: int (默认20, 最大100) — 每页条数

响应格式（包在 success_response 信封内）：
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "uuid",
        "action": "login",
        "actor_username": "admin",
        "actor_role": "admin",
        "ip_address": "127.0.0.1",
        "detail": "{\"method\": \"password\"}",
        "resource_type": null,
        "resource_id": null,
        "success": true,
        "created_at": "2026-03-28T14:00:00Z"
      }
    ],
    "total": 100,
    "page": 1,
    "page_size": 20
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建审计日志前端页面并注册路由</name>
  <files>
    frontend/src/pages/AuditLogs.tsx,
    frontend/src/pages/index.ts,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/App.tsx,
    frontend/src/pages/index.ts,
    frontend/src/pages/Workspace.tsx,
    frontend/src/components/AuthProvider.tsx,
    frontend/src/components/PageContainer.tsx
  </read_first>
  <action>
    1. 创建 `frontend/src/pages/AuditLogs.tsx`：
       - 导出 `AuditLogsPage` 组件
       - 使用与项目现有页面一致的样式和模式（参考 Workspace.tsx 或 Imports.tsx 的结构）
       - 页面标题：`审计日志`
       - 顶部筛选区域：
         - 操作类型下拉选择：所有 / login / login_failed / export / import / aggregate / user_create / user_update / user_disable / employee_verify / employee_verify_failed
         - 操作类型的中文显示映射：login=登录成功, login_failed=登录失败, export=数据导出, import=数据导入, aggregate=数据融合, user_create=创建用户, user_update=编辑用户, user_disable=禁用用户, employee_verify=员工验证成功, employee_verify_failed=员工验证失败
         - 时间范围：起始日期 + 结束日期（使用 input type="date"）
         - 查询按钮
       - 表格列：
         - 时间（created_at，格式化为 YYYY-MM-DD HH:mm:ss）
         - 操作类型（action，显示中文映射）
         - 操作人（actor_username）
         - 角色（actor_role，中文映射：admin=管理员, hr=HR, employee=员工, unknown=未知）
         - IP 地址（ip_address）
         - 结果（success ? "成功" : "失败"，成功用绿色，失败用红色）
         - 详情（detail，JSON 字符串截断显示前50字符，hover 显示完整内容）
       - 分页控件：显示总数、当前页、上一页/下一页按钮
       - API 调用使用 fetch，URL 为 `/api/v1/audit-logs`，携带 Authorization header（从 AuthProvider 或 localStorage 获取 token）
       - 加载状态和错误处理

    2. 在 `frontend/src/pages/index.ts` 中添加：
       ```typescript
       export * from './AuditLogs';
       ```

    3. 在 `frontend/src/App.tsx` 中：
       - 在 imports 中添加 `AuditLogsPage`
       - 在 admin-only 的 `<RoleRoute allowedRoles={['admin']}>` 块内添加：
         ```tsx
         <Route path="/audit-logs" element={<AuditLogsPage />} />
         ```
       - 确保审计日志路由在 ProtectedRoute 和 admin RoleRoute 内部（仅管理员可访问）
  </action>
  <verify>
    <automated>cd D:/execl_mix/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/AuditLogs.tsx 包含 `export` 和 `AuditLogsPage`
    - frontend/src/pages/AuditLogs.tsx 包含 `audit-logs` (API 调用)
    - frontend/src/pages/index.ts 包含 `AuditLogs`
    - frontend/src/App.tsx 包含 `AuditLogsPage` 和 `audit-logs` 路由
    - frontend/src/App.tsx 中 audit-logs 路由在 `RoleRoute allowedRoles={['admin']}` 块内
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>
    管理员可在 /audit-logs 页面查看审计日志，支持按操作类型和时间范围筛选，分页浏览，TypeScript 编译通过。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证 Phase 3 安全加固全部功能</name>
  <files>N/A</files>
  <action>
    人工验证完成 Phase 3 安全加固全部功能：后端审计日志系统、身份证号脱敏、登录频率限制、CORS 配置修复、前端审计日志页面。
    验证步骤：
    1. 启动后端：cd D:/execl_mix 然后 uvicorn backend.app.main:app --reload
    2. 启动前端：cd D:/execl_mix/frontend 然后 npm run dev
    3. 测试登录频率限制：连续用错误密码登录5次，第6次应返回429
    4. 用管理员账号登录，访问 /audit-logs 页面，应看到刚才的登录失败记录
    5. 在审计日志页面：选择操作类型"登录失败"筛选，应只显示登录失败记录
    6. 确认全部测试通过：python -m pytest tests/ -v
  </action>
  <verify>人工确认所有验证步骤通过</verify>
  <done>用户确认 Phase 3 安全加固全部功能正常工作</done>
</task>

</tasks>

<verification>
全部任务完成后验证：
1. 管理员可在 /audit-logs 看到审计日志
2. 筛选功能正常（按操作类型、时间范围）
3. 分页功能正常
4. 非管理员无法访问审计日志页面
5. TypeScript 编译通过
</verification>

<success_criteria>
- D-06: 管理员可在系统界面查看审计日志，支持按时间和操作类型筛选
- 审计日志页面仅管理员可访问
- 页面显示中文操作类型和角色名称
- 分页和筛选功能正常工作
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-hardening/03-02-SUMMARY.md`
</output>
