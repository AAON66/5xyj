---
phase: 03-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/audit_log.py
  - backend/app/models/__init__.py
  - backend/app/schemas/audit_log.py
  - backend/app/services/audit_service.py
  - backend/app/utils/masking.py
  - backend/app/api/v1/audit.py
  - backend/app/api/v1/router.py
  - backend/app/api/v1/auth.py
  - backend/app/main.py
  - backend/app/core/config.py
  - tests/test_audit.py
  - tests/test_masking.py
  - tests/test_security.py
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04]

must_haves:
  truths:
    - "未认证请求访问 PII 端点返回 401"
    - "登录端点连续5次失败后被锁定15分钟"
    - "登录成功/失败事件被记录到 audit_logs 表"
    - "管理员可查询审计日志（按操作类型和时间筛选）"
    - "员工角色调用 API 时身份证号被脱敏为前3后4格式"
    - "管理员/HR 角色看到完整身份证号"
    - "CORS 从 settings.backend_cors_origins 读取，不再硬编码 ['*']"
  artifacts:
    - path: "backend/app/models/audit_log.py"
      provides: "AuditLog SQLAlchemy 模型"
      contains: "class AuditLog"
    - path: "backend/app/services/audit_service.py"
      provides: "审计日志写入函数"
      exports: ["log_audit"]
    - path: "backend/app/utils/masking.py"
      provides: "身份证号脱敏函数"
      exports: ["mask_id_number"]
    - path: "backend/app/api/v1/audit.py"
      provides: "审计日志查询端点"
      contains: "router"
    - path: "backend/app/schemas/audit_log.py"
      provides: "审计日志 Pydantic schema"
      contains: "class AuditLogRead"
    - path: "tests/test_audit.py"
      provides: "审计日志测试"
      min_lines: 30
    - path: "tests/test_masking.py"
      provides: "脱敏函数测试"
      min_lines: 20
    - path: "tests/test_security.py"
      provides: "端点认证和频率限制测试"
      min_lines: 30
  key_links:
    - from: "backend/app/api/v1/audit.py"
      to: "backend/app/models/audit_log.py"
      via: "SQLAlchemy query"
      pattern: "db\\.query\\(AuditLog\\)"
    - from: "backend/app/api/v1/auth.py"
      to: "backend/app/services/audit_service.py"
      via: "log_audit 调用"
      pattern: "log_audit\\("
    - from: "backend/app/api/v1/router.py"
      to: "backend/app/api/v1/audit.py"
      via: "router include with admin role"
      pattern: "audit_router.*require_role.*admin"
---

<objective>
构建 Phase 3 安全加固的所有后端基础设施：AuditLog 模型与审计服务、身份证号脱敏工具、登录频率限制、审计日志查询端点、CORS 配置修复，以及所有对应的测试。

Purpose: 为后端建立完整的安全加固能力（审计、脱敏、限流），并通过测试验证所有 SEC 需求。
Output: AuditLog 模型、audit_service、masking 工具、audit 查询 API、login rate limiter、CORS 修复、完整测试套件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-security-hardening/03-CONTEXT.md
@.planning/phases/03-security-hardening/03-RESEARCH.md

@backend/app/models/base.py
@backend/app/dependencies.py
@backend/app/api/v1/router.py
@backend/app/api/v1/auth.py
@backend/app/services/rate_limiter.py
@backend/app/main.py
@backend/app/core/config.py
@backend/app/api/v1/responses.py

<interfaces>
<!-- 现有代码中执行器需要的关键接口 -->

From backend/app/models/base.py:
```python
class Base(DeclarativeBase):
    metadata = MetaData(naming_convention=NAMING_CONVENTION)

class UUIDPrimaryKeyMixin:
    id: Mapped[str] = mapped_column(Uuid(as_uuid=False), primary_key=True, default=lambda: str(uuid4()))

class CreatedAtMixin:
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
```

From backend/app/dependencies.py:
```python
def require_role(*allowed_roles: str) -> Callable: ...
def require_authenticated_user(request, credentials) -> AuthUser: ...
def get_db() -> Generator[Session, None, None]: ...
```

From backend/app/core/auth.py:
```python
@dataclass
class AuthUser:
    username: str
    role: str  # "admin" | "hr" | "employee"
```

From backend/app/services/rate_limiter.py:
```python
class RateLimiter:
    def __init__(self, max_failures: int = 5, lockout_seconds: int = 900): ...
    def is_locked(self, key: str) -> bool: ...
    def record_failure(self, key: str) -> bool: ...
    def reset(self, key: str) -> None: ...
```

From backend/app/api/v1/responses.py:
```python
def success_response(data: Any, message: str = "ok", status_code: int = 200) -> JSONResponse: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 AuditLog 模型、审计服务、脱敏工具、审计日志 schema 和查询端点</name>
  <files>
    backend/app/models/audit_log.py,
    backend/app/models/__init__.py,
    backend/app/schemas/audit_log.py,
    backend/app/services/audit_service.py,
    backend/app/utils/masking.py,
    backend/app/api/v1/audit.py,
    tests/test_audit.py,
    tests/test_masking.py
  </files>
  <read_first>
    backend/app/models/base.py,
    backend/app/models/__init__.py,
    backend/app/dependencies.py,
    backend/app/api/v1/responses.py,
    backend/app/services/rate_limiter.py,
    tests/conftest.py
  </read_first>
  <behavior>
    - mask_id_number(None) returns None
    - mask_id_number("") returns ""
    - mask_id_number("abc") returns "abc" (len < 7, 原样返回)
    - mask_id_number("1234567") returns "123***7" (刚好7位)
    - mask_id_number("310105199001011234") returns "310***********1234" (18位身份证号)
    - mask_id_number("A1234567(8)") returns "A12****67(8)" (10位港澳证件号)
    - log_audit 写入 AuditLog 记录到数据库，包含 action, actor_username, actor_role, ip_address, detail, resource_type, resource_id, success 字段
    - log_audit 的 detail 参数为 dict 时序列化为 JSON 字符串存储
    - log_audit 的 detail 参数为 None 时存储 None
    - GET /api/v1/audit-logs 返回分页审计日志列表，按 created_at 降序
    - GET /api/v1/audit-logs?action=login 只返回 action=login 的记录
    - GET /api/v1/audit-logs 非 admin 角色返回 403
    - GET /api/v1/audit-logs 未认证返回 401
    - AuditLog 模型只有 created_at，没有 updated_at（只追加不修改，per D-08）
  </behavior>
  <action>
    1. 创建 `backend/app/utils/__init__.py`（空文件）和 `backend/app/utils/masking.py`：
       - `def mask_id_number(id_number: Optional[str]) -> Optional[str]:`
       - 规则（per D-09）：`if not id_number or len(id_number) < 7: return id_number`
       - 正常脱敏：`return id_number[:3] + '*' * (len(id_number) - 7) + id_number[-4:]`

    2. 创建 `backend/app/models/audit_log.py`：
       - `class AuditLog(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):`
       - `__tablename__ = "audit_logs"`
       - 字段（per D-07）：
         - `action: Mapped[str] = mapped_column(String(50), nullable=False, index=True)` — 操作类型枚举值：login, logout, login_failed, employee_verify, employee_verify_failed, export, import, aggregate, user_create, user_update, user_disable
         - `actor_username: Mapped[str] = mapped_column(String(100), nullable=False, index=True)`
         - `actor_role: Mapped[str] = mapped_column(String(20), nullable=False)`
         - `ip_address: Mapped[Optional[str]] = mapped_column(String(45), nullable=True)`
         - `detail: Mapped[Optional[str]] = mapped_column(Text, nullable=True)` — JSON 字符串
         - `resource_type: Mapped[Optional[str]] = mapped_column(String(50), nullable=True)`
         - `resource_id: Mapped[Optional[str]] = mapped_column(String(100), nullable=True)`
         - `success: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)`
       - 注意：只用 `CreatedAtMixin`，不用 `TimestampMixin`（per D-08，审计日志只读不可修改）
       - 注意：不设置任何 ForeignKey（审计日志独立于其他表）

    3. 在 `backend/app/models/__init__.py` 中添加 `from backend.app.models.audit_log import AuditLog`

    4. 创建 `backend/app/schemas/audit_log.py`：
       ```python
       from pydantic import BaseModel
       from datetime import datetime
       from typing import Optional

       class AuditLogRead(BaseModel):
           id: str
           action: str
           actor_username: str
           actor_role: str
           ip_address: Optional[str] = None
           detail: Optional[str] = None
           resource_type: Optional[str] = None
           resource_id: Optional[str] = None
           success: bool
           created_at: datetime

           model_config = ConfigDict(from_attributes=True)

       class AuditLogListResponse(BaseModel):
           items: list[AuditLogRead]
           total: int
           page: int
           page_size: int
       ```

    5. 创建 `backend/app/services/audit_service.py`：
       ```python
       import json
       from typing import Optional
       from sqlalchemy.orm import Session
       from backend.app.models.audit_log import AuditLog

       def log_audit(
           db: Session,
           action: str,
           actor_username: str,
           actor_role: str,
           ip_address: Optional[str] = None,
           detail: Optional[dict] = None,
           resource_type: Optional[str] = None,
           resource_id: Optional[str] = None,
           success: bool = True,
       ) -> None:
           entry = AuditLog(
               action=action,
               actor_username=actor_username,
               actor_role=actor_role,
               ip_address=ip_address,
               detail=json.dumps(detail, ensure_ascii=False) if detail else None,
               resource_type=resource_type,
               resource_id=resource_id,
               success=success,
           )
           db.add(entry)
           db.commit()
       ```

    6. 创建 `backend/app/api/v1/audit.py`：
       - `router = APIRouter(prefix='/audit-logs', tags=['audit'])`
       - `@router.get('')` 端点，接受参数：
         - `action: Optional[str] = Query(None)` — 按操作类型筛选
         - `start_time: Optional[str] = Query(None)` — 起始时间 ISO 格式
         - `end_time: Optional[str] = Query(None)` — 结束时间 ISO 格式
         - `page: int = Query(1, ge=1)`
         - `page_size: int = Query(20, ge=1, le=100)`
       - 构建查询：`db.query(AuditLog).order_by(AuditLog.created_at.desc())`
       - 如果 action 不为空，添加 `.filter(AuditLog.action == action)`
       - 如果 start_time 不为空，解析为 datetime 并 `.filter(AuditLog.created_at >= parsed_start)`
       - 如果 end_time 不为空，解析为 datetime 并 `.filter(AuditLog.created_at <= parsed_end)`
       - 分页：`total = query.count()`，`items = query.offset((page-1)*page_size).limit(page_size).all()`
       - 返回 `success_response(AuditLogListResponse(items=items, total=total, page=page, page_size=page_size).model_dump(mode='json'))`

    7. 在 `backend/app/api/v1/router.py` 中：
       - `from backend.app.api.v1.audit import router as audit_router`
       - `api_router.include_router(audit_router, dependencies=[Depends(require_role("admin"))])` — per D-06，仅管理员可查看

    8. 创建 `tests/test_masking.py`：测试 mask_id_number 的所有 behavior 中列出的用例

    9. 创建 `tests/test_audit.py`：
       - 测试 log_audit 写入数据库
       - 测试 log_audit detail 为 dict 时 JSON 序列化
       - 测试 GET /api/v1/audit-logs 返回审计日志
       - 测试 GET /api/v1/audit-logs?action=login 筛选
       - 测试非 admin 角色返回 403
  </action>
  <verify>
    <automated>cd D:/execl_mix && python -m pytest tests/test_masking.py tests/test_audit.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/utils/masking.py 包含 `def mask_id_number(`
    - backend/app/models/audit_log.py 包含 `class AuditLog(` 且包含 `CreatedAtMixin` 且不包含 `TimestampMixin`
    - backend/app/services/audit_service.py 包含 `def log_audit(`
    - backend/app/api/v1/audit.py 包含 `router = APIRouter(prefix='/audit-logs'`
    - backend/app/api/v1/router.py 包含 `audit_router` 和 `require_role("admin")`
    - backend/app/schemas/audit_log.py 包含 `class AuditLogRead(`
    - tests/test_masking.py 和 tests/test_audit.py 全部通过
  </acceptance_criteria>
  <done>
    AuditLog 模型建表成功，log_audit 函数可写入审计记录，mask_id_number 正确脱敏，审计日志查询端点可分页筛选，所有测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 登录频率限制 + 审计日志接入 auth 端点 + CORS 修复 + 端点安全测试</name>
  <files>
    backend/app/api/v1/auth.py,
    backend/app/main.py,
    backend/app/core/config.py,
    tests/test_security.py
  </files>
  <read_first>
    backend/app/api/v1/auth.py,
    backend/app/services/rate_limiter.py,
    backend/app/services/audit_service.py,
    backend/app/main.py,
    backend/app/core/config.py,
    backend/app/dependencies.py,
    tests/conftest.py
  </read_first>
  <action>
    1. 修改 `backend/app/api/v1/auth.py`：

       a. 添加登录频率限制（per D-04，复用 RateLimiter）：
          - 在模块顶部添加 `_login_rate_limiter = RateLimiter(max_failures=5, lockout_seconds=900)`
          - 在 `login_endpoint` 函数开头添加：
            ```python
            rate_key = f"login:{payload.username}"
            if _login_rate_limiter.is_locked(rate_key):
                raise HTTPException(status_code=status.HTTP_429_TOO_MANY_REQUESTS, detail="Too many failed login attempts. Please try again later.")
            ```
          - 在登录失败的 except 块中（InvalidCredentialsError 捕获后）：
            ```python
            _login_rate_limiter.record_failure(rate_key)
            ```
            然后再 raise HTTPException
          - 在登录成功后（issue_access_token 之后）：
            ```python
            _login_rate_limiter.reset(rate_key)
            ```

       b. 接入审计日志（per D-05）：
          - 导入 `from backend.app.services.audit_service import log_audit`
          - 导入 `from backend.app.dependencies import get_db`（已有）
          - 添加辅助函数获取客户端 IP：
            ```python
            def _get_client_ip(request: Request) -> str:
                forwarded = request.headers.get("x-forwarded-for")
                if forwarded:
                    return forwarded.split(",")[0].strip()
                return request.client.host if request.client else "unknown"
            ```
          - 在 login_endpoint 登录成功后调用：
            ```python
            log_audit(db, action="login", actor_username=user.username, actor_role=user.role, ip_address=_get_client_ip(request), detail={"method": "password"}, success=True)
            ```
          - 在 login_endpoint 登录失败时调用（在 except 块内，raise 之前）：
            ```python
            log_audit(db, action="login_failed", actor_username=payload.username, actor_role="unknown", ip_address=_get_client_ip(request), detail={"reason": str(exc)}, success=False)
            ```
          - 在 employee_verify_endpoint 验证成功后调用：
            ```python
            log_audit(db, action="employee_verify", actor_username=payload.employee_id, actor_role="employee", ip_address=_get_client_ip(request), success=True)
            ```
          - 在 employee_verify_endpoint 验证失败时调用（raise 之前）：
            ```python
            log_audit(db, action="employee_verify_failed", actor_username=payload.employee_id, actor_role="unknown", ip_address=_get_client_ip(request), success=False)
            ```

    2. 修改 `backend/app/main.py`（per STATE.md CORS 待办）：
       - 将 `allow_origins=["*"]` 改为 `allow_origins=runtime_settings.backend_cors_origins`
       - `backend_cors_origins` 已在 config.py 中定义，默认值为 `['*']`，生产环境通过环境变量覆盖

    3. 创建 `tests/test_security.py`：
       - `test_login_rate_limit`: 连续5次错误密码登录，第6次返回 429
       - `test_login_rate_limit_resets_on_success`: 成功登录后计数重置
       - `test_pii_endpoints_require_auth`: 遍历关键端点（/api/v1/dashboard, /api/v1/employees, /api/v1/imports, /api/v1/audit-logs），无 token 访问返回 401 或 403
       - `test_login_creates_audit_log`: 登录成功后 audit_logs 表有 action=login 的记录
       - `test_failed_login_creates_audit_log`: 登录失败后 audit_logs 表有 action=login_failed 的记录
  </action>
  <verify>
    <automated>cd D:/execl_mix && python -m pytest tests/test_security.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/api/v1/auth.py 包含 `_login_rate_limiter = RateLimiter(`
    - backend/app/api/v1/auth.py 包含 `log_audit(` 至少4处调用（login成功、login失败、employee_verify成功、employee_verify失败）
    - backend/app/api/v1/auth.py 包含 `_get_client_ip(`
    - backend/app/main.py 包含 `allow_origins=runtime_settings.backend_cors_origins` 且不包含 `allow_origins=["*"]`
    - tests/test_security.py 包含 `test_login_rate_limit` 和 `test_pii_endpoints_require_auth`
    - `python -m pytest tests/test_security.py tests/test_audit.py tests/test_masking.py -x` 全部通过
  </acceptance_criteria>
  <done>
    登录端点5次失败后锁定15分钟，登录/验证事件记录审计日志，CORS 从配置读取，所有安全测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: 审计日志接入其余端点 + 身份证号脱敏接入 API 响应</name>
  <files>
    backend/app/api/v1/aggregate.py,
    backend/app/api/v1/users.py,
    backend/app/services/batch_export_service.py,
    backend/app/api/v1/employees.py
  </files>
  <read_first>
    backend/app/api/v1/aggregate.py,
    backend/app/api/v1/users.py,
    backend/app/services/batch_export_service.py,
    backend/app/api/v1/employees.py,
    backend/app/schemas/employees.py,
    backend/app/services/audit_service.py,
    backend/app/utils/masking.py,
    backend/app/dependencies.py
  </read_first>
  <action>
    1. 在 `backend/app/api/v1/aggregate.py` 中接入审计日志：
       - 导入 `from backend.app.services.audit_service import log_audit`
       - 在融合/导入操作成功后调用 `log_audit(db, action="aggregate", actor_username=user.username, actor_role=user.role, ip_address=_get_client_ip(request), detail={...}, success=True)`
       - 需要在端点函数签名中添加 `request: Request` 参数和 `user: AuthUser = Depends(require_role("admin", "hr"))` 参数（如果尚未有的话）
       - 复制 `_get_client_ip` 辅助函数或将其提取到 `backend/app/utils/request_helpers.py`

    2. 在 `backend/app/api/v1/users.py` 中接入审计日志（per D-05：用户创建/编辑/禁用）：
       - 在用户创建端点成功后：`log_audit(db, action="user_create", actor_username=user.username, actor_role=user.role, ip_address=_get_client_ip(request), resource_type="user", resource_id=new_user.username, success=True)`
       - 在用户更新端点成功后：`log_audit(db, action="user_update", ...)`
       - 在用户禁用端点成功后：`log_audit(db, action="user_disable", ...)`
       - 需要在端点函数签名中添加 `request: Request` 参数（如果尚未有的话）
       - 需要注入当前用户：`user: AuthUser = Depends(require_role("admin"))`

    3. 在导出流程中接入审计日志：
       - 找到导出触发的端点（可能在 aggregate.py 或单独的 exports 路由中），在导出成功后调用 `log_audit(db, action="export", ...)`
       - 如果导出是在 `batch_export_service.py` 中触发，在 service 层添加审计日志调用（需要传入 db session 和用户信息）

    4. 在 `backend/app/api/v1/employees.py` 中实现身份证号脱敏（per D-09, D-11, D-12）：
       - 导入 `from backend.app.utils.masking import mask_id_number`
       - 导入 `from backend.app.dependencies import require_authenticated_user`
       - 在返回员工列表/详情的端点中，检查当前用户角色：
         - 如果 `user.role == "employee"`：遍历返回数据，对所有 `id_number` 字段调用 `mask_id_number()`
         - 如果 `user.role in ("admin", "hr")`：返回完整身份证号（per D-11）
       - 脱敏在 API 响应层处理，不修改数据库数据（per D-12）
       - 注意：导出 Excel 始终使用完整身份证号（per D-10），导出逻辑不经过这些端点

    5. 将 `_get_client_ip` 提取到 `backend/app/utils/request_helpers.py` 作为公共函数，避免在多个端点文件中重复：
       ```python
       from fastapi import Request

       def get_client_ip(request: Request) -> str:
           forwarded = request.headers.get("x-forwarded-for")
           if forwarded:
               return forwarded.split(",")[0].strip()
           return request.client.host if request.client else "unknown"
       ```
       - 更新 auth.py 中的调用为 `from backend.app.utils.request_helpers import get_client_ip`
  </action>
  <verify>
    <automated>cd D:/execl_mix && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/api/v1/aggregate.py 包含 `log_audit(`
    - backend/app/api/v1/users.py 包含 `log_audit(` 至少3处（user_create, user_update, user_disable）
    - backend/app/api/v1/employees.py 包含 `mask_id_number(`
    - backend/app/utils/request_helpers.py 包含 `def get_client_ip(`
    - `python -m pytest tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>
    所有关键操作（导入/融合/导出/用户管理）记录审计日志，员工角色 API 响应中身份证号脱敏显示，管理员/HR 看到完整身份证号。
  </done>
</task>

</tasks>

<verification>
全部任务完成后，运行以下验证：

1. `cd D:/execl_mix && python -m pytest tests/ -v` — 所有测试通过
2. 检查 audit_logs 表结构：`python -c "from backend.app.models.audit_log import AuditLog; print(AuditLog.__tablename__)"` 输出 `audit_logs`
3. 检查脱敏函数：`python -c "from backend.app.utils.masking import mask_id_number; print(mask_id_number('310105199001011234'))"` 输出 `310***********1234`
4. 检查 CORS 不再硬编码：`grep -n "allow_origins" backend/app/main.py` 应显示 `runtime_settings.backend_cors_origins`
</verification>

<success_criteria>
- SEC-01: 所有 PII 端点需要认证，无 token 访问返回 401（测试验证）
- SEC-02: 登录端点5次失败后锁定15分钟（测试验证）；员工验证已有限流（Phase 2）
- SEC-03: 登录/导出/导入/用户管理操作写入 audit_logs 表（测试验证）；管理员可通过 GET /api/v1/audit-logs 查询（测试验证）
- SEC-04: 员工角色 API 响应中身份证号显示为 310***1234 格式（代码审查）；管理员/HR 看到完整号码；导出不脱敏
- CORS 从 settings.backend_cors_origins 读取（代码审查）
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-hardening/03-01-SUMMARY.md`
</output>
