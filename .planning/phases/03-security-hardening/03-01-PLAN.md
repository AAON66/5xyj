---
phase: 03-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/audit_log.py
  - backend/app/models/__init__.py
  - backend/app/schemas/audit_log.py
  - backend/app/services/audit_service.py
  - backend/app/utils/masking.py
  - backend/app/utils/__init__.py
  - backend/app/utils/request_helpers.py
  - backend/app/api/v1/audit.py
  - backend/app/api/v1/router.py
  - backend/app/api/v1/auth.py
  - backend/app/api/v1/aggregate.py
  - backend/app/api/v1/users.py
  - backend/app/api/v1/employees.py
  - backend/app/services/batch_export_service.py
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/alembic/versions/003_add_audit_log.py
  - tests/test_audit.py
  - tests/test_masking.py
  - tests/test_security.py
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04]

must_haves:
  truths:
    - "未认证请求访问任何 PII 端点返回 401"
    - "登录端点连续5次失败后被锁定15分钟，成功登录后计数重置"
    - "登录成功/失败、员工验证、导出、导入/融合、用户管理事件均记录到 audit_logs 表"
    - "管理员可查询审计日志（按操作类型和时间筛选），非管理员不可访问"
    - "审计日志后端无 update/delete 端点，ORM 层无修改/删除入口（per D-08）"
    - "员工角色调用 API 时身份证号被脱敏为前3后4格式"
    - "管理员/HR 角色看到完整身份证号"
    - "导出 Excel 始终使用完整身份证号"
    - "auth_enabled=false 时安全功能不阻塞正常业务流程"
    - "审计日志 detail 字段不包含密码、token、完整身份证号等敏感信息"
  artifacts:
    - path: "backend/app/models/audit_log.py"
      provides: "AuditLog SQLAlchemy 模型"
      contains: "class AuditLog"
    - path: "backend/app/services/audit_service.py"
      provides: "审计日志写入函数"
      exports: ["log_audit"]
    - path: "backend/app/utils/masking.py"
      provides: "身份证号脱敏函数"
      exports: ["mask_id_number"]
    - path: "backend/app/utils/request_helpers.py"
      provides: "请求工具函数（IP提取）"
      exports: ["get_client_ip"]
    - path: "backend/app/api/v1/audit.py"
      provides: "审计日志查询端点（只读，无 PUT/PATCH/DELETE）"
      contains: "router"
    - path: "backend/app/schemas/audit_log.py"
      provides: "审计日志 Pydantic schema"
      contains: "class AuditLogRead"
    - path: "backend/alembic/versions/003_add_audit_log.py"
      provides: "AuditLog 数据库迁移脚本"
      contains: "audit_logs"
    - path: "tests/test_audit.py"
      provides: "审计日志测试"
      min_lines: 50
    - path: "tests/test_masking.py"
      provides: "脱敏函数测试"
      min_lines: 20
    - path: "tests/test_security.py"
      provides: "端点认证、频率限制、auth_enabled=false 测试"
      min_lines: 60
  key_links:
    - from: "backend/app/api/v1/audit.py"
      to: "backend/app/models/audit_log.py"
      via: "SQLAlchemy query"
      pattern: "db\\.query\\(AuditLog\\)"
    - from: "backend/app/api/v1/auth.py"
      to: "backend/app/services/audit_service.py"
      via: "log_audit 调用"
      pattern: "log_audit\\("
    - from: "backend/app/api/v1/router.py"
      to: "backend/app/api/v1/audit.py"
      via: "router include with admin role"
      pattern: "audit_router.*require_role.*admin"
    - from: "backend/app/api/v1/employees.py"
      to: "backend/app/utils/masking.py"
      via: "mask_id_number 调用"
      pattern: "mask_id_number\\("
---

<objective>
构建 Phase 3 安全加固的全部后端能力：AuditLog 模型与 Alembic 迁移、审计服务、身份证号脱敏工具、登录频率限制、审计日志接入所有关键端点、身份证号角色化脱敏接入 API 响应、审计日志只读查询端点、CORS 配置修复（附带修复）、完整测试套件（含 auth_enabled=false 分支测试）。

Purpose: 为后端建立完整的安全加固能力（审计、脱敏、限流），覆盖 SEC-01/02/03/04 全部需求，并通过自动化测试验证。
Output: AuditLog 模型+迁移、audit_service、masking 工具、audit 查询 API（只读）、login rate limiter、所有端点审计接入、角色化脱敏接入、CORS 修复、完整测试套件。

**PII 端点清单（SEC-01 验收基础，review concern #1 回应）：**

| 端点路由 | 包含 PII | 当前 require_role | 脱敏需求 |
|---------|---------|-----------------|---------|
| /api/v1/employees/* | id_number | admin, hr | 员工角色需脱敏（当前不允许访问） |
| /api/v1/dashboard/* | 含 NormalizedRecord (id_number) | admin, hr | 员工角色需脱敏（当前不允许访问） |
| /api/v1/imports/* | id_number in records | admin, hr | 员工角色需脱敏（当前不允许访问） |
| /api/v1/auth/employee-verify | id_number (入参) | public (有频率限制) | 不返回 PII，仅验证 |
| /api/v1/users/* | username (非 PII) | admin | 无需脱敏 |
| /api/v1/aggregate/* | 处理含 PII 文件 | admin, hr | 不直接返回 PII |
| /api/v1/compare/* | 含 NormalizedRecord | admin, hr | 员工角色需脱敏（当前不允许访问） |
| /api/v1/mappings/* | 表头映射（非 PII） | admin, hr | 无需脱敏 |
| /api/v1/system/* | 无 PII | public | 无需脱敏 |
| /api/v1/audit-logs | 审计日志（可含 username） | admin | 无需脱敏 |

结论：所有含 PII 的端点已被 require_role("admin", "hr") 保护，员工角色目前无法直接访问这些端点。SEC-01 已基本满足。本阶段需确认无遗漏并添加测试验证。

**审计日志 detail 字段安全约束（review concern #3 回应）：**
- 禁止写入：密码明文/哈希、JWT token、完整身份证号（超过6位的部分）
- 允许写入：用户名、操作描述、文件名、记录数、角色名、resource_type/resource_id
- 登录失败 detail 仅记录 `{"reason": "invalid_credentials"}`，不记录密码
- 员工验证失败 detail 仅记录 `{"reason": "not_found"}`，不记录身份证号
- 导出 detail 记录 `{"export_type": "...", "record_count": N}`
- 用户管理 detail 记录 `{"target_username": "...", "changes": ["field1", "field2"]}`，仅记录变更字段名不记录值

CORS 修复为附带修复（review concern，非核心交付），但因 STATE.md 明确记录为 Phase 3 待办，故一并处理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-security-hardening/03-CONTEXT.md
@.planning/phases/03-security-hardening/03-RESEARCH.md

@backend/app/models/base.py
@backend/app/models/__init__.py
@backend/app/dependencies.py
@backend/app/api/v1/router.py
@backend/app/api/v1/auth.py
@backend/app/api/v1/aggregate.py
@backend/app/api/v1/users.py
@backend/app/api/v1/employees.py
@backend/app/schemas/employees.py
@backend/app/services/rate_limiter.py
@backend/app/services/batch_export_service.py
@backend/app/main.py
@backend/app/core/config.py
@backend/app/api/v1/responses.py
@backend/alembic/env.py
@tests/conftest.py

<interfaces>
<!-- 现有代码中执行器需要的关键接口 -->

From backend/app/models/base.py:
```python
class Base(DeclarativeBase):
    metadata = MetaData(naming_convention=NAMING_CONVENTION)

class UUIDPrimaryKeyMixin:
    id: Mapped[str] = mapped_column(Uuid(as_uuid=False), primary_key=True, default=lambda: str(uuid4()))

class CreatedAtMixin:
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
```

From backend/app/dependencies.py:
```python
def require_role(*allowed_roles: str) -> Callable: ...
def require_authenticated_user(request, credentials) -> AuthUser: ...
def get_db() -> Generator[Session, None, None]: ...
```

From backend/app/core/auth.py:
```python
@dataclass
class AuthUser:
    username: str
    role: str  # "admin" | "hr" | "employee"
```

From backend/app/services/rate_limiter.py:
```python
class RateLimiter:
    def __init__(self, max_failures: int = 5, lockout_seconds: int = 900): ...
    def is_locked(self, key: str) -> bool: ...
    def record_failure(self, key: str) -> bool: ...
    def reset(self, key: str) -> None: ...
```

From backend/app/api/v1/responses.py:
```python
def success_response(data: Any, message: str = "ok", status_code: int = 200) -> JSONResponse: ...
```

From backend/app/api/v1/router.py (当前路由权限配置):
```python
# Public -- no auth required
api_router.include_router(auth_router)
api_router.include_router(system_router)

# Admin only
api_router.include_router(users_router, dependencies=[Depends(require_role("admin"))])

# Admin + HR
api_router.include_router(aggregate_router, dependencies=[Depends(require_role("admin", "hr"))])
api_router.include_router(compare_router, dependencies=[Depends(require_role("admin", "hr"))])
api_router.include_router(dashboard_router, dependencies=[Depends(require_role("admin", "hr"))])
api_router.include_router(imports_router, dependencies=[Depends(require_role("admin", "hr"))])
api_router.include_router(mappings_router, dependencies=[Depends(require_role("admin", "hr"))])
api_router.include_router(employees_router, dependencies=[Depends(require_role("admin", "hr"))])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 基础设施 — AuditLog 模型+迁移、审计服务、脱敏工具、IP 工具、审计查询端点（只读）、测试</name>
  <files>
    backend/app/models/audit_log.py,
    backend/app/models/__init__.py,
    backend/app/schemas/audit_log.py,
    backend/app/services/audit_service.py,
    backend/app/utils/__init__.py,
    backend/app/utils/masking.py,
    backend/app/utils/request_helpers.py,
    backend/app/api/v1/audit.py,
    backend/app/api/v1/router.py,
    backend/alembic/versions/003_add_audit_log.py,
    tests/test_audit.py,
    tests/test_masking.py
  </files>
  <read_first>
    backend/app/models/base.py,
    backend/app/models/__init__.py,
    backend/app/dependencies.py,
    backend/app/api/v1/responses.py,
    backend/app/api/v1/router.py,
    backend/app/services/rate_limiter.py,
    backend/alembic/env.py,
    tests/conftest.py
  </read_first>
  <behavior>
    - mask_id_number(None) returns None
    - mask_id_number("") returns ""
    - mask_id_number("abc") returns "abc" (len < 7, 原样返回)
    - mask_id_number("1234567") returns "123***7" (刚好7位，中间0个*变为***用 len-7=0 个星号，实际返回 "1234567" — 修正：len(id)-7=0 个星号拼接不对，应 return id_number[:3] + '*' * max(len(id_number) - 7, 1) + id_number[-4:] 当 len < 8 时原样返回)
    - mask_id_number("12345678") returns "123*5678" (8位)
    - mask_id_number("310105199001011234") returns "310***********1234" (18位身份证号，中间11个*)
    - log_audit 写入 AuditLog 记录到数据库，包含 action, actor_username, actor_role, ip_address, detail, resource_type, resource_id, success 字段
    - log_audit 的 detail 参数为 dict 时序列化为 JSON 字符串存储
    - log_audit 的 detail 参数为 None 时存储 None
    - GET /api/v1/audit-logs 返回分页审计日志列表，按 created_at 降序
    - GET /api/v1/audit-logs?action=login 只返回 action=login 的记录
    - GET /api/v1/audit-logs 非 admin 角色返回 403
    - GET /api/v1/audit-logs 未认证返回 401
    - AuditLog 模型只有 created_at，没有 updated_at（per D-08）
    - audit 路由只有 GET 端点，无 PUT/PATCH/DELETE（per D-08，只读不可修改/删除）
    - get_client_ip(request) 返回 x-forwarded-for 首个 IP 或 request.client.host
  </behavior>
  <action>
    1. 创建 `backend/app/utils/__init__.py`（空文件，如果不存在）

    2. 创建 `backend/app/utils/masking.py`：
       ```python
       from typing import Optional

       def mask_id_number(id_number: Optional[str]) -> Optional[str]:
           """将身份证号脱敏为前3后4格式（per D-09）。
           短于8位的原样返回，8位及以上显示前3后4中间*替代。"""
           if not id_number or len(id_number) < 8:
               return id_number
           return id_number[:3] + '*' * (len(id_number) - 7) + id_number[-4:]
       ```

    3. 创建 `backend/app/utils/request_helpers.py`：
       ```python
       from fastapi import Request

       def get_client_ip(request: Request) -> str:
           forwarded = request.headers.get("x-forwarded-for")
           if forwarded:
               return forwarded.split(",")[0].strip()
           return request.client.host if request.client else "unknown"
       ```

    4. 创建 `backend/app/models/audit_log.py`：
       ```python
       from __future__ import annotations
       from typing import Optional
       from sqlalchemy import Boolean, String, Text
       from sqlalchemy.orm import Mapped, mapped_column
       from backend.app.models.base import Base, UUIDPrimaryKeyMixin, CreatedAtMixin

       class AuditLog(UUIDPrimaryKeyMixin, CreatedAtMixin, Base):
           __tablename__ = "audit_logs"

           action: Mapped[str] = mapped_column(String(50), nullable=False, index=True)
           actor_username: Mapped[str] = mapped_column(String(100), nullable=False, index=True)
           actor_role: Mapped[str] = mapped_column(String(20), nullable=False)
           ip_address: Mapped[Optional[str]] = mapped_column(String(45), nullable=True)
           detail: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
           resource_type: Mapped[Optional[str]] = mapped_column(String(50), nullable=True)
           resource_id: Mapped[Optional[str]] = mapped_column(String(100), nullable=True)
           success: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)
       ```
       注意：只用 CreatedAtMixin，不用 TimestampMixin（per D-08 审计日志只读不可修改）。
       注意：不设置任何 ForeignKey（审计日志独立于其他表）。

    5. 在 `backend/app/models/__init__.py` 添加：
       - `from backend.app.models.audit_log import AuditLog`
       - 在 `__all__` 列表中添加 `"AuditLog"`

    6. 创建 Alembic 迁移文件 `backend/alembic/versions/003_add_audit_log.py`：
       - 查看 backend/alembic/versions/ 目录下现有迁移文件的命名规则和 revision ID 格式
       - 使用 `cd D:/execl_mix/backend && python -m alembic revision -m "add_audit_log_table"` 生成迁移文件
       - 在 upgrade() 中创建 audit_logs 表，包含所有字段和索引
       - 在 downgrade() 中 drop audit_logs 表
       - 如果项目不使用 alembic 迁移（使用 Base.metadata.create_all），则改为确认 AuditLog 模型在 models/__init__.py 中导入即可

    7. 创建 `backend/app/schemas/audit_log.py`：
       ```python
       from pydantic import BaseModel, ConfigDict
       from datetime import datetime
       from typing import Optional

       class AuditLogRead(BaseModel):
           id: str
           action: str
           actor_username: str
           actor_role: str
           ip_address: Optional[str] = None
           detail: Optional[str] = None
           resource_type: Optional[str] = None
           resource_id: Optional[str] = None
           success: bool
           created_at: datetime

           model_config = ConfigDict(from_attributes=True)

       class AuditLogListResponse(BaseModel):
           items: list[AuditLogRead]
           total: int
           page: int
           page_size: int
       ```

    8. 创建 `backend/app/services/audit_service.py`：
       ```python
       import json
       from typing import Optional
       from sqlalchemy.orm import Session
       from backend.app.models.audit_log import AuditLog

       def log_audit(
           db: Session,
           action: str,
           actor_username: str,
           actor_role: str,
           ip_address: Optional[str] = None,
           detail: Optional[dict] = None,
           resource_type: Optional[str] = None,
           resource_id: Optional[str] = None,
           success: bool = True,
       ) -> None:
           """写入审计日志。

           detail 字段安全约束：
           - 禁止写入密码明文/哈希、JWT token、完整身份证号
           - 仅记录操作描述性信息（用户名、文件名、记录数等）
           调用方有责任确保 detail 内容不含敏感信息。
           """
           entry = AuditLog(
               action=action,
               actor_username=actor_username,
               actor_role=actor_role,
               ip_address=ip_address,
               detail=json.dumps(detail, ensure_ascii=False) if detail else None,
               resource_type=resource_type,
               resource_id=resource_id,
               success=success,
           )
           db.add(entry)
           db.commit()
       ```

    9. 创建 `backend/app/api/v1/audit.py` — 注意：只有 GET 端点，不提供 PUT/PATCH/DELETE（per D-08 只读）：
       ```python
       from datetime import datetime
       from typing import Optional
       from fastapi import APIRouter, Depends, Query
       from sqlalchemy.orm import Session
       from backend.app.dependencies import get_db
       from backend.app.models.audit_log import AuditLog
       from backend.app.schemas.audit_log import AuditLogRead, AuditLogListResponse
       from backend.app.api.v1.responses import success_response

       router = APIRouter(prefix='/audit-logs', tags=['audit'])

       @router.get('')
       def list_audit_logs(
           db: Session = Depends(get_db),
           action: Optional[str] = Query(None),
           start_time: Optional[str] = Query(None),
           end_time: Optional[str] = Query(None),
           page: int = Query(1, ge=1),
           page_size: int = Query(20, ge=1, le=100),
       ):
           query = db.query(AuditLog).order_by(AuditLog.created_at.desc())
           if action:
               query = query.filter(AuditLog.action == action)
           if start_time:
               query = query.filter(AuditLog.created_at >= datetime.fromisoformat(start_time))
           if end_time:
               query = query.filter(AuditLog.created_at <= datetime.fromisoformat(end_time))
           total = query.count()
           items = query.offset((page - 1) * page_size).limit(page_size).all()
           return success_response(
               AuditLogListResponse(
                   items=[AuditLogRead.model_validate(i) for i in items],
                   total=total, page=page, page_size=page_size,
               ).model_dump(mode='json')
           )
       ```
       注意：此路由器只暴露 GET 方法。不创建 POST/PUT/PATCH/DELETE 端点。审计日志的写入仅通过 audit_service.log_audit() 内部调用，不通过 HTTP API。

    10. 在 `backend/app/api/v1/router.py` 中：
        - 添加 `from backend.app.api.v1.audit import router as audit_router`
        - 添加 `api_router.include_router(audit_router, dependencies=[Depends(require_role("admin"))])` — per D-06 仅管理员可查看

    11. 创建 `tests/test_masking.py`：测试 mask_id_number 的所有 behavior 用例

    12. 创建 `tests/test_audit.py`：
        - test_log_audit_writes_to_db — 调用 log_audit 后数据库有记录
        - test_log_audit_detail_json — detail dict 序列化为 JSON 字符串
        - test_log_audit_detail_none — detail 为 None 存 None
        - test_list_audit_logs — GET /api/v1/audit-logs 返回分页列表
        - test_list_audit_logs_filter_action — action 参数筛选
        - test_list_audit_logs_admin_only — 非 admin 返回 403
        - test_list_audit_logs_unauthenticated — 无 token 返回 401
        - test_audit_log_no_updated_at — AuditLog 模型没有 updated_at 属性
        - test_audit_no_delete_endpoint — DELETE /api/v1/audit-logs 返回 405
        - test_audit_no_put_endpoint — PUT /api/v1/audit-logs 返回 405
  </action>
  <verify>
    <automated>cd D:/execl_mix && python -m pytest tests/test_masking.py tests/test_audit.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/utils/masking.py 包含 `def mask_id_number(`
    - backend/app/models/audit_log.py 包含 `class AuditLog(` 且包含 `CreatedAtMixin` 且不包含 `TimestampMixin` 且不包含 `updated_at`
    - backend/app/services/audit_service.py 包含 `def log_audit(` 且 docstring 包含 `禁止写入密码`
    - backend/app/api/v1/audit.py 包含 `router = APIRouter(prefix='/audit-logs'` 且只有 `@router.get` 没有 `@router.post` `@router.put` `@router.patch` `@router.delete`
    - backend/app/api/v1/router.py 包含 `audit_router` 和 `require_role("admin")`
    - backend/app/schemas/audit_log.py 包含 `class AuditLogRead(`
    - backend/app/models/__init__.py 包含 `from backend.app.models.audit_log import AuditLog`
    - backend/app/utils/request_helpers.py 包含 `def get_client_ip(`
    - tests/test_masking.py 和 tests/test_audit.py 全部通过
    - tests/test_audit.py 包含 `test_audit_no_delete_endpoint` 和 `test_audit_no_put_endpoint`
  </acceptance_criteria>
  <done>
    AuditLog 模型建表成功（含 Alembic 迁移），log_audit 函数可写入审计记录且 docstring 明确 detail 安全约束，mask_id_number 正确脱敏，审计日志查询端点只读（仅 GET，无 PUT/DELETE），所有测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 登录限流 + 审计日志接入全部端点 + 角色化脱敏接入 + CORS 修复 + 完整安全测试（含 auth_enabled=false）</name>
  <files>
    backend/app/api/v1/auth.py,
    backend/app/api/v1/aggregate.py,
    backend/app/api/v1/users.py,
    backend/app/api/v1/employees.py,
    backend/app/services/batch_export_service.py,
    backend/app/main.py,
    tests/test_security.py
  </files>
  <read_first>
    backend/app/api/v1/auth.py,
    backend/app/api/v1/aggregate.py,
    backend/app/api/v1/users.py,
    backend/app/api/v1/employees.py,
    backend/app/schemas/employees.py,
    backend/app/services/batch_export_service.py,
    backend/app/services/rate_limiter.py,
    backend/app/services/audit_service.py,
    backend/app/utils/masking.py,
    backend/app/utils/request_helpers.py,
    backend/app/main.py,
    backend/app/core/config.py,
    backend/app/dependencies.py,
    tests/conftest.py
  </read_first>
  <action>
    **A. 登录频率限制（per D-04，review concern: 明确 key=username、成功重置）**

    修改 `backend/app/api/v1/auth.py`：

    1. 在模块顶部添加：
       ```python
       from backend.app.services.rate_limiter import RateLimiter
       from backend.app.services.audit_service import log_audit
       from backend.app.utils.request_helpers import get_client_ip

       _login_rate_limiter = RateLimiter(max_failures=5, lockout_seconds=900)
       ```

    2. 在 login_endpoint 函数中：
       - 函数签名添加 `request: Request` 参数（如果尚未有）
       - 在验证逻辑之前，添加频率限制检查：
         ```python
         rate_key = f"login:{payload.username}"  # key=username（per D-04）
         if _login_rate_limiter.is_locked(rate_key):
             raise HTTPException(
                 status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                 detail="登录失败次数过多，请15分钟后重试。"
             )
         ```
       - 在登录失败时（catch InvalidCredentialsError 或类似异常后，raise 之前）：
         ```python
         _login_rate_limiter.record_failure(rate_key)  # 仅失败时计数
         log_audit(db, action="login_failed", actor_username=payload.username,
                   actor_role="unknown", ip_address=get_client_ip(request),
                   detail={"reason": "invalid_credentials"}, success=False)
         # 注意 detail 只记录原因字符串，不记录密码（review concern #3）
         ```
       - 在登录成功后（token 生成之后）：
         ```python
         _login_rate_limiter.reset(rate_key)  # 成功后重置计数
         log_audit(db, action="login", actor_username=user.username,
                   actor_role=user.role, ip_address=get_client_ip(request),
                   detail={"method": "password"}, success=True)
         ```

    3. 在 employee_verify_endpoint 中接入审计日志：
       - 验证成功后：
         ```python
         log_audit(db, action="employee_verify", actor_username=payload.employee_id,
                   actor_role="employee", ip_address=get_client_ip(request), success=True)
         ```
       - 验证失败时（raise 之前）：
         ```python
         log_audit(db, action="employee_verify_failed", actor_username=payload.employee_id,
                   actor_role="unknown", ip_address=get_client_ip(request),
                   detail={"reason": "not_found"}, success=False)
         # 注意：不记录 id_number（review concern #3，防止审计日志泄露完整身份证号）
         ```

    **B. 审计日志接入其余端点（per D-05）**

    4. 修改 `backend/app/api/v1/aggregate.py`：
       - 导入 `from backend.app.services.audit_service import log_audit` 和 `from backend.app.utils.request_helpers import get_client_ip`
       - 在融合/导入操作成功后调用：
         ```python
         log_audit(db, action="aggregate", actor_username=user.username,
                   actor_role=user.role, ip_address=get_client_ip(request),
                   detail={"file_count": N, "record_count": M}, success=True)
         ```
       - 端点函数签名需要 `request: Request`（如不存在则添加）
       - detail 只记录文件数和记录数，不记录文件内容或 PII

    5. 修改 `backend/app/api/v1/users.py`：
       - 导入同上
       - 用户创建成功后：`log_audit(db, action="user_create", ..., resource_type="user", resource_id=new_user.username, detail={"target_username": new_user.username}, success=True)`
       - 用户更新成功后：`log_audit(db, action="user_update", ..., resource_type="user", resource_id=target_username, detail={"target_username": target_username, "changed_fields": ["role"]}, success=True)` — 仅记录变更字段名，不记录新旧值
       - 用户禁用成功后：`log_audit(db, action="user_disable", ..., resource_type="user", resource_id=target_username, success=True)`

    6. 在导出端点/服务中接入审计日志：
       - 找到导出触发点（查看 batch_export_service.py 和相关端点）
       - 在导出成功后调用：`log_audit(db, action="export", ..., detail={"export_type": "salary/tool", "record_count": N}, success=True)`
       - detail 只记录导出类型和记录数

    **C. 身份证号角色化脱敏接入 API 响应（review concern #2：明确三个分支）**

    7. 修改 `backend/app/api/v1/employees.py`：
       - 导入 `from backend.app.utils.masking import mask_id_number`
       - 在返回员工列表/详情的端点中，根据用户角色决定是否脱敏：
         - **admin/HR 分支**（per D-11）：返回完整 id_number，不调用 mask_id_number
         - **employee 分支**（per D-09）：遍历返回数据，对所有 id_number 字段调用 mask_id_number()
         - **export 分支**（per D-10）：导出 Excel 始终使用完整 id_number，导出逻辑在 batch_export_service 中直接读取数据库，不经过这些 API 端点，所以无需处理
       - 注意：当前 employees 端点已被 require_role("admin", "hr") 保护，员工角色实际无法访问。但仍应在端点内部实现角色检查逻辑，为后续 Phase 5 员工门户做准备。实现方式：
         ```python
         # 在端点函数中获取当前用户
         user: AuthUser = Depends(require_authenticated_user)
         # 返回前检查
         if user.role == "employee":
             for record in results:
                 record.id_number = mask_id_number(record.id_number)
         ```

    **D. CORS 配置修复（附带修复，per STATE.md 待办）**

    8. 修改 `backend/app/main.py`：
       - 将 `allow_origins=["*"]` 改为 `allow_origins=runtime_settings.backend_cors_origins`
       - backend_cors_origins 在 config.py 中已定义，默认值 `['*']`，生产环境通过 BACKEND_CORS_ORIGINS 环境变量覆盖

    **E. 完整安全测试套件**

    9. 创建 `tests/test_security.py`，包含以下测试：

       SEC-02 频率限制测试：
       - `test_login_rate_limit_blocks_after_5_failures` — 连续5次错误密码登录同一用户名，第6次返回 429
       - `test_login_rate_limit_resets_on_success` — 4次失败后1次成功登录，再1次失败不触发锁定（计数已重置）
       - `test_login_rate_limit_key_is_username` — 不同用户名的失败互不影响

       SEC-03 审计日志测试：
       - `test_login_success_creates_audit_log` — 登录成功后 audit_logs 表有 action="login" 的记录
       - `test_login_failed_creates_audit_log` — 登录失败后有 action="login_failed" 的记录
       - `test_login_failed_audit_no_password` — 登录失败的审计日志 detail 不包含 "password" 字样（review concern #3）
       - `test_employee_verify_failed_audit_no_id_number` — 员工验证失败的审计日志 detail 不包含完整身份证号（review concern #3）

       SEC-01 端点认证测试：
       - `test_pii_endpoints_require_auth` — 遍历关键端点列表，无 token 访问返回 401 或 403：
         - GET /api/v1/dashboard/overview
         - GET /api/v1/employees
         - GET /api/v1/imports
         - GET /api/v1/audit-logs
         - GET /api/v1/users
         - GET /api/v1/compare/summary

       auth_enabled=false 开发模式测试（review concern #4，per D-02）：
       - `test_auth_disabled_pii_endpoints_accessible` — Settings(auth_enabled=False) 时 PII 端点可正常访问
       - `test_auth_disabled_audit_log_still_records` — auth_enabled=False 时审计日志仍正常记录，actor 为 "local-dev"
       - `test_auth_disabled_rate_limiter_still_works` — auth_enabled=False 时频率限制器仍正常工作

       SEC-04 脱敏测试：
       - `test_employee_role_sees_masked_id` — 模拟 employee 角色访问返回 PII 的端点，id_number 被脱敏（如果员工角色有权限访问的端点存在的话）
       - `test_admin_role_sees_full_id` — admin 角色看到完整 id_number
       - `test_hr_role_sees_full_id` — hr 角色看到完整 id_number

       CORS 测试：
       - `test_cors_uses_settings_not_wildcard` — 验证 main.py 中 allow_origins 使用 runtime_settings.backend_cors_origins
  </action>
  <verify>
    <automated>cd D:/execl_mix && python -m pytest tests/test_security.py tests/test_audit.py tests/test_masking.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/api/v1/auth.py 包含 `_login_rate_limiter = RateLimiter(max_failures=5, lockout_seconds=900)`
    - backend/app/api/v1/auth.py 包含 `rate_key = f"login:{payload.username}"` （key=username）
    - backend/app/api/v1/auth.py 包含 `_login_rate_limiter.reset(rate_key)` （成功重置）
    - backend/app/api/v1/auth.py 包含 `log_audit(` 至少4处调用
    - backend/app/api/v1/auth.py 中 login_failed 审计的 detail 不包含 password 字段
    - backend/app/api/v1/aggregate.py 包含 `log_audit(`
    - backend/app/api/v1/users.py 包含 `log_audit(` 至少3处
    - backend/app/api/v1/employees.py 包含 `mask_id_number(`
    - backend/app/main.py 包含 `allow_origins=runtime_settings.backend_cors_origins` 且不包含 `allow_origins=["*"]`
    - tests/test_security.py 包含 `test_login_rate_limit_blocks_after_5_failures`
    - tests/test_security.py 包含 `test_auth_disabled_pii_endpoints_accessible`（per D-02）
    - tests/test_security.py 包含 `test_login_failed_audit_no_password`（审计日志不泄露密码）
    - tests/test_security.py 包含 `test_pii_endpoints_require_auth`
    - `python -m pytest tests/test_security.py tests/test_audit.py tests/test_masking.py -x` 全部通过
  </acceptance_criteria>
  <done>
    登录端点5次失败后锁定15分钟（key=username，成功重置），所有关键操作（登录/验证/导入/导出/用户管理）记录审计日志且 detail 不含敏感信息，员工角色 API 响应中身份证号脱敏（admin/HR 全量），auth_enabled=false 开发模式测试通过，CORS 从配置读取，所有安全测试通过。
  </done>
</task>

</tasks>

<verification>
全部任务完成后，运行以下验证：

1. `cd D:/execl_mix && python -m pytest tests/ -v` — 所有测试通过（含 test_masking, test_audit, test_security 及现有测试）
2. 检查 audit_logs 表结构：`python -c "from backend.app.models.audit_log import AuditLog; print(AuditLog.__tablename__); assert not hasattr(AuditLog, 'updated_at')"` 输出 `audit_logs`
3. 检查脱敏函数：`python -c "from backend.app.utils.masking import mask_id_number; assert mask_id_number('310105199001011234') == '310***********1234'; print('OK')"` 输出 `OK`
4. 检查 CORS 不再硬编码：`grep -n "allow_origins" backend/app/main.py` 应显示 `runtime_settings.backend_cors_origins`
5. 检查审计路由只读：`grep -c "@router\.\(post\|put\|patch\|delete\)" backend/app/api/v1/audit.py` 应输出 `0`
6. 检查审计日志无敏感信息：`grep -r "password" backend/app/api/v1/auth.py` 中 log_audit 调用处不应包含 password 字段值
</verification>

<success_criteria>
- SEC-01: 所有 PII 端点需要认证（PII 端点清单见 objective，测试验证无 token 返回 401/403）
- SEC-02: 登录端点5次失败后锁定15分钟（key=username，成功重置，测试验证）；员工验证已有限流（Phase 2）
- SEC-03: 登录/导出/导入/用户管理操作写入 audit_logs 表（测试验证）；审计日志 detail 不含密码/token/完整身份证号；管理员可通过 GET /api/v1/audit-logs 查询（测试验证）；审计日志只读无 update/delete 端点
- SEC-04: 员工角色 API 响应中身份证号显示为 310***1234 格式；管理员/HR 全量；导出不脱敏
- auth_enabled=false 开发模式下所有功能正常（per D-02，测试验证）
- CORS 从 settings.backend_cors_origins 读取（附带修复）
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-hardening/03-01-SUMMARY.md`
</output>
