---
phase: 22-oauth
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/src/pages/Login.tsx
  - frontend/src/services/feishu.ts
  - frontend/src/components/CandidateSelectModal.tsx
autonomous: false
requirements:
  - OAUTH-01
  - OAUTH-02
  - OAUTH-03
must_haves:
  truths:
    - "飞书登录后 matched/auto_bound/new_user 状态直接登录跳转主页"
    - "pending_candidates 状态弹出候选人选择 Modal"
    - "候选列表展示姓名 + 部门 + 工号后4位"
    - "用户点选候选人后调用 confirm-bind 完成绑定并登录"
  artifacts:
    - path: "frontend/src/components/CandidateSelectModal.tsx"
      provides: "候选人选择 Modal 组件"
      contains: "CandidateSelectModal"
    - path: "frontend/src/pages/Login.tsx"
      provides: "OAuth 回调处理 pending_candidates 状态"
      contains: "pending_candidates"
    - path: "frontend/src/services/feishu.ts"
      provides: "confirmFeishuBind API 函数"
      contains: "confirmFeishuBind"
  key_links:
    - from: "frontend/src/pages/Login.tsx"
      to: "frontend/src/services/feishu.ts"
      via: "feishuOAuthCallback + confirmFeishuBind 调用"
      pattern: "confirmFeishuBind"
    - from: "frontend/src/pages/Login.tsx"
      to: "frontend/src/components/CandidateSelectModal.tsx"
      via: "组件导入和状态传递"
      pattern: "CandidateSelectModal"
---

<objective>
前端处理飞书 OAuth 回调的四种状态，新建 CandidateSelectModal 组件处理同名多人场景，用户选择后调用 confirm-bind 完成绑定登录。

Purpose: 实现 OAUTH-01/02/03 的前端交互，让用户能完成飞书登录的完整流程。
Output: Login.tsx 支持 pending_candidates 状态 + CandidateSelectModal 组件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-oauth/22-CONTEXT.md
@.planning/phases/22-oauth/22-RESEARCH.md
@.planning/phases/22-oauth/22-01-SUMMARY.md

@frontend/src/pages/Login.tsx
@frontend/src/services/feishu.ts
@frontend/src/services/authSession.ts
@frontend/src/hooks/useAuth.ts

<interfaces>
From Plan 01 后端 API 契约:
```
POST /auth/feishu/callback 返回:
  status: "matched" | "auto_bound" | "new_user" → { access_token, role, username, display_name }
  status: "pending_candidates" → { pending_token, feishu_name, candidates: [{employee_master_id, person_name, department, employee_id_masked}] }

POST /auth/feishu/confirm-bind:
  Body: { pending_token: string, employee_master_id: string }
  Returns: { access_token, role, username, display_name }
```

From frontend/src/services/authSession.ts:
```typescript
interface AuthSessionData {
  accessToken: string;
  role: AuthRole;
  username: string;
  displayName: string;
  expiresAt: string;
  signedInAt: string;
}
function writeAuthSession(data: AuthSessionData): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: feishu.ts API 扩展 + CandidateSelectModal + Login.tsx 集成</name>
  <files>
    frontend/src/services/feishu.ts
    frontend/src/components/CandidateSelectModal.tsx
    frontend/src/pages/Login.tsx
  </files>
  <read_first>
    frontend/src/services/feishu.ts
    frontend/src/pages/Login.tsx
    frontend/src/services/authSession.ts
    frontend/src/services/api.ts
  </read_first>
  <action>
    1. **扩展 `feishu.ts`**:
       - 新增 `Candidate` 接口: `{ employee_master_id: string; person_name: string; department: string; employee_id_masked: string }`
       - 新增 `FeishuOAuthResult` 联合类型:
         ```typescript
         type FeishuOAuthResult =
           | { status: 'matched' | 'auto_bound' | 'new_user'; access_token: string; role: string; username: string; display_name: string }
           | { status: 'pending_candidates'; pending_token: string; feishu_name: string; candidates: Candidate[] }
         ```
       - 修改 `feishuOAuthCallback` 返回类型为 `Promise<FeishuOAuthResult>`
       - 新增 `confirmFeishuBind(pendingToken: string, employeeMasterId: string)`: POST `/auth/feishu/confirm-bind`，返回 `{ access_token, role, username, display_name }`

    2. **新建 `CandidateSelectModal.tsx`** (per D-04, D-06):
       - Props: `{ open: boolean; candidates: Candidate[]; feishuName: string; loading: boolean; onSelect: (employeeMasterId: string) => void; onCancel: () => void }`
       - 使用 Ant Design `Modal` + `List`
       - Modal 标题: "选择您的身份"
       - 提示文字: `系统发现多个与「{feishuName}」同名的员工，请选择您的身份以完成绑定：`
       - List.Item 展示: `person_name`（title）、`部门 | 工号 ****1234`（description）
       - 点击 List.Item 触发 onSelect(employee_master_id)
       - onCancel 关闭 Modal

    3. **修改 `Login.tsx`**:
       - 新增 state: `candidates: Candidate[]`, `pendingToken: string`, `showCandidateModal: boolean`, `pendingFeishuName: string`, `bindLoading: boolean`
       - 修改 OAuth callback useEffect:
         - `feishuOAuthCallback(code, state)` 返回后检查 `result.status`
         - `pending_candidates`: 设置 candidates/pendingToken/pendingFeishuName，打开 Modal
         - `matched`/`auto_bound`/`new_user`: writeAuthSession + 跳转 (保持现有逻辑)
       - 新增 `handleCandidateSelect(employeeMasterId)`:
         - 设置 bindLoading=true
         - 调用 `confirmFeishuBind(pendingToken, employeeMasterId)`
         - 成功后 writeAuthSession + 跳转
         - 失败后 message.error
         - 最终 bindLoading=false
       - 渲染 `<CandidateSelectModal>` 在 return JSX 中
       - 清除 URL 中的 code/state 参数（使用 `window.history.replaceState`）以防刷新重复触发
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "confirmFeishuBind" frontend/src/services/feishu.ts
    - grep -q "pending_candidates" frontend/src/services/feishu.ts
    - grep -q "Candidate" frontend/src/services/feishu.ts
    - grep -q "CandidateSelectModal" frontend/src/components/CandidateSelectModal.tsx
    - grep -q "pending_candidates" frontend/src/pages/Login.tsx
    - grep -q "CandidateSelectModal" frontend/src/pages/Login.tsx
    - grep -q "confirmFeishuBind" frontend/src/pages/Login.tsx
  </acceptance_criteria>
  <done>
    - feishu.ts 新增 Candidate 接口 + FeishuOAuthResult 类型 + confirmFeishuBind 函数
    - CandidateSelectModal 组件展示候选人列表（姓名+部门+脱敏工号）
    - Login.tsx 处理 pending_candidates 状态，弹出 Modal，选择后完成绑定登录
    - lint 通过，build 成功
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 飞书登录流程验证</name>
  <files>frontend/src/pages/Login.tsx</files>
  <action>
    人工验证飞书 OAuth 登录完整流程。验证步骤：
    1. 启动后端 `uvicorn backend.app.main:app --reload` 和前端 `npm run dev`
    2. 确认飞书 OAuth 已启用（系统设置中 feishu_oauth_enabled = true）
    3. 在登录页点击"使用飞书登录"按钮
    4. 完成飞书扫码或自动登录
    5. 根据 EmployeeMaster 数据情况，验证以下场景之一：
       - 如已绑定（有 feishu_open_id）：应直接登录进入系统
       - 如姓名唯一匹配：应自动绑定并登录
       - 如同名多人：应弹出候选人 Modal，显示姓名+部门+工号后4位
       - 如无匹配：应创建新 employee 用户并登录
    6. 登录后在 /me 接口确认 feishu_bound 状态
  </action>
  <verify>人工验证，用户输入 "approved" 或描述问题</verify>
  <done>用户确认飞书登录流程正常工作</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 -> confirm-bind API | 前端持有 pending_token，需确保 token 未被篡改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Information Disclosure | CandidateSelectModal | mitigate | 候选列表仅展示 person_name + department + employee_id 后4位，不暴露完整工号/身份证 (per D-06) |
| T-22-09 | Spoofing | Login.tsx OAuth callback | accept | 前端依赖后端 state cookie 验证，前端本身不做安全校验（后端已覆盖 T-22-05） |
</threat_model>

<verification>
1. `npm run lint` 通过
2. `npm run build` 通过
3. 人工验证飞书登录完整流程
</verification>

<success_criteria>
- 飞书登录后根据匹配结果展示正确的 UI（直接登录或候选人 Modal）
- CandidateSelectModal 正确展示候选人信息（脱敏工号）
- 用户选择候选人后成功绑定并登录
- lint + build 通过
</success_criteria>

<output>
After completion, create `.planning/phases/22-oauth/22-02-SUMMARY.md`
</output>
