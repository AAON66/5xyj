---
phase: 21-feishu-field-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/feishu.py
  - backend/app/api/v1/feishu_settings.py
  - frontend/src/services/feishu.ts
  - tests/test_feishu_field_mapping.py
autonomous: true
requirements: [FMAP-01, FMAP-04]
must_haves:
  truths:
    - "FeishuFieldInfo schema 包含 ui_type 字段且 API 透传飞书返回的 ui_type"
    - "suggest-mapping API 接收飞书字段列表，返回匹配建议（含 canonical_field 和 confidence）"
    - "英文字段名（如 person_name）可通过精确匹配 fallback 返回正确建议"
    - "无法匹配的字段出现在 unmatched 列表中"
  artifacts:
    - path: "backend/app/schemas/feishu.py"
      provides: "FeishuFieldInfo with ui_type"
      contains: "ui_type"
    - path: "backend/app/api/v1/feishu_settings.py"
      provides: "suggest-mapping endpoint + ui_type pass-through"
      contains: "suggest-mapping"
    - path: "frontend/src/services/feishu.ts"
      provides: "suggestMapping + updated FeishuFieldInfo type"
      contains: "suggestMapping"
    - path: "tests/test_feishu_field_mapping.py"
      provides: "Unit tests for ui_type and suggest-mapping"
      contains: "test_suggest_mapping"
  key_links:
    - from: "backend/app/api/v1/feishu_settings.py"
      to: "backend/app/mappings/manual_field_aliases.py"
      via: "import MANUAL_ALIAS_RULES, normalize_signature, AliasRule"
      pattern: "MANUAL_ALIAS_RULES"
    - from: "frontend/src/services/feishu.ts"
      to: "/feishu/settings/configs/{config_id}/suggest-mapping"
      via: "apiClient.post"
      pattern: "suggest-mapping"
---

<objective>
后端扩展 FeishuFieldInfo 加 ui_type，get_feishu_fields 端点透传 ui_type，新增 suggest-mapping 匹配建议 API，前端服务层新增对应调用函数。

Purpose: 为 Plan 02 前端 UI 增强提供后端数据基础 -- 字段类型信息和智能匹配建议。
Output: 后端 2 个 API 变更 + 前端 2 个服务函数 + 测试覆盖。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-feishu-field-mapping/21-CONTEXT.md
@.planning/phases/21-feishu-field-mapping/21-RESEARCH.md

@backend/app/schemas/feishu.py
@backend/app/api/v1/feishu_settings.py
@backend/app/mappings/manual_field_aliases.py
@frontend/src/services/feishu.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/schemas/feishu.py (current):
```python
class FeishuFieldInfo(BaseModel):
    field_id: str
    field_name: str
    field_type: int
    description: Optional[str] = None
```

From backend/app/mappings/manual_field_aliases.py:
```python
def normalize_signature(signature: str) -> str: ...

@dataclass(frozen=True)
class AliasRule:
    canonical_field: str
    patterns: tuple[str, ...]
    confidence: float = 0.98
    regions: tuple[str, ...] = ()
    excludes: tuple[str, ...] = ()
    def matches(self, signature: str, region: Optional[str] = None) -> bool: ...

MANUAL_ALIAS_RULES: tuple[AliasRule, ...] = (...)
CANONICAL_FIELDS: set[str] = {...}  # 34 fields
```

From frontend/src/services/feishu.ts (current):
```typescript
export interface FeishuFieldInfo {
  field_id: string;
  field_name: string;
  field_type: number;
  description: string | null;
}
```

From frontend/src/pages/FeishuFieldMapping.tsx:
```typescript
const SYSTEM_FIELDS = [
  { key: 'person_name', label: '姓名' },
  // ... 23 fields total
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 -- 创建测试骨架 + 扩展 FeishuFieldInfo schema 加 ui_type</name>
  <files>tests/test_feishu_field_mapping.py, backend/app/schemas/feishu.py</files>
  <read_first>
    - backend/app/schemas/feishu.py
    - backend/app/mappings/manual_field_aliases.py
    - tests/test_feishu_settings_api.py
  </read_first>
  <behavior>
    - Test: FeishuFieldInfo(field_id="fld1", field_name="姓名", field_type=1, ui_type="Text").ui_type == "Text"
    - Test: FeishuFieldInfo(field_id="fld1", field_name="姓名", field_type=1).ui_type is None (default)
    - Test: suggest_field_mapping([{"field_name": "姓名", "field_id": "fld1"}]) returns [{"canonical_field": "person_name", "confidence": >= 0.9}]
    - Test: suggest_field_mapping([{"field_name": "person_name", "field_id": "fld2"}]) returns match via exact key fallback
    - Test: suggest_field_mapping([{"field_name": "无关字段ABC", "field_id": "fld3"}]) returns empty suggestions, fld3 in unmatched
    - Test: suggest_field_mapping([{"field_name": "养老保险(单位)", "field_id": "fld4"}]) returns pension_company
  </behavior>
  <action>
1. 修改 `backend/app/schemas/feishu.py` 的 `FeishuFieldInfo` class，在 `field_type: int` 之后新增字段：
   ```python
   ui_type: Optional[str] = None  # 飞书 API 返回的 ui_type，如 "Text", "Number", "SingleSelect"
   ```

2. 创建 `tests/test_feishu_field_mapping.py`，包含以下测试：
   - `test_feishu_field_info_has_ui_type`: 验证 FeishuFieldInfo schema 接受 ui_type 参数
   - `test_feishu_field_info_ui_type_default_none`: 验证 ui_type 默认为 None
   - `test_suggest_mapping_chinese_name`: "姓名" -> person_name, confidence >= 0.9
   - `test_suggest_mapping_english_key`: "person_name" -> person_name (exact key fallback)
   - `test_suggest_mapping_unmatched`: "无关字段ABC" -> unmatched list
   - `test_suggest_mapping_pension_company`: "养老保险(单位)" -> pension_company

   测试中 suggest_field_mapping 函数先 import 会失败（RED 阶段），因为函数尚未实现。schema 测试应当通过。

   参考现有 `tests/test_feishu_settings_api.py` 的导入模式。suggest_field_mapping 函数将在 Task 2 中实现，测试导入路径为：
   ```python
   from backend.app.api.v1.feishu_settings import suggest_field_mapping
   ```
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python -m pytest tests/test_feishu_field_mapping.py::test_feishu_field_info_has_ui_type tests/test_feishu_field_mapping.py::test_feishu_field_info_ui_type_default_none -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/schemas/feishu.py contains `ui_type: Optional[str] = None`
    - tests/test_feishu_field_mapping.py contains `test_feishu_field_info_has_ui_type`
    - tests/test_feishu_field_mapping.py contains `test_suggest_mapping_chinese_name`
    - tests/test_feishu_field_mapping.py contains `test_suggest_mapping_english_key`
    - tests/test_feishu_field_mapping.py contains `test_suggest_mapping_unmatched`
    - tests/test_feishu_field_mapping.py contains `test_suggest_mapping_pension_company`
    - Schema tests pass: pytest exits 0 for ui_type tests
  </acceptance_criteria>
  <done>FeishuFieldInfo schema 包含 ui_type 字段，6 个测试用例已创建（schema 测试通过，suggest 测试待 Task 2 实现后通过）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 后端 get_feishu_fields 透传 ui_type + 新增 suggest-mapping 端点</name>
  <files>backend/app/api/v1/feishu_settings.py</files>
  <read_first>
    - backend/app/api/v1/feishu_settings.py
    - backend/app/mappings/manual_field_aliases.py
    - backend/app/schemas/feishu.py
    - tests/test_feishu_field_mapping.py
    - frontend/src/pages/FeishuFieldMapping.tsx
  </read_first>
  <behavior>
    - get_feishu_fields endpoint extracts ui_type from feishu API response and includes it in FeishuFieldInfo
    - suggest_field_mapping function: for each feishu field, iterate MANUAL_ALIAS_RULES (filtered by system_fields if provided), return best match with highest confidence
    - English exact key fallback: if no AliasRule matches, normalize both field_name and each system_field key, match if equal
    - POST /feishu/settings/configs/{config_id}/suggest-mapping endpoint accepts {"feishu_fields": [...], "system_fields": [...]} and returns {"suggestions": [...], "unmatched": [...]}
  </behavior>
  <action>
1. 在 `backend/app/api/v1/feishu_settings.py` 的 `get_feishu_fields` 端点中，修改 field_infos 构造，增加 `ui_type=f.get("ui_type", None)`：
   ```python
   field_infos = [
       FeishuFieldInfo(
           field_id=f.get("field_id", ""),
           field_name=f.get("field_name", ""),
           field_type=f.get("type", 0),
           ui_type=f.get("ui_type", None),
           description=f.get("description", None),
       ).model_dump()
       for f in fields
   ]
   ```

2. 在同一文件中新增 `suggest_field_mapping` 纯函数（非路由，方便测试直接调用）：
   ```python
   from backend.app.mappings.manual_field_aliases import (
       MANUAL_ALIAS_RULES, normalize_signature,
   )

   def suggest_field_mapping(
       feishu_fields: list[dict],
       system_fields: list[str] | None = None,
   ) -> dict:
       suggestions = []
       matched_field_ids = set()
       for ff in feishu_fields:
           field_name = ff["field_name"]
           field_id = ff.get("field_id", "")
           best_match = None
           best_confidence = 0.0
           best_rule = ""
           for rule in MANUAL_ALIAS_RULES:
               if system_fields and rule.canonical_field not in system_fields:
                   continue
               if rule.matches(field_name, region=None):
                   if rule.confidence > best_confidence:
                       best_match = rule.canonical_field
                       best_confidence = rule.confidence
                       best_rule = " + ".join(rule.patterns)
           # English exact key fallback
           if best_match is None:
               normalized = normalize_signature(field_name)
               candidates = system_fields if system_fields else []
               for sf in candidates:
                   if normalized == normalize_signature(sf):
                       best_match = sf
                       best_confidence = 1.0
                       best_rule = "exact_key_match"
                       break
           if best_match:
               suggestions.append({
                   "feishu_field_id": field_id,
                   "feishu_field_name": field_name,
                   "canonical_field": best_match,
                   "confidence": best_confidence,
                   "matched_rule": best_rule,
               })
               matched_field_ids.add(field_id)
       unmatched = [ff.get("field_id", "") for ff in feishu_fields if ff.get("field_id", "") not in matched_field_ids]
       return {"suggestions": suggestions, "unmatched": unmatched}
   ```

3. 新增 POST 端点路由：
   ```python
   from pydantic import BaseModel as PydanticBaseModel
   from typing import List

   class SuggestMappingRequest(PydanticBaseModel):
       feishu_fields: list[dict]
       system_fields: list[str] | None = None

   @router.post(
       "/configs/{config_id}/suggest-mapping",
       summary="获取字段映射建议",
       description="基于同义词规则库，为飞书字段推荐系统字段映射。",
   )
   async def suggest_mapping_endpoint(
       config_id: str,
       body: SuggestMappingRequest,
       request: Request,
       db: Session = Depends(get_db),
   ):
       config = db.get(SyncConfig, config_id)
       if not config:
           return error_response("NOT_FOUND", "同步配置不存在", 404)
       result = suggest_field_mapping(body.feishu_fields, body.system_fields)
       return success_response(result)
   ```

   将 `SuggestMappingRequest` 类放在路由函数之前。将 `suggest_field_mapping` 函数放在路由函数之前、import 区域之后。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python -m pytest tests/test_feishu_field_mapping.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/api/v1/feishu_settings.py contains `ui_type=f.get("ui_type", None)`
    - backend/app/api/v1/feishu_settings.py contains `def suggest_field_mapping(`
    - backend/app/api/v1/feishu_settings.py contains `suggest-mapping`
    - backend/app/api/v1/feishu_settings.py contains `MANUAL_ALIAS_RULES`
    - backend/app/api/v1/feishu_settings.py contains `exact_key_match`
    - All 6 tests in test_feishu_field_mapping.py pass (pytest exits 0)
  </acceptance_criteria>
  <done>get_feishu_fields 透传 ui_type，suggest-mapping 端点可用，所有 6 个测试通过</done>
</task>

<task type="auto">
  <name>Task 3: 前端服务层新增 suggestMapping 调用 + 更新 FeishuFieldInfo 类型</name>
  <files>frontend/src/services/feishu.ts</files>
  <read_first>
    - frontend/src/services/feishu.ts
    - frontend/src/pages/FeishuFieldMapping.tsx
  </read_first>
  <action>
1. 修改 `frontend/src/services/feishu.ts` 中的 `FeishuFieldInfo` interface，新增 `ui_type`:
   ```typescript
   export interface FeishuFieldInfo {
     field_id: string;
     field_name: string;
     field_type: number;
     ui_type: string | null;  // 新增：如 "Text", "Number", "SingleSelect"
     description: string | null;
   }
   ```

2. 新增 `MappingSuggestion` interface 和 `SuggestMappingResponse` interface：
   ```typescript
   export interface MappingSuggestion {
     feishu_field_id: string;
     feishu_field_name: string;
     canonical_field: string;
     confidence: number;
     matched_rule: string;
   }

   export interface SuggestMappingResponse {
     suggestions: MappingSuggestion[];
     unmatched: string[];
   }
   ```

3. 新增 `suggestMapping` 函数：
   ```typescript
   export async function suggestMapping(
     configId: string,
     feishuFields: Array<{ field_name: string; field_id: string }>,
     systemFields?: string[],
   ): Promise<SuggestMappingResponse> {
     const response = await apiClient.post<ApiSuccessResponse<SuggestMappingResponse>>(
       `/feishu/settings/configs/${configId}/suggest-mapping`,
       { feishu_fields: feishuFields, system_fields: systemFields },
     );
     return response.data.data;
   }
   ```
   将此函数放在 `fetchFeishuFields` 函数之后。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/services/feishu.ts contains `ui_type: string | null`
    - frontend/src/services/feishu.ts contains `export interface MappingSuggestion`
    - frontend/src/services/feishu.ts contains `export interface SuggestMappingResponse`
    - frontend/src/services/feishu.ts contains `export async function suggestMapping(`
    - frontend/src/services/feishu.ts contains `suggest-mapping`
    - TypeScript compilation has no new errors related to feishu.ts
  </acceptance_criteria>
  <done>前端 FeishuFieldInfo 包含 ui_type，suggestMapping 服务函数可用，TypeScript 编译无新错误</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 -> suggest-mapping API | 前端传入 feishu_fields 和 system_fields，需验证输入 |
| suggest-mapping -> MANUAL_ALIAS_RULES | 内部调用，可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | suggest-mapping endpoint | mitigate | Pydantic SuggestMappingRequest 验证请求体结构，feishu_fields 必须为 list[dict] |
| T-21-02 | Information Disclosure | suggest-mapping response | accept | 返回的 canonical_field 和 matched_rule 不含敏感信息，仅字段名和匹配规则 |
| T-21-03 | Denial of Service | suggest-mapping large input | mitigate | feishu_fields 列表长度受限于飞书多维表格字段数（通常 < 200），Pydantic 验证防止畸形数据 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj && python -m pytest tests/test_feishu_field_mapping.py -x -v` -- 所有测试通过
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit` -- 无新 TypeScript 错误
3. `cd /Users/mac/PycharmProjects/5xyj && python -m pytest tests/ -x -q` -- 全量测试不回归
</verification>

<success_criteria>
- FeishuFieldInfo schema 包含 ui_type 且 API 透传
- suggest-mapping 端点返回包含 suggestions 和 unmatched 的响应
- 中文字段名 "姓名" 匹配到 person_name（confidence >= 0.9）
- 英文字段名 "person_name" 通过 fallback 匹配成功
- 无法匹配的字段出现在 unmatched 列表
- 前端 suggestMapping 函数类型正确
</success_criteria>

<output>
After completion, create `.planning/phases/21-feishu-field-mapping/21-01-SUMMARY.md`
</output>
