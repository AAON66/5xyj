---
phase: 21-feishu-field-mapping
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - frontend/src/pages/FeishuFieldMapping.tsx
autonomous: false
requirements: [FMAP-01, FMAP-02, FMAP-03, FMAP-04]
must_haves:
  truths:
    - "每个飞书字段节点旁显示彩色类型 Tag（文本=蓝，数字=绿，日期=橙，单选/多选=紫，其他=灰）"
    - "悬停 Tag 显示 Tooltip 包含完整类型名和 type 枚举值"
    - "点击自动匹配后，高置信度(>=0.9)连线为实线，低置信度(<0.9)连线为虚线"
    - "用户可手动删除自动连线或新增连线覆盖自动建议"
    - "点击保存时，如 person_name 或 employee_id 未映射，弹出警告 Modal"
    - "保存前弹出预览 Modal 显示完整映射关系表（系统字段 | 中文名 | 飞书字段 | 类型）"
  artifacts:
    - path: "frontend/src/pages/FeishuFieldMapping.tsx"
      provides: "完整的字段映射 UI（类型 Tag + 智能匹配 + 两步 Modal）"
      contains: "FEISHU_TYPE_LABELS"
  key_links:
    - from: "frontend/src/pages/FeishuFieldMapping.tsx"
      to: "frontend/src/services/feishu.ts"
      via: "import suggestMapping"
      pattern: "suggestMapping"
    - from: "FeishuColumnNode"
      to: "Tag, Tooltip from antd"
      via: "JSX composition"
      pattern: "Tag.*Tooltip"
---

<objective>
前端 FeishuFieldMapping 页面增强：(1) FeishuColumnNode 显示类型 Tag + Tooltip；(2) 自动匹配改用后端 suggest-mapping API，实线/虚线区分置信度；(3) 保存流程增加两步 Modal（关键字段警告 + 映射预览）。

Purpose: 用户能看到字段类型信息、获得智能匹配推荐、保存前检查完整性。
Output: 完整增强的 FeishuFieldMappingPage.tsx。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-feishu-field-mapping/21-CONTEXT.md
@.planning/phases/21-feishu-field-mapping/21-RESEARCH.md
@.planning/phases/21-feishu-field-mapping/21-01-SUMMARY.md

@frontend/src/pages/FeishuFieldMapping.tsx
@frontend/src/services/feishu.ts
@frontend/src/theme/useSemanticColors.ts

<interfaces>
<!-- From Plan 01 outputs (services/feishu.ts after Plan 01) -->

```typescript
export interface FeishuFieldInfo {
  field_id: string;
  field_name: string;
  field_type: number;
  ui_type: string | null;
  description: string | null;
}

export interface MappingSuggestion {
  feishu_field_id: string;
  feishu_field_name: string;
  canonical_field: string;
  confidence: number;
  matched_rule: string;
}

export interface SuggestMappingResponse {
  suggestions: MappingSuggestion[];
  unmatched: string[];
}

export async function suggestMapping(
  configId: string,
  feishuFields: Array<{ field_name: string; field_id: string }>,
  systemFields?: string[],
): Promise<SuggestMappingResponse>;
```

```typescript
// SYSTEM_FIELDS (already in FeishuFieldMapping.tsx)
const SYSTEM_FIELDS = [
  { key: 'person_name', label: '姓名' },
  { key: 'id_number', label: '证件号码' },
  { key: 'employee_id', label: '工号' },
  // ... 23 total
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FeishuColumnNode 类型 Tag + Tooltip + 自动匹配改用后端 API</name>
  <files>frontend/src/pages/FeishuFieldMapping.tsx</files>
  <read_first>
    - frontend/src/pages/FeishuFieldMapping.tsx
    - frontend/src/services/feishu.ts
    - frontend/src/theme/useSemanticColors.ts
  </read_first>
  <action>
修改 `frontend/src/pages/FeishuFieldMapping.tsx`，共 4 个改动区域：

**改动 1: 新增 import 和类型映射常量**

在文件顶部 import 区域新增：
```typescript
import { Tag, Tooltip } from 'antd';
import {
  fetchFeishuFields,
  fetchSyncConfigs,
  saveSyncConfigMapping,
  suggestMapping,
  type FeishuFieldInfo,
  type SyncConfig,
} from '../services/feishu';
```
（在现有 import 中增加 `Tag, Tooltip` 到 antd import，增加 `suggestMapping` 到 feishu import）

在 SYSTEM_FIELDS 定义之后、SystemFieldNode 之前，新增飞书类型映射常量：
```typescript
const FEISHU_TYPE_LABELS: Record<string, { label: string; color: string }> = {
  Text: { label: '文本', color: 'blue' },
  Email: { label: '邮箱', color: 'blue' },
  Barcode: { label: '条码', color: 'blue' },
  Phone: { label: '电话', color: 'blue' },
  Url: { label: '链接', color: 'blue' },
  Number: { label: '数字', color: 'green' },
  Progress: { label: '进度', color: 'green' },
  Currency: { label: '货币', color: 'green' },
  Rating: { label: '评分', color: 'green' },
  DateTime: { label: '日期', color: 'orange' },
  CreatedTime: { label: '创建时间', color: 'orange' },
  ModifiedTime: { label: '更新时间', color: 'orange' },
  SingleSelect: { label: '单选', color: 'purple' },
  MultiSelect: { label: '多选', color: 'purple' },
  Checkbox: { label: '复选框', color: 'default' },
  User: { label: '人员', color: 'default' },
  Attachment: { label: '附件', color: 'default' },
  Formula: { label: '公式', color: 'default' },
  Lookup: { label: '查找引用', color: 'default' },
  SingleLink: { label: '单向关联', color: 'default' },
  DuplexLink: { label: '双向关联', color: 'default' },
  Location: { label: '地理位置', color: 'default' },
  GroupChat: { label: '群组', color: 'default' },
  CreatedUser: { label: '创建人', color: 'default' },
  ModifiedUser: { label: '修改人', color: 'default' },
  AutoNumber: { label: '自动编号', color: 'default' },
};

function getTypeInfo(uiType: string | null | undefined): { label: string; color: string } {
  if (!uiType) return { label: '未知', color: 'default' };
  return FEISHU_TYPE_LABELS[uiType] ?? { label: uiType, color: 'default' };
}
```

**改动 2: 修改 FeishuColumnNode 组件**

将 FeishuColumnNode 从：
```typescript
function FeishuColumnNode({ data }: { data: { label: string } }) {
```
改为：
```typescript
function FeishuColumnNode({ data }: { data: { label: string; uiType?: string | null; fieldType?: number } }) {
  const colors = useSemanticColors();
  const typeInfo = getTypeInfo(data.uiType);
  return (
    <div
      style={{
        width: 280,
        height: 40,
        background: colors.BG_CONTAINER,
        border: `1px solid ${colors.BORDER}`,
        borderRadius: 8,
        display: 'flex',
        alignItems: 'center',
        padding: '0 12px',
        fontSize: 14,
        color: colors.TEXT,
        gap: 8,
      }}
    >
      <Handle
        type="target"
        position={Position.Left}
        style={{ background: colors.BRAND }}
      />
      <span style={{ flex: 1, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
        {data.label}
      </span>
      <Tooltip title={`${typeInfo.label} (type=${data.fieldType ?? '?'}, ui_type=${data.uiType ?? 'N/A'})`}>
        <Tag color={typeInfo.color} style={{ margin: 0, flexShrink: 0 }}>
          {typeInfo.label}
        </Tag>
      </Tooltip>
    </div>
  );
}
```

**改动 3: 修改 feishuNodes 构造和飞书节点 x 坐标**

在 useEffect 的 load 函数中，将 feishuNodes 构造从：
```typescript
const feishuNodes: Node[] = fields.map((field, index) => ({
  id: `fs-${field.field_id}`,
  type: 'feishuColumn',
  position: { x: 450, y: index * 52 },
  data: { label: field.field_name },
  draggable: false,
}));
```
改为：
```typescript
const feishuNodes: Node[] = fields.map((field, index) => ({
  id: `fs-${field.field_id}`,
  type: 'feishuColumn',
  position: { x: 500, y: index * 52 },
  data: { label: field.field_name, uiType: field.ui_type, fieldType: field.field_type },
  draggable: false,
}));
```
注意 x 从 450 改为 500（为加宽的节点留出连线空间）。

**改动 4: handleAutoMatch 改用后端 suggestMapping API**

将现有的 handleAutoMatch 回调完全替换为：
```typescript
const handleAutoMatch = useCallback(async () => {
  if (!configId || feishuFields.length === 0) return;
  try {
    const systemFieldKeys = SYSTEM_FIELDS.map((f) => f.key);
    const result = await suggestMapping(
      configId,
      feishuFields.map((f) => ({ field_name: f.field_name, field_id: f.field_id })),
      systemFieldKeys,
    );

    const newEdges: Edge[] = result.suggestions.map((s) => ({
      id: `edge-${s.canonical_field}-${s.feishu_field_id}`,
      source: `sys-${s.canonical_field}`,
      target: `fs-${s.feishu_field_id}`,
      type: 'smoothstep',
      style: {
        stroke: colors.BRAND,
        strokeWidth: 2,
        ...(s.confidence < 0.9 ? { strokeDasharray: '5 5' } : {}),
      },
      data: { confidence: s.confidence, isAutoSuggestion: true },
    }));

    setEdges(newEdges);
    message.success(`自动匹配完成，已匹配 ${newEdges.length} 个字段（${result.unmatched.length} 个未匹配）`);
  } catch (err) {
    message.error(normalizeApiError(err).message);
  }
}, [configId, feishuFields, setEdges, colors.BRAND]);
```

注意 handleAutoMatch 现在是 async 函数。调用处 `<Button onClick={handleAutoMatch}>` 改为 `<Button onClick={() => void handleAutoMatch()}>` 以正确处理 Promise。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - FeishuFieldMapping.tsx contains `FEISHU_TYPE_LABELS`
    - FeishuFieldMapping.tsx contains `getTypeInfo`
    - FeishuFieldMapping.tsx contains `Tag color={typeInfo.color}`
    - FeishuFieldMapping.tsx contains `Tooltip title=`
    - FeishuFieldMapping.tsx contains `suggestMapping(`
    - FeishuFieldMapping.tsx contains `strokeDasharray`
    - FeishuFieldMapping.tsx contains `confidence`
    - FeishuFieldMapping.tsx contains `width: 280`
    - FeishuFieldMapping.tsx contains `uiType: field.ui_type`
    - TypeScript compilation has no errors in FeishuFieldMapping.tsx
  </acceptance_criteria>
  <done>飞书字段节点显示类型 Tag + Tooltip，自动匹配改用后端 API 并区分实线/虚线置信度</done>
</task>

<task type="auto">
  <name>Task 2: 保存流程两步 Modal -- 关键字段警告 + 映射预览</name>
  <files>frontend/src/pages/FeishuFieldMapping.tsx</files>
  <read_first>
    - frontend/src/pages/FeishuFieldMapping.tsx
    - frontend/src/services/feishu.ts
  </read_first>
  <action>
在 `frontend/src/pages/FeishuFieldMapping.tsx` 中修改保存流程，新增两步 Modal：

**改动 1: 定义关键字段列表常量**

在 SYSTEM_FIELDS 之后新增：
```typescript
const REQUIRED_FIELDS = [
  { key: 'person_name', label: '姓名', level: 'required' as const },
  { key: 'employee_id', label: '工号', level: 'required' as const },
  { key: 'id_number', label: '证件号码', level: 'recommended' as const },
];
```

**改动 2: 新增 state 变量**

在组件内现有 state 变量旁新增：
```typescript
const [warningModalOpen, setWarningModalOpen] = useState(false);
const [previewModalOpen, setPreviewModalOpen] = useState(false);
const [missingFields, setMissingFields] = useState<Array<{ key: string; label: string; level: string }>>([]);
const [mappingPreview, setMappingPreview] = useState<Array<{ sysKey: string; sysLabel: string; fsName: string; fsType: string }>>([]);
```

**改动 3: 新增 import Table 组件**

在 antd import 中增加 `Table`：
```typescript
import { Button, Modal, Space, Spin, Table, Typography, message, theme } from 'antd';
```

**改动 4: 替换 handleSave 函数**

将现有 handleSave 完全替换为以下三个函数：

```typescript
// 构建当前映射 mapping 快照
const buildCurrentMapping = useCallback(() => {
  const mapping: Record<string, string> = {};
  for (const edge of edges) {
    const sysKey = edge.source.replace('sys-', '');
    const fsFieldId = edge.target.replace('fs-', '');
    const fsField = feishuFields.find((f) => f.field_id === fsFieldId);
    if (fsField) {
      mapping[sysKey] = fsField.field_name;
    }
  }
  return mapping;
}, [edges, feishuFields]);

// 点击保存 -> 检查关键字段 -> 弹窗
const handleSaveClick = useCallback(() => {
  const mapping = buildCurrentMapping();

  // 检查关键字段
  const missing = REQUIRED_FIELDS.filter((rf) => !mapping[rf.key]);
  setMissingFields(missing);

  // 构建预览数据
  const preview = SYSTEM_FIELDS.map((sf) => {
    const fsName = mapping[sf.key] ?? '';
    const fsField = feishuFields.find((f) => f.field_name === fsName);
    const typeInfo = fsField?.ui_type ? getTypeInfo(fsField.ui_type) : null;
    return {
      sysKey: sf.key,
      sysLabel: sf.label,
      fsName,
      fsType: typeInfo?.label ?? '-',
    };
  }).filter((row) => row.fsName);  // 只显示已映射的

  setMappingPreview(preview);

  if (missing.length > 0) {
    setWarningModalOpen(true);
  } else {
    setPreviewModalOpen(true);
  }
}, [buildCurrentMapping, feishuFields]);

// 确认保存
const doSave = useCallback(async () => {
  if (!configId) return;
  setSaving(true);
  setPreviewModalOpen(false);
  setWarningModalOpen(false);
  try {
    const mapping = buildCurrentMapping();
    await saveSyncConfigMapping(configId, mapping);
    message.success('映射已保存');
  } catch (err) {
    message.error(normalizeApiError(err).message);
  } finally {
    setSaving(false);
  }
}, [configId, buildCurrentMapping]);
```

**改动 5: 修改保存按钮 onClick**

将保存按钮的 `onClick={() => void handleSave()}` 改为 `onClick={handleSaveClick}`。

**改动 6: 在 return JSX 的 ReactFlow div 之后（闭合的 div 之前）新增两个 Modal**

警告 Modal:
- title="关键字段未映射"
- open={warningModalOpen}
- onCancel={() => setWarningModalOpen(false)}
- footer 包含两个按钮: "返回补全" (关闭 Modal) 和 "仍然保存" (type="primary" danger, 关闭警告 Modal 并打开预览 Modal)
- body 包含 missingFields 列表，required 级别用红色 #ff4d4f，recommended 用黄色 #faad14

预览 Modal:
- title="映射预览"
- open={previewModalOpen}
- width={640}
- onCancel={() => setPreviewModalOpen(false)}
- footer 包含 "取消" 和 "确认保存" (type="primary", loading={saving}, onClick={() => void doSave())
- body 包含 Ant Design Table, dataSource={mappingPreview}, rowKey="sysKey", pagination={false}, size="small"
- columns: 系统字段(sysKey, 160px) | 中文名(sysLabel, 120px) | 飞书字段(fsName, 180px) | 字段类型(fsType, 100px)
- 如果 missingFields.length > 0，Table 下方显示黄色警告文字
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit 2>&1 | head -30 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FeishuFieldMapping.tsx contains `REQUIRED_FIELDS`
    - FeishuFieldMapping.tsx contains `person_name.*required`
    - FeishuFieldMapping.tsx contains `employee_id.*required`
    - FeishuFieldMapping.tsx contains `warningModalOpen`
    - FeishuFieldMapping.tsx contains `previewModalOpen`
    - FeishuFieldMapping.tsx contains text for warning modal title
    - FeishuFieldMapping.tsx contains text for preview modal title
    - FeishuFieldMapping.tsx contains text for back button
    - FeishuFieldMapping.tsx contains text for force-save button
    - FeishuFieldMapping.tsx contains text for confirm button
    - FeishuFieldMapping.tsx contains `buildCurrentMapping`
    - FeishuFieldMapping.tsx contains `Table` imported from antd
    - TypeScript compilation exits 0
    - ESLint has no errors
  </acceptance_criteria>
  <done>保存按钮点击后先检查关键字段 -> 弹出警告 Modal（如有缺失）-> 预览 Modal 显示映射表 -> 确认保存</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证 -- 飞书字段映射 UI 完整体验</name>
  <files>frontend/src/pages/FeishuFieldMapping.tsx</files>
  <action>人工验证 Task 1 和 Task 2 的完整 UI 效果。按 how-to-verify 步骤逐项检查。</action>
  <what-built>完整的飞书字段映射增强 UI：类型 Tag + 智能匹配 + 两步 Modal 保存流程</what-built>
  <how-to-verify>
    1. 启动后端和前端（`uvicorn backend.app.main:app --reload` + `cd frontend && npm run dev`）
    2. 进入飞书字段映射页面（需已有 SyncConfig 和飞书凭证）
    3. 验证飞书字段节点：
       - 每个节点右侧显示彩色 Tag（文本=蓝，数字=绿，日期=橙等）
       - 悬停 Tag 显示 Tooltip（完整类型名 + type 枚举值）
    4. 点击"自动匹配"按钮：
       - 已匹配字段自动连线
       - 高置信度(>=0.9)连线为实线
       - 低置信度(<0.9)连线为虚线
       - 用户可拖拽删除/新增连线
    5. 不映射 person_name，点击"保存映射"：
       - 弹出警告 Modal，列出未映射的关键字段
       - 点"返回补全"关闭 Modal
       - 点"仍然保存"进入预览 Modal
    6. 完成所有映射后点击"保存映射"：
       - 直接弹出预览 Modal（无警告）
       - 表格显示：系统字段 | 中文名 | 飞书字段 | 字段类型
       - 点"确认保存"完成保存
  </how-to-verify>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build</automated>
  </verify>
  <done>用户确认所有 UI 功能符合预期：类型 Tag 颜色正确、匹配连线样式正确、Modal 流程正确</done>
  <resume-signal>输入 "approved" 或描述需要修复的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 -> 后端 API | suggestMapping 调用需认证，已有 apiClient 带 token |
| 用户操作 -> Modal 流程 | 用户可跳过警告直接保存（"仍然保存"按钮），这是设计意图 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | 前端跳过 Modal 直接调用保存 API | accept | 保存 API 本身无强制校验关键字段，Modal 是 UX 提醒而非安全控制 |
| T-21-05 | Spoofing | suggestMapping API 需要认证 | mitigate | 已有 apiClient 自动附加 Bearer token，后端 router 在认证中间件保护下 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && npx tsc --noEmit` -- TypeScript 编译无错误
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint` -- ESLint 通过
3. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build` -- 构建成功
4. 人工验证：类型 Tag 颜色正确，自动匹配连线样式正确，Modal 流程正确
</verification>

<success_criteria>
- 飞书字段节点显示类型 Tag（颜色按 CONTEXT.md 决策：文本=蓝，数字=绿，日期=橙，单选/多选=紫，其他=灰）
- Tag 上 Tooltip 显示完整类型信息
- 自动匹配使用后端 API，高置信度实线/低置信度虚线
- 保存时检查 person_name + employee_id 必须映射 + id_number 建议映射
- 未映射弹出警告 Modal 有"返回补全"和"仍然保存"
- 保存前预览 Modal 展示映射关系表
- 所有前端构建和 lint 通过
</success_criteria>

<output>
After completion, create `.planning/phases/21-feishu-field-mapping/21-02-SUMMARY.md`
</output>
