---
phase: 12-integration-wiring-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/feishu.ts
  - frontend/src/layouts/MainLayout.tsx
autonomous: true
requirements: [FEISHU-05, FEISHU-03, API-01]
gap_closure: true

must_haves:
  truths:
    - "Feishu OAuth authorize-url request reaches backend without 404"
    - "Feishu OAuth callback request reaches backend without 404"
    - "FeishuFieldMapping page loads Feishu column definitions from API without 404"
    - "API Keys page appears in sidebar navigation for admin users"
  artifacts:
    - path: "frontend/src/services/feishu.ts"
      provides: "Corrected OAuth and fields API paths"
      contains: "/auth/feishu/authorize-url"
    - path: "frontend/src/layouts/MainLayout.tsx"
      provides: "API Keys nav item in sidebar"
      contains: "api-keys"
  key_links:
    - from: "frontend/src/services/feishu.ts"
      to: "backend/app/api/v1/feishu_auth.py"
      via: "OAuth API calls"
      pattern: "/auth/feishu/authorize-url"
    - from: "frontend/src/services/feishu.ts"
      to: "backend/app/api/v1/feishu_settings.py"
      via: "Fields API call"
      pattern: "feishu-fields"
    - from: "frontend/src/layouts/MainLayout.tsx"
      to: "frontend/src/pages/ApiKeys.tsx"
      via: "Sidebar navigation item"
      pattern: "api-keys"
---

<objective>
Fix three cross-phase integration wiring issues found during v1.0 milestone audit: Feishu OAuth path mismatch, Feishu fields endpoint path mismatch, and missing API Keys sidebar navigation entry.

Purpose: Close the last 3 gaps blocking v1.0 milestone completion.
Output: Two corrected files that align frontend API paths with backend routes and add missing navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Backend router prefixes (DO NOT CHANGE — frontend must match these) -->

From backend/app/api/v1/feishu_auth.py:
```python
router = APIRouter(prefix="/auth/feishu", tags=["飞书认证"])
# Serves: GET /auth/feishu/authorize-url
# Serves: POST /auth/feishu/callback
```

From backend/app/api/v1/feishu_settings.py:
```python
# Serves: GET /feishu/settings/configs/{config_id}/feishu-fields
```

From frontend/src/layouts/MainLayout.tsx:
```typescript
interface NavItem {
  key: string;
  icon: React.ReactNode;
  label: string;
  roles: string[];
}
const ALL_NAV_ITEMS: NavItem[] = [
  // ... existing items ending with employee/query
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Feishu OAuth and fields API paths in frontend service</name>
  <files>frontend/src/services/feishu.ts</files>
  <read_first>
    - frontend/src/services/feishu.ts (full file — find exact lines with wrong paths)
    - backend/app/api/v1/feishu_auth.py (confirm router prefix is /auth/feishu)
    - backend/app/api/v1/feishu_settings.py (confirm endpoint path is feishu-fields)
  </read_first>
  <action>
    Fix FEISHU-05 (OAuth path mismatch):
    - Line ~219: Change `'/feishu/oauth/authorize-url'` to `'/auth/feishu/authorize-url'`
    - Line ~240: Change `'/feishu/oauth/callback'` to `'/auth/feishu/callback'`

    Fix FEISHU-03 (Fields endpoint mismatch):
    - Line ~118: Change `` `/feishu/settings/configs/${configId}/fields` `` to `` `/feishu/settings/configs/${configId}/feishu-fields` ``

    These are exact string replacements. Do NOT change any other paths or logic in the file.
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -n "authorize-url\|oauth/callback\|configs/.*fields" frontend/src/services/feishu.ts</automated>
  </verify>
  <acceptance_criteria>
    - feishu.ts contains `/auth/feishu/authorize-url` (not `/feishu/oauth/authorize-url`)
    - feishu.ts contains `/auth/feishu/callback` (not `/feishu/oauth/callback`)
    - feishu.ts contains `feishu-fields` (not just `fields`)
    - No other lines in the file are changed
  </acceptance_criteria>
  <done>All three frontend API paths match their corresponding backend router endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Add API Keys navigation item to sidebar</name>
  <files>frontend/src/layouts/MainLayout.tsx</files>
  <read_first>
    - frontend/src/layouts/MainLayout.tsx (full file — find ALL_NAV_ITEMS array and icon imports)
    - frontend/src/pages/ApiKeys.tsx (confirm page exists and route path)
  </read_first>
  <action>
    Fix API-01 (Nav missing):

    1. Add `KeyOutlined` to the @ant-design/icons import statement (line ~13-30). Insert it alphabetically among existing imports.

    2. Add a new entry to ALL_NAV_ITEMS array, after the audit-logs entry (line ~61) and before the employee/query entry:
       ```typescript
       { key: '/api-keys', icon: <KeyOutlined />, label: 'API 密钥', roles: ['admin'] },
       ```

    3. Add `'api-keys': 'API 密钥'` to the LABEL_MAP object (line ~75 area) for breadcrumb display.

    Only these three insertions. Do NOT modify any other code.
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -n "KeyOutlined\|api-keys\|API 密钥" frontend/src/layouts/MainLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MainLayout.tsx imports KeyOutlined from @ant-design/icons
    - ALL_NAV_ITEMS contains entry with key '/api-keys', label 'API 密钥', roles ['admin']
    - LABEL_MAP contains 'api-keys': 'API 密钥'
    - npm run build passes without errors (run from frontend/)
  </acceptance_criteria>
  <done>API Keys page is accessible from sidebar navigation for admin users</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -c '/auth/feishu/authorize-url' frontend/src/services/feishu.ts` returns 1
2. `grep -c '/auth/feishu/callback' frontend/src/services/feishu.ts` returns 1
3. `grep -c 'feishu-fields' frontend/src/services/feishu.ts` returns at least 1
4. `grep -c "api-keys" frontend/src/layouts/MainLayout.tsx` returns at least 2 (nav item + label map)
5. `cd frontend && npm run build` completes without errors
</verification>

<success_criteria>
- All three integration gaps from v1.0 milestone audit are closed
- Frontend Feishu OAuth paths match backend /auth/feishu/* routes
- Frontend Feishu fields path matches backend feishu-fields endpoint
- API Keys page appears in admin sidebar navigation
- Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-wiring-fix/12-01-SUMMARY.md`
</output>
