---
phase: 13-foundation-deploy-compat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/parsers/header_extraction.py
  - backend/app/parsers/workbook_discovery.py
  - backend/app/exporters/export_utils.py
  - backend/app/mappings/manual_field_aliases.py
  - backend/app/validators/non_detail_row_filter.py
  - backend/app/services/region_detection_service.py
  - backend/app/services/housing_fund_service.py
  - backend/app/services/matching_service.py
  - backend/app/services/header_normalizer.py
  - backend/app/services/validation_service.py
  - backend/app/services/employee_service.py
  - backend/app/services/batch_runtime_service.py
  - backend/app/services/normalization_service.py
  - backend/app/services/llm_mapping_service.py
  - backend/app/services/batch_export_service.py
  - backend/app/services/compare_service.py
  - backend/requirements.txt
  - backend/requirements.server.txt
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "所有 Python 文件不含 slots=True，Python 3.9 环境可正常导入"
    - "requirements.txt 中所有依赖版本锁定了上界，确保兼容 Python 3.9"
    - "未使用的依赖已移除，requirements.server.txt 合并到主文件后删除"
    - "全部后端测试通过"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "统一的依赖文件，包含版本上界"
      contains: "fastapi>=0.115.0,<0.130.0"
    - path: "backend/app/parsers/header_extraction.py"
      provides: "slots=True 已移除"
  key_links:
    - from: "backend/requirements.txt"
      to: "Python 3.9 runtime"
      via: "pip install"
      pattern: "<0\\.130\\.0|<2\\.3\\.0|<3\\.0\\.0"
---

<objective>
Python 3.9 兼容性修复 + 依赖清理

Purpose: 系统部署到 Python 3.9 云服务器的阻断性问题，必须首先解决
Output: 所有 `slots=True` 移除、依赖版本锁定上界、未使用依赖清理、requirements 合并
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-foundation-deploy-compat/13-CONTEXT.md
@.planning/phases/13-foundation-deploy-compat/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除所有 @dataclass(slots=True) 并补全 future annotations</name>
  <files>
    backend/app/parsers/header_extraction.py,
    backend/app/parsers/workbook_discovery.py,
    backend/app/exporters/export_utils.py,
    backend/app/mappings/manual_field_aliases.py,
    backend/app/validators/non_detail_row_filter.py,
    backend/app/services/region_detection_service.py,
    backend/app/services/housing_fund_service.py,
    backend/app/services/matching_service.py,
    backend/app/services/header_normalizer.py,
    backend/app/services/validation_service.py,
    backend/app/services/employee_service.py,
    backend/app/services/batch_runtime_service.py,
    backend/app/services/normalization_service.py,
    backend/app/services/llm_mapping_service.py,
    backend/app/services/batch_export_service.py,
    backend/app/services/compare_service.py
  </files>
  <read_first>
    backend/app/parsers/header_extraction.py,
    backend/app/parsers/workbook_discovery.py,
    backend/app/exporters/export_utils.py,
    backend/app/mappings/manual_field_aliases.py,
    backend/app/validators/non_detail_row_filter.py,
    backend/app/services/region_detection_service.py,
    backend/app/services/housing_fund_service.py,
    backend/app/services/matching_service.py,
    backend/app/services/header_normalizer.py,
    backend/app/services/validation_service.py,
    backend/app/services/employee_service.py,
    backend/app/services/batch_runtime_service.py,
    backend/app/services/normalization_service.py,
    backend/app/services/llm_mapping_service.py,
    backend/app/services/batch_export_service.py,
    backend/app/services/compare_service.py
  </read_first>
  <action>
    Per D-01: 在所有上述文件中，将 `@dataclass(slots=True)` 替换为 `@dataclass`。如果原来同时有 `frozen=True`（即 `@dataclass(slots=True, frozen=True)`），则替换为 `@dataclass(frozen=True)`。

    共 31 处需修改，按 RESEARCH.md 清单：
    - parsers/header_extraction.py (3 处)
    - parsers/workbook_discovery.py (2 处)
    - exporters/export_utils.py (2 处)
    - validators/non_detail_row_filter.py (3 处)
    - services/region_detection_service.py (3 处)
    - services/housing_fund_service.py (2 处)
    - services/matching_service.py (1 处)
    - services/header_normalizer.py (2 处)
    - services/validation_service.py (2 处)
    - services/employee_service.py (2 处)
    - services/batch_runtime_service.py (3 处)
    - services/normalization_service.py (4 处)
    - services/llm_mapping_service.py (1 处)
    - services/batch_export_service.py (1 处)
    - services/compare_service.py -- 如果 grep 找到则修改，否则跳过（RESEARCH 标注为可能已移除）

    Per D-02: 检查所有 15 个缺少 `from __future__ import annotations` 的文件（主要是 `__init__.py`、`enums.py`、`router.py`），在首行添加该 import。

    Per D-04: 运行 `grep -rn 'match ' backend/app/ --include='*.py' | grep -v '#'` 和 `grep -rn 'isinstance.*|' backend/app/ --include='*.py'` 验证无 match/case 和 isinstance(x, A|B) 运行时用法。

    注意事项：
    - 不要修改任何业务逻辑
    - 不要修改类的属性或方法签名
    - 全局搜索 `__dict__` 和 `__slots__` 确认无代码依赖这些行为
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -rn "slots=True" backend/app/ --include="*.py" | wc -l | xargs test 0 -eq && echo "PASS: no slots=True remaining" && pytest backend/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "slots=True" backend/app/ --include="*.py"` 返回 0 行
    - `grep -rL "from __future__ import annotations" backend/app/**/*.py` 返回 0 个文件（除空的 __init__.py）
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>所有 @dataclass(slots=True) 已移除，from __future__ import annotations 已补全，后端测试全部通过</done>
</task>

<task type="auto">
  <name>Task 2: 依赖版本锁定 + 未使用依赖清理 + requirements 合并</name>
  <files>backend/requirements.txt, backend/requirements.server.txt</files>
  <read_first>
    backend/requirements.txt,
    backend/requirements.server.txt
  </read_first>
  <action>
    Per D-03: 将 `backend/requirements.txt` 中的精确版本号改为带上界的范围约束：
    - `fastapi==0.115.0` -> `fastapi>=0.115.0,<0.130.0`
    - `pydantic==2.10.3` -> `pydantic>=2.10.3,<2.13.0`
    - `pydantic-settings==2.6.1` -> `pydantic-settings>=2.6.1,<2.12.0`
    - `pandas==2.2.3` -> `pandas>=2.2.3,<3.0.0`
    - `sqlalchemy==2.0.36` -> `sqlalchemy>=2.0.36,<2.1.0`
    - `openpyxl==3.1.5` -> `openpyxl>=3.1.5`
    - `uvicorn[standard]==0.32.0` -> `uvicorn[standard]>=0.32.0`
    - `python-multipart==0.0.12` -> `python-multipart>=0.0.12`
    - `alembic==1.14.0` -> `alembic>=1.14.0`
    - `httpx==0.28.1` -> `httpx>=0.28.1`
    - `python-dotenv==1.0.1` -> `python-dotenv>=1.0.1`
    - `pytest==8.3.4` -> `pytest>=8.3.4`

    Per D-09: 添加 `xlrd>=2.0.0` 到 requirements.txt

    Per D-11: 移除以下未使用依赖：
    - `psycopg2-binary==2.9.10` -- 系统只用 SQLite
    - `asyncpg==0.30.0` -- 系统只用 SQLite
    - `loguru==0.7.3` -- 未使用，系统用 stdlib logging

    Per D-10: 将 `requirements.server.txt` 中实际使用的依赖合并到主 `requirements.txt`：
    - 添加 `PyJWT>=2.12.0` -- 实际被 auth 模块使用
    - 添加 `pwdlib[bcrypt]>=0.3.0` -- 实际被 auth 模块使用
    - 注意：`python-jose[cryptography]` 和 `passlib[bcrypt]` 不添加（未使用）
    - 删除 `backend/requirements.server.txt` 文件

    验证 PyJWT 和 pwdlib 是否真的在代码中使用：
    ```
    grep -rn "import jwt\|from jwt\|import PyJWT" backend/app/
    grep -rn "import pwdlib\|from pwdlib" backend/app/
    ```
    如果未使用则不添加，只删除 server.txt。

    最终 requirements.txt 内容（按字母排序）：
    ```
    alembic>=1.14.0
    fastapi>=0.115.0,<0.130.0
    httpx>=0.28.1
    openpyxl>=3.1.5
    pandas>=2.2.3,<3.0.0
    pydantic>=2.10.3,<2.13.0
    pydantic-settings>=2.6.1,<2.12.0
    PyJWT>=2.12.0        # 仅当 grep 确认使用时才添加
    pwdlib[bcrypt]>=0.3.0 # 仅当 grep 确认使用时才添加
    pytest>=8.3.4
    python-dotenv>=1.0.1
    python-multipart>=0.0.12
    sqlalchemy>=2.0.36,<2.1.0
    uvicorn[standard]>=0.32.0
    xlrd>=2.0.0
    ```
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -c "psycopg2" backend/requirements.txt | xargs test 0 -eq && grep -c "asyncpg" backend/requirements.txt | xargs test 0 -eq && grep -c "loguru" backend/requirements.txt | xargs test 0 -eq && grep "fastapi" backend/requirements.txt | grep -q "<0.130.0" && grep "xlrd" backend/requirements.txt | grep -q ">=2.0.0" && test ! -f backend/requirements.server.txt && echo "PASS: dependencies cleaned"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/requirements.txt` 包含 `fastapi>=0.115.0,<0.130.0`
    - `backend/requirements.txt` 包含 `pandas>=2.2.3,<3.0.0`
    - `backend/requirements.txt` 包含 `pydantic>=2.10.3,<2.13.0`
    - `backend/requirements.txt` 包含 `sqlalchemy>=2.0.36,<2.1.0`
    - `backend/requirements.txt` 包含 `xlrd>=2.0.0`
    - `backend/requirements.txt` 不包含 `psycopg2-binary`、`asyncpg`、`loguru`
    - `backend/requirements.server.txt` 已删除
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>依赖版本全部锁定上界，未使用依赖已移除，requirements.server.txt 已合并删除</done>
</task>

</tasks>

<verification>
- `grep -rn "slots=True" backend/app/ --include="*.py"` 返回 0 行
- `grep "fastapi" backend/requirements.txt` 显示 `>=0.115.0,<0.130.0`
- `test ! -f backend/requirements.server.txt` 成功
- `pytest backend/tests/ -x -q` 全部通过
</verification>

<success_criteria>
- 后端代码在 Python 3.9 环境下可以正常导入（无 slots=True、无 PEP 604 运行时用法）
- 依赖版本锁定确保不会自动升级到不兼容 Python 3.9 的版本
- 未使用的依赖已清理，减少安装复杂度
- 全部后端测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-foundation-deploy-compat/13-01-SUMMARY.md`
</output>
