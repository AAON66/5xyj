---
phase: 13-foundation-deploy-compat
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/utils/request_helpers.py
  - backend/app/core/config.py
  - backend/tests/test_request_helpers.py
  - docs/nginx-reverse-proxy.md
  - frontend/src/pages/AuditLogs.tsx
  - backend/app/api/v1/aggregate.py
  - backend/app/api/v1/anomaly.py
  - backend/app/api/v1/auth.py
  - backend/app/services/mapping_service.py
autonomous: true
requirements: [INFRA-03, INFRA-04]

must_haves:
  truths:
    - "get_client_ip() 支持 X-Real-IP 头作为备选"
    - "get_client_ip() 只在请求来自 trusted proxy 时信任代理头"
    - "Settings 中有 trusted_proxies 配置项，默认包含 127.0.0.1 和 ::1"
    - "nginx 反向代理配置示例文档已创建"
    - "审计日志页面在所有 IP 均为 127.0.0.1 时显示提示信息"
    - "所有 log_audit 调用都传入 resource_type、resource_id 和 ip_address"
  artifacts:
    - path: "backend/app/utils/request_helpers.py"
      provides: "增强的 IP 解析逻辑"
      contains: "x-real-ip"
    - path: "backend/app/core/config.py"
      provides: "trusted_proxies 配置项"
      contains: "trusted_proxies"
    - path: "backend/tests/test_request_helpers.py"
      provides: "IP 解析单元测试"
      contains: "test_get_client_ip"
    - path: "docs/nginx-reverse-proxy.md"
      provides: "nginx 配置示例"
      contains: "X-Forwarded-For"
    - path: "frontend/src/pages/AuditLogs.tsx"
      provides: "127.0.0.1 警告提示"
      contains: "127.0.0.1"
  key_links:
    - from: "backend/app/utils/request_helpers.py"
      to: "backend/app/core/config.py"
      via: "get_settings() for trusted_proxies"
      pattern: "get_settings.*trusted_proxies"
---

<objective>
审计日志 IP 解析增强 + 审计日志内容补全 + nginx 配置文档

Purpose: 确保审计日志在反向代理环境下记录真实客户端 IP（INFRA-03），同时补全所有 log_audit 调用点缺失的 resource_type/resource_id/ip_address 字段（INFRA-04）
Output: get_client_ip() 增强、trusted_proxies 配置、单元测试、所有审计调用点补全、nginx 文档、前端 127.0.0.1 提示
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-foundation-deploy-compat/13-CONTEXT.md
@.planning/phases/13-foundation-deploy-compat/13-RESEARCH.md

<interfaces>
From backend/app/utils/request_helpers.py (current):
```python
def get_client_ip(request: Request) -> str:
    """Extract client IP from request, respecting X-Forwarded-For header."""
    forwarded = request.headers.get("x-forwarded-for")
    if forwarded:
        return forwarded.split(",")[0].strip()
    return request.client.host if request.client else "unknown"
```

From backend/app/core/config.py (Settings class, need to add field):
```python
class Settings(BaseSettings):
    # ... existing fields ...
    runtime_environment: str = 'local'
    # trusted_proxies: list[str] -- TO BE ADDED
```

Audit log call sites with MISSING fields:
- aggregate.py:60 — missing resource_type, resource_id
- anomaly.py:111 — missing ip_address, resource_type, resource_id
- auth.py:43 (login_failed) — missing resource_type
- auth.py:59 (login) — missing resource_type
- auth.py:103 (verify_failed) — missing resource_type
- auth.py:113 (verify) — missing resource_type, detail
- mapping_service.py:88 — missing ip_address, resource_type, resource_id

Well-structured call sites (model to follow):
- api_keys.py:63,129 — has all fields: action, actor, role, ip, resource_type, resource_id, detail
- users.py:46,87,106 — has all fields
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_client_ip() 增强 + trusted_proxies 配置 + 单元测试</name>
  <files>
    backend/app/utils/request_helpers.py,
    backend/app/core/config.py,
    backend/tests/test_request_helpers.py
  </files>
  <read_first>
    backend/app/utils/request_helpers.py,
    backend/app/core/config.py,
    backend/app/services/audit_service.py
  </read_first>
  <behavior>
    - Test 1: 无代理头时返回 request.client.host
    - Test 2: 有 X-Forwarded-For 头且直连 IP 在 trusted_proxies 中时，返回 X-Forwarded-For 第一个 IP
    - Test 3: 有 X-Forwarded-For 头但直连 IP 不在 trusted_proxies 中时，忽略 header 返回直连 IP
    - Test 4: 有 X-Real-IP 头且直连 IP 在 trusted_proxies 中时，返回 X-Real-IP
    - Test 5: 同时有 X-Forwarded-For 和 X-Real-IP 时，X-Forwarded-For 优先
    - Test 6: X-Forwarded-For 含多个 IP（"1.2.3.4, 5.6.7.8"）时取第一个
    - Test 7: request.client 为 None 时返回 "unknown"
    - Test 8: 集成测试 — 通过 FastAPI TestClient 发送带 X-Forwarded-For 的请求，验证端到端行为
  </behavior>
  <action>
    Per D-06: 在 `backend/app/core/config.py` 的 Settings 类中添加 `trusted_proxies` 字段：
    ```python
    trusted_proxies: list[str] = Field(default_factory=lambda: ['127.0.0.1', '::1'])
    ```
    放在 `runtime_environment` 字段附近。

    Per D-05: 重写 `backend/app/utils/request_helpers.py` 中的 `get_client_ip()`：
    ```python
    from __future__ import annotations

    from fastapi import Request

    from backend.app.core.config import get_settings


    def get_client_ip(request: Request) -> str:
        """Extract client IP from request, respecting reverse proxy headers.

        Priority: X-Forwarded-For (first IP) > X-Real-IP > direct connection.
        Only trusts proxy headers if the direct connection is from a trusted proxy.
        """
        direct_ip = request.client.host if request.client else "unknown"

        if direct_ip == "unknown":
            return direct_ip

        settings = get_settings()
        trust_headers = direct_ip in settings.trusted_proxies

        if trust_headers:
            # X-Forwarded-For: client, proxy1, proxy2
            forwarded = request.headers.get("x-forwarded-for")
            if forwarded:
                return forwarded.split(",")[0].strip()

            # X-Real-IP: single IP set by nginx
            real_ip = request.headers.get("x-real-ip")
            if real_ip:
                return real_ip.strip()

        return direct_ip
    ```

    回应 REVIEWS.md HIGH 级别关注：实现了 trusted proxy 检查，
    只有直连 IP 在 trusted_proxies 列表中时才信任代理头，防止客户端伪造。

    创建测试文件 `backend/tests/test_request_helpers.py`：
    - 7 个单元测试：使用 `unittest.mock.patch` mock `get_settings()` 和 mock Request 对象
    - 1 个集成测试（回应 REVIEWS.md 建议）：创建一个最小 FastAPI app，用 TestClient 发送带
      X-Forwarded-For 的请求，验证 get_client_ip 返回正确的客户端 IP
    - 每个测试为独立函数

    确认 `audit_service.py` 中调用 `get_client_ip(request)` 时不传额外参数，
    函数签名变更不需要修改调用方。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && pytest backend/tests/test_request_helpers.py -v && pytest backend/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/core/config.py` 包含 `trusted_proxies: list[str]`
    - `backend/app/utils/request_helpers.py` 包含 `x-real-ip` 处理逻辑
    - `backend/app/utils/request_helpers.py` 包含 `settings.trusted_proxies` 检查逻辑
    - `backend/tests/test_request_helpers.py` 至少包含 8 个 test 函数
    - `backend/tests/test_request_helpers.py` 包含 `TestClient`（集成测试）
    - `pytest backend/tests/test_request_helpers.py -v` 全部通过
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>get_client_ip() 支持 X-Real-IP + trusted proxies，8 个测试（含 1 个集成测试）全通过</done>
</task>

<task type="auto">
  <name>Task 2: 审计日志调用点补全 + nginx 文档 + 前端 127.0.0.1 提示</name>
  <files>
    backend/app/api/v1/aggregate.py,
    backend/app/api/v1/anomaly.py,
    backend/app/api/v1/auth.py,
    backend/app/services/mapping_service.py,
    docs/nginx-reverse-proxy.md,
    frontend/src/pages/AuditLogs.tsx
  </files>
  <read_first>
    backend/app/api/v1/aggregate.py,
    backend/app/api/v1/anomaly.py,
    backend/app/api/v1/auth.py,
    backend/app/services/mapping_service.py,
    backend/app/api/v1/api_keys.py,
    frontend/src/pages/AuditLogs.tsx
  </read_first>
  <action>
    回应 REVIEWS.md MEDIUM 级别关注（INFRA-04 未覆盖）：
    补全所有 log_audit 调用点缺失的字段。以 api_keys.py 和 users.py 为模板（已有完整字段）。

    **aggregate.py** (line ~60):
    当前：`log_audit(db, action="aggregate", actor_username=user.username, actor_role=user.role, ip_address=get_client_ip(request), detail={"file_count": len(files), "batch_name": batch_name or ""}, success=True)`
    修改为：添加 `resource_type="batch"` 和 `resource_id=batch_name or "unnamed"`

    **anomaly.py** (line ~111):
    当前：`log_audit(db, "anomaly_status_update", user.username, user.role, detail={...})`
    修改为：改为 keyword 参数风格，添加缺失的 `ip_address=get_client_ip(request)`, `resource_type="anomaly"`, `resource_id=str(request_body.anomaly_ids)`
    确保 `request` 参数在函数签名中可用（检查端点函数参数）。
    如果端点函数无 `request: Request` 参数，需要添加。同时确保 `get_client_ip` 已导入。

    **auth.py** login_failed (line ~43):
    添加 `resource_type="session"`, `resource_id=payload.username`

    **auth.py** login (line ~59):
    添加 `resource_type="session"`, `resource_id=user.username`

    **auth.py** employee_verify_failed (line ~103):
    添加 `resource_type="session"`, `resource_id=payload.employee_id`

    **auth.py** employee_verify (line ~113):
    添加 `resource_type="session"`, `resource_id=payload.employee_id`
    添加 `detail={"method": "three_factor"}` （三要素验证）

    **mapping_service.py** (line ~88):
    当前缺少 ip_address、resource_type、resource_id。
    mapping_service.log_audit 不在 API 端点中直接调用，而是从 service 层调用，无法直接获取 Request 对象。
    修改策略：给 mapping_service 的调用方（mapping 相关的 API 端点）传入 ip_address 参数。
    先读取 mapping_service.py 找到 log_audit 调用上下文，检查是否已有 ip_address 参数。
    添加 `resource_type="field_mapping"`, `resource_id=str(mapping_id)`。
    对于 ip_address：检查 mapping_service 的 public API 是否已接受 ip_address 参数，
    如果没有则添加 `ip_address: Optional[str] = None` 参数，并从调用方（API 端点）传入。

    **nginx 配置文档**：
    Per D-07: 创建 `docs/nginx-reverse-proxy.md`（需先 `mkdir -p docs`），内容：

    ```markdown
    # Nginx 反向代理配置

    当系统部署在 nginx 反向代理后面时，需要配置以下 header 以确保审计日志记录真实客户端 IP。

    ## 推荐配置

    ```nginx
    server {
        listen 80;
        server_name your-domain.com;

        location / {
            proxy_pass http://127.0.0.1:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    ```

    ## 说明

    - `X-Real-IP`: nginx 设置为客户端真实 IP（$remote_addr）
    - `X-Forwarded-For`: 追加客户端 IP 到转发链
    - 系统优先读取 `X-Forwarded-For`，备选 `X-Real-IP`
    - 只有来自 `trusted_proxies` 配置中的 IP 发起的请求才会信任代理头

    ## 环境变量

    在 `.env` 文件中可配置信任的代理 IP：

    ```
    TRUSTED_PROXIES=["127.0.0.1", "::1", "10.0.0.1"]
    ```

    默认值：`["127.0.0.1", "::1"]`

    ## 验证

    配置完成后，访问系统并检查审计日志页面中的 IP 地址列。
    如果仍显示 127.0.0.1，请检查 nginx 配置是否正确加载。
    ```

    **前端 127.0.0.1 提示**：
    Per D-08: 修改 `frontend/src/pages/AuditLogs.tsx`。
    回应 REVIEWS.md LOW 级别关注（false-positive）：
    条件改为：至少有 5 条日志记录且全部为 127.0.0.1 时才显示提示（排除少量记录的 dev 场景）。

    1. 从 antd 导入 `Alert`（如尚未导入）
    2. 在审计日志数据加载后计算：
       ```tsx
       const allLocalIp = useMemo(() => {
         if (!auditLogs || auditLogs.length < 5) return false;
         return auditLogs.every(
           (log) => !log.ip_address || log.ip_address === '127.0.0.1'
         );
       }, [auditLogs]);
       ```
       注意：先阅读文件确认数据变量名。

    3. 在 `<Table>` 组件之前渲染：
       ```tsx
       {allLocalIp && (
         <Alert
           type="warning"
           showIcon
           message="所有审计日志的 IP 地址均为 127.0.0.1"
           description="这通常是因为系统通过反向代理（如 nginx）访问但未配置 X-Forwarded-For 头。请参考部署文档配置 nginx。"
           style={{ marginBottom: 16 }}
         />
       )}
       ```
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -c "resource_type" backend/app/api/v1/aggregate.py | xargs test 0 -lt && grep -c "resource_type" backend/app/api/v1/anomaly.py | xargs test 0 -lt && grep -c "resource_type.*session" backend/app/api/v1/auth.py | xargs test 0 -lt && test -f docs/nginx-reverse-proxy.md && grep -q "X-Forwarded-For" docs/nginx-reverse-proxy.md && grep -q "allLocalIp\|all_local_ip" frontend/src/pages/AuditLogs.tsx && cd frontend && npm run build && cd .. && pytest backend/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `aggregate.py` 的 log_audit 调用包含 `resource_type="batch"`
    - `anomaly.py` 的 log_audit 调用包含 `ip_address=get_client_ip(request)` 和 `resource_type="anomaly"`
    - `auth.py` 的所有 4 个 log_audit 调用包含 `resource_type="session"`
    - `auth.py` employee_verify 调用包含 `detail={"method": "three_factor"}`
    - `mapping_service.py` 的 log_audit 调用包含 `resource_type="field_mapping"`
    - `docs/nginx-reverse-proxy.md` 存在且包含 `proxy_set_header X-Forwarded-For`
    - `docs/nginx-reverse-proxy.md` 包含 `TRUSTED_PROXIES` 环境变量说明
    - `frontend/src/pages/AuditLogs.tsx` 包含 127.0.0.1 检测逻辑
    - `frontend/src/pages/AuditLogs.tsx` 包含 `Alert` 组件
    - `npm run build` 成功
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>所有审计日志调用点已补全 resource_type/resource_id/ip_address，nginx 文档已创建，前端有 127.0.0.1 提示</done>
</task>

</tasks>

<verification>
- `pytest backend/tests/test_request_helpers.py -v` 全部通过
- `pytest backend/tests/ -x -q` 全部通过
- `npm run build` 成功
- `docs/nginx-reverse-proxy.md` 存在
- `grep -c "resource_type" backend/app/api/v1/aggregate.py` >= 1
- `grep -c "resource_type" backend/app/api/v1/anomaly.py` >= 1
- 审计日志页面有 127.0.0.1 提示
</verification>

<success_criteria>
- 审计日志在反向代理环境下可记录真实客户端 IP
- 不受信任的代理无法通过伪造 X-Forwarded-For 欺骗系统
- 所有 log_audit 调用点都有完整的 resource_type、resource_id、ip_address
- 部署者可参照 nginx 文档正确配置
- 全部 IP 为 127.0.0.1 时前端有明确提示（最少 5 条记录才触发，避免 false-positive）
</success_criteria>

<output>
After completion, create `.planning/phases/13-foundation-deploy-compat/13-04-SUMMARY.md`
</output>
</content>
</invoke>