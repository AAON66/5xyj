---
phase: 13-foundation-deploy-compat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AppShell.tsx
  - frontend/src/components/GlobalFeedback.tsx
  - frontend/src/components/PageContainer.tsx
  - frontend/src/components/SectionState.tsx
  - frontend/src/components/SurfaceNotice.tsx
  - frontend/src/components/index.ts
  - frontend/src/pages/SimpleAggregate.tsx
autonomous: true
requirements: [FUSE-02, FUSE-04, INFRA-02]

must_haves:
  truths:
    - "5 个废弃组件文件已删除，前端构建成功"
    - "快速融合页面社保和公积金上传区域各显示文件计数"
    - "服务器有主档时，员工主档默认选择 existing"
    - "服务器无主档时，员工主档默认选择 none"
    - "用户手动切换模式后，API 回调不会覆盖用户选择"
  artifacts:
    - path: "frontend/src/components/index.ts"
      provides: "更新后的 barrel export（不含废弃组件）"
    - path: "frontend/src/pages/SimpleAggregate.tsx"
      provides: "文件计数 + 主档智能默认值"
  key_links:
    - from: "frontend/src/pages/SimpleAggregate.tsx"
      to: "fetchEmployeeMasters API"
      via: "useEffect callback sets employeeMasterMode"
      pattern: "setEmployeeMasterMode.*existing"
---

<objective>
前端废弃组件清理 + 快速融合页面小修复

Purpose: 清理 v1.0 遗留前端组件，修复融合页面文件计数和主档默认值的用户体验问题
Output: 5 个组件文件删除、barrel export 更新、文件计数显示、主档智能默认
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-foundation-deploy-compat/13-CONTEXT.md
@.planning/phases/13-foundation-deploy-compat/13-RESEARCH.md

<interfaces>
From frontend/src/components/index.ts (current):
```typescript
export * from "./ApiFeedbackProvider";
export * from "./AuthProvider";
export * from "./PageContainer";
export * from "./SectionState";
export * from "./SurfaceNotice";
export * from "./WorkflowSteps";
```

From frontend/src/pages/SimpleAggregate.tsx (relevant state):
```typescript
const [socialFiles, setSocialFiles] = useState<File[]>([]);
const [housingFundFiles, setHousingFundFiles] = useState<File[]>([]);
const [employeeMasterMode, setEmployeeMasterMode] = useState<'none' | 'upload' | 'existing'>('none');
const [existingEmployeeMasterCount, setExistingEmployeeMasterCount] = useState(0);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 5 个废弃组件 + 更新 barrel export</name>
  <files>
    frontend/src/components/AppShell.tsx,
    frontend/src/components/GlobalFeedback.tsx,
    frontend/src/components/PageContainer.tsx,
    frontend/src/components/SectionState.tsx,
    frontend/src/components/SurfaceNotice.tsx,
    frontend/src/components/index.ts
  </files>
  <read_first>
    frontend/src/components/index.ts,
    frontend/src/components/AppShell.tsx,
    frontend/src/components/GlobalFeedback.tsx,
    frontend/src/components/PageContainer.tsx,
    frontend/src/components/SectionState.tsx,
    frontend/src/components/SurfaceNotice.tsx
  </read_first>
  <action>
    Per D-15: 删除以下 5 个废弃组件文件：
    - `frontend/src/components/AppShell.tsx`
    - `frontend/src/components/GlobalFeedback.tsx`
    - `frontend/src/components/PageContainer.tsx`
    - `frontend/src/components/SectionState.tsx`
    - `frontend/src/components/SurfaceNotice.tsx`

    更新 `frontend/src/components/index.ts`，移除对已删除组件的 re-export：
    - 移除 `export * from "./PageContainer";`
    - 移除 `export * from "./SectionState";`
    - 移除 `export * from "./SurfaceNotice";`
    - 保留 `export * from "./ApiFeedbackProvider";`
    - 保留 `export * from "./AuthProvider";`
    - 保留 `export * from "./WorkflowSteps";`

    注意：`AppShell` 和 `GlobalFeedback` 不在 barrel export 中，但需搜索其他文件确认无直接 import：
    ```
    grep -rn "AppShell\|GlobalFeedback" frontend/src/ --include="*.tsx" --include="*.ts"
    ```
    如果有其他文件引用这些组件，必须同步清理引用。

    同样搜索 `PageContainer`、`SectionState`、`SurfaceNotice` 是否在 index.ts 之外被直接引用：
    ```
    grep -rn "PageContainer\|SectionState\|SurfaceNotice" frontend/src/ --include="*.tsx" --include="*.ts"
    ```
    如果有页面 import 了这些组件，需要移除 import 语句并替换为合适的内容（根据上下文判断，可能是直接删除引用或替换为 div）。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && test ! -f src/components/AppShell.tsx && test ! -f src/components/GlobalFeedback.tsx && test ! -f src/components/PageContainer.tsx && test ! -f src/components/SectionState.tsx && test ! -f src/components/SurfaceNotice.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/AppShell.tsx` 不存在
    - `frontend/src/components/GlobalFeedback.tsx` 不存在
    - `frontend/src/components/PageContainer.tsx` 不存在
    - `frontend/src/components/SectionState.tsx` 不存在
    - `frontend/src/components/SurfaceNotice.tsx` 不存在
    - `frontend/src/components/index.ts` 不含 `PageContainer`、`SectionState`、`SurfaceNotice`
    - `npm run build` 成功
  </acceptance_criteria>
  <done>5 个废弃组件已删除，barrel export 已更新，前端构建成功</done>
</task>

<task type="auto">
  <name>Task 2: 快速融合页面文件计数 + 主档智能默认值</name>
  <files>frontend/src/pages/SimpleAggregate.tsx</files>
  <read_first>frontend/src/pages/SimpleAggregate.tsx</read_first>
  <action>
    Per D-17（文件计数）：在 `SimpleAggregate.tsx` 中：
    1. 在社保文件上传区域（`<Dragger>` 组件）下方添加文件计数文案，使用 AntD `Typography.Text`：
       ```tsx
       {socialDisplayList.length > 0 && (
         <Text type="secondary" style={{ marginTop: 4 }}>{socialDisplayList.length} 个文件</Text>
       )}
       ```
       注意：使用页面中已有的 `socialDisplayList`（如果存在）或 `socialFiles` 数组的 length。先阅读文件确认实际使用的变量名。

    2. 同样在公积金文件上传区域下方添加：
       ```tsx
       {housingDisplayList.length > 0 && (
         <Text type="secondary" style={{ marginTop: 4 }}>{housingDisplayList.length} 个文件</Text>
       )}
       ```

    3. 在文件选择卡片底部（两个上传区域之后）添加汇总计数：
       ```tsx
       <Text type="secondary">
         共 {socialDisplayList.length + housingDisplayList.length} 个文件
         （社保 {socialDisplayList.length} | 公积金 {housingDisplayList.length}）
       </Text>
       ```

    Per D-18（主档智能默认值）：
    1. 添加一个 `useRef` 标记用户是否已手动操作主档模式选择：
       ```tsx
       const employeeMasterManualRef = useRef(false);
       ```

    2. 在 `employeeMasterMode` 的 set 操作被用户触发的地方（Radio.Group 的 onChange），添加标记：
       ```tsx
       onChange={(e) => {
         employeeMasterManualRef.current = true;
         setEmployeeMasterMode(e.target.value);
       }}
       ```

    3. 修改获取员工主档数量的 `useEffect` 回调（约 line 192-200），在设置 count 后添加智能默认值逻辑：
       ```tsx
       .then((payload) => {
         if (active) {
           setExistingEmployeeMasterCount(payload.total);
           // D-18: 智能默认值 -- 仅在用户未手动操作时设置
           if (!employeeMasterManualRef.current) {
             if (payload.total > 0) {
               setEmployeeMasterMode('existing');
             }
             // payload.total === 0 时保持初始值 'none'
           }
         }
       })
       ```

    注意（Pitfall 5）：必须使用 ref 而非 state 来跟踪手动操作，避免异步回调覆盖用户已做的选择。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build && grep -c "个文件" src/pages/SimpleAggregate.tsx | xargs test 0 -lt && grep -c "employeeMasterManualRef" src/pages/SimpleAggregate.tsx | xargs test 0 -lt && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `SimpleAggregate.tsx` 包含 `个文件` 字符串（文件计数文案）
    - `SimpleAggregate.tsx` 包含 `employeeMasterManualRef` （防竞态 ref）
    - `SimpleAggregate.tsx` 包含 `setEmployeeMasterMode('existing')` 在 useEffect 回调中
    - `npm run build` 成功
  </acceptance_criteria>
  <done>社保/公积金文件计数显示正常，主档默认值根据服务器数据智能切换且不覆盖用户手动选择</done>
</task>

</tasks>

<verification>
- 5 个废弃组件文件均已删除
- `npm run build` 成功
- SimpleAggregate.tsx 包含文件计数和主档智能默认值逻辑
</verification>

<success_criteria>
- 前端构建成功，无 "Module not found" 错误
- 快速融合页面显示社保 N 个文件 | 公积金 N 个文件 + 总计
- 有主档时默认 existing，无主档时默认 none，用户手动切换后不被覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/13-foundation-deploy-compat/13-02-SUMMARY.md`
</output>
