---
phase: 13-foundation-deploy-compat
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - backend/app/mappings/regions.py
  - backend/app/utils/filename_utils.py
  - backend/app/validators/constants.py
  - backend/app/services/region_detection_service.py
  - backend/app/services/import_service.py
  - backend/app/services/aggregate_service.py
  - backend/app/services/matching_service.py
  - backend/app/exporters/template_exporter.py
  - backend/app/api/v1/employees.py
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "REGION_LABELS 只在 backend/app/mappings/regions.py 中定义，其他文件全部 import"
    - "FILENAME_NOISE, DATE_PATTERN, _infer_company_name_from_filename 只在 backend/app/utils/filename_utils.py 中定义"
    - "ID_NUMBER_PATTERN, NON_MAINLAND_ID_NUMBER_PATTERN 只在 backend/app/validators/constants.py 中定义"
    - "自助查询端点有 require_authenticated_user 依赖"
    - "全部后端测试通过"
  artifacts:
    - path: "backend/app/mappings/regions.py"
      provides: "REGION_LABELS 单一定义"
      contains: "REGION_LABELS"
    - path: "backend/app/utils/filename_utils.py"
      provides: "文件名工具函数单一定义"
      contains: "FILENAME_NOISE"
    - path: "backend/app/validators/constants.py"
      provides: "ID 号正则单一定义"
      contains: "ID_NUMBER_PATTERN"
  key_links:
    - from: "backend/app/services/import_service.py"
      to: "backend/app/mappings/regions.py"
      via: "from backend.app.mappings.regions import REGION_LABELS"
      pattern: "from backend\\.app\\.mappings\\.regions import"
    - from: "backend/app/services/import_service.py"
      to: "backend/app/utils/filename_utils.py"
      via: "from backend.app.utils.filename_utils import"
      pattern: "from backend\\.app\\.utils\\.filename_utils import"
    - from: "backend/app/services/matching_service.py"
      to: "backend/app/validators/constants.py"
      via: "from backend.app.validators.constants import"
      pattern: "from backend\\.app\\.validators\\.constants import"
---

<objective>
技术债常量合并 + 自助查询端点认证修复

Purpose: 消除 4 处常量重复定义（REGION_LABELS、FILENAME_NOISE/DATE_PATTERN、ID_NUMBER_PATTERN），修复自助查询端点缺少显式认证
Output: 3 个新共享模块 + 调用方统一 import + 端点认证一致
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-foundation-deploy-compat/13-CONTEXT.md
@.planning/phases/13-foundation-deploy-compat/13-RESEARCH.md
@.planning/phases/13-foundation-deploy-compat/13-01-SUMMARY.md

<interfaces>
From backend/app/services/region_detection_service.py (canonical REGION_LABELS):
```python
REGION_LABELS = {
    'guangzhou': '广州', 'shenzhen': '深圳', 'hangzhou': '杭州',
    'xiamen': '厦门', 'wuhan': '武汉', 'changsha': '长沙',
}
```

From backend/app/services/import_service.py (duplicated constants):
```python
FILENAME_NOISE = (...)  # tuples of noise strings
DATE_PATTERN = re.compile(...)
def _infer_company_name_from_filename(...): ...
```

From backend/app/services/matching_service.py (duplicated regex):
```python
ID_NUMBER_PATTERN = re.compile(r'...')
NON_MAINLAND_ID_NUMBER_PATTERN = re.compile(r'...')
```

From backend/app/api/v1/employees.py (self-service endpoint):
```python
@router.post('/self-service/query', ...)
def employee_self_service_query_endpoint(
    payload: EmployeeSelfServiceQueryInput = Body(...),
    db: Session = Depends(get_db),
):  # NOTE: missing _user=Depends(require_authenticated_user)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 常量合并到共享模块（REGION_LABELS + FILENAME_NOISE + ID_NUMBER_PATTERN）</name>
  <files>
    backend/app/mappings/regions.py,
    backend/app/utils/filename_utils.py,
    backend/app/validators/constants.py,
    backend/app/services/region_detection_service.py,
    backend/app/services/import_service.py,
    backend/app/services/aggregate_service.py,
    backend/app/services/matching_service.py,
    backend/app/exporters/template_exporter.py
  </files>
  <read_first>
    backend/app/services/region_detection_service.py,
    backend/app/services/import_service.py,
    backend/app/services/aggregate_service.py,
    backend/app/services/matching_service.py,
    backend/app/exporters/template_exporter.py,
    backend/app/mappings/__init__.py,
    backend/app/utils/__init__.py,
    backend/app/validators/__init__.py
  </read_first>
  <action>
    Per D-12（REGION_LABELS 合并）：
    1. 新建 `backend/app/mappings/regions.py`，将 `REGION_LABELS` 字典从 `region_detection_service.py` 移入：
       ```python
       from __future__ import annotations

       REGION_LABELS: dict[str, str] = {
           'guangzhou': '广州',
           'shenzhen': '深圳',
           'hangzhou': '杭州',
           'xiamen': '厦门',
           'wuhan': '武汉',
           'changsha': '长沙',
       }
       ```
    2. 修改以下文件，删除本地 REGION_LABELS 定义，改为 import：
       - `backend/app/services/region_detection_service.py`: 删除 line 20 附近的 REGION_LABELS 定义，添加 `from backend.app.mappings.regions import REGION_LABELS`
       - `backend/app/services/import_service.py`: 删除本地 REGION_LABELS 定义，添加 `from backend.app.mappings.regions import REGION_LABELS`
       - `backend/app/exporters/template_exporter.py`: 删除本地 REGION_LABELS 定义，添加 `from backend.app.mappings.regions import REGION_LABELS`
       - `backend/app/services/aggregate_service.py`: 如果已从 region_detection_service import，改为从 regions.py import

    Per D-13（FILENAME_NOISE / DATE_PATTERN / _infer_company_name_from_filename 合并）：
    1. 新建 `backend/app/utils/filename_utils.py`：
       - 从 `import_service.py` 中提取 `FILENAME_NOISE` 元组、`DATE_PATTERN` 正则、`_infer_company_name_from_filename` 函数
       - 函数改名为 `infer_company_name_from_filename`（移除下划线前缀，因为它现在是公开 API）
       - 文件首行添加 `from __future__ import annotations`
    2. 修改 `import_service.py` 和 `aggregate_service.py`：删除本地定义，改为 `from backend.app.utils.filename_utils import FILENAME_NOISE, DATE_PATTERN, infer_company_name_from_filename`
    3. 更新所有对 `_infer_company_name_from_filename` 的调用为 `infer_company_name_from_filename`

    Per D-14（ID_NUMBER_PATTERN / NON_MAINLAND_ID_NUMBER_PATTERN 合并）：
    1. 新建 `backend/app/validators/constants.py`：
       - 从 `matching_service.py` 中提取两个正则常量
       - 文件首行添加 `from __future__ import annotations`
       ```python
       from __future__ import annotations
       import re

       ID_NUMBER_PATTERN = re.compile(r'...')  # 从 matching_service.py 复制精确值
       NON_MAINLAND_ID_NUMBER_PATTERN = re.compile(r'...')  # 从 matching_service.py 复制精确值
       ```
    2. 修改 `matching_service.py` 和 `template_exporter.py`：删除本地定义，改为 `from backend.app.validators.constants import ID_NUMBER_PATTERN, NON_MAINLAND_ID_NUMBER_PATTERN`

    关键注意事项：
    - 阅读每个文件的完整定义后再提取，确保值完全一致
    - 如果不同文件的"重复"常量值略有差异，以 canonical source 为准
    - 确保新模块的 `__init__.py` 存在（已有的 mappings/utils/validators 目录应已有）
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && python -c "from backend.app.mappings.regions import REGION_LABELS; print('regions OK')" && python -c "from backend.app.utils.filename_utils import FILENAME_NOISE, DATE_PATTERN; print('filename_utils OK')" && python -c "from backend.app.validators.constants import ID_NUMBER_PATTERN; print('constants OK')" && pytest backend/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/mappings/regions.py` 存在且包含 `REGION_LABELS`
    - `backend/app/utils/filename_utils.py` 存在且包含 `FILENAME_NOISE`、`DATE_PATTERN`、`infer_company_name_from_filename`
    - `backend/app/validators/constants.py` 存在且包含 `ID_NUMBER_PATTERN`、`NON_MAINLAND_ID_NUMBER_PATTERN`
    - `grep -rn "^REGION_LABELS" backend/app/ --include="*.py"` 只在 `mappings/regions.py` 中出现
    - `grep -rn "^FILENAME_NOISE" backend/app/ --include="*.py"` 只在 `utils/filename_utils.py` 中出现
    - `grep -rn "^ID_NUMBER_PATTERN" backend/app/ --include="*.py"` 只在 `validators/constants.py` 中出现
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>3 组重复常量各自合并到单一共享模块，所有调用方统一 import，测试全通过</done>
</task>

<task type="auto">
  <name>Task 2: 自助查询端点添加显式认证依赖</name>
  <files>backend/app/api/v1/employees.py</files>
  <read_first>
    backend/app/api/v1/employees.py,
    backend/app/api/v1/router.py,
    backend/app/dependencies.py
  </read_first>
  <action>
    Per D-16: 给 `/employees/self-service/query` 端点添加显式的 `_user=Depends(require_authenticated_user)` 参数。

    修改 `backend/app/api/v1/employees.py` 中的 `employee_self_service_query_endpoint` 函数签名：

    Before:
    ```python
    def employee_self_service_query_endpoint(
        payload: EmployeeSelfServiceQueryInput = Body(...),
        db: Session = Depends(get_db),
    ):
    ```

    After:
    ```python
    def employee_self_service_query_endpoint(
        payload: EmployeeSelfServiceQueryInput = Body(...),
        db: Session = Depends(get_db),
        _user=Depends(require_authenticated_user),
    ):
    ```

    确认 `require_authenticated_user` 已在文件顶部 import（当前 line 10 已有）。

    说明：虽然 router 级别的 `require_role("admin", "hr")` 已提供安全保护，但添加端点级别的 `require_authenticated_user` 保持代码一致性，与该文件中其他所有端点的模式统一。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj && grep -A 5 "self-service/query" backend/app/api/v1/employees.py | grep -q "require_authenticated_user" && pytest backend/tests/ -x -q && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `employees.py` 中 `employee_self_service_query_endpoint` 函数签名包含 `_user=Depends(require_authenticated_user)`
    - `pytest backend/tests/ -x -q` 全部通过
  </acceptance_criteria>
  <done>自助查询端点有显式认证依赖，与其他端点保持一致</done>
</task>

</tasks>

<verification>
- `grep -rn "^REGION_LABELS\s*=" backend/app/` 只返回 `mappings/regions.py`
- `grep -rn "^FILENAME_NOISE\s*=" backend/app/` 只返回 `utils/filename_utils.py`
- `grep -rn "^ID_NUMBER_PATTERN\s*=" backend/app/` 只返回 `validators/constants.py`
- self-service 端点有 require_authenticated_user
- `pytest backend/tests/ -x -q` 全部通过
</verification>

<success_criteria>
- 常量不再重复定义，任何修改只需改一处
- 自助查询端点认证一致
- 全部后端测试通过
- 双模板导出功能不受影响（常量合并为纯重构，不改业务逻辑）
</success_criteria>

<output>
After completion, create `.planning/phases/13-foundation-deploy-compat/13-03-SUMMARY.md`
</output>
