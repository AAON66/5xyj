---
phase: 01-export-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/exporters/export_utils.py
  - backend/app/exporters/salary_exporter.py
  - backend/app/exporters/tool_exporter.py
  - backend/app/exporters/template_exporter.py
  - backend/app/exporters/__init__.py
  - backend/tests/test_salary_regression.py
  - backend/tests/test_api_compatibility.py
autonomous: true
requirements: [EXPORT-02, EXPORT-03]

must_haves:
  truths:
    - "Salary template export output is byte-identical to before the split"
    - "Exporter code is in three separate files with no shared mutable state"
    - "All existing tests still pass after the split"
    - "Public API surface (imports from backend.app.exporters) is unchanged"
  artifacts:
    - path: "backend/app/exporters/export_utils.py"
      provides: "Shared pure functions and constants"
      contains: "_amount, _normalize_export_text, _merge_export_records, EXPORT_AMOUNT_FIELDS"
    - path: "backend/app/exporters/salary_exporter.py"
      provides: "Salary template writing logic"
      contains: "_rewrite_salary_sheet, _salary_row_values, SALARY_HEADERS"
    - path: "backend/app/exporters/tool_exporter.py"
      provides: "Tool template writing logic"
      contains: "_rewrite_tool_sheet, _tool_row_values, TOOL_HEADERS"
    - path: "backend/tests/test_salary_regression.py"
      provides: "Salary regression snapshot test"
      contains: "test_salary_export_regression_snapshot"
    - path: "backend/tests/test_api_compatibility.py"
      provides: "Public API import compatibility guard"
      contains: "test_public_api_imports"
  key_links:
    - from: "backend/app/exporters/template_exporter.py"
      to: "export_utils.py, salary_exporter.py, tool_exporter.py"
      via: "re-exports for backward compatibility"
      pattern: "from backend.app.exporters.salary_exporter import"
    - from: "backend/app/exporters/__init__.py"
      to: "template_exporter.py"
      via: "public API unchanged"
      pattern: "export_dual_templates"
---

<objective>
Split the 1160-line monolithic template_exporter.py into three focused modules (salary_exporter.py, tool_exporter.py, export_utils.py) and create a Salary regression snapshot test that locks the current perfect output before any Tool template changes. Guard the public API surface with an explicit compatibility test.

Purpose: Protect the working Salary export from accidental breakage during Tool template fixes (Plan 02), and make the codebase maintainable by separating concerns. Addresses review concern: mechanical split can break behavior through import cycles.
Output: Three new exporter modules, updated template_exporter.py as a thin facade, Salary regression tests, and an API compatibility test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/exporters/template_exporter.py
@backend/app/exporters/__init__.py
@backend/app/services/batch_export_service.py
@backend/tests/test_template_exporter.py
@backend/tests/test_template_exporter_regression.py
@backend/app/models/enums.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared utilities into export_utils.py, salary logic into salary_exporter.py, tool logic into tool_exporter.py</name>
  <files>
    backend/app/exporters/export_utils.py,
    backend/app/exporters/salary_exporter.py,
    backend/app/exporters/tool_exporter.py,
    backend/app/exporters/template_exporter.py,
    backend/app/exporters/__init__.py
  </files>
  <read_first>
    backend/app/exporters/template_exporter.py,
    backend/app/exporters/__init__.py,
    backend/app/services/batch_export_service.py,
    backend/tests/test_template_exporter.py,
    backend/tests/test_template_exporter_regression.py
  </read_first>
  <action>
    **BEFORE any changes:** Capture the current public API surface. Run:
    ```
    python -c "import backend.app.exporters as e; print(sorted([x for x in dir(e) if not x.startswith('_')]))"
    ```
    Save this output — the same command must produce the same list after the split.

    Create three new files by extracting from template_exporter.py:

    **Helper ownership rule (addresses review concern about helper placement):**
    - Pure shared helpers (used by both salary and tool) go in export_utils.py
    - Salary-only helpers (_resolved_personal_large_medical, _resolved_company_medical, _resolved_company_large_medical) go in salary_exporter.py
    - Tool-only helpers go in tool_exporter.py
    - If a helper is currently only used by salary but will be needed by tool after decoupling in Plan 02, place it in export_utils.py now to avoid a future move. Specifically: _resolved_personal_large_medical, _resolved_company_medical, _resolved_company_large_medical, _resolved_housing_fund_values, _resolved_personal_social_burden, _resolved_personal_housing_burden — ALL go in export_utils.py since Plan 02 will need them in tool_exporter.py.

    **export_utils.py** - All shared constants and pure utility functions:
    - Constants: EXPORT_AMOUNT_FIELDS, EXPORT_TEXT_FIELDS, SOCIAL_AMOUNT_FIELDS, SOURCE_KIND_BY_AMOUNT_FIELD, HOUSING_SOURCE_KIND, SOCIAL_SOURCE_KIND, REGION_LABELS, HEADER_LIKE_PERSON_VALUES, ID_NUMBER_PATTERN, NON_MAINLAND_ID_NUMBER_PATTERN, HEADER_NORMALIZATION_TRANSLATION, HOUSING_METADATA_HEADER_MARKERS, EXPLICIT_HOUSING_COMPANY_HEADER_TOKENS, EXPLICIT_HOUSING_PERSONAL_HEADER_TOKENS, EXPLICIT_HOUSING_COMBINED_HEADER_TOKENS, XIAMEN_MEDICAL_COMPANY_HEADER, XIAMEN_MATERNITY_HEADER, HOUSING_BURDEN_CANDIDATE_RATIO, SOCIAL_BURDEN_CANDIDATE_RATIO
    - Utility functions: _amount, _normalize_export_text, _normalize_export_header, _normalize_id_number, _decimal_from_raw_value, _region_label, _is_exportable_record, _has_any_business_value, _is_inferred_housing_only_record, _has_explicit_housing_breakdown, _has_explicit_housing_amount_key (the second version at line 1138, which supersedes the first at line 1123)
    - Merge functions: _merge_export_records, _export_merge_key, _copy_export_record, _merge_two_records, _merge_raw_payloads, _merge_text_value, _merge_amount_value, _should_accumulate_amount, _source_signatures_for_kind, _source_signature_count_for_kind, _source_key_for_kind, _social_burden_source_key, _housing_burden_source_key
    - Sheet rewrite functions: _rewrite_sheet_in_place, _snapshot_row, _apply_row_snapshot, _populate_output_row, _detect_existing_data_last_row
    - Housing/burden functions: _resolved_housing_fund_values, _build_social_burden_context, _build_housing_burden_context, _resolved_personal_social_burden, _resolved_personal_housing_burden, _select_burden_allowance
    - Resolved amount helpers (shared by salary and tool after Plan 02 decoupling): _resolved_personal_large_medical, _resolved_company_medical, _resolved_company_large_medical
    - Xiamen extraction: _extract_xiamen_social_amounts, _extract_xiamen_source_amount
    - Dataclasses: ExportArtifactResult, DualTemplateExportResult
    - Exception: ExportServiceError
    - Template resolution: _resolve_template_path, _discover_template_candidates, _discover_template_candidates_from_manifest

    **salary_exporter.py** - Salary-specific logic:
    - SALARY_HEADERS constant (the list of 18 header strings)
    - SALARY_SHEET_HEADER_ROW = 1
    - SALARY_DATA_START_ROW = 2
    - _rewrite_salary_sheet function
    - _salary_row_values function
    - Import shared functions from export_utils (including _resolved_personal_large_medical, _resolved_company_medical, _resolved_company_large_medical)

    **tool_exporter.py** - Tool-specific logic:
    - TOOL_HEADERS constant (the list of 41 header entries including None placeholders)
    - TOOL_SHEET_HEADER_ROW = 6
    - TOOL_DATA_START_ROW = 7
    - _rewrite_tool_sheet function
    - _tool_row_values function
    - Import _salary_row_values from salary_exporter (since _tool_row_values calls it — Plan 02 will decouple this)
    - Import shared functions from export_utils

    **template_exporter.py** becomes a thin facade (~40 lines):
    - Import and re-export: ExportServiceError, ExportArtifactResult, DualTemplateExportResult from export_utils
    - Import _rewrite_salary_sheet from salary_exporter
    - Import _rewrite_tool_sheet from tool_exporter
    - Keep only the export_dual_templates function and _export_template_variant function
    - All other functions are removed (now live in their respective modules)

    **__init__.py** remains unchanged — it already imports from template_exporter.py which still exports the public API.

    CRITICAL RULES:
    - Do NOT modify any function logic — this is a pure mechanical extraction
    - Do NOT rename any functions or change any signatures
    - Do NOT change how _tool_row_values calls _salary_row_values — preserve the coupling for now (Plan 02 will fix it)
    - Ensure no circular imports: export_utils has zero local imports, salary_exporter imports from export_utils, tool_exporter imports from export_utils and salary_exporter, template_exporter imports from all three
    - Verify there is NO shared mutable state between modules (all module-level variables are tuples, frozensets, or compiled regex — immutable)

    **AFTER the split:** Re-run the public API surface check:
    ```
    python -c "import backend.app.exporters as e; print(sorted([x for x in dir(e) if not x.startswith('_')]))"
    ```
    Output must match the pre-split list exactly. If any symbol is missing, fix the facade re-exports.
  </action>
  <verify>
    <automated>cd D:/execl_mix && python -m pytest backend/tests/test_template_exporter.py backend/tests/test_template_exporter_regression.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def " backend/app/exporters/export_utils.py` returns 25+ (shared functions extracted)
    - `grep -c "def " backend/app/exporters/salary_exporter.py` returns 2-4 (salary-specific functions)
    - `grep -c "def " backend/app/exporters/tool_exporter.py` returns 2-3 (tool-specific functions)
    - `wc -l backend/app/exporters/template_exporter.py` returns under 80 lines (thin facade)
    - `grep "from backend.app.exporters" backend/app/exporters/salary_exporter.py` shows import from export_utils only
    - `grep "from backend.app.exporters" backend/app/exporters/tool_exporter.py` shows imports from export_utils and salary_exporter only
    - `python -c "from backend.app.exporters import export_dual_templates, ExportServiceError, DualTemplateExportResult, ExportArtifactResult; print('OK')"` prints OK
    - All existing tests pass with zero failures
    - Public API surface (dir(backend.app.exporters)) is identical before and after
  </acceptance_criteria>
  <done>
    Three new module files exist. template_exporter.py is under 80 lines. All existing tests pass unchanged. No function logic was modified. Public API (export_dual_templates, ExportServiceError, DualTemplateExportResult, ExportArtifactResult) still importable from backend.app.exporters. Helper ownership is clear: shared helpers in export_utils, salary-only in salary_exporter, tool-only in tool_exporter.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Salary regression snapshot tests and API compatibility guard</name>
  <files>
    backend/tests/test_salary_regression.py,
    backend/tests/test_api_compatibility.py
  </files>
  <read_first>
    backend/tests/test_template_exporter.py,
    backend/tests/test_template_exporter_regression.py,
    backend/tests/support/export_fixtures.py,
    backend/app/exporters/salary_exporter.py
  </read_first>
  <behavior>
    - Test 1 (test_salary_header_row_matches_expected): Salary template header row cells A1-R1 match SALARY_HEADERS entries.
    - Test 2 (test_salary_data_values_match_row_builder): For each data row, cell values match _salary_row_values output position-by-position.
    - Test 3 (test_salary_row_count_matches_input): Exactly N data rows written for N input records.
    - Test 4 (test_salary_regression_with_housing_fund): Export with a record that has housing fund values (personal + company). Verify housing columns (H, P in the 18-column layout) contain the correct housing amounts. Addresses review concern: edge case with housing.
    - Test 5 (test_salary_regression_with_burden_values): Export with records that trigger burden calculation (personal_social_burden, personal_housing_burden). Verify burden columns (Q, R) are non-zero and correct. Addresses review concern: edge case with burden.
    - Test 6 (test_salary_regression_multi_region): Export with records from at least 2 different regions (Shenzhen + Guangzhou if available). Verify region-specific resolution does not break. Addresses review concern: single sample too narrow.
    - Test 7 (test_public_api_imports): Assert that `from backend.app.exporters import export_dual_templates, ExportServiceError, DualTemplateExportResult, ExportArtifactResult` works. Assert these are callable/classes. Addresses review concern: public API compatibility guard.
    - Test 8 (test_public_api_no_missing_symbols): Import backend.app.exporters, check that a hardcoded list of expected public symbols exists in dir(). Addresses review concern: API surface guard.
  </behavior>
  <action>
    **test_salary_regression.py** — Create 6 tests:

    Tests 1-3 (basic regression): Use Shenzhen sample ('深圳创造欢乐') with min_records=2. Use existing test infrastructure (require_sample_workbook, resolve_required_export_templates, build_normalized_models, standardize_workbook). Write artifacts to ROOT_DIR / '.test_artifacts' / 'salary_regression'.

    Test 4 (housing edge case): If housing fund data is available in Shenzhen samples, use those records. Otherwise, construct a mock NormalizedModel with housing_fund_personal and housing_fund_company set to non-zero Decimal values. Verify salary output includes correct housing amounts.

    Test 5 (burden edge case): Use records that have enough data to trigger burden calculation. If burden context requires multiple records from the same company/region, include at least 3 records. Verify the burden columns contain expected values.

    Test 6 (multi-region): Load samples from two different regions. Export both together. Verify all records appear in output regardless of region.

    Reuse and extend existing test patterns from test_template_exporter_regression.py where possible, rather than inventing a new testing style. Addresses review concern about parallel testing styles.

    **test_api_compatibility.py** — Create 2 tests:

    Test 7: Import all public symbols. Assert export_dual_templates is callable, ExportServiceError is a subclass of Exception, DualTemplateExportResult and ExportArtifactResult are importable.

    Test 8: Assert the full list of expected public names exists in the module's namespace. Hardcode the list based on what was captured in Task 1's pre-split check.

    CRITICAL: All tests must pass NOW (green) since they lock existing behavior. If any test fails, it means the split in Task 1 broke something — investigate and fix the split, not the test.
  </action>
  <verify>
    <automated>cd D:/execl_mix && python -m pytest backend/tests/test_salary_regression.py backend/tests/test_api_compatibility.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_salary_regression.py` returns 6
    - `grep -c "def test_" backend/tests/test_api_compatibility.py` returns 2
    - `grep "SALARY_HEADERS" backend/tests/test_salary_regression.py` finds at least one match
    - `grep "_salary_row_values" backend/tests/test_salary_regression.py` finds at least one match
    - `grep "housing" backend/tests/test_salary_regression.py` finds at least one match (housing edge case covered)
    - `grep "burden" backend/tests/test_salary_regression.py` finds at least one match (burden edge case covered)
    - `grep "export_dual_templates" backend/tests/test_api_compatibility.py` finds at least one match
    - All 8 tests pass green on first run
  </acceptance_criteria>
  <done>
    Six Salary regression tests exist and pass. They lock: (1) header row content, (2) data row values match _salary_row_values output, (3) exact row count, (4) housing fund edge case, (5) burden calculation edge case, (6) multi-region support. Two API compatibility tests guard the public import surface. Any future change that breaks Salary export or the public API will be caught.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m pytest backend/tests/ -x -v` — all tests pass (existing + new regression + API compatibility tests)
2. `python -c "from backend.app.exporters import export_dual_templates; print('public API OK')"` — backward compatibility confirmed
3. `wc -l backend/app/exporters/template_exporter.py` — under 80 lines (thin facade)
4. `wc -l backend/app/exporters/export_utils.py backend/app/exporters/salary_exporter.py backend/app/exporters/tool_exporter.py` — total roughly matches original 1160 lines
</verification>

<success_criteria>
1. Exporter code lives in 3 separate modules (export_utils, salary_exporter, tool_exporter) with template_exporter as thin facade
2. Zero shared mutable state between modules
3. Helper ownership is explicit: shared helpers in export_utils, template-specific in their modules
4. All existing tests pass without modification
5. 6 new Salary regression tests pass, covering: header structure, data values, row count, housing edge case, burden edge case, multi-region
6. 2 new API compatibility tests guard the public import surface
7. Public API (export_dual_templates) is backward-compatible — no changes needed in batch_export_service.py or any API route
</success_criteria>

<output>
After completion, create `.planning/phases/01-export-stabilization/01-01-SUMMARY.md`
</output>
