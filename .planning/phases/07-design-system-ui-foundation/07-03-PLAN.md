---
phase: 07-design-system-ui-foundation
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - frontend/src/pages/DataManagement.tsx
  - frontend/src/pages/Employees.tsx
  - frontend/src/pages/EmployeeCreate.tsx
  - frontend/src/pages/Imports.tsx
  - frontend/src/pages/ImportBatchDetail.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-04]

must_haves:
  truths:
    - "数据管理页使用 Ant Table+Select+DatePicker 筛选和展示数据"
    - "员工管理页使用 Ant Table+Modal+Drawer 进行 CRUD 操作"
    - "批次管理页使用 Ant Table+Tag 展示导入批次列表"
    - "五个页面均无自定义 CSS 类"
  artifacts:
    - path: "frontend/src/pages/DataManagement.tsx"
      provides: "Ant Design 数据管理页"
      contains: "Table"
    - path: "frontend/src/pages/Employees.tsx"
      provides: "Ant Design 员工管理页"
      contains: "Drawer"
    - path: "frontend/src/pages/Imports.tsx"
      provides: "Ant Design 批次管理页"
      contains: "Tag"
  key_links:
    - from: "frontend/src/pages/DataManagement.tsx"
      to: "useSearchParams"
      via: "URL 状态持久化"
      pattern: "useSearchParams"
---

<objective>
重写五个数据密集型页面为 Ant Design 组件

Purpose: DataManagement、Employees、Imports 是 HR 日常高频使用页面，数据表格和筛选交互的质量直接影响工作效率。

Output:
- DataManagement.tsx 使用 Ant Table/Select/DatePicker/Card
- Employees.tsx 使用 Ant Table/Modal/Drawer/Form/Button
- EmployeeCreate.tsx 使用 Ant Form/Input/Select/Button
- Imports.tsx 使用 Ant Table/Tag/Button
- ImportBatchDetail.tsx 使用 Ant Descriptions/Table/Tag
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-design-system-ui-foundation/07-CONTEXT.md
@.planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
@.planning/phases/07-design-system-ui-foundation/07-01-SUMMARY.md

@frontend/src/pages/DataManagement.tsx
@frontend/src/pages/Employees.tsx
@frontend/src/pages/EmployeeCreate.tsx
@frontend/src/pages/Imports.tsx
@frontend/src/pages/ImportBatchDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 DataManagement + EmployeeCreate — 筛选表格和表单页</name>
  <files>
    frontend/src/pages/DataManagement.tsx,
    frontend/src/pages/EmployeeCreate.tsx
  </files>
  <read_first>
    frontend/src/pages/DataManagement.tsx,
    frontend/src/pages/EmployeeCreate.tsx,
    .planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    **DataManagement.tsx 重写**（per D-01, D-12, D-15, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "数据管理"
    - 筛选卡片: `Card` 包裹筛选控件:
      - `Row gutter={[16, 16]}` 排列 `Col` 包裹的筛选组件
      - 地区筛选: `Select` placeholder="选择地区" allowClear options 从 API 加载
      - 公司筛选: `Select` placeholder="选择公司" allowClear options 从 API 加载
      - 月份筛选: `DatePicker` picker="month" 或 `Select`
      - 搜索: `Input.Search` placeholder="搜索姓名或工号" onSearch
      - 筛选按钮: `Button type="primary"` "查询" + `Button` "重置"
    - 数据表格: `Card` 包裹 `Table`:
      - `columns` 数组定义所有列（dataIndex、title、sorter、render）
      - 金额列右对齐 `align: 'right'`
      - 身份证号列使用脱敏显示
      - 状态列使用 `Tag` 组件（匹配/未匹配/异常）
      - `pagination` 配置: `{ current, pageSize, total, showSizeChanger, showTotal }`
      - `loading` 使用 Skeleton（per D-09）
      - `rowKey` 设置为记录的唯一标识
    - 空状态: `Empty` 组件（per D-11）

    关键保留逻辑:
    - `useSearchParams` URL 状态持久化完整保留（per Phase 06 决策）
    - 筛选变更重置分页到第 0 页的逻辑保留
    - API 调用和 useEffect 数据加载逻辑保留
    - 移除所有自定义 CSS 类

    **EmployeeCreate.tsx 重写**（per D-15, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "新增员工"
    - 表单卡片: `Card` 包裹 `Form`:
      - `Form` layout="vertical"
      - `Form.Item` label="姓名" name="name" rules=[{required: true}] -> `Input`
      - `Form.Item` label="工号" name="employee_id" rules=[{required: true}] -> `Input`
      - `Form.Item` label="身份证号" name="id_number" rules=[{required: true}] -> `Input`
      - `Form.Item` label="所属公司" name="company" -> `Select` 或 `Input`
      - `Form.Item` label="地区" name="region" -> `Select`
      - 底部: `Space` 包裹 `Button type="primary"` "保存" + `Button` "返回"
    - 操作成功: `message.success('员工创建成功')`（per D-13）

    关键保留逻辑:
    - API 提交逻辑保留
    - 表单验证逻辑保留
    - 创建成功后路由跳转保留
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - DataManagement.tsx 包含 `Table` 和 `Select` 导入（从 antd）
    - DataManagement.tsx 包含 `useSearchParams`（保留 URL 状态持久化）
    - DataManagement.tsx 不包含 `className="panel-` 或 `PageContainer`
    - DataManagement.tsx 包含 `pagination`
    - EmployeeCreate.tsx 包含 `Form` 和 `Form.Item` 导入
    - EmployeeCreate.tsx 不包含 `PageContainer`
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>DataManagement 使用 Ant Table+Select+DatePicker 筛选表格，EmployeeCreate 使用 Ant Form 表单，业务逻辑完整保留</done>
</task>

<task type="auto">
  <name>Task 2: 重写 Employees + Imports + ImportBatchDetail — CRUD 和批次管理页</name>
  <files>
    frontend/src/pages/Employees.tsx,
    frontend/src/pages/Imports.tsx,
    frontend/src/pages/ImportBatchDetail.tsx
  </files>
  <read_first>
    frontend/src/pages/Employees.tsx,
    frontend/src/pages/Imports.tsx,
    frontend/src/pages/ImportBatchDetail.tsx,
    .planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    **Employees.tsx 重写**（per D-01, D-12, D-15, D-17, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "员工主档" + `Button type="primary"` "新增员工"（右对齐）
    - 搜索栏: `Card` 包裹 `Row` 排列筛选控件（搜索框、公司、地区 Select）
    - 员工表格: `Card` 包裹 `Table`:
      - columns: 姓名、工号、身份证号（脱敏）、公司、地区、操作
      - 操作列: `Space` 包裹 `Button` type="link" "编辑" + `Button` type="link" danger "删除"
      - pagination、loading、rowKey 配置
    - 编辑抽屉: `Drawer` 组件（per D-17 — 复杂表单用 Drawer）:
      - `Drawer` title="编辑员工" width={480} open={drawerVisible} onClose
      - 内嵌 `Form` layout="vertical"，包含姓名、工号、身份证号、公司、地区字段
      - 底部 `Button type="primary"` "保存" + `Button` "取消"
    - 删除确认: `Modal.confirm` 组件（per D-17）:
      - title="确认删除"
      - content="此操作不可撤销，确定要删除该记录吗？"
      - okText="确认删除" okType="danger"
    - 批量导入（如果现有代码有）: `Upload` 组件或 `Modal` 包裹上传
    - 操作成功/失败: `message.success` / `message.error`（per D-13）

    关键保留逻辑:
    - CRUD API 调用逻辑完整保留
    - 搜索、筛选、分页逻辑保留
    - 批量导入逻辑保留

    **Imports.tsx 重写**（per D-12, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "批次管理"
    - 批次表格: `Card` 包裹 `Table`:
      - columns: 批次名称、文件名、记录数、导入时间、操作人、状态
      - 状态列: `Tag` 组件，颜色映射: success="已完成"/processing="处理中"/error="失败"/default="待处理"
      - 批次名称列: 可点击链接到 `/imports/:batchId` 详情页
      - pagination 配置
      - rowKey="batch_id" 或对应唯一字段

    关键保留逻辑:
    - API 加载批次列表逻辑保留
    - 路由导航逻辑保留

    **ImportBatchDetail.tsx 重写**（per D-12, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} + 返回按钮（`Button` icon={ArrowLeftOutlined} onClick 回到 /imports）
    - 批次信息卡片: `Card` 包裹 `Descriptions`:
      - `Descriptions` bordered column={2}
      - 字段: 批次名称、文件名、导入时间、操作人、记录数、状态（Tag）
    - 记录明细表格: `Card` 标题="记录明细" 包裹 `Table`:
      - columns 定义每条记录的字段
      - pagination 配置
      - rowKey 配置

    关键保留逻辑:
    - useParams 获取 batchId 的逻辑保留
    - API 加载批次详情逻辑保留
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Employees.tsx 包含 `Drawer` 导入（per D-17）
    - Employees.tsx 包含 `Modal.confirm` 或 `Modal`（per D-17）
    - Employees.tsx 包含 `Table` 导入
    - Employees.tsx 不包含 `PageContainer`
    - Imports.tsx 包含 `Table` 和 `Tag` 导入
    - Imports.tsx 不包含 `PageContainer`
    - ImportBatchDetail.tsx 包含 `Descriptions` 导入
    - ImportBatchDetail.tsx 不包含 `PageContainer`
    - 三个文件均不包含自定义 CSS 类名（不含 `className="panel-` 或类似模式）
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>Employees 使用 Ant Table+Drawer+Modal CRUD 操作，Imports 使用 Ant Table+Tag 批次列表，ImportBatchDetail 使用 Ant Descriptions+Table 详情页，业务逻辑完整保留</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` 成功
- `cd frontend && npm run lint` 无 error
- 五个页面文件均不包含 PageContainer 引用
- 五个页面文件均不包含自定义 CSS 类名
</verification>

<success_criteria>
- DataManagement 页面使用 Ant Table+Select+DatePicker 筛选表格布局，保留 useSearchParams URL 持久化
- Employees 页面使用 Ant Table+Drawer+Modal CRUD 操作（per D-17 Drawer 用于编辑）
- EmployeeCreate 页面使用 Ant Form 表单布局
- Imports 页面使用 Ant Table+Tag 批次列表
- ImportBatchDetail 页面使用 Ant Descriptions+Table 详情展示
- 所有五个页面的业务逻辑完整保留，无自定义 CSS 类
- TypeScript 编译和 Vite 构建通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-ui-foundation/07-03-SUMMARY.md`
</output>
