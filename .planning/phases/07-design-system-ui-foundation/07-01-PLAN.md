---
phase: 07-design-system-ui-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/theme/index.ts
  - frontend/src/theme/animations.module.css
  - frontend/src/layouts/MainLayout.tsx
  - frontend/src/layouts/MainLayout.module.css
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/components/ApiFeedbackProvider.tsx
  - frontend/src/components/index.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "应用启动后使用飞书蓝主题（#3370FF 主色、#F5F6F7 背景）"
    - "侧边栏为深色（#1F2329），支持折叠/展开"
    - "顶部有白色 Header 栏，左侧面包屑、右侧用户信息和登出按钮"
    - "页面切换时内容区域有淡入+上移动画效果"
    - "导航菜单根据用户角色过滤显示项"
  artifacts:
    - path: "frontend/src/theme/index.ts"
      provides: "飞书风格 Ant Design ThemeConfig"
      exports: ["theme"]
      contains: "colorPrimary"
    - path: "frontend/src/layouts/MainLayout.tsx"
      provides: "Ant Layout + Sider + Header + Content 主布局"
      exports: ["MainLayout"]
      contains: "Layout.Sider"
    - path: "frontend/src/theme/animations.module.css"
      provides: "页面切换动画 CSS 类"
      contains: "pageEnterActive"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/theme/index.ts"
      via: "ConfigProvider theme prop"
      pattern: "ConfigProvider.*theme"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/layouts/MainLayout.tsx"
      via: "ProtectedLayout 使用 MainLayout"
      pattern: "MainLayout"
    - from: "frontend/src/layouts/MainLayout.tsx"
      to: "frontend/src/theme/animations.module.css"
      via: "AnimatedContent 组件引用动画类"
      pattern: "animations"
---

<objective>
建立 Ant Design 5 基础设施和飞书风格主布局

Purpose: 为所有页面迁移提供统一的主题配置、布局框架和动画系统。这是 Phase 7 的基石——后续所有页面重写都依赖此计划创建的 theme、MainLayout 和 ConfigProvider 包装。

Output:
- 安装 antd 和 @ant-design/icons 依赖
- 飞书风格 ThemeConfig（所有 token）
- MainLayout 组件（深色侧边栏 + 白色 Header + 灰色内容区 + 页面切换动画）
- main.tsx 包装 ConfigProvider + AntApp
- App.tsx 使用 MainLayout 替换 AppShell
- ApiFeedbackProvider 改用 App.useApp()
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-system-ui-foundation/07-CONTEXT.md
@.planning/phases/07-design-system-ui-foundation/07-RESEARCH.md
@.planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md

@frontend/src/main.tsx
@frontend/src/App.tsx
@frontend/src/components/AppShell.tsx
@frontend/src/components/ApiFeedbackProvider.tsx
@frontend/src/components/GlobalFeedback.tsx
@frontend/src/components/index.ts
@frontend/package.json

<interfaces>
<!-- 现有导航项和角色过滤逻辑（必须迁移到 Ant Menu items） -->

From frontend/src/components/AppShell.tsx:
```typescript
const navigationItems = [
  { to: '/aggregate', label: '快速融合', hint: '上传后直接触发融合、匹配和双模板导出。' },
  { to: '/dashboard', label: '处理看板', hint: '查看批次状态、异常分布与导出情况。' },
  { to: '/compare', label: '月度对比', hint: '按左右拆分差异视图查看并在线修正。' },
  { to: '/imports', label: '批次管理', hint: '查看原始文件、解析结果和处理详情。' },
  { to: '/mappings', label: '映射修正', hint: '处理低置信度字段映射与人工纠偏。' },
  { to: '/results', label: '校验匹配', hint: '查看校验问题和工号匹配结果。' },
  { to: '/exports', label: '导出结果', hint: '核对薪酬模板与工具表产物。' },
  { to: '/employees', label: '员工主档', hint: '导入、维护与审计员工主数据。' },
  { to: '/data-management', label: '数据管理', hint: '筛选、浏览和审计全量社保数据。', roles: ['admin', 'hr'] },
  { to: '/audit-logs', label: '审计日志', hint: '查看系统操作记录（仅管理员）。', adminOnly: true },
  { to: '/employee/query', label: '员工查询', hint: '免登录员工自助查询入口。' },
];
```

From frontend/src/hooks/apiFeedbackContext.ts (ApiFeedbackProvider 接口):
```typescript
interface ApiFeedbackContextValue {
  pendingRequests: number;
  lastError: ApiClientError | null;
  clearError: () => void;
}
```

From frontend/src/App.tsx (路由结构，ProtectedLayout 需改用 MainLayout):
```typescript
function ProtectedLayout() {
  return (
    <AppShell>
      <Outlet />
    </AppShell>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装依赖 + 创建飞书主题配置 + 页面切换动画</name>
  <files>
    frontend/package.json,
    frontend/src/theme/index.ts,
    frontend/src/theme/animations.module.css
  </files>
  <read_first>
    frontend/package.json,
    .planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    1. 安装 Ant Design 依赖（per D-01, D-10）:
       ```bash
       cd frontend && npm install antd@^5.29.3 @ant-design/icons@^5
       ```

    2. 创建 `frontend/src/theme/index.ts`，导出 ThemeConfig 对象（per D-06, D-07）:
       - 完整复制 UI-SPEC.md 中的 ThemeConfig 代码（包含 token 和 components 两个部分）
       - `colorPrimary: '#3370FF'`（飞书蓝）
       - `colorBgLayout: '#F5F6F7'`（浅灰背景）
       - `colorText: '#1F2329'`（主文本色）
       - Layout.siderBg: '#1F2329'（深色侧边栏）
       - Layout.headerBg: '#FFFFFF'（白色顶栏）
       - Layout.headerHeight: 56
       - Menu 暗色主题 token（darkItemBg、darkItemColor、darkItemSelectedBg 等）
       - Table.headerBg: '#F5F6F7', Table.rowHoverBg: '#F0F5FF'
       - Card.paddingLG: 20, Card.borderRadiusLG: 8
       - Button.primaryShadow: '0 2px 0 rgba(51, 112, 255, 0.1)'
       - 导出类型为 `ThemeConfig`（从 antd 导入）

    3. 创建 `frontend/src/theme/animations.module.css`（per D-08）:
       ```css
       .pageEnter {
         opacity: 0;
         transform: translateY(8px);
       }
       .pageEnterActive {
         opacity: 1;
         transform: translateY(0);
         transition: opacity 300ms ease-in-out, transform 300ms ease-in-out;
       }
       ```
  </action>
  <verify>
    <automated>cd frontend && npm ls antd @ant-design/icons && node -e "const t = require('./src/theme/index.ts'); console.log('SKIP')" || (npx tsc --noEmit --project tsconfig.json 2>&1 | head -20)</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` 的 dependencies 中包含 `"antd": "^5.29.3"`
    - `frontend/package.json` 的 dependencies 中包含 `"@ant-design/icons": "^5"`（或具体 5.x 版本）
    - `frontend/src/theme/index.ts` 文件存在，包含 `export const theme: ThemeConfig`
    - `frontend/src/theme/index.ts` 包含 `colorPrimary: '#3370FF'`
    - `frontend/src/theme/index.ts` 包含 `siderBg: '#1F2329'`
    - `frontend/src/theme/animations.module.css` 包含 `.pageEnter` 和 `.pageEnterActive` 类
  </acceptance_criteria>
  <done>antd 和 @ant-design/icons 已安装，飞书主题配置文件和动画 CSS 已创建，TypeScript 编译无类型错误</done>
</task>

<task type="auto">
  <name>Task 2: 创建 MainLayout + 更新 main.tsx/App.tsx + 重写 ApiFeedbackProvider + 清理旧组件</name>
  <files>
    frontend/src/layouts/MainLayout.tsx,
    frontend/src/layouts/MainLayout.module.css,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/components/ApiFeedbackProvider.tsx,
    frontend/src/components/index.ts
  </files>
  <read_first>
    frontend/src/components/AppShell.tsx,
    frontend/src/components/GlobalFeedback.tsx,
    frontend/src/components/ApiFeedbackProvider.tsx,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/hooks/apiFeedbackContext.ts,
    frontend/src/hooks/index.ts,
    frontend/src/services/aggregateSessionStore.ts,
    frontend/src/theme/index.ts
  </read_first>
  <action>
    1. 创建 `frontend/src/layouts/MainLayout.tsx`（per D-03, D-04, D-05, D-08）:
       - 从 antd 导入 `Layout, Menu, Breadcrumb, Button, Avatar, Dropdown, Alert, Space`
       - 从 @ant-design/icons 导入图标: `UploadOutlined, DashboardOutlined, SwapOutlined, ImportOutlined, ToolOutlined, CheckCircleOutlined, ExportOutlined, TeamOutlined, DatabaseOutlined, AuditOutlined, UserOutlined, LogoutOutlined, MenuFoldOutlined, MenuUnfoldOutlined, UserAddOutlined, SearchOutlined`
       - 解构 `const { Header, Sider, Content } = Layout`
       - 创建 `buildMenuItems(userRole: string)` 函数:
         - 定义全部导航项数组，每项有 key(路径)、icon(Ant图标组件)、label(中文名称)、roles(允许角色数组)
         - 导航项列表（从 AppShell.tsx 迁移）:
           - `/aggregate` — UploadOutlined — '快速融合' — ['admin','hr']
           - `/dashboard` — DashboardOutlined — '处理看板' — ['admin','hr']
           - `/compare` — SwapOutlined — '月度对比' — ['admin','hr']
           - `/imports` — ImportOutlined — '批次管理' — ['admin','hr']
           - `/mappings` — ToolOutlined — '映射修正' — ['admin','hr']
           - `/results` — CheckCircleOutlined — '校验匹配' — ['admin','hr']
           - `/exports` — ExportOutlined — '导出结果' — ['admin','hr']
           - `/employees` — TeamOutlined — '员工主档' — ['admin','hr']
           - `/data-management` — DatabaseOutlined — '数据管理' — ['admin','hr']
           - `/audit-logs` — AuditOutlined — '审计日志' — ['admin']
           - `/employee/query` — SearchOutlined — '员工查询' — ['employee']
         - 过滤: `allItems.filter(item => item.roles.includes(userRole))`
         - 返回 MenuProps['items'] 格式
       - 创建 `AnimatedContent` 组件（per D-08）:
         - 使用 `useLocation()` 监听 `location.pathname` 变化
         - 切换时先设 show=false，下一帧 requestAnimationFrame 设 show=true
         - className 根据 show 切换 `animations.pageEnter` / `animations.pageEnterActive`
         - 从 `../theme/animations.module.css` 导入 animations
       - 创建 `AggregateBanner` 组件:
         - 从现有 GlobalFeedback.tsx 迁移聚合进度横幅逻辑
         - 使用 `useAggregateSession` hook 获取聚合状态
         - running 状态: 显示 `<Alert type="info" banner message="快速聚合正在后台运行" description={进度信息} action={<Space><Link to="/">回到聚合页</Link><Button onClick={cancelAggregateSession}>取消聚合</Button></Space>} />`
         - done 状态: 显示 `<Alert type="success" banner message="快速聚合记录已保留" description={进度信息} action={...} />`
         - 使用 `useApiFeedback` hook 获取 lastError
         - lastError 存在时: 显示 `<Alert type="error" banner closable message={lastError.code} description={lastError.message} onClose={clearError} />`
       - 创建 `MainLayout` 组件:
         - 使用 `useAuth()` 获取 user、logout
         - 使用 `useLocation()` 获取当前路径
         - 使用 `useNavigate()` 处理菜单点击导航
         - 状态: `const [collapsed, setCollapsed] = useState(false)`
         - Layout 最外层 `style={{ minHeight: '100vh' }}`
         - Sider: `theme="dark"`, `collapsible`, `collapsed={collapsed}`, `onCollapse={setCollapsed}`, `width={220}`, `collapsedWidth={64}`
         - Sider 顶部（展开时）: 显示系统名称 "社保公积金管理系统"（白色文字，padding 16px 24px）
         - Sider 顶部（折叠时）: 显示 "社保" 缩写
         - Menu: `theme="dark"`, `mode="inline"`, `selectedKeys={[location.pathname]}`, `items={buildMenuItems(user?.role || '')}`, `onClick={({key}) => navigate(key)}`
         - Header: `style={{ background: '#fff', padding: '0 24px', display: 'flex', alignItems: 'center', justifyContent: 'space-between', borderBottom: '1px solid #DEE0E3' }}`
         - Header 左侧: Breadcrumb（从当前路径生成面包屑项）
         - Header 右侧: Space 包含用户显示名 + Dropdown 菜单（退出登录项，点击调用 logout）
         - Content: `style={{ margin: '0', padding: '24px', background: '#F5F6F7', minHeight: 'calc(100vh - 56px)' }}`
         - Content 内部: 先渲染 AggregateBanner，再渲染 `<AnimatedContent><Outlet /></AnimatedContent>`

    2. 创建 `frontend/src/layouts/MainLayout.module.css`:
       ```css
       .logo { padding: 16px 24px; color: #fff; font-size: 16px; font-weight: 600; white-space: nowrap; overflow: hidden; }
       .logoCollapsed { padding: 16px; color: #fff; font-size: 14px; font-weight: 600; text-align: center; }
       ```

    3. 更新 `frontend/src/main.tsx`（per D-06, D-07）:
       - 从 antd 导入 `ConfigProvider, App as AntApp`
       - 从 `antd/locale/zh_CN` 导入 `zhCN`
       - 从 `./theme` 导入 `theme`
       - 移除 `import './styles.css'`（暂时保留文件本身，后续计划删除）
       - 移除 `ApiFeedbackProvider` 导入和包装
       - 包装顺序（从外到内）: `React.StrictMode > ConfigProvider(theme, componentSize="small", locale={zhCN}) > AntApp > BrowserRouter > AuthProvider > ApiFeedbackProvider > App`
       - 注意: ApiFeedbackProvider 仍然保留，但移到 AntApp 内部以便使用 App.useApp()

    4. 更新 `frontend/src/App.tsx`:
       - 将 `AppShell` 导入替换为 `import { MainLayout } from './layouts/MainLayout'`
       - ProtectedLayout 函数改为: `return <MainLayout />`（MainLayout 内部已有 Outlet）
       - 移除 `<AppShell>` 和 `<Outlet />` 包装
       - 删除 `AuthRouteState` 组件中的自定义 CSS 类（`auth-route-state` 等），改用 Ant Design `Spin` 组件:
         ```typescript
         import { Spin } from 'antd';
         function AuthRouteState({ message }: { message: string }) {
           return <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh', background: '#F5F6F7' }}><Spin tip={message} size="large"><div style={{ padding: 50 }} /></Spin></div>;
         }
         ```

    5. 更新 `frontend/src/components/ApiFeedbackProvider.tsx`:
       - 保留现有 context 逻辑（pendingRequests、lastError、clearError 不变）
       - 在 Provider 内部增加 Ant App.useApp() 集成:
         - 当 lastError 变化时，调用 `message.error(lastError.message)` 显示错误 toast（per D-13）
       - 注意: ApiFeedbackProvider 必须在 AntApp 内部才能使用 App.useApp()

    6. 更新 `frontend/src/components/index.ts`:
       - 移除 `AppShell`、`GlobalFeedback`、`PageContainer`、`SectionState`、`SurfaceNotice` 的导出
       - 保留 `ApiFeedbackProvider` 和 `AuthProvider` 导出
       - 注意: 暂时不删除旧组件文件（页面仍可能引用），后续计划会清理
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/layouts/MainLayout.tsx` 文件存在
    - MainLayout.tsx 包含 `Layout.Sider` 或 `Sider`
    - MainLayout.tsx 包含 `theme="dark"`
    - MainLayout.tsx 包含 `collapsible`
    - MainLayout.tsx 包含 `width={220}` 或 `width: 220`
    - MainLayout.tsx 包含 `Breadcrumb`
    - MainLayout.tsx 包含 `buildMenuItems`
    - MainLayout.tsx 包含 `AnimatedContent`
    - MainLayout.tsx 包含 `useAggregateSession` 或 `AggregateBanner`
    - `frontend/src/main.tsx` 包含 `ConfigProvider`
    - `frontend/src/main.tsx` 包含 `componentSize="small"` 或 `componentSize: 'small'`
    - `frontend/src/main.tsx` 包含 `zhCN`
    - `frontend/src/main.tsx` 不包含 `import './styles.css'`（或已注释）
    - `frontend/src/App.tsx` 包含 `MainLayout`
    - `frontend/src/App.tsx` 不包含 `AppShell`
    - `frontend/src/components/index.ts` 不包含 `AppShell`
    - `frontend/src/components/index.ts` 不包含 `GlobalFeedback`
    - TypeScript 编译无错误: `npx tsc --noEmit` 退出码为 0
  </acceptance_criteria>
  <done>MainLayout（深色侧边栏+白色Header+灰色内容区+动画切换+聚合横幅）已创建，main.tsx 包装了 ConfigProvider+AntApp+zhCN 中文+compact 模式，App.tsx 使用 MainLayout 替换 AppShell，TypeScript 编译通过</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` 成功（TypeScript 编译 + Vite 构建）
- `cd frontend && npm run lint` 无 error（warning 可接受）
- 手动验证: 启动 `npm run dev`，登录后看到深色侧边栏、白色 Header、浅灰背景
</verification>

<success_criteria>
- antd@5.x 和 @ant-design/icons@5.x 已安装
- 飞书主题 token 完整配置（主色 #3370FF、背景 #F5F6F7、深色侧边栏 #1F2329）
- MainLayout 实现侧边栏折叠、面包屑导航、用户退出、角色过滤菜单、页面切换动画、聚合进度横幅
- ConfigProvider 包装全局，compact 模式，中文 locale
- TypeScript 和 Vite 构建通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-ui-foundation/07-01-SUMMARY.md`
</output>
