---
phase: 07-design-system-ui-foundation
plan: 04
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - frontend/src/pages/EmployeeSelfService.tsx
  - frontend/src/pages/Portal.tsx
  - frontend/src/pages/Results.tsx
  - frontend/src/pages/Exports.tsx
  - frontend/src/pages/Mappings.tsx
  - frontend/src/pages/Compare.tsx
  - frontend/src/pages/AuditLogs.tsx
  - frontend/src/pages/Workspace.tsx
  - frontend/src/pages/NotFound.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-04]

must_haves:
  truths:
    - "九个页面全部使用 Ant Design 组件，无自定义 CSS 类"
    - "员工自助查询页使用 Ant Form + Result/Descriptions 展示查询结果"
    - "所有表格页使用 Ant Table（size=small 紧凑模式）"
    - "NotFound 页面使用 Ant Result status=404"
  artifacts:
    - path: "frontend/src/pages/EmployeeSelfService.tsx"
      provides: "Ant Design 员工自助查询页"
      contains: "Form"
    - path: "frontend/src/pages/NotFound.tsx"
      provides: "Ant Design 404 页面"
      contains: "Result"
    - path: "frontend/src/pages/Compare.tsx"
      provides: "Ant Design 月度对比页"
      contains: "Tabs"
  key_links:
    - from: "frontend/src/pages/EmployeeSelfService.tsx"
      to: "useAuth hook"
      via: "员工身份验证"
      pattern: "useAuth|verifyEmployee"
---

<objective>
重写剩余九个页面为 Ant Design 组件，完成全量迁移

Purpose: 完成 Phase 7 的 D-01 一次性迁移承诺——所有页面统一使用 Ant Design 5 组件，无任何遗留自定义组件。

Output:
- EmployeeSelfService/Portal — 员工端页面
- Results/Exports/Mappings — 数据处理页面
- Compare — 月度对比页面
- AuditLogs — 审计日志页面
- Workspace (admin/hr) — 工作台页面
- NotFound — 404 页面
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-design-system-ui-foundation/07-CONTEXT.md
@.planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
@.planning/phases/07-design-system-ui-foundation/07-01-SUMMARY.md

@frontend/src/pages/EmployeeSelfService.tsx
@frontend/src/pages/Portal.tsx
@frontend/src/pages/Results.tsx
@frontend/src/pages/Exports.tsx
@frontend/src/pages/Mappings.tsx
@frontend/src/pages/Compare.tsx
@frontend/src/pages/AuditLogs.tsx
@frontend/src/pages/Workspace.tsx
@frontend/src/pages/NotFound.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写员工端 + 数据处理页面（EmployeeSelfService + Portal + Results + Exports + Mappings）</name>
  <files>
    frontend/src/pages/EmployeeSelfService.tsx,
    frontend/src/pages/Portal.tsx,
    frontend/src/pages/Results.tsx,
    frontend/src/pages/Exports.tsx,
    frontend/src/pages/Mappings.tsx
  </files>
  <read_first>
    frontend/src/pages/EmployeeSelfService.tsx,
    frontend/src/pages/Portal.tsx,
    frontend/src/pages/Results.tsx,
    frontend/src/pages/Exports.tsx,
    frontend/src/pages/Mappings.tsx,
    .planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    **EmployeeSelfService.tsx 重写**（per D-15, D-13, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "员工社保查询"
    - 查询表单: `Card` 包裹 `Form` layout="vertical":
      - Form.Item name="employee_id" label="工号" -> `Input`
      - Form.Item name="id_number" label="身份证号" -> `Input`
      - Form.Item name="name" label="姓名" -> `Input`
      - `Button type="primary"` htmlType="submit" "查询"
    - 查询结果区域（查询成功后显示）:
      - 如果已有数据: 使用 `Card` + `Descriptions` 展示个人信息
      - 如果无数据: `Empty` description="未找到匹配的社保记录"
      - 缴费明细: `Table` 展示月度社保明细
    - 加载中: `Skeleton`（per D-09）
    - 错误: `Result` status="error"（per D-11 相关）

    关键保留: useAuth hook、API 调用逻辑、查询参数处理

    **Portal.tsx 重写**（per D-12, UI-01）:

    页面结构:
    - 使用 `Card` + `Descriptions` 展示员工个人社保信息
    - 使用 `Table` 展示缴费历史明细
    - 加载中: `Skeleton`
    - 空状态: `Empty`

    关键保留: 数据加载逻辑

    **Results.tsx 重写**（per D-12, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "校验匹配"
    - 如果有 Tabs 切换视图: 使用 Ant `Tabs` 组件
    - 结果表格: `Card` 包裹 `Table`:
      - 状态列使用 `Tag`（color 映射: success/warning/error）
      - pagination 配置
    - 操作: `Button` 组件

    关键保留: API 调用、筛选、分页逻辑

    **Exports.tsx 重写**（per D-12, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "导出结果"
    - 导出列表表格: `Card` 包裹 `Table`:
      - columns: 文件名、导出时间、模板类型、状态
      - 状态列: `Tag` 组件
      - 操作列: `Button type="link"` "下载"
    - 导出按钮: `Button type="primary"` icon={ExportOutlined}

    关键保留: 下载逻辑、API 调用

    **Mappings.tsx 重写**（per D-12, D-15, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "映射修正"
    - 映射表格: `Card` 包裹 `Table`:
      - columns: 原始字段名、标准字段（Select 下拉）、置信度（Tag）、来源
      - 置信度列: `Tag` color 根据高/中/低变化
      - 标准字段列: 使用 `Select` 组件允许修改映射
    - 保存修改: `Button type="primary"` "保存修改"
    - 操作反馈: `message.success` / `message.error`（per D-13）

    关键保留: 映射修改 API、置信度数据
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - EmployeeSelfService.tsx 包含 `Form` 和 `Descriptions` 导入
    - EmployeeSelfService.tsx 不包含 `PageContainer`
    - Portal.tsx 包含 `Descriptions` 或 `Table` 导入
    - Results.tsx 包含 `Table` 和 `Tag` 导入
    - Exports.tsx 包含 `Table` 导入
    - Mappings.tsx 包含 `Table` 和 `Select` 导入
    - 五个文件均不包含自定义 CSS 类（不含 `className="panel-` 等）
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>五个页面全部使用 Ant Design 组件重写，业务逻辑完整保留，无自定义 CSS 类</done>
</task>

<task type="auto">
  <name>Task 2: 重写 Compare + AuditLogs + Workspace + NotFound + 清理旧文件</name>
  <files>
    frontend/src/pages/Compare.tsx,
    frontend/src/pages/AuditLogs.tsx,
    frontend/src/pages/Workspace.tsx,
    frontend/src/pages/NotFound.tsx,
    frontend/src/styles.css,
    frontend/src/components/GlobalFeedback.tsx,
    frontend/src/components/PageContainer.tsx,
    frontend/src/components/SectionState.tsx,
    frontend/src/components/SurfaceNotice.tsx,
    frontend/src/components/AppShell.tsx
  </files>
  <read_first>
    frontend/src/pages/Compare.tsx,
    frontend/src/pages/AuditLogs.tsx,
    frontend/src/pages/Workspace.tsx,
    frontend/src/pages/NotFound.tsx,
    frontend/src/components/index.ts,
    frontend/src/pages/index.ts
  </read_first>
  <action>
    **Compare.tsx 重写**（per D-12, UI-01）— 这是最大的页面（1165行），需格外小心保留业务逻辑:

    页面结构:
    - 顶部: Typography.Title level={4} "月度对比"
    - 期间选择: `Card` 包裹 `Row` 排列两个 `Select` 或 `DatePicker`（选择对比月份）
    - 对比视图: `Tabs` 组件（如果有多个视图模式）
    - 对比表格: `Card` 包裹 `Table`:
      - 左右月份数据并排展示
      - 差异列高亮: 使用 `Tag` 或 render 函数根据变化量显示颜色
      - 金额增加: `style={{ color: '#F54A45' }}`（红色）
      - 金额减少: `style={{ color: '#00B42A' }}`（绿色）
    - 空状态: `Empty` description="请先选择两个对比期间"
    - 加载中: `Skeleton`

    关键保留: 对比算法、diff 逻辑、API 调用、所有计算逻辑

    **AuditLogs.tsx 重写**（per D-12, D-15, UI-01）:

    页面结构:
    - 顶部: Typography.Title level={4} "审计日志"
    - 筛选区: `Card` 包裹 `Row`:
      - `DatePicker.RangePicker` 选择日期范围
      - `Select` 选择操作类型
      - `Button type="primary"` "查询"
    - 日志表格: `Card` 包裹 `Table`:
      - columns: 时间、操作人、操作类型、详情
      - pagination 配置

    关键保留: 筛选逻辑、API 调用

    **Workspace.tsx 重写**（admin 和 hr 两个工作台，per UI-01）:

    页面结构:
    - 判断当前用户角色，显示对应内容
    - 管理员工作台: Typography.Title level={4} "管理员工作台" + `Row gutter={[16, 16]}` 排列功能卡片:
      - 每个功能入口: `Col span={8}` + `Card hoverable` + 图标 + 标题 + 描述 + `Button type="link"` 跳转
    - HR 工作台: 类似布局，显示 HR 相关功能入口
    - 使用 `Statistic` 展示关键数字（如果现有代码有）

    关键保留: 角色判断逻辑、导航逻辑

    **NotFound.tsx 重写**（per D-11, UI-01）:

    页面结构:
    - `Result` status="404" title="404" subTitle="抱歉，您访问的页面不存在。"
    - extra: `Button type="primary"` onClick 回到首页 "返回首页"

    **清理旧组件文件**（per D-01 一次性迁移完成后清理）:

    删除以下文件（所有页面已迁移完成，不再需要）:
    - `frontend/src/styles.css` — 全部自定义 CSS 已被 Ant Design 替换
    - `frontend/src/components/GlobalFeedback.tsx` — 已被 MainLayout 中的 AggregateBanner + Ant message 替换
    - `frontend/src/components/PageContainer.tsx` — 已被 Ant Layout Content + Breadcrumb 替换
    - `frontend/src/components/SectionState.tsx` — 已被 Ant Skeleton/Empty/Result 替换
    - `frontend/src/components/SurfaceNotice.tsx` — 已被 Ant Alert 替换
    - `frontend/src/components/AppShell.tsx` — 已被 MainLayout 替换

    更新 `frontend/src/components/index.ts`:
    - 只保留 `export * from './ApiFeedbackProvider'` 和 `export * from './AuthProvider'`

    确认 `frontend/src/pages/index.ts` 中所有页面导出正常。

    确认 `frontend/src/main.tsx` 中没有 `import './styles.css'` 引用。
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Compare.tsx 包含 `Table` 和 `Tabs` 导入（从 antd）
    - Compare.tsx 不包含 `PageContainer`
    - AuditLogs.tsx 包含 `Table` 和 `DatePicker` 导入
    - Workspace.tsx 包含 `Card` 和 `Statistic` 导入（或至少 Card）
    - NotFound.tsx 包含 `Result` 导入且包含 `status="404"` 或 `status='404'`
    - `frontend/src/styles.css` 文件不存在（已删除）
    - `frontend/src/components/GlobalFeedback.tsx` 文件不存在（已删除）
    - `frontend/src/components/PageContainer.tsx` 文件不存在（已删除）
    - `frontend/src/components/SectionState.tsx` 文件不存在（已删除）
    - `frontend/src/components/SurfaceNotice.tsx` 文件不存在（已删除）
    - `frontend/src/components/AppShell.tsx` 文件不存在（已删除）
    - `frontend/src/components/index.ts` 不包含 `GlobalFeedback`、`PageContainer`、`SectionState`、`SurfaceNotice`、`AppShell`
    - `npm run build` 成功退出（exit code 0）
    - `npm run lint` 无 error 级别问题
  </acceptance_criteria>
  <done>Compare+AuditLogs+Workspace+NotFound 全部重写为 Ant Design 组件；旧自定义组件文件（AppShell/GlobalFeedback/PageContainer/SectionState/SurfaceNotice）和 styles.css 已删除；整体构建成功</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` 成功（全量 TypeScript + Vite 构建）
- `cd frontend && npm run lint` 无 error
- 确认无任何页面文件引用 PageContainer、SectionState、SurfaceNotice、GlobalFeedback
- 确认 styles.css 已删除
- 确认所有 17 个页面文件均使用 antd 导入
</verification>

<success_criteria>
- 全部 17 个页面已迁移到 Ant Design 5 组件（UI-01 完成）
- 所有旧自定义组件已删除（AppShell、GlobalFeedback、PageContainer、SectionState、SurfaceNotice）
- styles.css 已删除，无自定义全局 CSS
- 整体 `npm run build` 和 `npm run lint` 通过
- Phase 7 D-01 一次性迁移承诺完成
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-ui-foundation/07-04-SUMMARY.md`
</output>
