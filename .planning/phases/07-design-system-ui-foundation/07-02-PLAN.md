---
phase: 07-design-system-ui-foundation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - frontend/src/pages/Login.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/SimpleAggregate.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "登录页使用 Ant Form + Tabs 组件，居中白色卡片，背景为 #F5F6F7"
    - "看板页使用 Ant Statistic + Card + Table 展示数据，顶部统计卡片为焦点"
    - "聚合页使用 Ant Upload.Dragger 拖拽上传区域，步骤展示用 Steps 组件"
    - "三个页面均无自定义 CSS 类（panel-card、dashboard-pill 等全部移除）"
  artifacts:
    - path: "frontend/src/pages/Login.tsx"
      provides: "Ant Design 登录页"
      contains: "Form.Item"
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "Ant Design 看板页"
      contains: "Statistic"
    - path: "frontend/src/pages/SimpleAggregate.tsx"
      provides: "Ant Design 聚合上传页"
      contains: "Upload.Dragger"
  key_links:
    - from: "frontend/src/pages/Login.tsx"
      to: "useAuth hook"
      via: "login/verifyEmployee 调用"
      pattern: "useAuth"
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "services/dashboard"
      via: "API 数据加载"
      pattern: "fetch|useEffect"
    - from: "frontend/src/pages/SimpleAggregate.tsx"
      to: "services/aggregateSessionStore"
      via: "聚合会话管理"
      pattern: "useAggregateSession|startAggregateSession"
---

<objective>
重写三个高优先级核心页面为 Ant Design 组件

Purpose: Login 是用户第一印象、Dashboard 是主工作台、SimpleAggregate 是核心业务流程页。这三个页面的视觉质量直接决定整体感受。

Output:
- Login.tsx 使用 Ant Form/Tabs/Card/Input/Button（居中卡片布局）
- Dashboard.tsx 使用 Ant Statistic/Card/Table/Tag（统计卡片+数据表格）
- SimpleAggregate.tsx 使用 Ant Upload.Dragger/Steps/Card/Progress/Button
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-design-system-ui-foundation/07-CONTEXT.md
@.planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
@.planning/phases/07-design-system-ui-foundation/07-01-SUMMARY.md

@frontend/src/pages/Login.tsx
@frontend/src/pages/Dashboard.tsx
@frontend/src/pages/SimpleAggregate.tsx
@frontend/src/hooks/index.ts
@frontend/src/services/aggregateSessionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 Login.tsx — Ant Design 登录页</name>
  <files>frontend/src/pages/Login.tsx</files>
  <read_first>
    frontend/src/pages/Login.tsx,
    frontend/src/hooks/index.ts,
    .planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    完全重写 Login.tsx，使用 Ant Design 组件替换所有自定义 HTML/CSS（per D-01, D-15, UI-01）。

    页面结构（per UI-SPEC 焦点点: 居中白色 Card，最大宽度 400px，垂直居中偏上 -40px）:
    - 最外层 div: `style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', minHeight: '100vh', background: '#F5F6F7', paddingBottom: 40 }}`
    - Card 组件: `style={{ width: 400, boxShadow: '0 1px 2px rgba(0,0,0,0.06)' }}`
    - Card 内顶部: 系统标题 "社保公积金管理系统"（Typography.Title level={3}，居中）
    - 使用 Ant `Tabs` 组件切换登录模式（如果现有页面有多种登录方式）:
      - Tab 1: "账号登录"（管理员/HR 用户名密码登录）
      - Tab 2: "员工查询"（工号+身份证号+姓名验证）

    账号登录 Tab:
    - `Form` 组件, `onFinish` 调用 `login(values.username, values.password)`
    - `Form.Item` name="username", rules=[{required: true, message: '请输入用户名'}]
      - `Input` prefix={UserOutlined} placeholder="用户名"
    - `Form.Item` name="password", rules=[{required: true, message: '请输入密码'}]
      - `Input.Password` prefix={LockOutlined} placeholder="密码"
    - `Button` type="primary" htmlType="submit" block loading={submitting}，文本 "登录"

    员工查询 Tab:
    - `Form` 组件, `onFinish` 调用 verifyEmployee
    - 三个 Form.Item: employee_id（工号）、id_number（身份证号）、name（姓名），均 required
    - `Button` type="primary" htmlType="submit" block loading={submitting}，文本 "验证身份"

    关键保留逻辑:
    - `useAuth()` hook 的 login 和 verifyEmployee 调用不变
    - 错误处理使用 `App.useApp()` 的 `message.error()`（per D-13, D-14）
    - 登录成功后的路由跳转逻辑不变
    - 移除所有 `login-shell`、`login-card`、`portal-kicker` 等自定义 CSS 类
    - 不使用 PageContainer 组件（登录页不在 MainLayout 内）
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Login.tsx 包含 `import { Card, Form, Input, Button, Tabs, Typography }` 等 antd 导入
    - Login.tsx 包含 `Form.Item`
    - Login.tsx 包含 `Input.Password`
    - Login.tsx 不包含 `className="login-` 或 `className='login-`（无自定义 login CSS 类）
    - Login.tsx 不包含 `PageContainer`
    - Login.tsx 包含 `useAuth`（保留认证逻辑）
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>Login.tsx 完全使用 Ant Design Card+Form+Tabs+Input+Button 组件，居中白色卡片布局，无自定义 CSS 类</done>
</task>

<task type="auto">
  <name>Task 2: 重写 Dashboard.tsx — Ant Design 看板页</name>
  <files>frontend/src/pages/Dashboard.tsx</files>
  <read_first>
    frontend/src/pages/Dashboard.tsx,
    .planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    完全重写 Dashboard.tsx，使用 Ant Design 组件替换所有自定义 HTML/CSS（per D-01, D-12, UI-01）。

    页面结构（per UI-SPEC 焦点点: 顶部统计卡片为视觉焦点）:
    - 顶部: Typography.Title level={4} "处理看板"
    - 统计卡片行: `Row gutter={[16, 16]}`，4个 `Col span={6}`，每个内嵌 `Card`:
      - 使用 `Statistic` 组件显示关键指标（总记录数、待处理、异常数、已导出等）
      - Statistic 的 `valueStyle` 用 24px 字号
      - 关键指标用主色 `#3370FF`

    数据表格区域:
    - 批次质量表格: `Card` 标题 "批次质量"，内嵌 `Table`:
      - columns 定义 dataIndex、title、key、sorter（数值列）
      - `rowKey="batch_id"`
      - `size="small"`（全局 compact 已设）
      - `pagination={false}` 或合理分页
      - 状态标签用 `Tag` 组件，颜色根据状态选择（success/warning/error）

    异常分布区域（如果现有代码有）:
    - 使用 Card + Table 或 Card + 自定义列表展示

    加载状态: 使用 `Skeleton` 组件（per D-09）:
    - 数据加载中时，统计卡片区域显示 `Skeleton.Input active`
    - 表格区域显示 `Skeleton active paragraph={{ rows: 5 }}`

    空状态: 使用 `Empty` 组件（per D-11），description="暂无数据"

    关键保留逻辑:
    - 现有 API 调用和 useEffect 数据加载逻辑完整保留
    - 链接到其他页面的导航逻辑保留
    - 移除所有 `dashboard-pill`、`panel-card`、`preview-table` 等自定义 CSS 类
    - 不使用 PageContainer 组件，直接在 Content 区域布局
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx 包含 `Statistic` 导入
    - Dashboard.tsx 包含 `Table` 导入（从 antd）
    - Dashboard.tsx 包含 `Card` 导入
    - Dashboard.tsx 包含 `Skeleton` 导入
    - Dashboard.tsx 不包含 `className="dashboard-` 或 `className="panel-` 或 `className="preview-table`（无自定义看板 CSS 类）
    - Dashboard.tsx 不包含 `PageContainer`
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>Dashboard.tsx 完全使用 Ant Statistic+Card+Table+Skeleton+Tag 组件，顶部统计卡片+数据表格布局，无自定义 CSS 类</done>
</task>

<task type="auto">
  <name>Task 3: 重写 SimpleAggregate.tsx — Ant Design 聚合上传页</name>
  <files>frontend/src/pages/SimpleAggregate.tsx</files>
  <read_first>
    frontend/src/pages/SimpleAggregate.tsx,
    frontend/src/services/aggregateSessionStore.ts,
    .planning/phases/07-design-system-ui-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    完全重写 SimpleAggregate.tsx，使用 Ant Design 组件替换所有自定义 HTML/CSS（per D-01, D-16, UI-01）。
    注意: 这是最大的页面（845行），重写时保持所有业务逻辑不变，只替换视图层。

    页面结构（per UI-SPEC 焦点点: Upload.Dragger 为视觉焦点）:
    - 顶部: Typography.Title level={4} "快速融合" + Typography.Text type="secondary" 描述文字

    步骤指示器（如果现有代码有步骤流程）:
    - `Steps` 组件（size="small"）显示当前聚合进度步骤
    - 步骤: 上传文件 -> 解析中 -> 匹配中 -> 导出

    上传区域:
    - `Card` 包裹 `Upload.Dragger`（per D-16）:
      - `Upload.Dragger` props: `multiple={true}`, `accept=".xlsx,.xls"`, `beforeUpload` 返回 false（手动上传）
      - 拖拽区内容: `<p><InboxOutlined style={{ fontSize: 48, color: '#3370FF' }} /></p>` + 说明文字
      - 文件列表使用 Upload 自带的 fileList 展示
    - 上传按钮: `Button type="primary"` "开始聚合"

    进度显示区域:
    - 聚合运行中: `Card` 内嵌 `Progress` 组件（percent={进度百分比}）+ 状态描述文字
    - 使用 `Spin` 包裹进度区域表示正在处理

    结果展示区域:
    - 聚合完成后: `Card` 内嵌 `Result` 组件（status="success"）或表格展示结果
    - 错误时: `Result` 组件 status="error" + 错误描述
    - 结果表格: `Table` 组件展示解析结果

    加载状态: `Skeleton` 组件（per D-09）
    空状态: `Empty` 组件（per D-11）

    操作按钮:
    - 使用 `Space` 组件排列多个按钮
    - 取消聚合: `Button` danger
    - 重新上传: `Button`
    - 导出结果: `Button type="primary"` + `ExportOutlined`

    关键保留逻辑:
    - `useAggregateSession` hook 和 aggregateSessionStore 的所有交互逻辑完整保留
    - 文件上传处理逻辑保留
    - SSE 或轮询进度逻辑保留
    - 结果展示和导出逻辑保留
    - 移除所有自定义 CSS 类（upload-zone、aggregate-panel 等）
    - 不使用 PageContainer 组件
    - 确认/警告弹窗用 `Modal.confirm`（per D-17）
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SimpleAggregate.tsx 包含 `Upload.Dragger` 或 `Upload` 导入
    - SimpleAggregate.tsx 包含 `Steps` 或 `Progress` 导入
    - SimpleAggregate.tsx 包含 `Card` 导入
    - SimpleAggregate.tsx 包含 `Result` 导入
    - SimpleAggregate.tsx 不包含 `PageContainer`
    - SimpleAggregate.tsx 不包含 `className="upload-` 或 `className="aggregate-`（无自定义上传 CSS 类）
    - SimpleAggregate.tsx 包含 `useAggregateSession`（保留聚合逻辑）
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>SimpleAggregate.tsx 完全使用 Ant Upload.Dragger+Steps+Progress+Card+Result+Table 组件，无自定义 CSS 类，聚合业务逻辑完整保留</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` 成功
- `cd frontend && npm run lint` 无 error
- 三个页面文件均不包含自定义 CSS 类名（grep 验证）
- 三个页面文件均不使用 PageContainer 组件
</verification>

<success_criteria>
- Login 页面使用 Ant Card+Form+Tabs 居中布局，无自定义 CSS
- Dashboard 页面使用 Ant Statistic+Card+Table+Skeleton，无自定义 CSS
- SimpleAggregate 页面使用 Ant Upload.Dragger+Steps+Progress+Card，无自定义 CSS
- 所有三个页面的业务逻辑（API 调用、状态管理、路由跳转）完整保留
- TypeScript 编译和 Vite 构建通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-ui-foundation/07-02-SUMMARY.md`
</output>
