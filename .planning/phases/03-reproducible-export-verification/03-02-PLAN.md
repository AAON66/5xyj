---
phase: 03-reproducible-export-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/tests/test_template_exporter.py
  - backend/tests/test_template_exporter_regression.py
  - backend/tests/test_aggregate_api.py
  - backend/tests/test_dashboard_api.py
autonomous: true
requirements:
  - VER-01
  - VER-02
must_haves:
  truths:
    - "Exporter unit/regression tests, aggregate export tests, and dashboard export coverage can rerun on another machine without Desktop-path edits."
    - "Mandatory export verification fails loudly when required templates or samples are missing instead of being skipped away."
    - "Aggregate verification no longer references the missing tracked file `data/templates/placeholder.xlsx`."
  artifacts:
    - path: "backend/tests/test_template_exporter.py"
      provides: "Unit-level dual-template export assertions wired to shared repo/default fixture helpers"
      contains: "resolve_required_export_templates"
    - path: "backend/tests/test_template_exporter_regression.py"
      provides: "Cross-region export regression wired to repo-controlled template fixtures"
      contains: "EXPORT_REGRESSION_CASES"
    - path: "backend/tests/test_aggregate_api.py"
      provides: "Aggregate export and oversized-upload coverage without nonexistent placeholder template dependency"
      contains: "test_aggregate_endpoint_rejects_oversized_upload"
  key_links:
    - from: "backend/tests/test_template_exporter_regression.py"
      to: "backend/tests/support/export_fixtures.py"
      via: "cross-region export regression resolves mandatory templates and samples through the shared helper"
      pattern: "resolve_required_export_templates"
    - from: "backend/tests/test_aggregate_api.py"
      to: "backend/tests/support/export_fixtures.py"
      via: "aggregate tests use shared template/sample helpers and temporary placeholder workbook creation"
      pattern: "create_placeholder_template_pair"
    - from: "backend/tests/test_dashboard_api.py"
      to: "backend/tests/support/export_fixtures.py"
      via: "dashboard export counts are exercised against the same supported template contract as the export and aggregate suites"
      pattern: "resolve_required_export_templates"
---

<objective>
Migrate the remaining export-related verification suites onto the shared repo-fixture contract and remove the last workstation-only or missing-fixture assumptions.

Purpose: Make Phase 3’s mandatory dual-template verification reproducible and fail-loud across exporter, aggregate, and dashboard coverage.
Output: Exporter, regression, aggregate, and dashboard tests all consume the same shared fixture resolver and no longer rely on Desktop paths or a nonexistent placeholder workbook.
</objective>

<execution_context>
@C:/Users/AAON/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AAON/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-reproducible-export-verification/03-RESEARCH.md
@.planning/phases/03-reproducible-export-verification/03-01-PLAN.md
@backend/tests/support/export_fixtures.py
@backend/tests/test_template_exporter.py
@backend/tests/test_template_exporter_regression.py
@backend/tests/test_aggregate_api.py
@backend/tests/test_dashboard_api.py

<interfaces>
From `backend/tests/support/export_fixtures.py` (created in Plan 03-01):
```python
@dataclass(frozen=True, slots=True)
class RequiredExportTemplates:
    salary: Path
    final_tool: Path
    manifest: Path

def resolve_required_export_templates(settings: Settings | None = None) -> RequiredExportTemplates: ...
def require_sample_workbook(keyword: str, *, housing: bool = False) -> Path: ...
def create_placeholder_template_pair(target_dir: Path) -> RequiredExportTemplates: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move exporter unit and cross-region regression suites to shared repo-fixture helpers</name>
  <read_first>
    - backend/tests/support/export_fixtures.py
    - backend/tests/test_template_exporter.py
    - backend/tests/test_template_exporter_regression.py
    - .planning/phases/03-reproducible-export-verification/03-RESEARCH.md
  </read_first>
  <files>backend/tests/test_template_exporter.py, backend/tests/test_template_exporter_regression.py</files>
  <behavior>
    - Test 1: Exporter happy-path and failure-path tests load the mandatory salary/final-tool templates from the shared helper instead of local `find_template()` functions.
    - Test 2: Cross-region regression keeps covering Guangzhou, Hangzhou, Xiamen, Shenzhen, Wuhan, and Changsha through the shared helper and fails loudly if required assets are missing.
    - Test 3: Exporter suites stop writing persistent repo-local artifacts when a temporary directory is sufficient for generated outputs.
  </behavior>
  <action>Refactor `backend/tests/test_template_exporter.py` and `backend/tests/test_template_exporter_regression.py` to import `resolve_required_export_templates()` and `require_sample_workbook()` from `backend/tests/support/export_fixtures.py`. Delete each file’s local `DESKTOP_ROOT`, `find_template()`, and `pytest.skip(...)` branches related to mandatory export templates or sample workbooks. Replace keyword-only template lookup with exact `RequiredExportTemplates.salary` and `RequiredExportTemplates.final_tool` usage. Where the tests only need disposable generated outputs, move them from `.test_artifacts/...` into `tmp_path` or a pytest-created temp subtree; keep the semantic workbook assertions and cross-region coverage intact. Do not reduce coverage or replace the real regression templates with generated placeholder workbooks in these suites.</action>
  <acceptance_criteria>
    - `backend/tests/test_template_exporter.py` contains `resolve_required_export_templates`.
    - `backend/tests/test_template_exporter.py` does not contain `DESKTOP_ROOT`.
    - `backend/tests/test_template_exporter.py` does not contain `def find_template`.
    - `backend/tests/test_template_exporter_regression.py` contains `resolve_required_export_templates`.
    - `backend/tests/test_template_exporter_regression.py` does not contain `DESKTOP_ROOT`.
    - `backend/tests/test_template_exporter_regression.py` still contains all six region cases in `EXPORT_REGRESSION_CASES`.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_template_exporter.py backend/tests/test_template_exporter_regression.py -x</automated>
  </verify>
  <done>The exporter unit and cross-region regression suites now run from the shared repo-fixture contract and retain their semantic workbook coverage without Desktop-path fallback or skip-heavy mandatory setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align aggregate and dashboard export coverage with the shared fixture contract</name>
  <read_first>
    - backend/tests/support/export_fixtures.py
    - backend/tests/test_aggregate_api.py
    - backend/tests/test_dashboard_api.py
    - backend/tests/test_export_api.py
    - .planning/phases/03-reproducible-export-verification/03-RESEARCH.md
  </read_first>
  <files>backend/tests/test_aggregate_api.py, backend/tests/test_dashboard_api.py</files>
  <behavior>
    - Test 1: Aggregate export, aggregate stream, and dashboard overview tests resolve mandatory templates through the shared helper instead of Desktop fallbacks.
    - Test 2: The aggregate oversized-upload regression uses a temporary valid workbook pair rather than the nonexistent tracked file `data/templates/placeholder.xlsx`.
    - Test 3: Missing required template fixtures now fail these suites loudly during setup instead of being skipped away.
  </behavior>
  <action>Refactor `backend/tests/test_aggregate_api.py` and `backend/tests/test_dashboard_api.py` to import the shared fixture helpers from `backend/tests/support/export_fixtures.py`. Remove `DESKTOP_ROOT`, local `find_template()`, and mandatory-template `pytest.skip(...)` branches from both files. For the aggregate oversized-upload test, replace `ROOT_DIR / 'data' / 'templates' / 'placeholder.xlsx'` with a temp-directory workbook pair created by `create_placeholder_template_pair(...)`, because that checked-in placeholder file does not exist. Keep the existing aggregate export happy paths, stream coverage, and dashboard exported-count assertions; the change is about reproducible setup and fail-loud behavior, not weaker coverage. Where feasible, keep test-local DB/upload/output directories under temp paths to avoid persistent artifact coupling.</action>
  <acceptance_criteria>
    - `backend/tests/test_aggregate_api.py` contains `resolve_required_export_templates` or `create_placeholder_template_pair`.
    - `backend/tests/test_aggregate_api.py` does not contain `DESKTOP_ROOT`.
    - `backend/tests/test_aggregate_api.py` does not contain `placeholder.xlsx`.
    - `backend/tests/test_dashboard_api.py` contains `resolve_required_export_templates`.
    - `backend/tests/test_dashboard_api.py` does not contain `DESKTOP_ROOT`.
    - Neither file contains `pytest.skip(` for mandatory export template lookup.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_aggregate_api.py backend/tests/test_dashboard_api.py -x</automated>
  </verify>
  <done>Aggregate and dashboard export verification now use the same supported repo/default fixture contract as the exporter suites, and the missing placeholder-template dependency has been removed.</done>
</task>

</tasks>

<verification>
Run:
1. `pytest backend/tests/test_template_exporter.py backend/tests/test_template_exporter_regression.py -x`
2. `pytest backend/tests/test_aggregate_api.py backend/tests/test_dashboard_api.py -x`

Confirm all export-related suites resolve templates through the shared helper, no Desktop fallback remains, and missing mandatory assets now fail loudly.
</verification>

<success_criteria>
- Exporter, aggregate, and dashboard verification suites all consume the shared repo/default template resolver.
- Mandatory template absence surfaces as a failure, not a skip, across the export-related test harness.
- The aggregate oversized-upload test no longer references the nonexistent `data/templates/placeholder.xlsx` file.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reproducible-export-verification/03-02-SUMMARY.md`
</output>
