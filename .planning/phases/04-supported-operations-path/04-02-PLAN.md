---
phase: 04-supported-operations-path
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - DEPLOYMENT_SERVER.md
  - OPERATIONS_RESCUE.md
  - scripts/operations/rescue/README.md
  - scripts/operations/rescue/
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/README.md
autonomous: true
requirements:
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "Operators can distinguish supported workflows from rescue or legacy scripts because ambiguous repo-root helpers are visibly relocated or renamed into a rescue-only surface."
    - "Server-specific notes and credentialed helpers no longer look like the canonical deployment path."
    - "Future agents can find the in-repo planning state and the discuss -> plan -> execute -> verify flow without relying on AGENTS.md edits."
  artifacts:
    - path: "OPERATIONS_RESCUE.md"
      provides: "Explicit classification of rescue, legacy, and server-specific operational helpers after the ambiguous helpers are moved out of the repo root"
      contains: "deploy_all.sh"
    - path: "scripts/operations/rescue/README.md"
      provides: "Filesystem-visible rescue surface that makes non-canonical tooling unmistakable at point of use"
      contains: "rescue"
    - path: "DEPLOYMENT_SERVER.md"
      provides: "Demoted server-specific note that points back to the canonical supported docs"
      contains: "legacy"
    - path: ".planning/README.md"
      provides: "Entry point for future agents into roadmap, state, and the standard GSD workflow"
      contains: "discuss"
    - path: ".planning/STATE.md"
      provides: "Current phase handoff that references the supported operations docs and next GSD step"
      contains: "Phase 4"
  key_links:
    - from: "scripts/operations/rescue/README.md"
      to: "OPERATIONS_RESCUE.md"
      via: "The relocated rescue directory points back to the rescue inventory and supported docs"
      pattern: "OPERATIONS_RESCUE.md"
    - from: "OPERATIONS_RESCUE.md"
      to: "DEPLOYMENT_SERVER.md"
      via: "Rescue inventory demotes server-specific notes and links back to the supported docs"
      pattern: "DEPLOYMENT_SERVER.md"
    - from: "DEPLOYMENT_SERVER.md"
      to: "OPERATIONS.md"
      via: "Legacy/server-specific note explicitly redirects normal operators to the canonical supported workflow"
      pattern: "OPERATIONS.md"
    - from: ".planning/README.md"
      to: ".planning/ROADMAP.md"
      via: "Future-agent handoff explains where planning state lives and how to continue the loop"
      pattern: "ROADMAP.md"
---

<objective>
Classify rescue and legacy operational tooling, then make the in-repo `.planning/` flow explicit for future agents without touching `AGENTS.md`.

Purpose: Satisfy `OPS-02` and `OPS-03` by separating unsupported rescue tooling from canonical workflows and making future handoff discoverable in-repo.
Output: A rescue-tool classification document, a demoted server-specific deployment note, and a `.planning/` handoff entry point that routes future work into discuss, plan, execute, and verify.
</objective>

<execution_context>
@C:/Users/AAON/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AAON/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-supported-operations-path/04-RESEARCH.md
@.planning/phases/04-supported-operations-path/04-01-PLAN.md
@DEPLOYMENT_SERVER.md
@deploy_all.sh
@start_service.py
@deploy.sh
@deploy_service.sh
@final_deploy.py
@final_fix.py
@restart_clean.py
@upload_and_start.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify rescue and legacy operational tooling</name>
  <read_first>
    - .planning/phases/04-supported-operations-path/04-RESEARCH.md
    - DEPLOYMENT_SERVER.md
    - deploy_all.sh
    - start_service.py
    - deploy.sh
    - deploy_service.sh
    - final_deploy.py
    - final_fix.py
    - restart_clean.py
    - upload_and_start.py
    - OPERATIONS.md
    - DEPLOYMENT.md
  </read_first>
  <files>OPERATIONS_RESCUE.md, DEPLOYMENT_SERVER.md, scripts/operations/rescue/README.md, scripts/operations/rescue/</files>
  <action>Create `OPERATIONS_RESCUE.md` as the explicit inventory for rescue, legacy, and server-specific scripts required by `OPS-02`. In the same task, make the separation filesystem-visible: relocate or rename the ambiguous repo-root helpers into a dedicated `scripts/operations/rescue/` or `scripts/operations/legacy/` surface, leaving only the supported local launchers at the repo root (`start_backend_local.*`, `start_frontend_local.*`, `start_project_local.*`) plus normal non-operational project files. The relocation must cover every root-level ad hoc deploy/restart/repair/server helper discovered in the repo inventory, including the already identified examples `absolute_path_deploy.py`, `auto_deploy.py`, `auto_deploy.sh`, `check_service.py`, `clean_cache_deploy.py`, `clean_deploy.py`, `clean_env_start.py`, `deploy.sh`, `deploy_all.sh`, `deploy_auto.py`, `deploy_complete.py`, `deploy_service.sh`, `final.py`, `final_deploy.py`, `final_deploy_clean.py`, `final_fix.py`, `fix_*.py`, `force_rebuild.py`, `kill_and_restart.py`, `manual_start.py`, `rebuild_venv.py`, `redeploy.py`, `restart_clean.py`, `setup_nginx.py`, `simple_start.py`, `start_service.py`, `upload_and_start.py`, `upload_fixed.py`, and similar one-off helpers. Preserve file contents and provenance as practical, but do not leave those helpers masquerading as supported entrypoints at the repo root. Add `scripts/operations/rescue/README.md` so anyone entering the rescue directory sees that the contents are non-canonical and should refer back to `OPERATIONS.md` / `DEPLOYMENT.md` for supported workflows. Update `DEPLOYMENT_SERVER.md` so it is visibly demoted to legacy or environment-specific notes, not the canonical deployment path. Keep the scripts available for investigation; do not delete them or rewrite runtime behavior beyond path/label changes.</action>
  <acceptance_criteria>
    - `OPERATIONS_RESCUE.md` exists.
    - `OPERATIONS_RESCUE.md` contains `absolute_path_deploy.py`, `check_service.py`, `clean_cache_deploy.py`, `clean_deploy.py`, `clean_env_start.py`, `deploy.sh`, `deploy_service.sh`, `deploy_all.sh`, `start_service.py`, `final.py`, `final_deploy.py`, `final_fix.py`, `force_rebuild.py`, `rebuild_venv.py`, `restart_clean.py`, `setup_nginx.py`, and `upload_and_start.py`.
    - `OPERATIONS_RESCUE.md` contains clear supported-versus-rescue classification language.
    - `scripts/operations/rescue/README.md` exists and contains `OPERATIONS.md`.
    - The only remaining root-level operational launcher scripts are `start_backend_local.cmd`, `start_backend_local.ps1`, `start_frontend_local.cmd`, `start_frontend_local.ps1`, `start_project_local.cmd`, and `start_project_local.ps1`.
    - Every other root-level deploy/restart/repair/server helper has been moved under `scripts/operations/rescue/` or renamed with an explicit legacy/rescue marker so it no longer appears to be canonical from the repo root.
    - `DEPLOYMENT_SERVER.md` contains `OPERATIONS.md`.
    - `DEPLOYMENT_SERVER.md` contains `legacy` or `server-specific`.
    - Neither document claims `DEPLOYMENT_SERVER.md` is the canonical supported deployment path.
  </acceptance_criteria>
  <verify>
    <automated>$allowed = @('start_backend_local.cmd','start_backend_local.ps1','start_frontend_local.cmd','start_frontend_local.ps1','start_project_local.cmd','start_project_local.ps1'); Get-ChildItem -Name *.py,*.sh,*.cmd,*.ps1 | Where-Object { $_ -notin $allowed -and $_ -match '(deploy|service|restart|rebuild|fix|upload|final|manual_start|simple_start|check_service|absolute_path|setup_nginx|clean_|redeploy|kill_and_restart|auto_)' }</automated>
    <automated>Get-ChildItem scripts/operations/rescue -Recurse -Name | Select-String -Pattern "absolute_path_deploy|auto_deploy|check_service|clean_cache_deploy|clean_deploy|clean_env_start|deploy|final|fix_|force_rebuild|kill_and_restart|manual_start|rebuild_venv|redeploy|restart_clean|setup_nginx|simple_start|start_service|upload_"</automated>
    <automated>Select-String -Path OPERATIONS_RESCUE.md, DEPLOYMENT_SERVER.md -Pattern "absolute_path_deploy.py|check_service.py|clean_cache_deploy.py|clean_deploy.py|clean_env_start.py|deploy.sh|deploy_service.sh|deploy_all.sh|start_service.py|final.py|final_deploy.py|final_fix.py|force_rebuild.py|rebuild_venv.py|restart_clean.py|setup_nginx.py|upload_and_start.py|legacy|server-specific|OPERATIONS.md"</automated>
  </verify>
  <done>Operators can identify rescue-only tooling immediately and server-specific notes no longer compete with the supported workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit .planning handoff guidance for future agents</name>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/04-supported-operations-path/04-01-PLAN.md
    - .planning/phases/04-supported-operations-path/04-RESEARCH.md
    - OPERATIONS.md
    - OPERATIONS_RESCUE.md
  </read_first>
  <files>.planning/README.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <action>Create `.planning/README.md` as the discoverable handoff entry point for `OPS-03`. It should explain what `PROJECT.md`, `ROADMAP.md`, `STATE.md`, and phase directories mean, and spell out the expected GSD loop for future work: discuss -> plan -> execute -> verify. Update `.planning/PROJECT.md` so the active hardening scope explicitly includes supported operations clarity and references `OPERATIONS.md` / `OPERATIONS_RESCUE.md` as the operator-facing docs. Update `.planning/STATE.md` so the Phase 4 resume notes tell future agents to follow the canonical docs first, treat rescue tooling as non-canonical, and continue through the GSD workflow rather than inventing new ad hoc paths. Do not edit `AGENTS.md`.</action>
  <acceptance_criteria>
    - `.planning/README.md` exists.
    - `.planning/README.md` contains `PROJECT.md`, `ROADMAP.md`, `STATE.md`, `discuss`, `plan`, `execute`, and `verify`.
    - `.planning/PROJECT.md` references `OPERATIONS.md` or `OPERATIONS_RESCUE.md`.
    - `.planning/STATE.md` references the supported operations docs.
    - `.planning/STATE.md` directs the next agent toward the standard GSD loop instead of rescue scripts.
    - No task touches `AGENTS.md`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "PROJECT.md|ROADMAP.md|STATE.md|discuss|plan|execute|verify|OPERATIONS.md|OPERATIONS_RESCUE.md|rescue" .planning/README.md .planning/PROJECT.md .planning/STATE.md</automated>
  </verify>
  <done>Future agents can orient themselves from `.planning/` alone and are explicitly steered toward the supported workflow instead of rescue tooling.</done>
</task>

</tasks>

<verification>
1. Run `rg -n "legacy|server-specific|OPERATIONS.md|deploy_all.sh|start_service.py" OPERATIONS_RESCUE.md DEPLOYMENT_SERVER.md`.
2. Run `rg -n "PROJECT.md|ROADMAP.md|STATE.md|discuss|plan|execute|verify|OPERATIONS.md|OPERATIONS_RESCUE.md" .planning/README.md .planning/PROJECT.md .planning/STATE.md`.
3. Confirm no instructions require editing `AGENTS.md`.
</verification>

<success_criteria>
- Rescue and legacy tooling are explicitly classified and do not masquerade as canonical workflows.
- `DEPLOYMENT_SERVER.md` is demoted to server-specific reference material that points back to the supported docs.
- `.planning/` contains an obvious handoff path for future agents to continue work through discuss, plan, execute, and verify.
</success_criteria>

<output>
After completion, create `.planning/phases/04-supported-operations-path/04-02-SUMMARY.md`
</output>
