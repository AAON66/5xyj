---
phase: 04-supported-operations-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - OPERATIONS.md
  - DEPLOYMENT.md
  - .planning/README.md
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "Operators can find one canonical local workflow without guessing between helper scripts, direct commands, and historical deployment notes."
    - "Operators can find one canonical supported deployment workflow without treating Docker, server-specific helpers, or rescue scripts as equivalent options."
    - "The supported local and deployment instructions match the current brownfield app behavior instead of inventing a new runtime path."
    - "Top-level operator docs give future maintainers and agents one obvious pointer into `.planning/` without requiring `AGENTS.md` edits."
  artifacts:
    - path: "OPERATIONS.md"
      provides: "Single supported-operations contract covering the canonical local launcher and the canonical deployment entrypoint"
      contains: "Supported local workflow"
    - path: "README.md"
      provides: "Top-level quick-start that points operators to the supported operations document first"
      contains: "OPERATIONS.md"
    - path: "DEPLOYMENT.md"
      provides: "Canonical deployment checklist narrowed to one supported Linux/systemd path"
      contains: "systemd"
    - path: ".planning/README.md"
      provides: "Discoverable planning entry point linked from top-level docs for future agents and maintainers"
      contains: "ROADMAP.md"
  key_links:
    - from: "OPERATIONS.md"
      to: "start_project_local.cmd"
      via: "Local workflow section names the supported wrapper and the exact backend/frontend commands it orchestrates"
      pattern: "start_project_local"
    - from: "OPERATIONS.md"
      to: "DEPLOYMENT.md"
      via: "Deployment section links to the single supported deployment checklist"
      pattern: "DEPLOYMENT.md"
    - from: "DEPLOYMENT.md"
      to: "backend/run.py"
      via: "Deployment commands align to the current uvicorn entrypoint and do not redefine runtime behavior"
      pattern: "backend.run|backend/run.py"
---

<objective>
Define the repository's single supported local workflow and single supported deployment workflow without changing how the brownfield app actually runs.

Purpose: Satisfy `OPS-01` by making the canonical operator path obvious and consistent across the repo.
Output: A new supported-operations source-of-truth doc plus aligned README and deployment docs that point to one local path and one deployment path.
</objective>

<execution_context>
@C:/Users/AAON/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AAON/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-supported-operations-path/04-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@README.md
@DEPLOYMENT.md
@start_project_local.cmd
@start_project_local.ps1
@start_backend_local.cmd
@start_backend_local.ps1
@start_frontend_local.cmd
@start_frontend_local.ps1
@backend/run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the canonical supported local workflow</name>
  <read_first>
    - .planning/phases/04-supported-operations-path/04-RESEARCH.md
    - README.md
    - start_project_local.cmd
    - start_project_local.ps1
    - start_backend_local.cmd
    - start_backend_local.ps1
    - start_frontend_local.cmd
    - start_frontend_local.ps1
    - backend/run.py
  </read_first>
  <files>OPERATIONS.md, README.md, .planning/README.md</files>
  <action>Create `OPERATIONS.md` as the single supported operations contract for `OPS-01`. The local section must name exactly one supported local workflow: launch the system via `start_project_local.cmd` or `start_project_local.ps1`, then document the direct backend/frontend commands underneath as the explanatory breakdown of what those wrappers do today (SQLite local DB, Alembic upgrade, `python -m backend.run`, frontend Vite on `127.0.0.1:5173`, backend on `127.0.0.1:8000`). Update `README.md` so quick-start points to `OPERATIONS.md` first, states that `start_project_local.*` is the canonical local path, and removes wording that makes historical deploy/fix scripts look interchangeable with normal operation. Add one short maintainer/agent handoff pointer from `README.md` or `OPERATIONS.md` into `.planning/README.md` so the supported docs and the in-repo planning loop are discoverable from the top level. Preserve the current app behavior; this task is documentation and operator clarity only, not script or runtime changes.</action>
  <acceptance_criteria>
    - `OPERATIONS.md` exists and contains a clearly labeled supported local workflow section.
    - `OPERATIONS.md` contains `start_project_local.cmd` and `start_project_local.ps1`.
    - `OPERATIONS.md` contains `127.0.0.1:8000` and `127.0.0.1:5173`.
    - `README.md` contains `OPERATIONS.md`.
    - `README.md` identifies `start_project_local` as the supported local entrypoint.
    - `README.md` or `OPERATIONS.md` contains `.planning/README.md`.
    - Neither file tells operators to use `deploy_all.sh` or `start_service.py` for local startup.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "OPERATIONS.md|start_project_local|127.0.0.1:8000|127.0.0.1:5173|deploy_all.sh|start_service.py" README.md OPERATIONS.md</automated>
  </verify>
  <done>The repo has one operator-facing local workflow and the top-level quick-start points to it instead of leaving script choice ambiguous.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow deployment guidance to one supported workflow</name>
  <read_first>
    - .planning/phases/04-supported-operations-path/04-RESEARCH.md
    - DEPLOYMENT.md
    - DEPLOYMENT_SERVER.md
    - backend/run.py
    - .planning/phases/03-reproducible-export-verification/03-01-SUMMARY.md
    - .planning/phases/03-reproducible-export-verification/03-02-SUMMARY.md
  </read_first>
  <files>DEPLOYMENT.md, OPERATIONS.md</files>
  <action>Rewrite `DEPLOYMENT.md` so the only supported deployment path for `OPS-01` is the current portable Linux deployment flow: provision Python/Node, create the virtualenv, install backend/frontend dependencies, run Alembic, build the frontend, configure systemd, and supply explicit template/auth settings. `OPERATIONS.md` should link to this checklist as the canonical deployment path. Keep the instructions aligned with completed brownfield hardening: Phase 1 non-default credentials/secrets still apply, and Phase 3 means template paths must come from repo-controlled fixtures or explicit configuration rather than workstation-only Desktop assumptions. Windows-local notes, Docker content, Nginx guidance, and server-specific notes may remain only as clearly demoted references; do not present them as equally supported deployment choices.</action>
  <acceptance_criteria>
    - `DEPLOYMENT.md` contains one clearly labeled supported deployment workflow.
    - `DEPLOYMENT.md` contains `systemd`.
    - `DEPLOYMENT.md` contains `AUTH_SECRET_KEY`, `ADMIN_PASSWORD`, and `HR_PASSWORD`.
    - `DEPLOYMENT.md` contains `SALARY_TEMPLATE_PATH` and `FINAL_TOOL_TEMPLATE_PATH`.
    - `OPERATIONS.md` links operators from the supported deployment section to `DEPLOYMENT.md`.
    - `DEPLOYMENT.md` does not present Docker or `10.0.0.60` server helpers as the canonical deployment path.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "systemd|AUTH_SECRET_KEY|ADMIN_PASSWORD|HR_PASSWORD|SALARY_TEMPLATE_PATH|FINAL_TOOL_TEMPLATE_PATH|10.0.0.60|Docker" DEPLOYMENT.md OPERATIONS.md</automated>
  </verify>
  <done>The repository now documents exactly one supported deployment path that matches the current app and hardening constraints instead of a grab bag of equally promoted modes.</done>
</task>

</tasks>

<verification>
1. Run `rg -n "start_project_local|127.0.0.1:8000|127.0.0.1:5173" README.md OPERATIONS.md`.
2. Run `rg -n "systemd|AUTH_SECRET_KEY|SALARY_TEMPLATE_PATH|FINAL_TOOL_TEMPLATE_PATH" DEPLOYMENT.md OPERATIONS.md`.
3. Confirm the docs never redirect normal operators to `deploy_all.sh`, `start_service.py`, or `10.0.0.60`-specific helpers as the supported path.
</verification>

<success_criteria>
- The repo exposes one canonical supported local workflow centered on `start_project_local.*`.
- The repo exposes one canonical supported deployment workflow centered on the Linux/systemd path.
- The supported instructions match the current brownfield startup behavior and Phase 1 / Phase 3 constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/04-supported-operations-path/04-01-SUMMARY.md`
</output>
