---
phase: 15-menu-reorganize-settings-nav
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - frontend/src/pages/Settings.tsx
  - frontend/src/pages/index.ts
  - frontend/src/App.tsx
  - frontend/src/layouts/MainLayout.tsx
autonomous: false
requirements: [UX-05]
must_haves:
  truths:
    - "管理员/HR 可以通过 /settings 路由访问统一设置页"
    - "设置页顶部有搜索框，输入关键词后隐藏不匹配的卡片"
    - "匹配的卡片中关键词文本高亮显示"
    - "审计日志、API 密钥、飞书设置卡片提供前往链接跳转到独立页面"
    - "清空搜索恢复全部卡片"
    - "无匹配结果时显示空状态提示"
    - "侧边栏管理分组中包含设置入口"
  artifacts:
    - path: "frontend/src/pages/Settings.tsx"
      provides: "统一设置页组件"
      exports: ["SettingsPage"]
    - path: "frontend/src/pages/index.ts"
      provides: "Settings 页面导出"
      contains: "Settings"
    - path: "frontend/src/App.tsx"
      provides: "/settings 路由"
      contains: "/settings"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Settings.tsx"
      via: "Route path=/settings"
      pattern: "/settings"
    - from: "frontend/src/layouts/MainLayout.tsx"
      to: "/settings"
      via: "管理组菜单项"
      pattern: "/settings"
---

<objective>
新建统一设置页 /settings，支持卡片分区展示各类设置项，搜索框快速过滤并高亮匹配关键词，审计日志/API 密钥/飞书设置卡片提供前往链接。在侧边栏管理分组中添加设置入口。

Purpose: 用户通过搜索快速定位设置项，不需要逐个翻找菜单（UX-05）
Output: Settings.tsx 页面 + 路由注册 + 菜单入口
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-menu-reorganize-settings-nav/15-CONTEXT.md
@.planning/phases/15-menu-reorganize-settings-nav/15-RESEARCH.md
@.planning/phases/15-menu-reorganize-settings-nav/15-UI-SPEC.md
@.planning/phases/15-menu-reorganize-settings-nav/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: MainLayout now uses grouped menu structure -->
From frontend/src/layouts/MainLayout.tsx (after Plan 01):
```typescript
interface NavItem {
  key: string;
  icon: React.ReactNode;
  label: string;
  roles: string[];
}

interface MenuGroupConfig {
  key: string;
  label: string;
  icon: React.ReactNode;
  children: NavItem[];
  defaultOpen: boolean;
}

const TOP_ITEM: NavItem = { key: '/aggregate', ... };
const MENU_GROUPS: MenuGroupConfig[] = [
  { key: 'group-common', label: '常用', ... },
  { key: 'group-analysis', label: '数据分析', ... },
  { key: 'group-admin', label: '管理', children: [...] },
];
// 管理组 children 中需要添加 /settings 项
```

From frontend/src/App.tsx:
```typescript
// Route structure uses RoleRoute for access control
<Route element={<RoleRoute allowedRoles={['admin', 'hr']} />}>
  // ... existing routes
</Route>
<Route element={<RoleRoute allowedRoles={['admin']} />}>
  // ... admin-only routes
</Route>
```

From frontend/src/pages/index.ts:
```typescript
// Export pattern: export * from './PageName'; or export { default as PageName } from './PageName';
```

From frontend/src/theme/useSemanticColors.ts:
```typescript
// Provides: BG_LAYOUT, BG_CONTAINER, BORDER, TEXT, TEXT_SECONDARY
// Usage: const colors = useSemanticColors();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Settings page with search and card grid</name>
  <files>frontend/src/pages/Settings.tsx, frontend/src/pages/index.ts, frontend/src/App.tsx, frontend/src/layouts/MainLayout.tsx</files>
  <read_first>
    - frontend/src/layouts/MainLayout.tsx (Plan 01 产物，需要在管理组添加 /settings 菜单项)
    - frontend/src/App.tsx (当前路由结构，需要添加 /settings 路由)
    - frontend/src/pages/index.ts (导出模式)
    - frontend/src/pages/AuditLogs.tsx (现有页面参考风格)
    - frontend/src/theme/useSemanticColors.ts (颜色 token 使用方式)
    - .planning/phases/15-menu-reorganize-settings-nav/15-UI-SPEC.md (IC-04 到 IC-06, 卡片顺序和内容)
  </read_first>
  <action>
**A. 创建 frontend/src/pages/Settings.tsx**

新建设置页组件，包含搜索过滤和卡片网格。

1. 定义卡片数据结构（per D-07, D-09, UI-SPEC 卡片顺序）：

```typescript
interface SettingsCard {
  key: string;
  title: string;
  description: string;
  keywords: string[];
  linkTo?: string;       // 前往链接（per D-09）
  roles: string[];       // 角色过滤
  content?: React.ReactNode;  // 内联内容（如主题切换）
}

const SETTINGS_CARDS: SettingsCard[] = [
  {
    key: 'theme',
    title: '外观设置',
    description: '切换亮色/暗黑主题模式',
    keywords: ['主题', '外观', '暗黑', '亮色', 'dark', 'light', '模式'],
    roles: ['admin', 'hr'],
    // content: 内联主题切换开关（使用 useThemeMode）
  },
  {
    key: 'account',
    title: '账号安全',
    description: '修改密码和账号安全设置',
    keywords: ['密码', '账号', '安全', '修改密码'],
    roles: ['admin', 'hr'],
  },
  {
    key: 'audit',
    title: '审计日志',
    description: '查看系统操作记录和用户行为追踪',
    keywords: ['日志', '审计', '操作记录', '行为'],
    linkTo: '/audit-logs',
    roles: ['admin'],
  },
  {
    key: 'api-keys',
    title: 'API 密钥',
    description: '管理 DeepSeek 等外部服务 API 密钥',
    keywords: ['密钥', 'API', 'DeepSeek', '外部服务'],
    linkTo: '/api-keys',
    roles: ['admin'],
  },
  {
    key: 'feishu',
    title: '飞书集成',
    description: '飞书同步和文档映射配置',
    keywords: ['飞书', '同步', '集成', '文档映射'],
    linkTo: '/feishu-settings',
    roles: ['admin'],
    // 仅在 feishu_sync_enabled 时显示（通过 useFeishuFeatureFlag）
  },
];
```

2. 搜索过滤逻辑（per D-08, IC-04）：
   - 使用 `useState` 维护 `searchTerm`
   - 过滤逻辑：`!searchTerm || title.includes(searchTerm) || description.includes(searchTerm) || keywords.some(k => k.includes(searchTerm))`
   - 大小写不敏感（toLowerCase 比较）
   - 先按角色过滤（使用 `useAuth` 获取 `user.role`），再按搜索词过滤

3. 关键词高亮函数（per D-08, IC-04）：
```typescript
function highlightText(text: string, keyword: string): React.ReactNode {
  if (!keyword) return text;
  const lowerText = text.toLowerCase();
  const lowerKeyword = keyword.toLowerCase();
  const index = lowerText.indexOf(lowerKeyword);
  if (index === -1) return text;
  return (
    <>
      {text.slice(0, index)}
      <mark style={{ background: '#FFF7E6', padding: 0, borderRadius: 2 }}>
        {text.slice(index, index + keyword.length)}
      </mark>
      {text.slice(index + keyword.length)}
    </>
  );
}
```
注意：使用 AntD token `colorWarningBg` 而非硬编码颜色。从 `theme` 中获取：`const { token } = theme.useToken();` 然后 `background: token.colorWarningBg`。

4. 页面布局（per IC-06, UI-SPEC）：
   - 页面标题：`<Typography.Title level={3}>系统设置</Typography.Title>`
   - 搜索框：`<Input.Search placeholder="搜索设置项..." allowClear onChange={e => setSearchTerm(e.target.value)} style={{ maxWidth: 400, marginBottom: 24 }} />`
   - 卡片网格：`<Row gutter={[24, 24]}>` 包裹 `<Col xs={24} md={12} lg={8}>` 每列一个 Card
   - 每个 Card：标题用 `highlightText(card.title, searchTerm)`，描述用 `highlightText(card.description, searchTerm)`
   - 有 `linkTo` 的卡片在底部显示 `<Link to={card.linkTo}>前往</Link>`（per D-09, IC-05）
   - 外观设置卡片内联显示主题切换：使用 `useThemeMode` 的 `isDark` 和 `toggleMode`，渲染 Switch 组件
   - 空搜索结果：显示 `<Empty description="未找到匹配的设置项" />` + `<Typography.Text type="secondary">请尝试其他关键词</Typography.Text>`

5. 飞书卡片条件显示：使用 `useFeishuFeatureFlag` 获取 `feishu_sync_enabled`，在过滤时排除未启用的飞书卡片

6. 导出：`export default function SettingsPage()` 或 `export function SettingsPage()`

**B. 更新 frontend/src/pages/index.ts**

添加一行导出：
```typescript
export { default as SettingsPage } from './Settings';
```
如果 Settings.tsx 使用命名导出则改为 `export * from './Settings';`

**C. 更新 frontend/src/App.tsx**

1. 在 import 区域添加 `SettingsPage`（从 `'./pages'` 导入）
2. 在 `<RoleRoute allowedRoles={['admin', 'hr']} />` 内添加路由：
```tsx
<Route path="/settings" element={<SettingsPage />} />
```
放在现有的 admin/hr 路由组内（`/data-management` 附近）。

**D. 更新 frontend/src/layouts/MainLayout.tsx**

在 `MENU_GROUPS` 的 `group-admin` 组 children 末尾添加设置菜单项：
```typescript
{ key: '/settings', icon: <SettingOutlined />, label: '系统设置', roles: ['admin', 'hr'] },
```

注意：SettingOutlined 已经在 MainLayout.tsx 中 import（当前用于飞书设置），无需新增 import。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/Settings.tsx 文件存在
    - Settings.tsx 包含 `SETTINGS_CARDS` 数组定义
    - Settings.tsx 包含 `searchTerm` state 或等价搜索状态
    - Settings.tsx 包含 `highlightText` 函数或等价高亮逻辑
    - Settings.tsx 包含 `<mark` 标签用于高亮
    - Settings.tsx 包含 `token.colorWarningBg` 或从 theme.useToken 获取高亮颜色
    - Settings.tsx 包含 `搜索设置项` placeholder 文本
    - Settings.tsx 包含 `未找到匹配的设置项` 空状态文本
    - Settings.tsx 包含 `前往` 链接文本
    - Settings.tsx 包含 `linkTo` 属性指向 `/audit-logs`、`/api-keys`、`/feishu-settings`
    - Settings.tsx 包含 `useFeishuFeatureFlag` 或 `feishu_sync_enabled`
    - Settings.tsx 包含 `useAuth` 用于角色过滤
    - frontend/src/pages/index.ts 包含 `Settings` 导出
    - frontend/src/App.tsx 包含 `path="/settings"` 路由
    - frontend/src/App.tsx 包含 `SettingsPage` import
    - frontend/src/layouts/MainLayout.tsx group-admin children 包含 `key: '/settings'`
    - `npm run lint` 通过
    - `npm run build` 通过
  </acceptance_criteria>
  <done>设置页创建完成，包含搜索过滤、关键词高亮、前往链接、角色过滤、飞书条件显示，路由注册完成，菜单入口添加完成</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify menu grouping and settings page</name>
  <files>N/A</files>
  <action>
用户手动验证 Plan 01（菜单分组）和 Plan 02（设置页）的完整功能。无需自动化操作，仅需在浏览器中验证交互行为。
  </action>
  <what-built>
    完整的菜单重组和设置页功能：
    1. 侧边栏三组 SubMenu 分组（常用/数据分析/管理）
    2. 快速融合置顶
    3. 折叠/展开状态 localStorage 持久化
    4. /settings 统一设置页（搜索过滤 + 关键词高亮 + 前往链接）
  </what-built>
  <how-to-verify>
    启动前端：`cd frontend && npm run dev`

    **菜单分组验证（UX-04）：**
    1. 以 admin 登录，确认侧边栏显示：快速融合（置顶）-> 常用（展开）-> 数据分析（折叠）-> 管理（折叠）
    2. 点击「数据分析」分组标题，确认展开显示 4 个子项
    3. 点击「常用」分组标题，确认可折叠
    4. 刷新页面，确认展开/折叠状态保持不变
    5. 点击侧边栏收缩按钮，确认 SubMenu 变为弹出模式
    6. 展开侧边栏，确认分组状态恢复

    **设置页验证（UX-05）：**
    7. 点击管理分组中的「系统设置」，确认导航到 /settings
    8. 确认页面显示搜索框和多个设置卡片
    9. 在搜索框输入「飞书」，确认只显示飞书相关卡片，关键词高亮
    10. 清空搜索框，确认所有卡片恢复
    11. 输入不存在的关键词，确认显示「未找到匹配的设置项」空状态
    12. 点击审计日志卡片的「前往」链接，确认跳转到 /audit-logs

    **暗黑模式验证：**
    13. 切换到暗黑模式，确认菜单分组和设置页在暗黑模式下视觉正常
  </how-to-verify>
  <verify>用户手动在浏览器中完成上述 13 个验证步骤</verify>
  <done>用户确认菜单分组和设置页功能完整，输入 "approved"</done>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 角色过滤 -> 设置页卡片 | admin-only 卡片（审计日志、API 密钥）不应对 hr 角色可见 |
| 角色过滤 -> /settings 路由 | /settings 路由仅允许 admin 和 hr 角色访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Elevation of Privilege | Settings.tsx 卡片过滤 | mitigate | SETTINGS_CARDS 中每个卡片有 roles 字段，渲染前按 user.role 过滤，admin-only 卡片不对 hr 显示。验证：grep 确认 roles filter 逻辑存在 |
| T-15-05 | Elevation of Privilege | /settings 路由 | mitigate | /settings 路由在 RoleRoute allowedRoles={['admin', 'hr']} 内注册，employee 无法访问。验证：App.tsx 中 /settings 在正确的 RoleRoute 内 |
| T-15-06 | Information Disclosure | 搜索框输入 | accept | 搜索框为纯前端过滤，不发送任何网络请求，无信息泄露风险 |
| T-15-07 | Spoofing | highlightText XSS | mitigate | 使用 React JSX 节点拼接而非 dangerouslySetInnerHTML，防止 XSS。验证：Settings.tsx 不包含 dangerouslySetInnerHTML |
</threat_model>

<verification>
1. `npm run lint` 通过
2. `npm run build` 通过
3. /settings 路由在 RoleRoute ['admin', 'hr'] 内
4. Settings.tsx 包含搜索过滤和高亮逻辑
5. Settings.tsx 不包含 dangerouslySetInnerHTML
6. MainLayout.tsx 管理组包含 /settings 菜单项
</verification>

<success_criteria>
- /settings 路由可访问，显示统一设置页
- 搜索框输入关键词后过滤卡片，高亮匹配文本
- 清空搜索恢复全部卡片
- 无匹配结果显示空状态提示
- 审计日志/API 密钥/飞书设置卡片有前往链接
- 角色过滤正确（admin 看到全部卡片，hr 只看到外观设置和账号安全）
- 侧边栏管理组包含系统设置入口
- lint + build 通过
- 手动验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/15-menu-reorganize-settings-nav/15-02-SUMMARY.md`
</output>
