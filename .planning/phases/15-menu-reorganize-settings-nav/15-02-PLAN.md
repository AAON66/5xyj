---
phase: 15-menu-reorganize-settings-nav
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - frontend/src/pages/Settings.tsx
  - frontend/src/pages/index.ts
  - frontend/src/App.tsx
  - frontend/src/layouts/MainLayout.tsx
autonomous: false
requirements: [UX-05]
must_haves:
  truths:
    - "管理员/HR 可以通过 /settings 路由访问统一设置页"
    - "设置页顶部有搜索框，输入关键词后隐藏不匹配的卡片"
    - "匹配的卡片中关键词文本高亮显示"
    - "搜索后自动滚动到首个匹配卡片"
    - "审计日志、API 密钥、飞书设置卡片提供前往链接跳转到独立页面"
    - "清空搜索恢复全部卡片"
    - "无匹配结果时显示空状态提示"
    - "侧边栏管理分组中包含系统设置入口，admin 和 hr 均可见"
  artifacts:
    - path: "frontend/src/pages/Settings.tsx"
      provides: "统一设置页组件"
      exports: ["SettingsPage"]
    - path: "frontend/src/pages/index.ts"
      provides: "Settings 页面导出"
      contains: "Settings"
    - path: "frontend/src/App.tsx"
      provides: "/settings 路由"
      contains: "/settings"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Settings.tsx"
      via: "Route path=/settings"
      pattern: "/settings"
    - from: "frontend/src/layouts/MainLayout.tsx"
      to: "/settings"
      via: "管理组菜单项"
      pattern: "/settings"
---

<objective>
新建统一设置页 /settings，支持卡片分区展示各类设置项，搜索框快速过滤并高亮匹配关键词，搜索后自动滚动到首个匹配卡片。审计日志/API 密钥/飞书设置卡片提供前往链接。在侧边栏管理分组中添加设置入口（admin 和 hr 均可见）。

Purpose: 用户通过搜索快速定位设置项，不需要逐个翻找菜单（UX-05）
Output: Settings.tsx 页面 + 路由注册 + 菜单入口
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-menu-reorganize-settings-nav/15-CONTEXT.md
@.planning/phases/15-menu-reorganize-settings-nav/15-RESEARCH.md
@.planning/phases/15-menu-reorganize-settings-nav/15-UI-SPEC.md
@.planning/phases/15-menu-reorganize-settings-nav/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: MainLayout now uses grouped menu structure -->
From frontend/src/layouts/MainLayout.tsx (after Plan 01):
```typescript
interface NavItem {
  key: string;
  icon: React.ReactNode;
  label: string;
  roles: string[];
}

interface MenuGroupConfig {
  key: string;
  label: string;
  icon: React.ReactNode;
  children: NavItem[];
  defaultOpen: boolean;
}

const TOP_ITEM: NavItem = { key: '/aggregate', ... };
const MENU_GROUPS: MenuGroupConfig[] = [
  { key: 'group-common', label: '常用', ... },
  { key: 'group-analysis', label: '数据分析', ... },
  { key: 'group-admin', label: '管理', children: [...] },
];
// 管理组 children 中需要添加 /settings 项

// LABEL_MAP already contains: settings: '系统设置' (added in Plan 01)
```

From frontend/src/App.tsx:
```typescript
// Route structure uses RoleRoute for access control
<Route element={<RoleRoute allowedRoles={['admin', 'hr']} />}>
  // ... existing admin+hr routes (dashboard, imports, exports, etc.)
</Route>
<Route element={<RoleRoute allowedRoles={['admin']} />}>
  // ... admin-only routes (audit-logs, api-keys, feishu-settings)
</Route>
```

From frontend/src/pages/index.ts:
```typescript
// Export pattern examples:
export { default as AnomalyDetectionPage } from './AnomalyDetection';
export * from './ApiKeys';
export * from './AuditLogs';
// ... etc
```

From frontend/src/theme/useSemanticColors.ts:
```typescript
// Provides: BG_LAYOUT, BG_CONTAINER, BORDER, TEXT, TEXT_SECONDARY
// Usage: const colors = useSemanticColors();
```

From frontend/src/hooks/useAuth.ts:
```typescript
// Provides: user (with user.role), logout, isAuthenticated
// Usage: const { user } = useAuth();
```

From frontend/src/theme/useThemeMode.ts:
```typescript
// Provides: isDark, toggleMode
// Usage: const { isDark, toggleMode } = useThemeMode();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Settings page with search, card grid, and auto-scroll navigation</name>
  <files>frontend/src/pages/Settings.tsx, frontend/src/pages/index.ts, frontend/src/App.tsx, frontend/src/layouts/MainLayout.tsx</files>
  <read_first>
    - frontend/src/layouts/MainLayout.tsx (Plan 01 产物，需要在管理组添加 /settings 菜单项)
    - frontend/src/App.tsx (当前路由结构，需要添加 /settings 路由)
    - frontend/src/pages/index.ts (导出模式)
    - frontend/src/pages/AuditLogs.tsx (现有页面参考风格)
    - frontend/src/theme/useSemanticColors.ts (颜色 token 使用方式)
    - frontend/src/hooks/useAuth.ts (角色获取方式)
    - frontend/src/hooks/useFeishuFeatureFlag.ts (飞书 feature flag)
    - frontend/src/theme/useThemeMode.ts (主题切换 hook)
    - .planning/phases/15-menu-reorganize-settings-nav/15-UI-SPEC.md (IC-04 到 IC-06, 卡片顺序和内容)
  </read_first>
  <action>
Addresses review concerns: 搜索导航行为、卡片范围收紧、角色可见性明确、highlightText 特殊字符转义。

**A. 创建 frontend/src/pages/Settings.tsx**

新建设置页组件。

1. 定义设置卡片配置数据结构。只保留有实际落点的卡片（addresses review concern MEDIUM: 移除无落点的占位卡片）：

```typescript
import { useState, useMemo, useRef, useEffect } from 'react';
import { Link } from 'react-router-dom';
import { Input, Card, Row, Col, Typography, Switch, Empty, theme } from 'antd';
import { useAuth } from '../hooks/useAuth';
import { useFeishuFeatureFlag } from '../hooks/useFeishuFeatureFlag';
import { useThemeMode } from '../theme/useThemeMode';
import { SunOutlined, MoonOutlined } from '@ant-design/icons';

const { Title, Text } = Typography;

interface SettingsCardConfig {
  key: string;
  title: string;
  description: string;
  keywords: string[];
  linkTo?: string;
  roles: string[];
  feishuOnly?: boolean;  // 仅在飞书开关启用时显示
}

const SETTINGS_CARDS: SettingsCardConfig[] = [
  {
    key: 'theme',
    title: '外观设置',
    description: '切换亮色/暗黑主题模式',
    keywords: ['主题', '外观', '暗黑', '亮色', 'dark', 'light', '模式'],
    roles: ['admin', 'hr'],
  },
  {
    key: 'audit',
    title: '审计日志',
    description: '查看系统操作记录和用户行为追踪',
    keywords: ['日志', '审计', '操作记录', '行为'],
    linkTo: '/audit-logs',
    roles: ['admin'],
  },
  {
    key: 'api-keys',
    title: 'API 密钥',
    description: '管理 DeepSeek 等外部服务 API 密钥',
    keywords: ['密钥', 'API', 'DeepSeek', '外部服务'],
    linkTo: '/api-keys',
    roles: ['admin'],
  },
  {
    key: 'feishu',
    title: '飞书集成',
    description: '飞书同步和文档映射配置',
    keywords: ['飞书', '同步', '集成', '文档映射'],
    linkTo: '/feishu-settings',
    roles: ['admin'],
    feishuOnly: true,
  },
];
```

注意：移除了 `account`（账号安全）卡片——Phase 16 才实现账号管理功能，当前无落点（addresses review concern MEDIUM: 占位卡片范围膨胀）。`theme`（外观设置）保留，因为它有实际内联内容（主题切换开关），与顶栏按钮不完全重复——设置页提供更详细的说明文案。

2. 转义函数防止正则特殊字符导致错误（addresses review concern LOW: highlightText 正则特殊字符转义）：

```typescript
function escapeRegExp(str: string): string {
  return str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}
```

3. 关键词高亮函数（per D-08, IC-04），使用 AntD theme token 获取高亮色：

```typescript
function highlightText(text: string, keyword: string, highlightBg: string): React.ReactNode {
  if (!keyword) return text;
  const escaped = escapeRegExp(keyword);
  const regex = new RegExp(`(${escaped})`, 'gi');
  const parts = text.split(regex);
  if (parts.length === 1) return text;
  return (
    <>
      {parts.map((part, i) =>
        regex.test(part) ? (
          <mark key={i} style={{ background: highlightBg, padding: 0, borderRadius: 2 }}>
            {part}
          </mark>
        ) : (
          part
        )
      )}
    </>
  );
}
```

注意：使用 `split(regex)` + `test` 而非 `indexOf`，这样可以处理多次出现的关键词。`regex` 在每次 `test` 后需要重置 lastIndex——由于用 `gi` flag 但每次只测一个 `part`，实际上安全。或者更简单：用 `part.toLowerCase() === keyword.toLowerCase()` 判断匹配。选择更安全的实现：

```typescript
function highlightText(text: string, keyword: string, highlightBg: string): React.ReactNode {
  if (!keyword) return text;
  const lowerText = text.toLowerCase();
  const lowerKeyword = keyword.toLowerCase();
  const parts: React.ReactNode[] = [];
  let lastIndex = 0;
  let index = lowerText.indexOf(lowerKeyword, lastIndex);
  let partKey = 0;
  while (index !== -1) {
    if (index > lastIndex) {
      parts.push(text.slice(lastIndex, index));
    }
    parts.push(
      <mark key={partKey++} style={{ background: highlightBg, padding: 0, borderRadius: 2 }}>
        {text.slice(index, index + keyword.length)}
      </mark>
    );
    lastIndex = index + keyword.length;
    index = lowerText.indexOf(lowerKeyword, lastIndex);
  }
  if (lastIndex < text.length) {
    parts.push(text.slice(lastIndex));
  }
  return parts.length > 0 ? <>{parts}</> : text;
}
```

这个实现避免正则问题，直接用 `indexOf` 循环查找所有出现位置，同时 `escapeRegExp` 函数保留备用。

4. 搜索 + 自动滚动导航（addresses review concern HIGH: "导航"行为未定义）：

```typescript
export default function SettingsPage() {
  const { user } = useAuth();
  const { feishu_sync_enabled } = useFeishuFeatureFlag();
  const { isDark, toggleMode } = useThemeMode();
  const { token } = theme.useToken();
  const [searchTerm, setSearchTerm] = useState('');
  const firstMatchRef = useRef<HTMLDivElement>(null);
  const cardRefs = useRef<Record<string, HTMLDivElement | null>>({});

  const userRole = user?.role || '';

  // 过滤卡片：角色 + 飞书开关 + 搜索词
  const visibleCards = useMemo(() => {
    return SETTINGS_CARDS.filter(card => {
      if (!card.roles.includes(userRole)) return false;
      if (card.feishuOnly && !feishu_sync_enabled) return false;
      if (!searchTerm) return true;
      const term = searchTerm.toLowerCase();
      return (
        card.title.toLowerCase().includes(term) ||
        card.description.toLowerCase().includes(term) ||
        card.keywords.some(k => k.toLowerCase().includes(term))
      );
    });
  }, [userRole, feishu_sync_enabled, searchTerm]);

  // 搜索后自动滚动到首个匹配卡片（addresses review concern HIGH: 搜索导航）
  useEffect(() => {
    if (searchTerm && visibleCards.length > 0) {
      const firstKey = visibleCards[0].key;
      const el = cardRefs.current[firstKey];
      if (el) {
        el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
      }
    }
  }, [searchTerm, visibleCards]);

  const highlightBg = token.colorWarningBg;

  return (
    <div>
      <Title level={3}>系统设置</Title>
      <Input.Search
        placeholder="搜索设置项..."
        allowClear
        onChange={e => setSearchTerm(e.target.value)}
        style={{ maxWidth: 400, marginBottom: 24 }}
      />
      {visibleCards.length === 0 && searchTerm ? (
        <Empty description={
          <div>
            <div>未找到匹配的设置项</div>
            <Text type="secondary">请尝试其他关键词</Text>
          </div>
        } />
      ) : (
        <Row gutter={[24, 24]}>
          {visibleCards.map((card, idx) => (
            <Col xs={24} md={12} lg={8} key={card.key}>
              <div ref={el => { cardRefs.current[card.key] = el; }}>
                <Card
                  title={highlightText(card.title, searchTerm, highlightBg)}
                  style={idx === 0 && searchTerm ? { boxShadow: `0 0 0 2px ${token.colorPrimary}` } : undefined}
                >
                  <p>{highlightText(card.description, searchTerm, highlightBg)}</p>
                  {/* 外观设置：内联主题切换 */}
                  {card.key === 'theme' && (
                    <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
                      <SunOutlined />
                      <Switch checked={isDark} onChange={toggleMode} />
                      <MoonOutlined />
                      <Text type="secondary">{isDark ? '暗黑模式' : '亮色模式'}</Text>
                    </div>
                  )}
                  {/* 前往链接 */}
                  {card.linkTo && (
                    <Link to={card.linkTo} style={{ color: token.colorPrimary }}>前往</Link>
                  )}
                </Card>
              </div>
            </Col>
          ))}
        </Row>
      )}
    </div>
  );
}
```

关键导航行为：搜索后首个匹配卡片获得 `colorPrimary` 描边聚焦 + `scrollIntoView` 自动滚动。独立页面卡片保留"前往"按钮。

**B. 更新 frontend/src/pages/index.ts**

添加一行导出：
```typescript
export { default as SettingsPage } from './Settings';
```

**C. 更新 frontend/src/App.tsx**

1. 在 import 区域添加 `SettingsPage`（从 `'./pages'` 导入，添加到已有的解构导入中）
2. 在 `<RoleRoute allowedRoles={['admin', 'hr']} />` 内添加路由（addresses review concern MEDIUM: /settings 对 admin 和 hr 都可见）：
```tsx
<Route path="/settings" element={<SettingsPage />} />
```
放在现有的 `/data-management` 路由附近。

**D. 更新 frontend/src/layouts/MainLayout.tsx**

在 `MENU_GROUPS` 的 `group-admin` 组 children 末尾添加设置菜单项（addresses review concern MEDIUM: 明确对 admin 和 hr 都可见）：
```typescript
{ key: '/settings', icon: <SettingOutlined />, label: '系统设置', roles: ['admin', 'hr'] },
```

注意：SettingOutlined 已经在 MainLayout.tsx 中 import，无需新增 import。放在 `group-admin` 的 children 数组最后一项（在 `/api-keys` 之后）。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/Settings.tsx 文件存在
    - Settings.tsx 包含 `SETTINGS_CARDS` 数组定义
    - Settings.tsx 不包含 key 为 `'account'` 的卡片（无落点卡片已移除）
    - Settings.tsx 包含 `searchTerm` state
    - Settings.tsx 包含 `highlightText` 函数
    - Settings.tsx 包含 `<mark` 标签用于高亮
    - Settings.tsx 包含 `token.colorWarningBg` 或从 theme.useToken 获取高亮颜色
    - Settings.tsx 包含 `scrollIntoView` 实现自动滚动导航
    - Settings.tsx 包含 `boxShadow` 或 `colorPrimary` 实现首个匹配卡片聚焦描边
    - Settings.tsx 包含 `搜索设置项` placeholder 文本
    - Settings.tsx 包含 `未找到匹配的设置项` 空状态文本
    - Settings.tsx 包含 `请尝试其他关键词` 文本
    - Settings.tsx 包含 `前往` 链接文本
    - Settings.tsx 包含 `linkTo` 属性指向 `/audit-logs`、`/api-keys`、`/feishu-settings`
    - Settings.tsx 包含 `feishuOnly` 或 `feishu_sync_enabled` 飞书条件过滤
    - Settings.tsx 包含 `useAuth` 用于角色过滤
    - Settings.tsx 包含 `useThemeMode` 用于主题切换内联内容
    - Settings.tsx 不包含 `dangerouslySetInnerHTML`
    - frontend/src/pages/index.ts 包含 `SettingsPage` 导出
    - frontend/src/App.tsx 包含 `path="/settings"` 路由
    - frontend/src/App.tsx 包含 `SettingsPage` import
    - frontend/src/App.tsx 中 /settings 路由在 `allowedRoles={['admin', 'hr']}` 的 RoleRoute 内
    - frontend/src/layouts/MainLayout.tsx group-admin children 包含 `key: '/settings'`
    - MainLayout.tsx 中 /settings 菜单项 roles 为 `['admin', 'hr']`
    - `npm run lint` 通过
    - `npm run build` 通过
  </acceptance_criteria>
  <done>设置页创建完成：搜索过滤 + 关键词高亮 + 首个匹配卡片自动滚动聚焦 + 前往链接 + 角色过滤 + 飞书条件显示 + 主题切换内联，路由注册完成，菜单入口在管理组中对 admin 和 hr 可见</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>N/A</files>
  <action>用户手动验证 Plan 01（菜单分组）和 Plan 02（设置页）的完整功能。无需自动化操作，仅需在浏览器中验证交互行为。</action>
  <name>Task 2: Verify menu grouping and settings page</name>
  <what-built>
    完整的菜单重组和设置页功能：
    1. 侧边栏三组 SubMenu 分组（常用/数据分析/管理）
    2. 快速融合置顶
    3. 折叠/展开状态 localStorage 持久化
    4. 子路由页面正确高亮父菜单项
    5. /settings 统一设置页（搜索过滤 + 关键词高亮 + 自动滚动 + 前往链接）
  </what-built>
  <how-to-verify>
    启动前端：`cd frontend && npm run dev`

    **菜单分组验证（UX-04）：**
    1. 以 admin 登录，确认侧边栏显示：快速融合（置顶）-> 常用（展开）-> 数据分析（折叠）-> 管理（折叠）
    2. 点击「数据分析」分组标题，确认展开显示 4 个子项（月度对比、跨期对比、异常检测、映射修正）
    3. 点击「常用」分组标题，确认可折叠
    4. 刷新页面（F5），确认展开/折叠状态保持不变
    5. 点击侧边栏收缩按钮，确认 SubMenu 变为弹出模式
    6. 展开侧边栏，确认分组状态恢复
    7. 点击「批次管理」进入批次列表，再点击某个批次进入详情页（/imports/:batchId），确认「批次管理」菜单项高亮且「常用」分组展开

    **设置页验证（UX-05）：**
    8. 点击管理分组中的「系统设置」，确认导航到 /settings
    9. 确认页面标题为「系统设置」，显示搜索框和设置卡片
    10. 在搜索框输入「飞书」，确认只显示飞书相关卡片，关键词高亮，首个匹配卡片有蓝色描边
    11. 清空搜索框，确认所有卡片恢复
    12. 输入不存在的关键词（如「xyz」），确认显示「未找到匹配的设置项」空状态
    13. 点击审计日志卡片的「前往」链接，确认跳转到 /audit-logs
    14. 确认外观设置卡片内有主题切换开关，点击可切换暗黑/亮色模式

    **暗黑模式验证：**
    15. 切换到暗黑模式，确认菜单分组和设置页在暗黑模式下视觉正常（无白色块、无不可读文字）
  </how-to-verify>
  <verify>用户手动在浏览器中完成上述 15 个验证步骤</verify>
  <done>用户确认菜单分组和设置页功能完整，输入 "approved"</done>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 角色过滤 -> 设置页卡片 | admin-only 卡片（审计日志、API 密钥）不应对 hr 角色可见 |
| 角色过滤 -> /settings 路由 | /settings 路由仅允许 admin 和 hr 角色访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Elevation of Privilege | Settings.tsx 卡片过滤 | mitigate | SETTINGS_CARDS 中每个卡片有 roles 字段，渲染前按 user.role 过滤，admin-only 卡片（审计日志、API 密钥）不对 hr 显示。验证：grep 确认 `card.roles.includes(userRole)` 过滤逻辑存在 |
| T-15-05 | Elevation of Privilege | /settings 路由 | mitigate | /settings 路由在 `RoleRoute allowedRoles={['admin', 'hr']}` 内注册，employee 无法访问。验证：App.tsx 中 /settings 在正确的 RoleRoute 内 |
| T-15-06 | Information Disclosure | 搜索框输入 | accept | 搜索框为纯前端过滤，不发送任何网络请求，无信息泄露风险 |
| T-15-07 | Spoofing | highlightText XSS | mitigate | 使用 React JSX 节点拼接 + indexOf 循环实现高亮，不使用 dangerouslySetInnerHTML，防止 XSS。验证：Settings.tsx 不包含 dangerouslySetInnerHTML |
</threat_model>

<verification>
1. `npm run lint` 通过
2. `npm run build` 通过
3. /settings 路由在 RoleRoute ['admin', 'hr'] 内
4. Settings.tsx 包含搜索过滤、高亮逻辑、scrollIntoView 导航
5. Settings.tsx 不包含 dangerouslySetInnerHTML
6. Settings.tsx 不包含 key 为 'account' 的无落点占位卡片
7. MainLayout.tsx 管理组包含 /settings 菜单项且 roles 为 ['admin', 'hr']
</verification>

<success_criteria>
- /settings 路由可访问，显示统一设置页
- 搜索框输入关键词后过滤卡片，高亮匹配文本
- 搜索后自动滚动到首个匹配卡片并显示聚焦描边
- 清空搜索恢复全部卡片
- 无匹配结果显示空状态提示
- 审计日志/API 密钥/飞书设置卡片有前往链接
- 角色过滤正确（admin 看到审计日志/API密钥/飞书集成卡片，hr 只看到外观设置）
- 侧边栏管理组包含系统设置入口，admin 和 hr 均可见
- lint + build 通过
- 手动验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/15-menu-reorganize-settings-nav/15-02-SUMMARY.md`
</output>
