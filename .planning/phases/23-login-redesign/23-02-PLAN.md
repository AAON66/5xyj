---
phase: 23-login-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - frontend/src/components/ParticleWave.tsx
autonomous: true
requirements:
  - LOGIN-02
  - LOGIN-04
user_setup: []
nyquist_compliant: true

must_haves:
  truths:
    - "ParticleWave.tsx 渲染 Three.js Scene，2560 粒子（80×32 网格，落入 D-02 的 2000-3000 区间）"
    - "粒子在 vertex shader 中按双正弦波浪方程实时位移（D-01）"
    - "鼠标在画布上移动时，粒子在鼠标位置呈高斯隆起，鼠标离开后 0.8s 内平滑回落（D-03）"
    - "粒子颜色在 3 色谱之间按 Z 高度插值；isDark prop 切换时色谱相应切换（D-04, D-06）"
    - "粒子形状为圆形点（fragment shader 用 gl_PointCoord + smoothstep 画圆，D-05）"
    - "组件卸载时 geometry/material/renderer 全部 dispose 且 forceContextLoss 被调用（避免 React 18 Strict Mode context 泄露）"
    - "prefers-reduced-motion: reduce 下关闭动画和鼠标交互，渲染静态粒子网格"
    - "canvas DOM 带 data-testid='particle-wave-canvas' 和 data-particle-color attr（暴露当前 uniforms 主色供 Playwright 断言）"
  artifacts:
    - path: "frontend/src/components/ParticleWave.tsx"
      provides: "Three.js 3D 粒子波浪组件（懒加载入口）"
      min_lines: 150
      exports: ["default"]
  key_links:
    - from: "frontend/src/components/ParticleWave.tsx"
      to: "three (npm package)"
      via: "import { Scene, PerspectiveCamera, WebGLRenderer, Points, BufferGeometry, Float32BufferAttribute, ShaderMaterial, AdditiveBlending, Color, Vector2 } from 'three'"
      pattern: "from 'three'"
    - from: "ParticleWave vertexShader"
      to: "uTime / uMouse uniforms"
      via: "wave math in GPU"
      pattern: "sin\\(p\\.x"
    - from: "ParticleWave cleanup"
      to: "GPU resource release"
      via: "dispose() + forceContextLoss()"
      pattern: "renderer\\.dispose\\(\\)"
---

<objective>
实现 Phase 23 的核心视觉组件 — Three.js 3D 粒子波浪 `ParticleWave.tsx`。
- 2560 粒子（80×32 网格）在 GPU vertex shader 里按双正弦方程做波浪位移
- 鼠标在画布上产生柔和高斯隆起跟随，离开后 800ms 平滑衰减回落
- 暗/亮模式切换 3 色谱 uniforms（深海荧光 vs 品牌海面）
- 严格 cleanup：geometry / material / renderer dispose + forceContextLoss（Strict Mode 安全）
- 尊重 `prefers-reduced-motion`（静态网格 + 关闭鼠标交互）
- `webglcontextlost` 事件兜底，`document.hidden` 暂停渲染
- 暴露 `data-testid="particle-wave-canvas"` 和 `data-particle-color` attr（供 Plan 03 测试）
- 使用 `export default function ParticleWave(...)` 语法支持 `React.lazy(() => import(...))`

Purpose: LOGIN-02（3D 粒子波浪 + 鼠标交互）+ LOGIN-04（粒子色随暗黑模式切换）的唯一实现载体

Output: 单一新组件文件 `frontend/src/components/ParticleWave.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-login-redesign/23-CONTEXT.md
@.planning/phases/23-login-redesign/23-RESEARCH.md
@.planning/phases/23-login-redesign/23-UI-SPEC.md
@.planning/phases/23-login-redesign/23-VALIDATION.md
@.planning/phases/23-login-redesign/23-01-SUMMARY.md

<interfaces>
<!-- Downstream contract — Plan 03 imports this via React.lazy -->

```typescript
// default export signature Plan 03 will consume
export default function ParticleWave(props: ParticleWaveProps): JSX.Element;

interface ParticleWaveProps {
  isDark: boolean;
}
```

Plan 01 已提供（本 Plan 依赖但不修改）:
- `frontend/src/hooks/useWebGLSupport.ts` — 不直接使用（Plan 03 使用）
- `frontend/src/components/CssGradientBackground.tsx` — 不直接使用（Plan 03 作为 Suspense fallback）
- `frontend/tests/e2e/login-redesign.spec.ts` — 已引用本 Plan 要创建的 testid `particle-wave-canvas` 和 `data-particle-color` attr

锁定色谱（从 UI-SPEC Color 节 copy，不得改数值）:
- 亮模式: `#E8F0FF`(bright), `#3370FF`(mid), `#1D4AC7`(deep)
- 暗模式: `#7FA7FF`(bright), `#3370FF`(mid), `#1B2B4D`(deep)

锁定波浪参数（从 UI-SPEC 3D Canvas Contract）:
- freq_x=0.35, freq_y=0.25, amp=0.6, speed=0.6 rad/s, speed2=0.42
- 鼠标 gaussian sigma=3.5, amplitude=1.2, decay=0.8s（用 0.05 per-frame lerp ≈ 800ms 时间常数）
- camera fov=55, position (0,-14,12), lookAt (0,0,0)
- renderer antialias=true, alpha=true, pixelRatio=min(2, devicePixelRatio)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 ParticleWave.tsx（Three.js 粒子波浪组件）</name>
  <files>frontend/src/components/ParticleWave.tsx</files>
  <behavior>
    - 组件 mount 时创建 Scene / PerspectiveCamera / WebGLRenderer / Points，append canvas 到 mountRef DOM
    - vertex shader 内置双正弦波浪 + 高斯鼠标隆起，CPU 每帧只更新 uTime / uMouse / uMouseStrength 3 个 uniform
    - fragment shader 用 `gl_PointCoord + smoothstep` 绘制圆形 sprite（无外部纹理）
    - 色谱：3 uniforms（uColorBright / uColorMid / uColorDeep），isDark 分支赋不同色值
    - 鼠标移动：NDC 转 world 平面坐标 → uMouse uniform；uMouseStrength 从 0 lerp 到 1（进入）或 1 lerp 到 0（离开），lerp 系数 0.05（≈ 800ms 时间常数）
    - unmount cleanup: 调用 `geometry.dispose()`, `material.dispose()`, `renderer.dispose()`, `renderer.forceContextLoss()`, `mount.removeChild(renderer.domElement)`; `cancelAnimationFrame`; 移除所有 event listener
    - `document.hidden === true` 时跳过 RAF（不渲染，不累积 uTime）
    - `prefers-reduced-motion: reduce` 时：不启动 RAF、不挂鼠标监听、渲染一次静态网格（z=0）
    - `webglcontextlost` 事件：preventDefault + cancelAnimationFrame
    - canvas DOM 含 `data-testid="particle-wave-canvas"` 和 `data-particle-color={brightHex}`（Playwright 读取 data-particle-color 确认色谱切换）
    - isDark 变化时重新构建 effect（useEffect deps 含 isDark），保证色谱切换
  </behavior>
  <read_first>
    - .planning/phases/23-login-redesign/23-RESEARCH.md（Pattern 1 完整实现参考；Pitfalls 1/4/5/6 必须内化）
    - .planning/phases/23-login-redesign/23-UI-SPEC.md（3D Canvas Contract 节：逐行参数值全部生效）
    - .planning/phases/23-login-redesign/23-CONTEXT.md（D-01..D-06 决策原文）
    - frontend/src/components/CandidateSelectModal.tsx（项目组件风格参考）
    - frontend/src/hooks/useWebGLSupport.ts（Plan 01 创建；这是 WebGL 能力判定的对应件，本组件必须假定宿主已检测成功再懒加载，但仍需自带 `webglcontextlost` 兜底）
    - frontend/tsconfig.json（确认 strict 模式和 moduleResolution 以便 shader 字符串不被 TS 拒绝）
  </read_first>
  <action>
    创建 `frontend/src/components/ParticleWave.tsx`，一次性实现整个组件，**使用 default export**
    （供 Plan 03 `React.lazy(() => import('../components/ParticleWave'))` 消费）：

    ```typescript
    import { useEffect, useRef } from 'react';
    import {
      AdditiveBlending,
      BufferGeometry,
      Color,
      Float32BufferAttribute,
      PerspectiveCamera,
      Points,
      Scene,
      ShaderMaterial,
      Vector2,
      WebGLRenderer,
    } from 'three';

    interface ParticleWaveProps {
      isDark: boolean;
    }

    const GRID_X = 80;
    const GRID_Y = 32;
    const SPACING = 0.4;

    // UI-SPEC 锁定色谱（D-04, D-06）— 不要改
    const COLORS_LIGHT = { bright: '#E8F0FF', mid: '#3370FF', deep: '#1D4AC7' };
    const COLORS_DARK  = { bright: '#7FA7FF', mid: '#3370FF', deep: '#1B2B4D' };

    export default function ParticleWave({ isDark }: ParticleWaveProps) {
      const mountRef = useRef<HTMLDivElement>(null);
      const canvasRef = useRef<HTMLCanvasElement | null>(null);

      useEffect(() => {
        const mount = mountRef.current;
        if (!mount) return;

        const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        const palette = isDark ? COLORS_DARK : COLORS_LIGHT;

        // === 1. Scene / Camera / Renderer ===
        const scene = new Scene();
        const camera = new PerspectiveCamera(
          55,
          mount.clientWidth / Math.max(mount.clientHeight, 1),
          0.1,
          100,
        );
        camera.position.set(0, -14, 12);
        camera.lookAt(0, 0, 0);

        const renderer = new WebGLRenderer({ antialias: true, alpha: true });
        renderer.setPixelRatio(Math.min(2, window.devicePixelRatio));
        renderer.setSize(mount.clientWidth, mount.clientHeight);
        const canvas = renderer.domElement;
        canvas.setAttribute('data-testid', 'particle-wave-canvas');
        canvas.setAttribute('data-particle-color', palette.bright.toLowerCase());
        canvas.setAttribute('aria-hidden', 'true');
        canvas.setAttribute('role', 'presentation');
        canvasRef.current = canvas;
        mount.appendChild(canvas);

        // === 2. Geometry：80×32 网格 ===
        const geometry = new BufferGeometry();
        const count = GRID_X * GRID_Y;
        const positions = new Float32Array(count * 3);
        let i = 0;
        for (let x = 0; x < GRID_X; x++) {
          for (let y = 0; y < GRID_Y; y++, i++) {
            positions[i * 3 + 0] = (x - GRID_X / 2) * SPACING;
            positions[i * 3 + 1] = (y - GRID_Y / 2) * SPACING;
            positions[i * 3 + 2] = 0;
          }
        }
        geometry.setAttribute('position', new Float32BufferAttribute(positions, 3));

        // === 3. ShaderMaterial — 波浪数学全在 vertex shader ===
        const material = new ShaderMaterial({
          uniforms: {
            uTime:          { value: 0 },
            uMouse:         { value: new Vector2(0, 0) },
            uMouseStrength: { value: 0 },
            uColorBright:   { value: new Color(palette.bright) },
            uColorMid:      { value: new Color(palette.mid) },
            uColorDeep:     { value: new Color(palette.deep) },
          },
          vertexShader: `
            uniform float uTime;
            uniform vec2  uMouse;
            uniform float uMouseStrength;
            varying float vHeight;
            void main() {
              vec3 p = position;
              // 双正弦波浪（D-01）
              float wave = sin(p.x * 0.35 + uTime * 0.60) * 0.6
                         + sin(p.y * 0.25 + uTime * 0.42) * 0.4;
              // 高斯隆起（D-03）— sigma = 3.5, amplitude = 1.2
              float d = distance(p.xy, uMouse);
              float bulge = exp(-(d * d) / (2.0 * 3.5 * 3.5)) * 1.2 * uMouseStrength;
              p.z = wave + bulge;
              vHeight = p.z;
              vec4 mvPosition = modelViewMatrix * vec4(p, 1.0);
              gl_Position = projectionMatrix * mvPosition;
              gl_PointSize = (2.0 + bulge) * (300.0 / max(-mvPosition.z, 0.001));
            }
          `,
          fragmentShader: `
            uniform vec3 uColorBright;
            uniform vec3 uColorMid;
            uniform vec3 uColorDeep;
            varying float vHeight;
            void main() {
              // 圆形 sprite（D-05）— gl_PointCoord + smoothstep
              vec2 uv = gl_PointCoord - 0.5;
              float circle = smoothstep(0.5, 0.4, length(uv));
              if (circle <= 0.0) discard;
              // 3 色谱按 Z 高度插值（D-04, D-06）
              vec3 color = vHeight > 0.0
                ? mix(uColorMid, uColorBright, clamp(vHeight, 0.0, 1.0))
                : mix(uColorDeep, uColorMid, clamp(vHeight + 1.0, 0.0, 1.0));
              gl_FragColor = vec4(color, circle);
            }
          `,
          transparent: true,
          depthWrite: false,
          blending: AdditiveBlending,
        });

        const points = new Points(geometry, material);
        scene.add(points);

        // === 4. Animation + visibility + reduced-motion 分支 ===
        let rafId = 0;
        let mouseTargetStrength = 0;
        const clockStart = performance.now();
        let lastTime = clockStart;

        const renderStatic = () => renderer.render(scene, camera);

        const animate = () => {
          if (document.hidden) {
            rafId = requestAnimationFrame(animate);
            lastTime = performance.now(); // Pitfall 6: 重置时钟
            return;
          }
          rafId = requestAnimationFrame(animate);
          const now = performance.now();
          const delta = Math.min((now - lastTime) / 1000, 0.1); // clamp 0.1s 避免背台叠加爆炸
          lastTime = now;
          material.uniforms.uTime.value += delta;
          // 平滑衰减 mouseStrength（0.05 系数 ≈ 800ms 时间常数 per D-03）
          material.uniforms.uMouseStrength.value +=
            (mouseTargetStrength - material.uniforms.uMouseStrength.value) * 0.05;
          renderer.render(scene, camera);
        };

        if (reducedMotion) {
          // 静态网格 — 无 RAF，无鼠标交互
          renderStatic();
        } else {
          rafId = requestAnimationFrame(animate);
        }

        // === 5. Event listeners ===
        const onMove = (e: MouseEvent) => {
          if (reducedMotion) return;
          const rect = mount.getBoundingClientRect();
          const ndcX = ((e.clientX - rect.left) / rect.width) * 2 - 1;
          const ndcY = -((e.clientY - rect.top) / rect.height) * 2 + 1;
          // NDC → world：粒子平面宽 80*0.4/2=16，高 32*0.4/2=6.4
          material.uniforms.uMouse.value.set(ndcX * 16, ndcY * 6.4);
          mouseTargetStrength = 1.0;
        };
        const onLeave = () => {
          mouseTargetStrength = 0;
        };
        const onResize = () => {
          const w = mount.clientWidth;
          const h = Math.max(mount.clientHeight, 1);
          camera.aspect = w / h;
          camera.updateProjectionMatrix();
          renderer.setSize(w, h);
          if (reducedMotion) renderStatic();
        };
        const onContextLost = (e: Event) => {
          e.preventDefault(); // MDN 规定必须 preventDefault 才会尝试恢复
          cancelAnimationFrame(rafId);
        };

        if (!reducedMotion) {
          mount.addEventListener('mousemove', onMove);
          mount.addEventListener('mouseleave', onLeave);
        }
        window.addEventListener('resize', onResize);
        canvas.addEventListener('webglcontextlost', onContextLost);

        // === 6. Cleanup — Strict Mode 安全 ===
        return () => {
          cancelAnimationFrame(rafId);
          mount.removeEventListener('mousemove', onMove);
          mount.removeEventListener('mouseleave', onLeave);
          window.removeEventListener('resize', onResize);
          canvas.removeEventListener('webglcontextlost', onContextLost);
          geometry.dispose();
          material.dispose();
          renderer.dispose();
          renderer.forceContextLoss();
          if (canvas.parentNode === mount) {
            mount.removeChild(canvas);
          }
          canvasRef.current = null;
        };
      }, [isDark]);

      return (
        <div
          ref={mountRef}
          data-testid="particle-wave-mount"
          style={{ position: 'absolute', inset: 0, width: '100%', height: '100%' }}
          aria-hidden="true"
        />
      );
    }
    ```

    **严格要求：**
    1. **使用 `export default function ParticleWave`** — `React.lazy` 要求默认导出
    2. **色谱字面量不得修改** — UI-SPEC 锁定值 `#E8F0FF` / `#3370FF` / `#1D4AC7` / `#7FA7FF` / `#1B2B4D`
    3. **粒子总数 = GRID_X × GRID_Y = 80 × 32 = 2560**（落在 D-02 锁定的 2000-3000 区间；UI-SPEC 锁定为 2560（80×32 网格））
    4. **波浪参数** freq 0.35/0.25, speed 0.6/0.42, amp 0.6/0.4 — 与 UI-SPEC 3D Canvas Contract 表逐字对齐
    5. **鼠标 gaussian** sigma 3.5 / amplitude 1.2 / lerp 0.05 — D-03 + UI-SPEC
    6. **camera fov 55, pos (0,-14,12), lookAt (0,0,0)** — UI-SPEC
    7. **cleanup 必须含全部 5 件**：`geometry.dispose()`, `material.dispose()`, `renderer.dispose()`, `renderer.forceContextLoss()`, `mount.removeChild(canvas)` — RESEARCH Pitfall 1 / 安全威胁 T-23-01
    8. **`webglcontextlost` 回调必须 `event.preventDefault()`** — MDN 规定
    9. **`document.hidden` 分支用增量时间 clamp 0.1s** — Pitfall 6
    10. **canvas 必须含 `data-testid="particle-wave-canvas"` 和 `data-particle-color` attr** — 供 VALIDATION Wave 0 测试读取
    11. **`aria-hidden="true"` + `role="presentation"`** — 纯装饰 per UI-SPEC Accessibility Contract
    12. **useEffect deps `[isDark]`** — 暗黑模式切换要重建场景（避免手写 shader uniform diff；整 effect 重建的代价由 React 18 Strict Mode 保护验证过）
    13. **不导入 three 的 core side-effect 子路径**，只从 `'three'` 顶层 barrel 按需导入所列构造器

    **不得：**
    - 不引入 `@react-three/fiber` 或 `@react-three/drei`（RESEARCH 明令禁止）
    - 不从 CDN 或其他源加载 three（安全 T-23-02）
    - 不用 `dangerouslySetInnerHTML`（安全 T-23-03）
    - 不使用 `any`；若遇 TypeScript 类型窄化问题可用 `as` 具体类型
    - 不在 React state 里存粒子位置（Anti-Pattern 1）
    - 不在 fragment shader 输出前不 discard 透明像素（圆形外会盖到后面的粒子 additive blending）
    - 不硬编码 `window.innerWidth`（Pitfall 4）— 必须用 `mount.getBoundingClientRect()`

    本任务对应 LOGIN-02（粒子波浪 + 鼠标交互）和 LOGIN-04 的粒子色切换部分。LOGIN-04 的卡片玻璃态在 Plan 03 Task 2 完成。
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/ParticleWave.tsx && grep -Fq "export default function ParticleWave" src/components/ParticleWave.tsx && grep -Fq "from 'three'" src/components/ParticleWave.tsx && grep -Fq "ShaderMaterial" src/components/ParticleWave.tsx && grep -Fq "sin(p.x * 0.35" src/components/ParticleWave.tsx && grep -Fq "sin(p.y * 0.25" src/components/ParticleWave.tsx && grep -Fq "gl_PointCoord" src/components/ParticleWave.tsx && grep -Fq "smoothstep" src/components/ParticleWave.tsx && grep -Fq "AdditiveBlending" src/components/ParticleWave.tsx && grep -Fq "geometry.dispose()" src/components/ParticleWave.tsx && grep -Fq "material.dispose()" src/components/ParticleWave.tsx && grep -Fq "renderer.dispose()" src/components/ParticleWave.tsx && grep -Fq "renderer.forceContextLoss()" src/components/ParticleWave.tsx && grep -Fq "webglcontextlost" src/components/ParticleWave.tsx && grep -Fq "preventDefault" src/components/ParticleWave.tsx && grep -Fq "document.hidden" src/components/ParticleWave.tsx && grep -Fq "prefers-reduced-motion" src/components/ParticleWave.tsx && grep -Fq 'data-testid="particle-wave-canvas"' src/components/ParticleWave.tsx && grep -Fq 'data-particle-color' src/components/ParticleWave.tsx && grep -Fq "#E8F0FF" src/components/ParticleWave.tsx && grep -Fq "#7FA7FF" src/components/ParticleWave.tsx && grep -Fq "#1B2B4D" src/components/ParticleWave.tsx && grep -Fq "#1D4AC7" src/components/ParticleWave.tsx && grep -Fq "'#3370FF'" src/components/ParticleWave.tsx && ! grep -Fq "dangerouslySetInnerHTML" src/components/ParticleWave.tsx && ! grep -Fq "@react-three/fiber" src/components/ParticleWave.tsx && npx tsc --noEmit --project tsconfig.json && npm run lint -- src/components/ParticleWave.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `frontend/src/components/ParticleWave.tsx` 存在、> 150 行
    - 包含 `export default function ParticleWave`
    - 从 `'three'` 导入 Scene / PerspectiveCamera / WebGLRenderer / Points / BufferGeometry / Float32BufferAttribute / ShaderMaterial / AdditiveBlending / Color / Vector2
    - vertex shader 字面量包含 `sin(p.x * 0.35` 和 `sin(p.y * 0.25`（双正弦波浪）
    - vertex shader 包含 `exp(-(d * d) / (2.0 * 3.5 * 3.5))`（gaussian sigma 3.5）和 `* 1.2 * uMouseStrength`（amplitude 1.2）
    - fragment shader 包含 `gl_PointCoord` 和 `smoothstep`
    - 使用 `AdditiveBlending`
    - cleanup 字面量全部命中：`geometry.dispose()`, `material.dispose()`, `renderer.dispose()`, `renderer.forceContextLoss()`
    - 含 `webglcontextlost` 监听 + `preventDefault()` 调用
    - 含 `document.hidden` 分支（背台暂停）
    - 含 `prefers-reduced-motion` matchMedia 判定
    - 5 个色谱字面量全部命中：`#E8F0FF`, `#3370FF`, `#1D4AC7`, `#7FA7FF`, `#1B2B4D`
    - canvas 节点设置 `data-testid="particle-wave-canvas"` 和 `data-particle-color` attr
    - **不含** `dangerouslySetInnerHTML`
    - **不含** `@react-three/fiber` 或 `@react-three/drei` 导入
    - `cd frontend && npx tsc --noEmit` 无错误
    - `cd frontend && npm run lint src/components/ParticleWave.tsx` 无 error
    - `cd frontend && npm run build` 成功（验证 Vite 能把 three 打进独立 chunk — Plan 03 使用 React.lazy 后才能独立 chunk，这里只保证 build 通过）
  </acceptance_criteria>
  <done>
    ParticleWave.tsx 通过 TS/lint/build；波浪数学全在 GPU shader；cleanup 完整；暴露 testid 和 data-particle-color 给 Plan 03 的 E2E 测试
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPU 驱动 ↔ 浏览器 WebGL runtime | shader 字符串由前端静态字面量提供，不接受用户输入 |
| React render cycle ↔ Three.js imperative lifecycle | useEffect cleanup 必须幂等，否则 Strict Mode 双调用会叠加资源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Denial of Service | React 18 Strict Mode 下 effect 双调用造成 WebGL context 泄露 | mitigate | cleanup 函数必须调 `geometry.dispose() + material.dispose() + renderer.dispose() + renderer.forceContextLoss() + mount.removeChild(canvas)` 五件；grep 校验每一项字符串存在（RESEARCH Pitfall 1） |
| T-23-02 | Tampering | three 库供应链 | mitigate | Plan 01 已 pin 精确版本；本 Plan 仅 `from 'three'` 顶层导入，不用 CDN、不用 dynamic import 字符串构造 |
| T-23-05 | Denial of Service | 恶意 shader 字符串导致 GPU 挂起 | accept | shader 是静态字面量，由本 Plan 写死；不接受任何用户输入 — 无被投毒路径 |
| T-23-06 | Information Disclosure | canvas 截取 WebGL 指纹可用于用户识别 | accept | 登录页前置 canvas 不读 `gl.readPixels` 或传回后端；浏览器原生 antifingerprinting 生效；风险与 Web 通用 canvas 一致 |
| T-23-07 | Denial of Service | 切 tab 后台累积 uTime 引发波形爆炸 | mitigate | `document.hidden` 分支跳过 RAF 且重置 lastTime（Pitfall 6）；增量时间 clamp 0.1s |

**ASVS L1 mapping:**
- V5.1 Input Validation: shader 无用户输入 — N/A（accept）
- V14.2 Dependency: 三方库通过 Plan 01 pin — Mitigated
- WCAG 2.3.3 Reduced motion: 组件 mount 时读 matchMedia — Mitigated
</threat_model>

<verification>
## Plan-Level Checks

1. `cd frontend && npx tsc --noEmit` 整体无错误
2. `cd frontend && npm run lint` 整体无 error
3. `cd frontend && npm run build` 成功
4. Wave 0 测试 `particle — canvas renders when webgl is available` 预计仍然 RED（因为本 Plan 不改 Login.tsx，canvas 不会出现在 /login 路由上）—— Plan 03 完成后转绿
5. 手动开发验证（Plan 03 合并前临时方式）:
   - 在 Login.tsx 或 App.tsx 临时插入 `<ParticleWave isDark={false} />` 样式 `width:100%;height:400px;position:relative`，启动 `npm run dev`
   - 开浏览器 DevTools Console，刷新数次，不应出现 `Too many active WebGL contexts` 警告
   - 粒子呈蓝色海浪流动；鼠标悬停产生隆起跟随
   - （此步为开发者自测，非 CI 要求，完成后务必回滚临时代码）

## Decision Coverage

| Decision | Implemented in Task | Status |
|----------|--------------------|--------|
| D-01 海浪流动风格 | Task 1（双正弦 vertex shader） | Full |
| D-02 2000-3000 粒子 | Task 1（80×32 = 2560） | Full |
| D-03 鼠标柔和推波 | Task 1（gaussian sigma 3.5，lerp 0.05） | Full |
| D-04 #3370FF 渐变色谱 | Task 1（3 色谱 uniforms） | Full |
| D-05 圆形粒子 | Task 1（fragment shader smoothstep） | Full |
| D-06 暗黑深海荧光 | Task 1（isDark 切 COLORS_DARK） | Full |

## Requirement Coverage

| Requirement | Evidence |
|-------------|----------|
| LOGIN-02 | ParticleWave 组件 + shader + mouse interaction — 本 Plan 完整实现 |
| LOGIN-04 粒子色部分 | 3 色谱 uniforms + isDark 分支；完整表单卡片玻璃态在 Plan 03 Task 2 |
</verification>

<success_criteria>
- ParticleWave.tsx 通过 tsc / lint / build 三重门
- 2560 粒子在 GPU 上做波浪位移（grep 证 sin(p.x * 0.35) 和 sin(p.y * 0.25) 存在）
- 亮/暗色谱全部 5 个字面量存在，且按 isDark 分支使用
- cleanup 5 件（dispose × 3 + forceContextLoss + removeChild）全部 grep 命中
- `webglcontextlost` + `preventDefault()` 存在
- `document.hidden` 暂停 + `prefers-reduced-motion` 静态分支均存在
- canvas 暴露 `data-testid="particle-wave-canvas"` 和 `data-particle-color` attr
- 无 `dangerouslySetInnerHTML`、无 R3F 导入、无 CDN 加载
</success_criteria>

<output>
完成后写 `.planning/phases/23-login-redesign/23-02-SUMMARY.md`，记录：
- ParticleWave 默认导出签名
- 实际粒子总数、网格尺寸
- shader 字符串行数（LoC）
- cleanup 完整性自检结果
- 开发者自测（Strict Mode HMR 5 次刷新后 console 有无 WebGL 警告）
- 已知风险（A4 iPad 性能、A5 Safari backdrop-filter）留给 Plan 03 手动验证
</output>
