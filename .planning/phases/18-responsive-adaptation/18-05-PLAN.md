---
phase: 18-responsive-adaptation
plan: 05
type: execute
wave: 4
depends_on:
  - 18-01
  - 18-03
  - 18-04
files_modified:
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/Compare.tsx
  - frontend/src/pages/PeriodCompare.tsx
  - frontend/src/pages/Mappings.tsx
  - frontend/src/pages/FeishuSync.tsx
  - frontend/src/pages/FeishuSettings.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "Dashboard 的头部按钮、统计卡、分布卡和最近批次表在手机/平板上都可用"
    - "Compare / PeriodCompare 不通过压缩列宽来假装适配，必须保留横向滚动和关键列"
    - "Mappings / FeishuSync / FeishuSettings 的表单与表格在小屏转为单列或可滚动布局"
    - "本计划覆盖剩余响应式高风险页面，确保 UX-03 不留盲区"
  artifacts:
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "看板卡片与表格的响应式重排"
      contains: "xs={24}"
    - path: "frontend/src/pages/Compare.tsx"
      provides: "对比页小屏横向滚动/固定关键列"
      contains: "scroll={{ x:"
    - path: "frontend/src/pages/PeriodCompare.tsx"
      provides: "跨期对比页小屏可用性"
      contains: "scroll={{ x:"
    - path: "frontend/src/pages/FeishuSettings.tsx"
      provides: "配置页小屏单列表单"
      contains: "xs={24}"
  key_links:
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/WorkflowSteps.tsx"
      via: "dashboard keeps workflow shell responsive"
      pattern: "WorkflowSteps"
    - from: "frontend/src/pages/Compare.tsx"
      to: "frontend/src/pages/PeriodCompare.tsx"
      via: "wide comparison table patterns"
      pattern: "scroll"
    - from: "frontend/src/pages/FeishuSync.tsx"
      to: "frontend/src/pages/FeishuSettings.tsx"
      via: "shared feishu admin configuration UX"
      pattern: "Row"
---

<objective>
完成剩余高风险页面的响应式 sweep，补齐 Dashboard、对比页、映射页和飞书页的手机/平板布局，确保 UX-03 真正覆盖“全页面”。

Purpose: 避免前四份计划完成后仍有零散宽表格或配置页在小屏不可用，形成“只有主链路适配”的伪完成状态。
Output: 剩余关键页面的响应式审计与修正，最终 build 通过
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-responsive-adaptation/18-CONTEXT.md
@.planning/phases/18-responsive-adaptation/18-RESEARCH.md
@.planning/phases/18-responsive-adaptation/18-UI-SPEC.md
@.planning/phases/18-responsive-adaptation/18-VALIDATION.md

<interfaces>
Representative patterns to fix:
```tsx
// Dashboard.tsx
<Row gutter={[16, 16]}>
  <Col span={6}><Card><Statistic ... /></Card></Col>
</Row>

// Comparison pages
<Table ... scroll={{ x: true }} />

// Settings/admin pages
<Row gutter={16}>
  <Col span={12}>...</Col>
</Row>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 响应式审计并修正 Dashboard、Compare、PeriodCompare、Mappings、FeishuSync、FeishuSettings</name>
  <files>frontend/src/pages/Dashboard.tsx, frontend/src/pages/Compare.tsx, frontend/src/pages/PeriodCompare.tsx, frontend/src/pages/Mappings.tsx, frontend/src/pages/FeishuSync.tsx, frontend/src/pages/FeishuSettings.tsx</files>
  <read_first>
    - frontend/src/pages/Dashboard.tsx
    - frontend/src/pages/Compare.tsx
    - frontend/src/pages/PeriodCompare.tsx
    - frontend/src/pages/Mappings.tsx
    - frontend/src/pages/FeishuSync.tsx
    - frontend/src/pages/FeishuSettings.tsx
    - frontend/src/hooks/useResponsiveViewport.ts
    - .planning/phases/18-responsive-adaptation/18-UI-SPEC.md
  </read_first>
  <action>
1. 在 `frontend/src/pages/Dashboard.tsx` 中：
   - 顶部标题区按钮允许换行；手机端改为标题块在上、按钮块在下
   - 顶部 4 张统计卡使用 `xs={24} sm={12} lg={6}`
   - “系统健康”卡的 3 个 statistic 改为 `Row gutter={[16,16]}` + `xs={24} md={8}`
   - 4 张 distribution 卡使用 `xs={24} md={12} xl={6}`
   - 最近批次和质量明细表保留 `scroll.x`，确保批次名称仍固定左侧

2. 在 `frontend/src/pages/Compare.tsx` 和 `frontend/src/pages/PeriodCompare.tsx` 中：
   - 接入 `useResponsiveViewport()` 或等价断点判断
   - 任何宽表格/双表格容器在 `<992px` 时必须依赖横向滚动而不是压缩列宽
   - 保留身份/月份/状态等关键列为固定左列
   - 顶部筛选和统计卡在手机端改为单列或两列堆叠，不要求桌面视觉重做

3. 在 `frontend/src/pages/Mappings.tsx`、`FeishuSync.tsx`、`FeishuSettings.tsx` 中：
   - 表单 `Row/Col` 在手机端统一改为 `xs={24}`
   - 表格和列表增加 `scroll.x`
   - 操作按钮组允许换行，避免小屏一行塞满
   - 只允许把低优先级说明文案、长路径、长 JSON 文本设为 `responsive: ['lg']` 或折叠显示，不能隐藏关键操作入口
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/Dashboard.tsx src/pages/Compare.tsx src/pages/PeriodCompare.tsx src/pages/Mappings.tsx src/pages/FeishuSync.tsx src/pages/FeishuSettings.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/Dashboard.tsx` 顶部统计卡包含 `xs={24}`
    - `frontend/src/pages/Dashboard.tsx` 最近批次表继续固定左列 `批次名称`
    - `frontend/src/pages/Compare.tsx` 包含 `scroll={{ x:`
    - `frontend/src/pages/PeriodCompare.tsx` 包含 `scroll={{ x:`
    - `frontend/src/pages/FeishuSettings.tsx` 或 `frontend/src/pages/FeishuSync.tsx` 至少一处包含 `xs={24}`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/Dashboard.tsx src/pages/Compare.tsx src/pages/PeriodCompare.tsx src/pages/Mappings.tsx src/pages/FeishuSync.tsx src/pages/FeishuSettings.tsx` 退出码为 0
  </acceptance_criteria>
  <done>剩余高风险页面已完成响应式 sweep，关键表格和配置页在手机/平板可用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analysis and admin views | 不能为了小屏适配隐藏关键对比/配置操作，导致用户误判状态或无法执行管理动作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Information Disclosure / Integrity | Compare/PeriodCompare responsive layout | mitigate | 关键身份列、状态列和操作列保持可见；不通过删关键列来“适配”小屏，只通过横向滚动和布局堆叠处理 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/Dashboard.tsx src/pages/Compare.tsx src/pages/PeriodCompare.tsx src/pages/Mappings.tsx src/pages/FeishuSync.tsx src/pages/FeishuSettings.tsx`
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build`
3. 手动抽查 Dashboard、一个 Compare 页面、一个 Feishu/Settings 页面在 `375px` 和 `820px` 宽度下都可操作
</verification>

<success_criteria>
- Dashboard 的卡片、按钮和最近批次表在手机/平板不再挤压
- Compare / PeriodCompare 保持关键列和横向滚动，不出现信息缺失
- Mappings / Feishu* 页的表单与表格在小屏可读可点
- Phase 18 的“全页面”范围得到真正闭环
</success_criteria>

<output>
After completion, create `.planning/phases/18-responsive-adaptation/18-05-SUMMARY.md`
</output>
