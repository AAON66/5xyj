---
phase: 18-responsive-adaptation
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - frontend/src/components/ResponsiveFilterDrawer.tsx
  - frontend/src/pages/DataManagement.tsx
  - frontend/src/pages/Employees.tsx
  - frontend/src/pages/AuditLogs.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "DataManagement、Employees、AuditLogs 在 <992px 时使用筛选 Drawer，而不是内联筛选排满首屏"
    - "筛选 Drawer 使用 draft state；只有点击“应用筛选”才触发查询或 URL 更新"
    - "点击关闭 Drawer 不会污染当前已应用筛选"
    - "桌面端 >=992px 继续保留内联筛选布局"
    - "三个页面的关键身份列继续固定在左侧，并通过横向滚动保留关键信息"
  artifacts:
    - path: "frontend/src/components/ResponsiveFilterDrawer.tsx"
      provides: "统一筛选抽屉和底部应用/清空 footer"
      contains: "应用筛选"
    - path: "frontend/src/pages/DataManagement.tsx"
      provides: "URL 持久化的数据管理抽屉筛选"
      contains: "ResponsiveFilterDrawer"
    - path: "frontend/src/pages/Employees.tsx"
      provides: "员工主档筛选抽屉"
      contains: "ResponsiveFilterDrawer"
    - path: "frontend/src/pages/AuditLogs.tsx"
      provides: "审计日志筛选抽屉"
      contains: "ResponsiveFilterDrawer"
  key_links:
    - from: "frontend/src/components/ResponsiveFilterDrawer.tsx"
      to: "frontend/src/pages/DataManagement.tsx"
      via: "shared filter drawer wrapper"
      pattern: "ResponsiveFilterDrawer"
    - from: "frontend/src/pages/DataManagement.tsx"
      to: "frontend/src/services/dataManagement.ts"
      via: "apply filters by updating URL/search params"
      pattern: "updateParams"
    - from: "frontend/src/pages/Employees.tsx"
      to: "frontend/src/services/employees.ts"
      via: "apply employee query/region/company filters"
      pattern: "fetchEmployeeMasters"
    - from: "frontend/src/pages/AuditLogs.tsx"
      to: "frontend/src/services/authSession.ts"
      via: "audit log fetch remains authenticated"
      pattern: "readAuthSession"
---

<objective>
把筛选型数据页统一改造成“桌面内联 + 平板/手机抽屉”的响应式模式，并保证宽表格仍保留关键列和横向滚动能力。

Purpose: 解决小屏首屏被筛选控件占满、筛选一改就立刻请求、表格关键信息被挤压的问题。
Output: 可复用的 ResponsiveFilterDrawer + 三个核心数据页的抽屉筛选和宽表格合同
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-responsive-adaptation/18-CONTEXT.md
@.planning/phases/18-responsive-adaptation/18-RESEARCH.md
@.planning/phases/18-responsive-adaptation/18-UI-SPEC.md
@.planning/phases/18-responsive-adaptation/18-VALIDATION.md

<interfaces>
From frontend/src/pages/DataManagement.tsx:
```tsx
const regions = searchParams.get('region')?.split(',').filter(Boolean) ?? []
const companies = searchParams.get('company')?.split(',').filter(Boolean) ?? []
const periods = searchParams.get('period')?.split(',').filter(Boolean) ?? []
const matchStatus = searchParams.get('matchStatus') || 'matched'
```

Current filter UI:
```tsx
<Card>
  <Row>
    <Select mode="multiple" ... />
    <Select mode="multiple" ... />
    <Select mode="multiple" ... />
    <Select value={matchStatus} ... />
    <Input.Search ... />
  </Row>
</Card>
```

From frontend/src/pages/Employees.tsx:
```tsx
const [draftQuery, setDraftQuery] = useState('')
const [activeOnly, setActiveOnly] = useState(false)
const [selectedRegion, setSelectedRegion] = useState('')
const [selectedCompany, setSelectedCompany] = useState('')
```

From frontend/src/pages/AuditLogs.tsx:
```tsx
const [action, setAction] = useState('')
const [dateRange, setDateRange] = useState<[Dayjs | null, Dayjs | null] | null>(null)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增共享 ResponsiveFilterDrawer 组件</name>
  <files>frontend/src/components/ResponsiveFilterDrawer.tsx</files>
  <read_first>
    - frontend/src/components/MobileStickyActionBar.tsx
    - frontend/src/hooks/useResponsiveViewport.ts
    - .planning/phases/18-responsive-adaptation/18-UI-SPEC.md
  </read_first>
  <action>
1. 新增 `frontend/src/components/ResponsiveFilterDrawer.tsx`，组件 props 至少包含：
   - `title: string`
   - `open: boolean`
   - `onClose: () => void`
   - `onApply: () => void`
   - `onReset: () => void`
   - `activeCount: number`
   - `children: React.ReactNode`
   - `triggerLabel?: string`
2. 该组件本身负责渲染右侧 `Drawer` 和底部 footer，footer 必须固定显示两个按钮：
   - 主按钮 `应用筛选`
   - 次按钮 `清空`
   footer 样式使用 `token.colorBgContainer`、顶部边框和 `padding: '12px 16px calc(12px + env(safe-area-inset-bottom))'`
3. 组件不直接管理筛选值；它只负责承载 draft UI 和 apply/reset 交互。
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/components/ResponsiveFilterDrawer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ResponsiveFilterDrawer.tsx` 导出 `ResponsiveFilterDrawer`
    - `frontend/src/components/ResponsiveFilterDrawer.tsx` 包含按钮文案 `应用筛选`
    - `frontend/src/components/ResponsiveFilterDrawer.tsx` 包含按钮文案 `清空`
    - `frontend/src/components/ResponsiveFilterDrawer.tsx` 包含 `env(safe-area-inset-bottom)`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/components/ResponsiveFilterDrawer.tsx` 退出码为 0
  </acceptance_criteria>
  <done>共享筛选抽屉组件已就绪，后续页面只需提供 draft state 和 apply/reset 行为</done>
</task>

<task type="auto">
  <name>Task 2: 将 DataManagement 改为桌面内联 + 移动抽屉筛选，并保持 URL 持久化</name>
  <files>frontend/src/pages/DataManagement.tsx</files>
  <read_first>
    - frontend/src/pages/DataManagement.tsx
    - frontend/src/components/ResponsiveFilterDrawer.tsx
    - frontend/src/hooks/useResponsiveViewport.ts
    - .planning/phases/18-responsive-adaptation/18-UI-SPEC.md
  </read_first>
  <action>
1. 在 `frontend/src/pages/DataManagement.tsx` 中接入 `useResponsiveViewport()`，以 `<992px` 作为筛选 drawer 触发阈值。

2. 新增 `draftFilters` 状态对象，至少包含：
   - `regions: string[]`
   - `companies: string[]`
   - `periods: string[]`
   - `matchStatus: string`
   - `searchText: string`
   该 draft state 初始化自当前已应用的 URL/search state；每次打开 drawer 时都要同步一次当前 applied state。

3. 在 `<992px` 时：
   - 页面标题右侧只显示一个 `筛选` 按钮，按钮文案如 `筛选` 或 `筛选 (N)`
   - 内联筛选 card 不再默认渲染
   - 使用 `ResponsiveFilterDrawer` 承载地区/公司/账期/匹配状态/搜索框
   - 点击“应用筛选”时一次性调用 `updateParams`，写入 `region/company/period/matchStatus/page`
   - 点击关闭抽屉时丢弃未应用的 draft 修改
   - 点击“清空”时恢复 `matchStatus: 'matched'`、其余筛选为空、页码归零

4. 在 `>=992px` 时继续保留现有内联筛选布局，但更新为复用同一套事件处理逻辑，避免桌面和移动维护两套筛选规则。

5. 调整三张表的 scroll/fixed 合同：
   - 明细表 `scroll.x` 提升到至少 `1100`
   - 员工汇总表 `scroll.x` 提升到至少 `900`
   - 月份汇总表 `scroll.x` 提升到至少 `900`
   - 明细表固定左列继续是 `姓名`
   - 汇总表固定左列分别保留 `工号` 和 `月份`
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/DataManagement.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/DataManagement.tsx` 导入 `ResponsiveFilterDrawer`
    - `frontend/src/pages/DataManagement.tsx` 导入 `useResponsiveViewport`
    - `frontend/src/pages/DataManagement.tsx` 存在 `draftFilters` 或同等命名的抽屉草稿态
    - `frontend/src/pages/DataManagement.tsx` 在移动/平板分支渲染 `筛选` 触发按钮
    - `frontend/src/pages/DataManagement.tsx` 的 drawer apply 不直接在控件 `onChange` 中调用 `fetchNormalizedRecords`
    - `frontend/src/pages/DataManagement.tsx` 明细表包含 `scroll={{ x: 1100 }}`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/DataManagement.tsx` 退出码为 0
  </acceptance_criteria>
  <done>DataManagement 已具备移动筛选抽屉、草稿态应用/清空语义和更稳定的宽表格滚动合同</done>
</task>

<task type="auto">
  <name>Task 3: 将 Employees 与 AuditLogs 改为抽屉筛选并补齐宽表格/操作区响应式</name>
  <files>frontend/src/pages/Employees.tsx, frontend/src/pages/AuditLogs.tsx</files>
  <read_first>
    - frontend/src/pages/Employees.tsx
    - frontend/src/pages/AuditLogs.tsx
    - frontend/src/components/ResponsiveFilterDrawer.tsx
    - frontend/src/hooks/useResponsiveViewport.ts
    - .planning/phases/18-responsive-adaptation/18-UI-SPEC.md
  </read_first>
  <action>
1. 在 `frontend/src/pages/Employees.tsx` 中：
   - 接入 `useResponsiveViewport()`
   - 把搜索词、在职状态、地区、公司四项筛选收敛成一个移动/平板 drawer
   - `<992px` 时标题右侧保留上传/新增动作的换行布局，并新增 `筛选` 按钮；不再默认显示整行内联筛选
   - drawer apply 时统一执行：`setQuery(...)`、`setActiveOnly(...)`、`setSelectedRegion(...)`、`setSelectedCompany(...)`、`setPageIndex(0)`、`setLoading(true)`
   - 表格 `scroll.x` 至少设为 `980`，固定左列继续是 `工号`，右侧操作列继续固定在右侧

2. 在 `frontend/src/pages/AuditLogs.tsx` 中：
   - 接入 `useResponsiveViewport()`
   - `<992px` 时用 `ResponsiveFilterDrawer` 承载 `action` 和 `dateRange`
   - apply 时调用 `fetchLogs(1, draftAction, draftDateRange)` 并把 `page` 重置为 `1`
   - 关闭抽屉时回滚 draft，不污染当前日志结果
   - 表格增加 `scroll={{ x: 880 }}`，把“时间”列固定左侧，保留“结果”Tag 列和“详情”列
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/Employees.tsx src/pages/AuditLogs.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/Employees.tsx` 导入 `ResponsiveFilterDrawer`
    - `frontend/src/pages/Employees.tsx` 在 `<992px` 分支不再默认渲染完整筛选行
    - `frontend/src/pages/Employees.tsx` 表格包含 `scroll={{ x: 980 }}`
    - `frontend/src/pages/AuditLogs.tsx` 导入 `ResponsiveFilterDrawer`
    - `frontend/src/pages/AuditLogs.tsx` 表格包含 `scroll={{ x: 880 }}`
    - `frontend/src/pages/AuditLogs.tsx` 时间列为 `fixed: 'left'`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/Employees.tsx src/pages/AuditLogs.tsx` 退出码为 0
  </acceptance_criteria>
  <done>Employees 和 AuditLogs 已切换为桌面内联/移动抽屉筛选模式，并保持关键列固定与横向滚动可用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filter application state | 筛选抽屉必须把 draft state 和 applied state 明确隔离，避免用户误操作立即请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | Filter drawer draft state | mitigate | 关闭抽屉时回滚 draft 到当前 applied state；只有点击“应用筛选”才触发 URL 更新或重新查询 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/components/ResponsiveFilterDrawer.tsx src/pages/DataManagement.tsx src/pages/Employees.tsx src/pages/AuditLogs.tsx`
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build`
3. 手动验证 `<992px` 时三页都通过抽屉应用筛选，关闭抽屉不会即时改变列表
</verification>

<success_criteria>
- 三个筛选型页面在小屏不再被筛选控件挤满首屏
- 抽屉筛选明确区分 draft 和 applied state
- 宽表格仍可横向滚动访问关键列
- 桌面端内联筛选和现有效率型布局不回退
</success_criteria>

<output>
After completion, create `.planning/phases/18-responsive-adaptation/18-03-SUMMARY.md`
</output>
