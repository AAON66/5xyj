---
phase: 18-responsive-adaptation
plan: 04
type: execute
wave: 3
depends_on:
  - 18-01
files_modified:
  - frontend/src/pages/SimpleAggregate.tsx
  - frontend/src/pages/Results.tsx
  - frontend/src/pages/Exports.tsx
  - frontend/src/pages/Imports.tsx
  - frontend/src/pages/ImportBatchDetail.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "SimpleAggregate、Results、Exports 在 <768px 时都有唯一的 sticky primary action"
    - "Results 手机上的 sticky primary action 是“当前下一步动作”，不是两个固定按钮并排"
    - "次要动作继续留在页内，不进入固定底栏"
    - "Imports 与 ImportBatchDetail 的头部操作和摘要卡在手机端单列堆叠"
    - "Imports 与 ImportBatchDetail 的预览表/批次表继续支持横向滚动"
  artifacts:
    - path: "frontend/src/pages/SimpleAggregate.tsx"
      provides: "聚合页移动端唯一主操作"
      contains: "MobileStickyActionBar"
    - path: "frontend/src/pages/Results.tsx"
      provides: "校验/匹配页按阶段切换的移动主操作"
      contains: "MobileStickyActionBar"
    - path: "frontend/src/pages/Exports.tsx"
      provides: "导出页移动主操作"
      contains: "MobileStickyActionBar"
    - path: "frontend/src/pages/Imports.tsx"
      provides: "批次管理小屏单列布局"
      contains: "xs={24}"
    - path: "frontend/src/pages/ImportBatchDetail.tsx"
      provides: "批次详情小屏单列摘要和滚动预览"
      contains: "scroll={{ x:"
  key_links:
    - from: "frontend/src/components/MobileStickyActionBar.tsx"
      to: "frontend/src/pages/SimpleAggregate.tsx"
      via: "shared CTA shell"
      pattern: "MobileStickyActionBar"
    - from: "frontend/src/pages/Results.tsx"
      to: "frontend/src/services/runtime.ts"
      via: "validateBatch / matchBatch as staged primary actions"
      pattern: "validateBatch"
    - from: "frontend/src/pages/Exports.tsx"
      to: "frontend/src/services/runtime.ts"
      via: "exportBatch as mobile primary action"
      pattern: "exportBatch"
---

<objective>
完成长流程页面和导入详情页的响应式适配：在手机端把主操作固定到底部，把次要动作留在内容区，并让导入预览/详情页在小屏单列可读。

Purpose: 上传、校验、匹配、导出是用户最常走的操作链路，必须保证在手机端能完整完成而不是只能“看不能点”。
Output: 3 个 workflow 页面 sticky CTA + 2 个导入页面单列布局和可滚动预览
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-responsive-adaptation/18-CONTEXT.md
@.planning/phases/18-responsive-adaptation/18-RESEARCH.md
@.planning/phases/18-responsive-adaptation/18-UI-SPEC.md
@.planning/phases/18-responsive-adaptation/18-VALIDATION.md

<interfaces>
From frontend/src/pages/SimpleAggregate.tsx:
```tsx
<Space>
  <Button type="primary">{TEXT.start}</Button>
  {running && <Button danger>{TEXT.cancel}</Button>}
  {!running && hasSessionRecord && <Button>{TEXT.clear}</Button>}
  <Link to="/imports"><Button>{TEXT.advanced}</Button></Link>
</Space>
```

From frontend/src/pages/Results.tsx:
```tsx
<div style={{ display: 'flex', gap: 8, marginTop: 12 }}>
  <Button type="primary">执行数据校验</Button>
  <Button>执行工号匹配</Button>
</div>
```

From frontend/src/pages/Exports.tsx:
```tsx
<Button type="primary" icon={<ExportOutlined />}>执行双模板导出</Button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 SimpleAggregate、Results、Exports 接入移动端唯一 sticky primary action</name>
  <files>frontend/src/pages/SimpleAggregate.tsx, frontend/src/pages/Results.tsx, frontend/src/pages/Exports.tsx</files>
  <read_first>
    - frontend/src/pages/SimpleAggregate.tsx
    - frontend/src/pages/Results.tsx
    - frontend/src/pages/Exports.tsx
    - frontend/src/components/MobileStickyActionBar.tsx
    - frontend/src/hooks/useResponsiveViewport.ts
    - .planning/phases/18-responsive-adaptation/18-UI-SPEC.md
  </read_first>
  <action>
1. 在三个页面中都接入 `useResponsiveViewport()` 和 `MobileStickyActionBar`，并统一规则：仅 `isMobile` 时展示 sticky bar；平板和桌面继续保留页内按钮。

2. 在 `frontend/src/pages/SimpleAggregate.tsx` 中：
   - 手机上的 sticky primary action 文案固定为 `开始聚合并导出`
   - `running` 时 sticky primary action 保持同一位置并显示 loading/disabled，而不是消失
   - `取消当前聚合`、`清除当前记录`、`进入高级页面` 保留在页面内容区，不进入固定底栏
   - 页面主容器在移动端补 `paddingBottom: 96`

3. 在 `frontend/src/pages/Results.tsx` 中：
   - 计算“下一步主动作”：
     - 当当前批次尚未完成校验时，sticky primary 为 `执行数据校验`
     - 当校验结果已可用且匹配仍是下一步时，sticky primary 为 `执行工号匹配`
   - 手机上的固定底栏只保留这个“下一步动作”；另一个动作继续留在内容区但不固定
   - 页面主容器在移动端补 `paddingBottom: 96`

4. 在 `frontend/src/pages/Exports.tsx` 中：
   - 手机上的 sticky primary action 固定为 `执行双模板导出`
   - 原卡片中的按钮在移动端不再承担固定主入口职责
   - 页面主容器在移动端补 `paddingBottom: 96`
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/SimpleAggregate.tsx src/pages/Results.tsx src/pages/Exports.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/SimpleAggregate.tsx` 导入 `MobileStickyActionBar`
    - `frontend/src/pages/SimpleAggregate.tsx` 移动端 sticky 文案为 `开始聚合并导出`
    - `frontend/src/pages/Results.tsx` 导入 `MobileStickyActionBar`
    - `frontend/src/pages/Results.tsx` 存在根据校验/匹配状态切换主按钮文案的逻辑
    - `frontend/src/pages/Exports.tsx` 导入 `MobileStickyActionBar`
    - 三个页面在移动分支都包含 `paddingBottom: 96`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/SimpleAggregate.tsx src/pages/Results.tsx src/pages/Exports.tsx` 退出码为 0
  </acceptance_criteria>
  <done>3 个 workflow 页面在手机端都拥有唯一的固定主操作入口，且不会把次要动作塞进底栏</done>
</task>

<task type="auto">
  <name>Task 2: 调整 Imports 和 ImportBatchDetail 的小屏布局、摘要卡和预览表滚动</name>
  <files>frontend/src/pages/Imports.tsx, frontend/src/pages/ImportBatchDetail.tsx</files>
  <read_first>
    - frontend/src/pages/Imports.tsx
    - frontend/src/pages/ImportBatchDetail.tsx
    - frontend/src/hooks/useResponsiveViewport.ts
    - .planning/phases/18-responsive-adaptation/18-UI-SPEC.md
  </read_first>
  <action>
1. 在 `frontend/src/pages/Imports.tsx` 中：
   - 接入 `useResponsiveViewport()`
   - 顶部上传入口和批次列表继续使用 `Row/Col`，但在 `<768px` 时确保两个主卡片都 `xs={24}`
   - 上传入口顶部按钮区允许自动换行，不要在手机端保持一行 3 个按钮
   - “当前批次 / 首个命中文件” 两张摘要卡在手机端改为上下单列
   - 批次列表表格保留 `scroll.x`，固定左列为 `批次名称`，操作列固定右侧

2. 在 `frontend/src/pages/ImportBatchDetail.tsx` 中：
   - 接入 `useResponsiveViewport()`
   - 返回按钮和标题在手机端改为上下排列或自动换行，避免一行挤压
   - 批次基础信息 `Descriptions` 在手机端列数降为 `1`
   - “命中工作表 / 表头签名” 两张摘要卡在手机端改为上下单列
   - “映射结果 / 非明细行” 两列卡片在手机端改为单列
   - “标准化预览” 表继续使用 `scroll.x`，固定左列 `源行号`
   - “字段映射” 表格增加 `scroll.x`，确保 `原始表头 / 标准字段 / 映射来源 / 置信度 / 操作` 不在小屏挤爆
  </action>
  <verify>
    <automated>cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/Imports.tsx src/pages/ImportBatchDetail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/Imports.tsx` 导入 `useResponsiveViewport`
    - `frontend/src/pages/Imports.tsx` 顶部两张主卡在手机端使用 `xs={24}`
    - `frontend/src/pages/Imports.tsx` 批次列表表格包含固定左列 `批次名称`
    - `frontend/src/pages/ImportBatchDetail.tsx` 导入 `useResponsiveViewport`
    - `frontend/src/pages/ImportBatchDetail.tsx` `Descriptions` 在移动端列数为 `1`
    - `frontend/src/pages/ImportBatchDetail.tsx` 字段映射表格包含 `scroll={{ x:`
    - `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/Imports.tsx src/pages/ImportBatchDetail.tsx` 退出码为 0
  </acceptance_criteria>
  <done>Imports 与 ImportBatchDetail 已在手机端改为单列可读布局，预览与映射表格仍可横向滚动访问</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow primary actions | 固定底栏不能让用户在手机端重复触发相同动作或误把次要动作当主动作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Denial of Service | Mobile sticky primary action | mitigate | 复用现有 `loading` / `disabled` 状态，sticky bar 不新增第二套独立提交逻辑；次要动作留在内容区 |
</threat_model>

<verification>
1. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run lint -- src/pages/SimpleAggregate.tsx src/pages/Results.tsx src/pages/Exports.tsx src/pages/Imports.tsx src/pages/ImportBatchDetail.tsx`
2. `cd /Users/mac/PycharmProjects/5xyj/frontend && npm run build`
3. 手动验证手机端在 `SimpleAggregate / Results / Exports` 都只有一个固定主按钮，Imports/ImportBatchDetail 为单列布局
</verification>

<success_criteria>
- 上传、校验、匹配、导出链路在手机端都有明确的主操作入口
- 次要动作没有与 sticky 主按钮重复固定
- 导入批次和批次详情页在手机端不再出现横向布局挤压
- 预览表和映射表在小屏仍可滚动访问
</success_criteria>

<output>
After completion, create `.planning/phases/18-responsive-adaptation/18-04-SUMMARY.md`
</output>
