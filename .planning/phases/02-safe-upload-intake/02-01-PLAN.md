---
phase: 02-safe-upload-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/import_service.py
  - backend/app/api/v1/imports.py
  - backend/app/api/v1/aggregate.py
  - backend/tests/test_import_batches_api.py
  - backend/tests/test_aggregate_api.py
  - backend/tests/test_app_initialization.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
must_haves:
  truths:
    - "An oversized multipart upload is rejected even when content-length is missing, invalid, or understated."
    - "A failed streamed upload does not leave a persisted import batch, saved upload file, or ambiguous partial artifact behind."
    - "The current supported import and aggregate happy paths still succeed on existing regional regression samples after upload hardening."
  artifacts:
    - path: "backend/app/services/import_service.py"
      provides: "Authoritative stream-time upload size enforcement plus partial-file cleanup"
      contains: "_store_upload"
    - path: "backend/app/api/v1/imports.py"
      provides: "Explainable API response for oversized streamed uploads on /imports"
      contains: "HTTP_413_REQUEST_ENTITY_TOO_LARGE"
    - path: "backend/tests/test_import_batches_api.py"
      provides: "Regression tests for missing/incorrect content-length and cleanup behavior"
      contains: "payload_too_large"
  key_links:
    - from: "backend/app/api/v1/imports.py"
      to: "backend/app/services/import_service.py"
      via: "import endpoint translates streamed upload exceptions from create_import_batch"
      pattern: "create_import_batch"
    - from: "backend/app/api/v1/aggregate.py"
      to: "backend/app/services/import_service.py"
      via: "aggregate endpoint surfaces upload failures from run_simple_aggregate -> create_import_batch"
      pattern: "run_simple_aggregate"
    - from: "backend/tests/test_aggregate_api.py"
      to: "backend/app/api/v1/aggregate.py"
      via: "aggregate API tests prove upload hardening does not break the supported quick aggregate flow"
      pattern: "/api/v1/aggregate"
---

<objective>
Harden upload intake so streamed file writes enforce size limits authoritatively, fail with explainable API responses, clean up partial artifacts, and preserve the current import-to-export happy path.

Purpose: Close the current gap where upload limits can be bypassed when content-length is absent or incorrect, while keeping existing batch and aggregate workflows stable.
Output: A shared upload-storage enforcement path plus regression coverage for streamed overflow, cleanup, and supported pipeline behavior.
</objective>

<execution_context>
@C:/Users/AAON/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AAON/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-safe-upload-intake/02-CONTEXT.md
@.planning/phases/02-safe-upload-intake/02-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@backend/app/core/config.py
@backend/app/core/upload_guard.py
@backend/app/api/v1/imports.py
@backend/app/api/v1/aggregate.py
@backend/app/services/import_service.py
@backend/app/services/aggregate_service.py
@backend/tests/test_import_batches_api.py
@backend/tests/test_aggregate_api.py
@backend/tests/test_app_initialization.py

<interfaces>
From `backend/app/core/upload_guard.py`:
```python
class UploadGuardMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next) -> Response:
        content_type = request.headers.get("content-type", "")
        content_length = request.headers.get("content-length")
```

From `backend/app/services/import_service.py`:
```python
async def _store_upload(batch_dir: Path, upload: UploadFile) -> StoredUpload:
    with storage_path.open('wb') as handle:
        while True:
            chunk = await upload.read(UPLOAD_CHUNK_SIZE)
            if not chunk:
                break
            handle.write(chunk)
            hasher.update(chunk)
            file_size += len(chunk)
```

From `backend/app/api/v1/imports.py`:
```python
@router.post('', status_code=status.HTTP_201_CREATED)
async def create_import_batch_endpoint(...):
    try:
        batch = await create_import_batch(...)
    except InvalidUploadError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc))
```

From `backend/app/api/v1/aggregate.py`:
```python
except (InvalidUploadError, EmployeeImportError, ValueError) as exc:
    raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add authoritative streamed upload-size enforcement and deterministic cleanup</name>
  <read_first>
    - backend/app/services/import_service.py
    - backend/app/api/v1/imports.py
    - backend/app/api/v1/aggregate.py
    - backend/app/core/upload_guard.py
    - backend/app/core/config.py
    - .planning/phases/02-safe-upload-intake/02-CONTEXT.md
    - .planning/phases/02-safe-upload-intake/02-RESEARCH.md
  </read_first>
  <files>backend/app/services/import_service.py, backend/app/api/v1/imports.py, backend/app/api/v1/aggregate.py</files>
  <behavior>
    - Test 1: A multipart upload whose body exceeds `settings.max_upload_size_bytes` is rejected even when `content-length` is missing.
    - Test 2: A multipart upload whose declared `content-length` is smaller than the real body is still rejected during streamed write.
    - Test 3: When streamed overflow is detected, the partial file is deleted and the failed upload does not leave a persisted `ImportBatch` behind.
    - Test 4: Existing header-based middleware rejection for obviously oversized multipart requests still remains available as an early fast-path.
  </behavior>
  <action>In `backend/app/services/import_service.py`, add a dedicated oversized-upload exception path for streamed writes, such as `UploadTooLargeError`, and make `_store_upload()` enforce `settings.max_upload_size_bytes` while bytes are being read from `UploadFile`. The check must be authoritative: after each chunk read, accumulate `file_size`, detect any size above the configured limit, delete the partially written file, and raise the oversized-upload exception with an actionable message that includes the configured byte limit. Keep empty-file and invalid-extension handling intact. In `backend/app/api/v1/imports.py`, translate the oversized-upload exception to HTTP 413 while preserving existing 400 handling for other invalid-upload cases. In `backend/app/api/v1/aggregate.py`, surface the same oversized-upload condition as a bad request category with a deterministic streamed error payload or HTTP 413 response, but do not redesign the aggregate endpoint contract beyond what is needed to expose the correct failure reason. Do not remove `UploadGuardMiddleware`; keep it as an early header-based optimization while making service-layer enforcement the source of truth.</action>
  <acceptance_criteria>
    - `backend/app/services/import_service.py` contains `UploadTooLargeError` or another explicit oversized-upload exception name distinct from the generic service base exception.
    - `backend/app/services/import_service.py` contains a check that compares accumulated upload size against `max_upload_size_bytes` inside `_store_upload(`.
    - `backend/app/services/import_service.py` contains a cleanup branch that deletes `storage_path` before re-raising the oversized-upload failure.
    - `backend/app/api/v1/imports.py` contains `HTTP_413_REQUEST_ENTITY_TOO_LARGE` or `status.HTTP_413_REQUEST_ENTITY_TOO_LARGE` in the upload endpoint error translation.
    - `backend/app/api/v1/aggregate.py` contains explicit oversized-upload handling rather than relying only on the generic `InvalidUploadError` -> 400 branch.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_import_batches_api.py -k "upload or batch" -x</automated>
  </verify>
  <done>Streamed upload writes now enforce the configured size limit authoritatively, remove partial files on overflow, and surface a deterministic oversized-upload failure through the API boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests for streamed overflow, cleanup, and happy-path preservation</name>
  <read_first>
    - backend/tests/test_import_batches_api.py
    - backend/tests/test_aggregate_api.py
    - backend/tests/test_app_initialization.py
    - backend/app/api/v1/imports.py
    - backend/app/api/v1/aggregate.py
    - backend/app/services/import_service.py
    - backend/app/services/aggregate_service.py
    - .planning/phases/02-safe-upload-intake/02-CONTEXT.md
  </read_first>
  <files>backend/tests/test_import_batches_api.py, backend/tests/test_aggregate_api.py, backend/tests/test_app_initialization.py</files>
  <behavior>
    - Test 1: `/api/v1/imports` rejects an oversized upload when `content-length` is omitted.
    - Test 2: `/api/v1/imports` rejects an oversized upload when `content-length` is smaller than the actual multipart body.
    - Test 3: A failed oversized upload leaves no batch directory contents and no saved batch record behind.
    - Test 4: Existing aggregate happy-path export coverage still passes with real regression samples and both export templates.
    - Test 5: Aggregate upload handling for oversized input is covered so the quick aggregate path does not silently bypass the new streamed guardrail.
  </behavior>
  <action>Extend `backend/tests/test_import_batches_api.py` with API-level tests that build a real multipart request larger than the configured `max_upload_size_mb`, then exercise both a missing `content-length` case and an understated `content-length` case. Assert that the response reports `payload_too_large` or the chosen oversized-upload error contract, and assert that the test upload directory contains no orphaned file while the import-batch list remains empty after failure. Keep `backend/tests/test_app_initialization.py` focused on the middleware fast-path by preserving or slightly clarifying the existing header-based oversized multipart test rather than moving streamed-write assertions there. Extend `backend/tests/test_aggregate_api.py` with one oversized-upload failure test for `/api/v1/aggregate` or `/api/v1/aggregate/stream`, and keep at least one existing real-sample aggregate export happy-path test green so `PIPE-03` is proven in the same phase. Use the existing sample/template helpers already present in the aggregate tests instead of inventing a second fixture mechanism.</action>
  <acceptance_criteria>
    - `backend/tests/test_import_batches_api.py` contains a test name matching `test_create_import_batch_rejects_oversized_upload_without_content_length`.
    - `backend/tests/test_import_batches_api.py` contains a test name matching `test_create_import_batch_rejects_oversized_upload_with_understated_content_length`.
    - `backend/tests/test_import_batches_api.py` contains assertions that the batch list or persisted batch state is empty after oversized-upload failure.
    - `backend/tests/test_aggregate_api.py` contains a test name matching `test_aggregate_endpoint_rejects_oversized_upload` or `test_aggregate_stream_endpoint_rejects_oversized_upload`.
    - `backend/tests/test_aggregate_api.py` still contains `test_aggregate_endpoint_exports_both_templates_without_employee_master` and it passes unchanged or with only setup adjustments.
    - `backend/tests/test_app_initialization.py` still contains `test_upload_guard_rejects_oversized_multipart_payload`.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_import_batches_api.py backend/tests/test_aggregate_api.py backend/tests/test_app_initialization.py -x</automated>
  </verify>
  <done>The tests prove streamed oversized uploads now fail safely and cleanly while the supported import and aggregate happy paths still work with existing regression assets.</done>
</task>

</tasks>

<verification>
Run `pytest backend/tests/test_import_batches_api.py backend/tests/test_aggregate_api.py backend/tests/test_app_initialization.py -x` and confirm oversized streamed uploads fail cleanly while the existing aggregate happy path still exports both templates.
</verification>

<success_criteria>
- Oversized uploads are rejected during streaming even without trustworthy content-length headers.
- Failed uploads leave no ambiguous stored file or persisted batch artifacts behind.
- Existing import and aggregate regional happy paths continue to work after the upload hardening changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safe-upload-intake/02-01-SUMMARY.md`
</output>